# 关于 LabelHub

LabelHub 是一个面向数据标注项目的协作平台，目标是把任务创建、数据分发、人工标注、AI 预审、人工复核和结果导出放进同一条清晰流程里，让标注项目更容易管理、追踪和交付。

## 平台解决什么问题

很多数据标注工作并不只是“填表”。真实项目里通常还会遇到这些问题：

- 任务来源多，数据集、模板和题量分配容易混在一起。
- 多人协作时，标注员、审核员和项目负责人看到的信息不一致。
- 标注质量需要复核，但人工逐条检查成本高、进度慢。
- 项目结束后，结果、审核意见和过程记录需要统一导出和追溯。

LabelHub 围绕这些痛点设计，把“谁负责、做什么、做到哪一步、质量如何”集中到一个 Web 系统中。

## 三类角色如何协作

### Owner：负责组织项目

Owner 可以创建任务、设计标注模板、导入数据集、配置题量、邀请审核员、查看进度看板，并在任务结束后导出结果。对项目负责人来说，Owner 工作台更像一个标注项目控制台。

### Labeler：负责完成标注

Labeler 可以在任务市场领取任务，进入答题页填写结构化表单，保存草稿、批量提交，并处理审核打回的题目。标注过程中的问题也可以直接上报，方便后续复核和定位。

### Reviewer：负责把关质量

Reviewer 可以查看待审核任务，结合原始样本、标注答案和 AI 预审建议进行人工裁决。对于存在分歧或需要终审的样本，系统提供争议处理和时间线追踪能力。

## AI 预审带来的价值

LabelHub 不把 AI 当作单独的演示功能，而是放在审核流程中参与质检：

- 标注提交后，系统可以创建 AI 预审任务。
- 独立 Agent 领取预审任务，调用兼容 OpenAI 接口的大模型。
- AI 输出结构化审核建议、风险点和评分。
- Reviewer 在人工审核时参考 AI 结果，再做最终裁决。

这种设计保留了人工审核的决定权，同时让 AI 承担初筛、提醒和一致性检查的工作，适合展示“AI 辅助但不替代人工”的协作模式。

## 当前可演示能力

当前系统已经形成一条可本地联调、可按角色演示的完整业务闭环。评委可以不只看单个页面，而是沿着“项目负责人发布任务 -> 标注员完成作业 -> AI 预审 -> 审核员复核 -> 项目负责人追踪和导出”的路线观察系统完整度。

### 1. Owner 项目准备与任务发布

Owner 侧可以演示从零组织一个标注项目的过程：

- 创建和管理标注任务，配置任务名称、说明、截止时间、奖励规则和任务状态。
- 设计结构化标注模板，支持表单字段、分组容器、多 Tab 布局、字段校验和草稿保存。
- 导入数据集，预览样本内容，并把任务固定到指定数据范围内。
- 按标注员和审核员配置题量，让不同角色只看到自己需要处理的任务。
- 发布、暂停、恢复或删除任务，展示任务生命周期管理能力。

这部分适合展示系统不是简单的表单页面，而是具备“任务、模板、数据、人员和状态”统一管理能力。

### 2. Labeler 标注作业流程

Labeler 侧可以演示真实标注员的日常工作：

- 在任务市场查看可领取任务，并领取到自己的任务列表。
- 在“我的任务”中查看进度、剩余题量和当前作业状态。
- 进入答题页后，系统会按 Owner 配置的模板渲染结构化标注表单。
- 支持保存草稿、继续作答、单题提交和批量提交，避免作业中断后丢失内容。
- 对有疑问的样本可以上报问题，形成后续审核和追踪依据。
- 对审核打回的题目，可以在打回项页面查看原因并重新返工提交。

这部分适合展示 LabelHub 对标注员体验的覆盖：既能完成提交，也能处理草稿、返工和异常样本。

### 3. AI 预审与人工审核联动

系统已经接入 AI 预审的任务链路，可以演示“AI 辅助质检 + 人工最终裁决”的协作方式：

- 标注提交后，后端可生成 AI Review Job。
- 独立 Agent 登录后端，领取预审任务，调用大模型并回写结构化结果。
- AI 结果包含评分、风险提示、判断建议和原因说明，供人工审核参考。
- Reviewer 可以在审核工作台同时查看原始样本、Labeler 答案、AI 建议和人工裁决区。
- Reviewer 支持通过、打回、争议处理等动作，最终决定仍由人工完成。
- 系统保留题目时间线，方便追踪从分配、提交、AI 预审到人工审核的关键事件。

这部分适合展示 AI 能力不是孤立按钮，而是嵌入到质量审核流程中，帮助审核员提高判断效率。

### 4. Owner 进度追踪、质量查看与结果导出

Owner 侧还可以演示项目运行后的管理能力：

- 通过看板查看任务进度、提交情况、质量指标、到期风险和近期变化。
- 在审核管理中查看任务审核概览、审核员负载、审核日志和样本时间线。
- 对争议样本、打回项和审核结果进行集中查看，掌握项目质量状态。
- 创建导出任务，下载标注结果、审核结果和相关文件元数据。
- 下载后记录导出状态，便于确认交付结果是否已被获取。

这部分适合展示系统具备从“生产数据”到“交付结果”的闭环，而不只是完成标注提交。

### 5. 公共页面与演示配套能力

为了方便评委快速理解和验证，系统还提供了配套能力：

- 公开首页、关于页和文档中心，用于了解平台定位、项目资料、接口说明和阶段记录。
- 多角色登录、注册、退出和会话刷新，便于切换 Owner、Labeler、Reviewer、AI Reviewer 等角色。
- 演示账号初始化，降低本地演示准备成本。
- 外观主题设置，支持后台角色共享明暗模式和主题风格。
- Swagger UI 和接口文档，方便核对前后端接口契约。

因此，当前可演示的不只是一个页面原型，而是一套包含角色分工、业务流程、AI 质检、审核追踪和结果交付的标注协作系统。

## 适合怎样的场景

LabelHub 适合需要多人协作和质量复核的数据标注流程，例如文本分类、偏好对比、问答质量评估、内容安全审核和自定义结构化标注任务。

如果项目需要“任务可分配、过程可追踪、质量可复核、结果可导出”，LabelHub 可以作为一个完整的标注协作原型来演示。

## 如何体验

可以从公开首页进入系统介绍，也可以进入文档中心查看 README、接口说明、阶段计划、阶段实现和相关资料。

登录后，建议按 Owner、Labeler、Reviewer 三条角色路线体验：

1. 先用 Owner 创建或查看任务，理解项目组织方式。
2. 再用 Labeler 完成领取、作答和提交。
3. 最后用 Reviewer 查看 AI 预审结果并完成复核。

这样可以完整看到 LabelHub 从数据准备到结果交付的业务闭环。
