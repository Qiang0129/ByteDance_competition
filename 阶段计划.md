# 阶段计划

## 阶段目标
依据《项目实施计划书》3.2 的推荐仓库结构，在当前仓库根目录建立 LabelHub 项目的基础目录骨架，并保留现有 `datasets/`、`README.md` 等已有内容不做迁移。

## 实施步骤
1. 核对计划书中的推荐目录分层与关键文件。
2. 在仓库根目录创建 `frontend`、`backend`、`agent`、`infra`、`docs`、`submission` 等目录。
3. 为计划书中明确列出的关键文件创建最小占位文件；对空目录保留目录骨架，便于后续继续初始化工程。
4. 校验最终目录结构与计划书一致，并记录必要的合理补全项。

## 边界说明
- 本阶段仅建立仓库目录结构和最小占位文件，不初始化前端/后端具体工程脚手架。
- 计划书中的 `labelhub/` 视为当前仓库根目录，不额外再嵌套创建同名目录。
- `src/main/java/...` 按最小合理补全分别落为 `com/labelhub/backend` 与 `com/labelhub/agent`。
