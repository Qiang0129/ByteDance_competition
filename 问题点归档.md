![1779933002334](image/问题点归档/1779933002334.png)![1779933120211](image/问题点归档/1779933120211.png)![1779933129292](image/问题点归档/1779933129292.png)![1779933129494](image/问题点归档/1779933129494.png)![1779933129662](image/问题点归档/1779933129662.png)![1779933306301](image/问题点归档/1779933306301.png)# 问题点归档

> 说明：本文档按增量方式记录实施中遇到的问题、原因、处理方式和后续建议。

## 01 MySQL 初始化脚本路径执行问题

### 现象
首次通过 `mysql -e "source ..."` 执行 `backend/src/main/resources/db/migration/V1__init_labelhub_schema.sql` 时，MySQL 客户端提示无法打开脚本文件。

### 原因
Windows 路径包含中文目录，`mysql source` 在 `-e` 参数中解析该路径不稳定，导致文件打开失败。

### 处理
改用标准输入方式执行 SQL 文件：`mysql ... < backend/src/main/resources/db/migration/V1__init_labelhub_schema.sql`。该方式已成功完成 `labelhub` 建库建表。

### 后续建议
后续自动化脚本优先使用标准输入或后端迁移工具执行 SQL，避免在 MySQL `source` 命令中直接传递包含中文字符的绝对路径。


## 02 数据集导入未做真实 MySQL 写入联调

### 现象
本次已完成数据集后端接口、文件解析、前端上传/新建弹窗和构建验证,但未直接向本地 `labelhub` MySQL 执行新建/导入写入联调。

### 原因
数据集导入会写入 `files`、`datasets`、`items` 多张表,属于数据库写操作。为避免在未确认当前本地库数据状态时写入额外测试记录,本次只执行后端与前端构建验证。

### 处理
已通过 `backend/.\mvnw.cmd -q -DskipTests package` 和 `frontend/npm run build` 验证编译、类型和打包链路。接口实现保持事务边界,写入失败会整体回滚。

### 后续建议
在确认可写测试库或准备好可清理的测试数据后,用 Owner 账号登录并依次验证 `POST /api/datasets`、`POST /api/datasets/import`、`GET /api/datasets`、`GET /api/datasets/{id}/items` 的真实数据库闭环。


## 03 指派分发策略缺少指派名单模型

### 现象

任务创建表单已经支持 `assigned` 分发策略,任务市场也能按策略展示和筛选,但当前数据库只有 `assignments` 结果表,没有 Owner 配置“哪些标注员可被指派”的名单表或接口。

### 原因

现有 `assignments` 表可以表达“某个 item 已经分配给某个 labeler”,但不适合承载待分配名单、批量指派规则和 Owner 端配置。若在 `POST /api/tasks/{taskId}/claim` 中允许任意标注员领取 `assigned` 任务,会破坏指派语义。

### 处理

本次后端实现采取保守策略:如果当前标注员已经有该任务的预分配 assignment,接口返回该记录;否则 `assigned` 任务拒绝自助认领并返回 `ASSIGNED_TASK_NOT_CLAIMABLE`。

### 后续建议

后续需要补 `task_assignees` 或等价配置模型,并在 Owner 端提供指派标注员入口;发布或指派时预生成 `assignments`,Labeler 端只展示/领取属于自己的指派任务。
## 问题:单角色演示账号无法在顶栏切换到其它角色

- 现象:使用 `owner`、`labeler`、`reviewer` 这类单角色演示账号登录后,顶栏“切换角色”只显示当前账号已有角色,因此看起来无法切换到其它工作台。
- 原因:顶栏角色切换基于当前登录用户的 `roles` 列表,不是重新登录其它演示账号;原有三个演示账号在后端分别只绑定一个角色。
- 处理:保留顶栏“当前账号内切换角色”的语义,新增 `demo / demo123` 多角色演示账号,同时绑定 `owner`、`labeler`、`reviewer`。

## 问题:PowerShell 执行 npm.ps1 被策略拦截

- 现象:在 Windows PowerShell 中执行 `npm run build` 时,系统提示禁止加载 `npm.ps1`。
- 原因:当前机器 PowerShell 执行策略限制脚本运行,不是前端代码或依赖错误。
- 处理:改用 `npm.cmd run build` 执行同一 npm 构建脚本,前端构建已通过。
- 后续建议:本地验证前端时优先使用 `npm.cmd ...`,或在确认安全策略允许的情况下调整 PowerShell 执行策略。

## 问题:Labeler 答题页只显示第 1 / 1 题

- 现象:Owner 发布任务时关联了整个数据集,但 Labeler 认领或被指派后进入答题页只看到一条题目,进度显示 `第 1 / 1 题`。
- 原因:数据集确实已经通过 `items.task_id` 绑定到任务,但答题页按 `assignments` 计算当前 labeler 的可作答队列。原后端在认领或指派时只给 labeler 创建 1 条 assignment,没有把数据集里的多条 item 转成多条 assignment。
- 处理:认领和指派逻辑改为批量创建 assignment;历史只生成 1 条 assignment 的任务会在“我的任务”列表和答题页读取时补齐。
- 后续建议:后续 Owner 端可以增加“每人领取批量大小 / 是否允许多标注员重复标注同一 item”的显式配置,避免把分发策略隐藏在后端默认规则里。

## 问题:接口文档梳理发现 Reviewer/Dashboard 后端未落地

- 现象:`frontend/src/api/reviewer.ts` 和 `frontend/src/api/dashboard.ts` 已经预留完整调用方法,但后端 Controller 中没有 `/api/reviewer/**` 和 `/api/dashboard/**` endpoint。
- 原因:Reviewer 页面和 Dashboard 页面先按计划书完成了前端骨架和 API 调用层,后端仍停留在认证、任务、数据集、模板、认领和答题闭环阶段。
- 处理:已在 `接口文档.md` 中把这些接口标记为“前端已预留 / 后端待实现 / 未联调”,避免后续误判为已完成接口。
- 后续建议:优先实现 Reviewer 后端接口,打通 `annotations` 到人工审核的状态流转;随后实现 Dashboard 聚合统计接口。

## 问题:当前仓库缺少接口级自动化测试

- 现象:接口文档需要记录“是否已测试”,但仓库内未发现针对 REST API 的自动化测试用例。
- 原因:当前阶段主要通过前后端构建和人工页面联调验证功能,尚未补充 Spring Boot Controller/Service 集成测试或前端 API mock 测试。
- 处理:接口文档中的测试状态统一标注为“已构建验证,待补自动化测试”或“未联调”,不夸大测试覆盖。
- 后续建议:为登录、任务发布、模板发布、任务认领、草稿保存和标注提交优先补充接口级测试。

## 问题:模板页显示“尚未绑定任务”但任务发布页已选择模板

- 现象:Owner 任务发布页中任务已经选择某个已发布模板,但模板列表卡片仍显示“尚未绑定任务”。
- 原因:任务发布页保存的是 `tasks.reward_rule.schemaVersionId`;模板列表原查询只看 `task_schema_versions.task_id`,没有识别任务元数据里的模板引用关系。
- 处理:后端 `/api/schemas` 查询已改为同时识别 `task_schema_versions.task_id` 和 `tasks.reward_rule.schemaVersionId`,并在响应中的 `taskId/taskTitle` 返回可展示的任务信息。
- 后续建议:如果同一个模板被多个任务复用,后续可把接口响应从单个 `taskId/taskTitle` 扩展为 `boundTasks[]`,让模板页展示完整使用清单。

## 问题:Formily 官方 antd 适配包与当前 antd6 不匹配

- 现象:动态表单计划要求接入 Formily,但 `@formily/antd-v5` 的 peer dependency 要求 `antd ^5.13.0`,当前前端项目使用 `antd 6.4.3`。
- 原因:Formily 官方 antd 包暂未直接对齐当前项目的 antd6 版本;强行安装会引入 peer dependency 风险,降级 antd 又会影响现有全站 UI。
- 处理:保留 antd6,只引入 `@formily/core`、`@formily/react`、`@formily/json-schema`,并自建 `formilyAntd6` 字段适配层。
- 后续建议:若 Formily 官方发布 antd6 适配包,可评估替换自建适配层,但需要先做全站表单回归。

## 问题:Designer 拖拽浮层不跟随鼠标

- 现象:Owner 模板 Designer 中拖动左侧物料或中间画布字段时,拖拽浮层相对鼠标出现明显偏移,看起来像“没有跟着鼠标走”。
- 原因:dnd-kit `DragOverlay` 使用 `position: fixed` 按视口定位,但当前应用外层 `.app-shell-enter` 动画结束后仍保留 `transform: translateY(0)`。CSS transform 会改变 fixed 子元素的定位参照,导致 overlay 坐标被外层容器偏移。
- 处理:将 `DragOverlay` 通过 React Portal 挂载到 `document.body`,让浮层脱离 transformed ancestor,恢复按视口坐标跟随鼠标。
- 后续建议:后续若新增其他基于 fixed 定位的拖拽浮层、截图框或跟随光标菜单,优先确认其是否需要 portal 到 `body`。

## 问题:Designer 拖拽时缺少明确放入位置提示

- 现象:拖动物料或字段到画布已有组件附近时,只能看到浮层,但不知道松手后会插到目标组件上方还是下方。
- 原因:原实现只依赖 dnd-kit `over.id` 判断当前悬停在哪个字段上,没有进一步判断拖拽中心点位于目标字段上半区还是下半区,也没有把该方向可视化。
- 处理:新增 `dropIndicator` 状态,在 `onDragOver` 中计算 `before/after`,字段卡片按该状态渲染红色插入线;最终 `onDragEnd` 也使用同一方向执行插入和排序。
- 后续建议:后续如果支持分组容器或多 Tab 内部嵌套拖拽,需要把落点模型从单层 `fieldId + before/after` 扩展为 `containerId + fieldId + position`。

## 问题:Designer 插入提示线总显示在组件下方

- 现象:拖动物料或字段时,即使鼠标位于目标组件上半区,红色插入线仍显示在该组件下方。
- 原因:字段卡片和外层画布都是 droppable,默认碰撞检测容易命中 `canvas-droppable`,从而走“追加到最后一个字段下方”的兜底分支。该轮修复时方向判断先改为鼠标 Y 坐标,以便快速验证上/下半区。
- 处理:自定义 Designer 碰撞检测,优先命中具体字段卡片;方向判断改为使用当前鼠标 Y 坐标与目标卡片中心线比较。
- 后续建议:如果后续出现横向布局、分栏或嵌套容器,需要把落点方向从单一 `before/after` 扩展为支持 `inside/left/right` 等更完整的位置模型。

## 问题:插入方向应按拖拽组件位置而不是鼠标点判断

- 现象:红线虽然能区分上/下,但当鼠标点位于拖拽组件边缘或拖拽手柄附近时,提示线可能和用户看到的拖拽组件覆盖位置不一致。
- 原因:上一轮方向判断基于鼠标 Y 坐标,而用户期望基于拖动组件本身相对目标组件的上半区/下半区。
- 处理:方向判断改为使用拖拽组件当前矩形中心线与目标组件中心线比较,使提示线跟随拖拽组件的视觉位置。
- 后续建议:如果后续字段卡片高度差异很大或引入缩放画布,需要补充真实浏览器拖拽回归,确认 dnd-kit `active.rect.current.translated` 与视觉浮层保持一致。

## 问题:任务使用中的模板不能物理删除

- 现象:模板已经被任务或 annotation 引用时,用户仍希望在模板管理页删除它。如果直接物理删除 `task_schema_versions` 记录,任务运行时会找不到 schema,历史 annotation 的模板外键和语义追溯也会被破坏。
- 原因:任务通过 `tasks.reward_rule.schemaVersionId` 引用模板版本,annotation 通过 `annotations.schema_version_id` 外键引用模板版本。模板数据既是管理对象,也是运行时和历史审计数据。
- 处理:删除改为软删除,新增 `task_schema_versions.deleted_at`。Owner 管理列表和新任务模板选择过滤软删除模板,但 Labeler 认领、作答、提交和历史查看仍允许读取已软删除模板。
- 后续建议:当前没有恢复入口。后续如果需要恢复误删模板,可增加 Owner 端“归档模板”视图,并把 `deleted_at` 置回 `NULL`。


## 问题:antd v6 CSS-in-JS 注入样式优先级很高,暗色模式覆盖大量使用 !important

- 现象:暗色模式下 `.field-option-tag.ant-tag`、Input、Select、Card 等 antd 组件的白底深字背景无法用普通 class 选择器覆盖,即使写了 `:root[data-color-mode='dark'] .xxx { background: ... }` 也被压住。
- 原因:antd v6 用 `@ant-design/cssinjs` 在运行时注入 `:where(.css-xxx).ant-btn:hover` 样式,这些样式写到 `<style>` 标签里,优先级与作用域比静态 CSS 文件略复杂,加上 inline style + token 算法干扰。
- 处理:
  1. 全局通配 `:root[data-color-mode='dark']` 使用 `!important` 强制覆盖底色和文字色。
  2. SVG `<text>` 用 `fill` 属性,需要专门给 `.donut-total / .donut-sub` 等 class 写 `fill: ... !important`。
  3. 渐变文字 `background-clip: text` 在暗色下退回 `--lh-primary-light` 实色字。
- 后续建议:antd v7 升级时检查 `theme.darkAlgorithm` 是否能直接覆盖更多场景,届时减少 `!important` 用量。如需要进一步降耦合,可改用 antd 的 `ConfigProvider.theme.components` token 体系,但当前散落在 200+ 处的覆盖一次性迁移成本太高,先维持现状。

## 问题:渐变模式下 antd Button 边缘暴露 1~2px 瑕疵

- 现象:渐变模式下主按钮和登录提交按钮左边缘有"小咬痕",尤其在 hover 时明显。
- 原因:antd Button 默认 `border: 1px solid transparent` + `::before/::after` 伪元素做点击波纹效果,渐变背景下 border-box 与 padding-box 边缘色阶差异在 1~2px 内被肉眼感知。
- 处理:`border: none` 改为 `border: 1px solid transparent`(让 border 宽度始终一致避免尺寸跳变),`background-clip: padding-box` 让背景延伸到边缘,显式 `display: none` 隐藏 `::before/::after` 伪元素。代价是失去波纹动画。
- 后续建议:如果产品要求保留点击波纹,可改用 `outline + outline-offset` 替代,但视觉一致性优先。

## 问题:hover "进入任务市场"时"查看草稿"按钮跟着动

- 现象:Labeler 工作概览 hero 横幅里两个按钮挨得近,hover 第一个时第二个按钮位置出现 1px 漂移。
- 原因:`.overview-hero-cta` 写了 `border: none`(border-width: 0),但 antd v6 hover 时 CSS-in-JS 注入 `border: 1px solid xxx`,从 0px → 1px 的 border 变化让按钮总宽度增加 2px,把右边按钮挤动。反向 hover 不动是因为另一个按钮始终有 1px border。
- 处理:`border: none` 改为 `border: 1px solid transparent`,hover 时也保持 `border: 1px solid transparent !important`,确保 border 宽度始终是 1px。
- 类似场景:其它紧挨的按钮组遇到 hover 联动时,统一用 transparent border 而不是 `none`。

## 问题:暗色模式 SVG 文字看不见

- 现象:数据看板 / Reviewer 报表的环形图中心数字"100"在暗色下完全看不见。
- 原因:SVG `<text>` 元素用 `fill` 属性控制颜色,而不是 CSS `color`。暗色模式的通配规则 `color: #e5e7eb` 对 SVG text 无效。
- 处理:专门为 `.donut-total / .donut-sub` 等 SVG class 在暗色下写 `fill: #f8fafc !important / fill: #94a3b8 !important`。
- 类似场景:今后任何用 SVG 渲染文字的图表,暗色适配都要单独写 `fill` 规则。
## 问题:状态机正确性不能只靠前端按钮控制

- 现象:计划书要求"状态机正确性",如果只在前端隐藏按钮或靠页面流程约束,用户仍可通过接口直接提交非法状态变化,也无法形成审计闭环。
- 原因:状态流转属于后端数据一致性规则,必须在事务内校验当前状态、目标状态、操作者角色和业务前置条件。
- 处理:
  1. 新增 `StateMachineService`,统一校验 task / assignment / annotation / ai_review_job / export_job 状态迁移。
  2. 扩展 `audit_logs`,把操作者角色、from/to 状态、原因和快照写入数据库。
  3. 任务、认领、提交、Reviewer 审核、AI Review、Export 都接入审计。
- 取舍:
  - 本轮不新增 review batch / dispute 表,批次和争议从现有 task / human_reviews 派生。
  - `REVISE` 不直接改写 Labeler 答案,只记录审核意见,避免答案归属和审计链路不清。
  - AI 真实模型调用暂不做,先落地 job/result/status/audit 闭环。
- 后续建议:
  - 如果需要多人审核分派、锁定批次、争议终审委员会等能力,再引入独立 batch / dispute 表。
  - Export 文件生成与下载可在导出状态机稳定后继续实现。

## 问题:模板发布出来和当前 Designer 配置不一致

- 现象:在 Designer 中配置好 ShowItem、单选、多行文本和联动规则后,点击"发布",重新打开模板或绑定任务时看到的内容像是旧配置,不是刚刚画布上的配置。
- 复现条件:
  1. 新建模板并保存成真实草稿版本。
  2. 继续修改字段属性,例如字段名、选项 label、联动规则。
  3. 不点"保存草稿",直接点"发布"。
- 原因:`handlePublish` 只在本地临时 `draft-*` 时调用 `saveDraft()`。已有真实 `versionId` 的草稿发布时直接调用 `POST /schemas/{id}/publish`,后端发布的是数据库里上一次保存的旧 schema。
- 处理:
  1. 发布前始终执行 `saveDraft()`,确保当前画布配置先落库。
  2. 保存成功后再调用 publish。
  3. 保存/发布后保留当前选中的字段,避免属性面板跳回第一个字段造成误判。
- 验证:用 `question_prompt / quality_result / reject_reason` 模板做接口级测试,发布后重新拉取字段、选项和 reaction 均保持一致。
- 后续建议:后续可以在 Designer 顶部增加"未保存改动"状态提示,但当前已经通过发布前强制保存解决数据不一致问题。

## 问题：任务发布只提示 Not Found 且模板发布后刷新看不到

### 现象

Owner 在任务发布 Drawer 点击“立即发布”时，页面顶部只显示 `Not Found`。同时用户反馈刚刚在 Designer 保存并发布的模板刷新后不见了。

### 排查

使用真实接口验证：

1. `demo / demo123`
   - 创建独立模板草稿成功。
   - 发布模板成功。
   - 刷新 `/api/schemas` 能查到该模板。
   - 使用该模板、数据集和指派策略创建任务成功。

2. `owner / owner123`
   - 使用已有已发布模板和自有数据集创建指派任务成功。

因此当前后端任务发布主链路是可用的；截图里的泛化 `Not Found` 需要结合后端业务错误码继续判断具体对象。

### 原因

1. 前端 `ApiError` 原来只把 HTTP `statusText` 作为错误文本，后端返回的 `{ code, message }` 被保存在 `payload` 中但没有展示。
2. 独立模板和任务的绑定关系保存在 `tasks.reward_rule.schemaVersionId`，部分后端查询仍只按旧关系 `task_schema_versions.task_id` 找模板，导致任务/答题回显路径对独立模板不够稳。
3. 如果用户在一个 Owner 账号下创建模板/数据集，又切到另一个 Owner 账号发布任务，后端按当前 Owner 重新校验时会返回 `SCHEMA_NOT_FOUND` 或 `DATASET_NOT_FOUND`。

### 处理

- 前端统一展示后端业务错误 `message/code`，不再只显示 `Not Found`。
- 后端任务查询和答题页 assignment 查询优先按 `tasks.reward_rule.schemaVersionId` 读取独立模板，兼容旧的 `task_id` 模板。
- 用 `demo` 和 `owner` 两个账号分别做接口级发布任务验证。

### 后续建议

如果页面再次报错，直接看新的提示文案即可判断下一步：

- `dataset not found`：当前登录 Owner 不是这个数据集的创建者，或页面选中了已失效数据集。
- `schema not found`：当前登录 Owner 不是这个模板的创建者，或模板已软删除。
- `published task requires a published schema`：模板不是已发布状态，可能刚被收回。

## 问题：任务物理删除会级联清空标注数据

### 现象

Owner 删除任务后，Labeler 端仍可能在旧列表或旧答题链接中看到任务；如果直接物理删除 `tasks`，数据库外键会级联删除 `assignments` 和 `annotations`，不符合“已标注数据作废但保留”的产品语义。

### 原因

1. 原 `DELETE /api/tasks/{taskId}` 执行 `DELETE FROM tasks`。
2. `assignments.task_id -> tasks.id ON DELETE CASCADE` 会删除认领记录。
3. `annotations.assignment_id -> assignments.id ON DELETE CASCADE` 会继续删除标注答案。
4. Labeler 查询没有任务删除态过滤，也没有 `voided` 终态。

### 处理

- 为 `tasks` 增加 `deleted_at`，任务删除改为软删除。
- 删除任务时把相关 `assignments.status` 和 `annotations.status` 标记为 `voided`，保留 `answer_json`。
- 清理相关 drafts，避免旧页面继续保存草稿。
- Labeler 市场、我的任务、答题页、草稿和提交接口全部识别删除/作废状态。
- Reviewer、AI Review、Export 查询排除 `voided` annotation。

### 取舍

- 本轮没有做任务回收站和恢复入口；软删除任务只从常规管理端和 Labeler 流程隐藏。
- 已导出的历史文件不回滚；删除后仅保证后续流程不再处理作废 annotation。
- 如果后续需要展示作废标注，需要基于 `audit_logs` 和 `annotations.status = 'voided'` 单独做归档视图。

## 问题：旧 `required` 联动只做条件必填，不能控制字段显示

- 现象：模板配置“质量判断 = 不通过 -> 问题说明必填”后，Labeler 答题页初始就显示“问题说明”，用户预期是点击“不通过”后才显示。
- 原因：旧 `required` 动作只修改 `required` 状态，不修改 `visible` 状态；Renderer 和后端校验没有“显示并必填”的复合动作。
- 处理：新增 `visibleRequired` 动作；Designer 新增“显示并必填”选项；Renderer、前端提交校验、后端 Schema 校验、后端答案校验全部支持该动作。
- 兼容取舍：旧模板中的 `required` 自动按“显示并必填”处理，避免已经发布的模板需要重建。代价是如果历史上存在“始终显示但条件必填”的 `required` 用法，本轮会改变其显示行为。
- 后续建议：如果产品后续需要两种语义并存，可新增 `conditionalRequired` 表示“始终显示但条件必填”，并逐步迁移旧模板。

## 问题：联动字段隐藏后，选择“不通过”仍不显示

- 现象：修复默认隐藏后，目标字段在“通过”和“不通过”下都不显示。
- 原因：联动规则的匹配值可能填写的是选项 label（如“不通过”），但单选组件真实值是选项 value（如 `option_b`），导致条件没有命中；同时 Formily 字段 Schema 复用可能让可见性变化刷新不够稳定。
- 处理：前后端联动匹配都支持 option value 和 label 双向匹配；Renderer 按运行时规则状态重建 SchemaField。
- 后续建议：Designer 的匹配值输入后续应改成根据来源字段类型动态选择。如果来源字段是单选/多选，应直接展示选项下拉，减少手填 label/value 的误差。

## 问题：模板列表显示草稿但删除接口提示 only draft schema can be deleted

- 现象：Owner 模板卡片显示“草稿”，点击删除时后端返回 `only draft schema can be deleted`。
- 原因：列表响应使用 `normalizeStatus`，把非 `published` 展示为 `draft`；删除接口和 SQL 只认数据库状态精确等于小写 `draft`，历史状态值大小写、空格或旧状态会导致前后端语义不一致。
- 处理：后端删除和更新判断统一使用规范化状态；软删除 SQL 改为只拒绝 `published`。
- 后续建议：后续若新增更多模板状态，应把状态枚举收敛为明确白名单，避免“非 published 都是 draft”的兜底语义继续扩大。

## 问题：任务软删除后复用同一数据集导致 Labeler 认领返回 500

- 现象：Labeler 在任务市场点击“立即认领”时，`POST /api/tasks/{taskId}/claim` 返回 500。当前本地库中任务 `14` 可见且有剩余额度，但认领失败。
- 根因：`assignments` 原唯一键为 `(item_id, labeler_id)`，没有包含 `task_id`。任务 `11` 删除后留下 `voided` 历史 assignment，任务 `14` 复用同一数据集 item 时，同一个 Labeler 再次认领相同 item 会触发唯一键冲突。该冲突之前没有被转换成业务错误，最终变成 500。
- 处理：
  1. 新增 `V8__scope_assignment_unique_key_by_task.sql`，把唯一键升级为 `(task_id, item_id, labeler_id)`。
  2. 初始化脚本 `V1__init_labelhub_schema.sql` 同步调整唯一键。
  3. 可认领 item 查询只排除当前任务内已有有效 assignment，允许不同任务复用同一数据项。
  4. 插入 assignment 时捕获唯一键冲突并转换为 409 `NO_AVAILABLE_ITEM`，避免数据库异常冒泡。
- 取舍：本轮允许同一 Labeler 在不同任务中重新标注同一 dataset item，因为不同任务可能绑定不同模板、截止时间和审核流程；同一任务内仍保持唯一约束。
- 后续建议：数据集绑定任务当前是直接更新 items.task_id，历史 assignment 仍保留旧 task_id。后续如果要让同一数据集同时服务多个并行任务，更稳的模型是新增 task-item 映射表或任务数据快照表，但这会超出“不新增表”的当前约束。
## 问题：模板联动修改后未按预期保存，Labeler 端不显示条件字段

- 现象：Owner 收回模板后把匹配值从“不通过”改成 `option_b`，保存并发布后数据库中 `quality_result` 的联动仍是 `value = "不通过"`、`action = "required"`；Labeler 选择“不通过”后“问题说明”不显示。
- 根因：
  1. 收回模板后 Designer 默认选中第一个字段，用户容易在 ShowItem 的联动页改值，而不是在“质量判断”字段的联动页改值。
  2. 旧联动规则使用 `required` 表示条件必填，但产品语义已经改为“显示并必填”。
  3. 匹配值存在 label/value 混用：单选真实提交值是 `option_b`，界面显示 label 是“不通过”。
  4. Renderer 之前主要依赖 Formily 的 `x-visible`，隐藏字段状态刷新不够直接。
- 处理：
  1. Designer 联动面板显示当前配置字段，收回后尽量保持原选中字段。
  2. 来源字段有选项时，匹配值使用下拉选择，显示 `label (value)`，保存 value。
  3. 前后端保存和发布前统一规范化：`required` 转 `visibleRequired`，选项 label 转 value。
  4. Renderer 编译时直接跳过隐藏字段，草稿和提交都过滤隐藏字段。
- 取舍：旧 `required` 全部兼容为“显示并必填”。如果后续确实需要“字段始终显示、只是条件必填”，应新增独立动作，例如 `conditionalRequired`，避免和当前语义冲突。

## 问题：模板原地收回修改后，旧标注无法按旧模板回放

- 现象：模板使用同一个 `schemaVersionId` 原地收回、修改、重新发布后，历史 annotation 只有 `schema_version_id + answer_json`，无法知道提交时的字段结构。
- 根因：缺少 annotation 级别的 schema 快照。同一个 `task_schema_versions.id` 被原地修改后，旧答案引用的 schema 结构也随之变化。
- 处理：
  1. 新增 `annotations.schema_snapshot_json` 保存提交当时完整 schema。
  2. `latestAnnotation` 返回 `schemaSnapshot`，Labeler 查看已提交题目时按快照只读渲染。
  3. 重新修改时切回任务当前 schema，并通过 `schemaDigest` 防止旧页面误提交。
- 取舍：迁移前已经被覆盖的旧 schema 无法完整恢复，只能使用当前 schema 做 best-effort 回填；迁移后新提交的数据可严格回放。

## 问题：刚领取任务显示“继续答题”

### 现象

Labeler 刚领取任务，还没有进入答题页或保存草稿，“我的任务”操作按钮仍显示“继续答题”。

### 原因

`MyTasks.tsx` 原先只用任务聚合后的 `pendingCount > 0` 判断按钮文案。只要 assignment 状态是 `claimed`，就会被当成“待答”，无法区分“刚开始”和“已有草稿继续”。

### 处理

`GET /api/assignments/mine` 增加 `hasDraft` 与 `hasSubmittedAnnotation`。前端根据入口 assignment 的状态和这两个派生字段显示“开始答题 / 继续答题 / 查看/修改答卷”等文案。

## 问题：字段渲染和回显依赖运行时值结构猜测

### 现象

tags、multi-choice、json 等字段在前端渲染、校验或已提交答案回显时，部分逻辑依赖 `Array.isArray`、`typeof` 或 `field.kind` 猜测答案结构。模板原地修改或旧快照回放时，容易出现字段语义和展示方式不一致。

### 原因

Schema 字段缺少稳定的答案语义类型。`kind` 更偏 Designer 物料/组件类型，不能完全承担答案值结构和校验语义。

### 处理

后端在 Schema 规范化阶段补齐 `semanticType`，前端 Renderer、提交过滤、本地校验优先读取该字段。旧模板没有 `semanticType` 时继续按 `kind` fallback，保证兼容。

## 问题：单题提交即进入审核不符合批次提交预期

### 现象

Labeler 在答题页每道题都看到“提交并继续”。点击后会立即创建 annotation、删除草稿，并进入 AI 预审/人工审核。用户期望是前几题只保存草稿，最后一题统一提交整批已领取题目。

### 原因

旧流程把每个 assignment 当成独立提交单元，前端“下一题”只是跳转，没有强制保存；后端也只有 `POST /assignments/{assignmentId}/submit` 单题提交接口。

### 处理

新增 `POST /api/tasks/{taskId}/assignments/submit` 任务级批量提交接口。前端改为逐题保存草稿，非最后一题隐藏提交按钮；最后一题统一提交本任务下当前 Labeler 已领取的全部草稿。批量提交采用全有或全无事务，任一题缺草稿或校验失败时不创建任何 annotation。
## 问题：AI 预审只有 Job 雏形，缺少真实 Agent、规则和独立后台

### 现象

系统已有 `ai_review_jobs` 和 `ai_review_results` 的基础表结构，但没有可配置规则、规则快照、真实 LLM Worker、`ai_reviewer` 角色后台，也没有把 AI job 完成/失败/重试和 annotation 流转完整纳入状态机审计。

### 原因

早期实现把 AI 预审当成后端占位流程，Labeler 提交后只创建默认 job，缺少 Prompt、评分维度、阈值、重试策略等业务配置，也没有独立 Agent 消费队列。这样无法满足课题要求中的 AI Agent 能力和可审计闭环。

### 处理

1. 新增 `ai_review_rules`，并在 job 中保存 `rule_id` 与 `rule_snapshot_json`。
2. 扩展 result 字段，保存总分、风险标签、证据、模型名、延迟和原始响应。
3. 新增 `ai_reviewer` 与 `system_agent` 角色。
4. 在 `agent/` 实现独立 Spring Boot Worker，只通过后端 API 领取和回写 job。
5. AI job 与 annotation 的 AI 流转统一写入 `audit_logs`，状态迁移由 `StateMachineService` 校验。
6. API Key 只读取 `LABELHUB_LLM_API_KEY`，不写入仓库、不入库。

### 取舍

- AI 判定为 PASS 后仍默认进入人工复核，而不是直接 accepted，避免模型误判直接影响最终数据。
- `ai_reviewer` 当前可以管理规则和重试 job；如果后续要区分只读查看和规则管理，需要拆分 permission。
- Agent 使用真实 Responses API，不做 mock；缺少 API Key 时 job 会失败并进入后端重试/人工兜底策略。

## 问题：Agent 失败日志只显示 500，无法定位失败来源

### 现象

Agent 能领取 AI review job，但日志只显示 `500 INTERNAL_ERROR`，审计日志中的 `reason` 也只有后端返回的错误文本。无法判断失败发生在 LLM Responses API 调用、后端 complete 回写，还是 fail 回写。

### 原因

`AiReviewWorker` 原先把 LLM 调用和后端 complete 回写放在同一个 `try/catch` 中，所有异常统一打印为 `AI review job ... failed`，再调用后端 fail。后端全局异常处理也没有记录未处理异常堆栈。

### 处理

1. Agent 拆分失败来源：
   - `LLM_CALL_FAILED`
   - `BACKEND_COMPLETE_FAILED`
   - `BACKEND_FAIL_WRITEBACK_FAILED`
2. 后端全局异常处理增加 error 级别堆栈日志。
3. 用临时 API Key 验证 `https://www.pqapi.store/v1` 的 `/models` 和 `/responses` 均可用，Agent 默认 base URL 改为该地址。

### 取舍

- 真实 API Key 仍只通过本机环境变量设置，不写入 README、配置文件或数据库。
- 后端响应继续隐藏堆栈，只在服务端日志保留详细异常。

## 问题：AI Reviewer 多个菜单显示成同一个页面

### 现象

`/ai-reviewer`、`/ai-reviewer/jobs`、`/ai-reviewer/rules` 点击后看起来都是同一个页面，用户无法区分概览、Job 队列和规则管理。

### 原因

`AiReviewerDashboard.tsx` 直接 `return <OwnerAiReview />`，三个路由都复用 Owner 的 AI 预审规则页，没有按路由拆分内容。

### 处理

1. `/ai-reviewer` 改为 AI 审核概览页。
2. `/ai-reviewer/jobs` 显示 Job 队列。
3. `/ai-reviewer/rules` 显示规则管理。
4. Owner `/owner/ai-review` 保留原有规则与执行记录 Tab。

## 问题：AI 审核 Job 长时间 running 无法人工处理

### 现象

AI review job 被 Agent 领取后停留在 `running` 很久，后台只能查看状态，不能取消或重新排队。用户需要在 Job 队列中对执行中的任务进行“取消并重新排队”，失败任务可以“重新执行”。

### 原因

原状态机只有 `pending -> running -> succeeded/failed` 和 `failed -> pending`，没有人工把 `running` 拉回 `pending` 的入口。同时缺少执行代次 token，如果直接把 running 改回 pending，旧 Agent 稍后 complete/fail 回写可能覆盖新一轮执行结果。

### 处理

1. `ai_review_jobs` 增加 `run_token` 和 `cancel_reason`。
2. Agent 领取 Job 时获得 `runToken`，complete/fail 回写必须携带 token。
3. 后端校验 token，不一致返回 `AI_REVIEW_RUN_STALE`，不修改 Job 或 annotation。
4. 新增 `POST /api/ai-review/jobs/{jobId}/cancel`，把 `running` Job 取消并直接回到 `pending`。
5. 前端 running Job 显示“取消并重新排队”，超过 10 分钟显示“疑似卡住”；failed Job 显示“重新执行”。

### 取舍

- 不新增 `cancelled` 状态，取消后直接回到 `pending`，符合当前产品选择。
- 为避免旧 Agent 回写污染新执行，必须引入 `runToken`，这是取消后直接回排队语义下的必要保护。
## 2026-05-30 模型配置 localStorage 假保存与权限边界

- 问题：`/ai-reviewer/settings/model` 原先只写入浏览器 localStorage，Agent 仍使用启动时的 `LABELHUB_LLM_*` 环境变量，因此页面保存不会影响真实 AI 预审调用。
- 修复：新增后端 `ai_model_configs` 持久化配置，Agent 每次处理 Job 前读取后端 runtime 配置；没有 active 配置时才回退环境变量。
- 安全取舍：API Key 需要在后端可解密以供 Agent 调用，因此采用 AES-GCM 加密存库，而不是不可逆 hash；加密密钥依赖 `LABELHUB_MODEL_CONFIG_SECRET`，该环境变量丢失会导致已保存 Key 无法解密。
- 权限取舍：模型配置只允许 `owner` 和 `ai_reviewer` 管理，`reviewer` 虽可人工审核但不能修改模型调用配置；`system_agent` 只允许读取 runtime，不能调用管理接口。
- 运维风险：当前不做模型配置版本回滚入口；如误改配置，需要 Owner 或 AI Reviewer 重新保存正确配置。
## 2026-05-30 AI 审核 Job 批量删除前端有按钮但后端缺接口

- 问题：AI Reviewer Job 队列中“删除选中”按钮调用 `/api/ai-review/jobs/batch-delete`，但后端没有对应接口，导致页面提示批量删除失败，Network 中出现 500。
- 修复：补齐后端批量删除接口和前端响应类型，失败时展示后端具体错误信息。
- 取舍：不允许直接删除 `pending` / `running` Job。运行中 Job 可能已被 Agent 领取，直接删除会造成迟到回写、annotation 状态卡住或审计链路断裂；需要先取消并重新排队，或等待其进入 `failed` / `succeeded` 后再删除。
- 仍需注意：当前删除是物理删除 Job 记录，结果表依赖外键级联删除；如后续需要保留 Job 历史，应改成 `deleted_at` 软删除。
## 2026-05-30 AI Reviewer 端错误暴露规则管理能力

- 问题：AI Reviewer 后台展示“规则管理”菜单，并复用了 Owner 的规则管理面板，导致 AI Reviewer 可以创建、编辑、启停和删除 AI 预审规则，与文档中“规则由 Owner 配置、AI Reviewer 负责运行态审核后台”的职责不一致。
- 修复：前端移除 AI Reviewer 端规则管理入口和路由；后端规则接口权限收紧为 Owner only。
- 取舍：AI Reviewer 概览不再请求规则列表，也不再展示规则数量；如后续需要 AI Reviewer 只读查看规则，应新增独立只读接口和只读页面，而不是复用可编辑 Owner 面板。
## 2026-05-30 Reviewer 待审队列依赖 sample fallback

- 问题：Reviewer 侧 `/reviewer/ai` 和 `/reviewer/ai/:taskId` 前端已经切到“按任务聚合 + 三栏工作台”，但后端缺少 `/api/reviewer/ai-review/tasks` 和 `/api/reviewer/ai-review/tasks/{taskId}/annotations`，页面只能回落到 sample 数据，真实 Labeler 提交后的 AI 预审结果无法进入该页面。
- 修复：补齐两个 Reviewer AI 待审接口，并按页面语义只返回待人工裁决的真实 AI result。
- 数据取舍：待审队列排除已经产生任意人工审核记录的 annotation；因此通过、打回、修订意见、升级争议后都会从队列消失，避免同一条重复进入普通待审。
- 兼容处理：旧批次接口保留；Reviewer 待审队列使用新聚合接口，概览页仍可继续使用旧批次接口。
## 2026-05-30 Reviewer 工作台审核轮次只能前端拼接

- 问题：状态机已经写入 AI 预审和人工复审相关审计，但 Reviewer 工作台右栏时间线仍由前端用 `submittedAt` 和 `aiResult` 临时拼接，无法明确表达“第一轮审核（AI预审）/第二轮审核（人工复审）”。
- 修复：`AnnotationToReviewResponse` 新增 `reviewTimeline`，后端按现有 AI result、AI job 完成时间和最新 human review 生成两阶段时间线。
- 取舍：本次不新增独立 timeline 表，也不新增状态；时间线是状态机审计和业务表的展示聚合。若后续要展示完整审计流水，可再单独实现 `GET /api/reviewer/annotations/{annotationId}/timeline` 读取 `audit_logs`。
## 2026-05-30 `NEED_HUMAN_REVIEW` 展示为“转人工”容易误解

- 问题：Reviewer 待审队列中把 `NEED_HUMAN_REVIEW` 显示为“转人工”，容易被理解成把任务从 AI 后台转派到人工后台的操作。
- 实际语义：该枚举只是 AI 预审结论，表示 AI 不确定或风险较高，需要 Reviewer 人工重点复核；本系统默认所有 AI 结论都会进入人工复审。
- 修复：Reviewer 待审队列和工作台统一改为“人工复核”。
- 兼容：接口枚举值仍保持 `NEED_HUMAN_REVIEW`，不影响后端状态机和统计逻辑。
## 2026-05-30 Labeler 工作概览依赖静态样例

- 问题：`/labeler` 工作概览使用前端硬编码 KPI、审核分布、最近批次和题型样例,无法反映当前 Labeler 的真实提交、打回、奖励预估和审核结果。
- 修复：新增 `GET /api/labeler/overview` 聚合接口,并改造 `LabelerOverview.tsx` 只读取真实接口数据。
- 约束：不保留静态样例作为降级；接口失败时展示错误态和重试入口,无数据时展示真实 0 值或 Empty。
- 验证：`backend/.\mvnw.cmd -q -DskipTests package`、`frontend/npm.cmd run build` 均通过。
## 2026-05-30 Labeler 草稿箱依赖静态样例且删除无后端动作

- 问题：`/labeler/drafts` 草稿箱使用前端硬编码草稿样例,“同步最新草稿”“继续编辑”“删除”均未接入真实草稿列表和删除接口。
- 修复：新增 `GET /api/labeler/drafts` 返回当前 Labeler 自己的真实草稿列表,新增 `DELETE /api/assignments/{assignmentId}/draft` 只删除草稿记录。
- 约束：不保留静态样例作为降级；接口失败时展示错误态和重试入口,无草稿时展示真实空态。
- 删除语义：只删除 `drafts` 表记录,不删除 assignment、annotation 或任务领取状态；草稿不存在但 assignment 校验通过时仍返回成功。
## 2026-05-30 Labeler 打回项依赖静态样例且未区分 AI/人工语义

- 问题：`/labeler/returned` 使用前端硬编码打回项样例,无法反映当前 Labeler 的真实返修任务；同时页面只写“打回”,没有区分 AI `REJECT` 预审建议和 Reviewer 人工复审正式打回。
- 修复：新增 `GET /api/labeler/returned-items`,页面改为真实接口驱动,并提供全部/人工复审打回/AI预打回筛选。
- 语义边界：人工复审打回才是正式 `returned` 状态,允许“修改并重提”；AI预打回只是 `AI_PRE_REJECT` 展示项,annotation 仍待人工复审,按钮禁用为“等待人工复审”。
- 约束：不保留静态样例作为降级；接口失败展示错误态和重试入口,无数据展示真实空态。
## 2026-05-30 答题页翻页按钮没有真正固定到底部

- 问题：上一轮把「上一题/下一题」从顶栏移动到右侧填写卡的表单后方,但短题字段很少时按钮仍紧贴字段显示在卡片上半部分,没有满足“固定在填写卡底部”的交互要求。
- 根因：只给 `.answer-pager` 设置了 `margin-top:auto`,但 Ant Design `Card` 本体和 `Card body` 没有形成可撑满高度的纵向 flex 容器,auto margin 没有可分配的剩余空间。
- 修复：右侧填写卡新增 `answer-form-card`;`.answer-form-card.ant-card` 与 `.answer-form-card.ant-card > .ant-card-body` 均改为纵向 flex,body 设置 `flex:1`,翻页条用 `margin-top:auto` 吸附到底部。
- 约束：只改布局,不改保存、提交、上一题/下一题跳转和状态机。
## 2026-05-30 Owner 数据看板发布公告入口与题目反馈闭环不一致

- 问题：Owner 数据看板右上角仍展示「发布公告」静态入口,但当前业务实际需要 Owner 查看 Labeler 在答题页提交的题目问题；同时 Labeler「报告问题」此前只是前端预留/演示成功,没有形成数据库落库和 Owner 可见闭环。
- 修复：新增 `issues` 表,实现 `POST /api/assignments/{assignmentId}/issues` 真实落库并写 `audit_logs`;新增 `GET /api/dashboard/issue-feedback` 仅返回当前 Owner 名下任务的反馈；OwnerDashboard 将入口改为「题目反馈」并通过 Drawer 展示真实反馈列表。
- 约束：本轮 Owner 只读查看,不做“已处理/忽略”；题目反馈不使用静态样例或演示成功降级。


## 03 答题页:离散字段触发后没有自动跳下一题

### 现象
用户在答题页开启「填完自动下一题」开关后,点击单选题(如「通过/不通过」)选项,题目状态从未答 → 已答,但页面停留在当前题,没有跳到下一题。
出现场景:任意离散字段(单选/多选/tags)作为本题最后一个必填项时复现。

### 根因
两个 `useEffect` 在同一次 `setAnswer` 后并行重跑,执行时序与状态依赖打架:

1. `onChange` 触发后 React 调度同一批更新:`setAnswer(nextAnswer)` + `setDirty(true)`。
2. 两个 effect 都因 `answer / dirty` 变化重跑:
   - 草稿 effect 走「离散立即保存」路径:`setTimeout(0)` 立刻发请求,`saveCurrentDraft` 成功后调 `setDirty(false)`。
   - 自动跳题 effect 也排了一个 `setTimeout(200)` 准备跳题。
3. 草稿请求 200ms 内通常已经返回,`setDirty(false)` 触发自动跳题 effect 再跑一次:
   - 这次 effect 第一行守卫 `if (!dirty) return` 命中,
   - cleanup 函数清掉 200ms 跳题 timer,
   - 自动跳题被静默取消。

简单说:把"用户编辑过当前题"绑在 `dirty` 上,而 `dirty` 在草稿落库后会自然清零,导致跳题 timer 还没触发就被自身的清理函数清掉了。

### 修复

`frontend/src/pages/labeler/AnswerPage.tsx`:

1. 新增 `hasEditedRef = useRef<boolean>(false)`,只在 `LabelHubFormRenderer.onChange` 中设为 `true`,在切到新题(`applyItem`)时重置为 `false`。
2. 自动跳题 effect 的守卫从 `if (!dirty) return` 改为 `if (!hasEditedRef.current) return`。
3. 自动跳题 effect 的依赖数组中移除 `dirty`(改为依赖 `answer`,变更触发不变)。
4. 注释里说明清楚不能复用 `dirty` 的原因,避免后续维护时被"看起来没用的 ref"误删回老路径。

修复后:
- 离散字段:点完即触发,200ms 内自动跳下一题,中间的草稿落库不再影响跳题 timer。
- 连续字段:停止输入后 800ms 跳题,行为不变。
- 必填缺失/校验失败:`collectValidationErrors().length > 0` 仍能拦掉跳题。
- 回到上一题修改:`advancedAssignmentsRef` 已在跳前写入,守卫继续生效,不会重复跳。
- 切到新题:`applyItem` 中 `hasEditedRef.current = false`,新题重新计时。

### 验证
- `frontend/npm run build` 通过。
- 手动复现:开启开关 → 进入只有「通过/不通过」单选的题目 → 点「通过」→ 顶栏闪现「已自动保存,即将进入下一题」→ ~200ms 切到下一题。

### 后续建议
- effect 依赖里如果同时存在「触发条件」与「会被回调清零的状态」,优先把触发条件提到 ref,避免清零让 effect 自我取消。
- 类似模式后续如果出现在 Reviewer 工作台或 Owner 审核页(都有"操作完成 → 自动跳下一条"诉求),可以直接复用本期"hasEditedRef + force 保存"的写法。

## 2026-05-31 AI 预审评分体系混用 5 分制和 100 分制

### 现象

AI 预审规则编辑页、默认规则种子、Agent JSON Schema、前端评分条和样例数据仍混用旧 5 分制:维度 `maxScore=5`,默认 PASS 阈值 `4.0`,NEED_HUMAN 阈值 `3.0`。用户要求每一项指标满分改为 100 分,默认 PASS 阈值 80,默认 NEED_HUMAN 阈值 70。

### 根因

评分口径分散在数据库迁移、后端规则校验、Agent 输出 schema、Owner 规则编辑页、AI Reviewer 队列展示、Reviewer 工作台色调和 sample JSON 中。此前只在局部按 5 分制实现,没有统一的 100 分制常量和迁移脚本。

### 处理

1. 后端新增 `V14__ai_review_score_scale_100.sql`,将现有规则维度、旧阈值、旧 job snapshot 和明确来自 5 分制 snapshot 的历史结果迁移到 100 分制。
2. `AiReviewService` 强制新规则维度 `maxScore=100`,默认阈值为 80 / 70,并校验 Agent 回写 `scores` 和 `totalScore` 不超过 100 分制上限。
3. Agent Responses JSON Schema 约束维度分数和 `totalScore` 均为 0~100。
4. Owner / AI Reviewer / Reviewer 前端展示全部改为 100 分制口径。
5. `ai-review-rules.json` 与 `ai-review-jobs.json` 样例改为 100 分制。

### 约束

- 本轮不新增表,只新增迁移脚本和代码校验。
- `V14__ai_review_score_scale_100.sql` 需要通过 Flyway 或手动执行后才会影响已有数据库数据。
- 接口枚举值 `PASS / NEED_HUMAN_REVIEW / REJECT` 不变。



## 2026-05-31 任务截止后状态与标注权限未闭环

### 现象

Owner 任务管理页面中任务已经超过截止时间,但仍显示“发布中”。Labeler 任务广场原先直接过滤掉已截止任务,既不能展示“已截止”,也没有对截止时未完成的 assignment 做真实结算。

### 根因

任务状态机只支持 Owner 手动流转,没有“截止时间到达”触发的系统结算动作。任务广场 SQL 还额外过滤 `deadline >= CURRENT_TIMESTAMP`,导致已截止任务不可见。草稿、未完成 assignment、item 可再次认领等状态没有统一收口。

### 处理

1. 新增截止结算服务:自动扫描过期任务,有效草稿自动提交,无效草稿/无草稿作废 assignment 并释放 item。
2. 任务状态机允许系统执行截止结算,并允许 Owner 将 `ended` 任务续期发布为 `published`。
3. 任务广场返回已截止任务的展示字段 `expired / claimable / statusLabel`,前端显示“已截止”且不可领取。
4. Owner 任务管理对 ended 任务提供“续期发布”入口,要求设置未来截止时间。
5. 新增 V15 迁移,允许被截止释放的同一题在续期后被同一 Labeler 再次认领。

### 约束

- 有效草稿必须通过完整 schema 校验才会自动提交。
- 未完成认领使用 `voided` 保留审计链,不物理删除 assignment。
- 续期发布复用原任务和剩余 pending item,不自动生成新任务。



## 2026-05-31 Reviewer AI 预审裁决按钮存在伪成功和语义缺口

### 现象

Reviewer AI 预审工作台底部「打回 / 直接修订 / 通过·入库」按钮中,前端在接口失败时仍会移除条目并提示成功。打回提交的是 `note`,而后端正式必填字段是 `reason`,导致填写了审核意见也可能被后端判定缺少打回原因。「直接修订」后端原先只写 `human_reviews(decision='revise')`,没有真正修改答案并入库。

### 根因

前端保留了演示模式吞错逻辑,后端接口字段和前端 payload 未完全对齐;`REVISE` 没有接收修订后的 `answerJson`,也没有生成新的 accepted revision。按钮文案“审核员就地改写并入库”与真实后端行为不一致。

### 处理

1. 后端裁决接口兼容 `reason/note`,打回继续强制原因必填。
2. `REVISE` 改为要求 `answerJson`,基于原 annotation 的 schema snapshot 校验答案,无变化返回 `NO_REVISION_CHANGE`。
3. `REVISE` 成功后原 annotation 标记 `revised`,写入 diff,并创建新的 latest accepted revision,assignment/item 同步 accepted。
4. Reviewer 工作台移除样例降级和伪成功,接口失败时保留条目并展示真实错误。
5. 前端直接修订使用 Drawer + `LabelHubFormRenderer`,批量打回要求统一原因。

### 约束

- 本轮不新增数据库表。
- 直接修订不重跑 AI 预审。
- 缺少 `schemaFields` 的历史/异常数据不能直接修订,前端禁用按钮。
## 2026-05-31 人工复审打回项受任务截止锁定

### 现象

Labeler 在 `/labeler/returned` 点击人工复审打回项的“修改并重提”后进入答题页,如果原任务已经到截止时间并被结算为 ended,页面会显示锁定查看模式,无法保存草稿或提交返修。

### 根因

答题页后端权限统一使用任务 deadline 和 `task.status = published` 作为硬门禁。`assignment.status = returned` 没有独立返修权限,因此正式人工打回项也被任务截止规则拦截。

### 处理

1. 为 `assignments` 增加 `resubmit_deadline`。
2. Reviewer 人工复审 `RETURN` 时写入 `now + 48 hours`。
3. `returned` 且返修窗口内的 assignment 允许保存草稿和单题重提,即使任务已 ended。
4. 截止结算不再作废或释放人工 returned assignment。
5. 打回项页面显示返修截止时间,过期后禁用按钮并提示“返修已过期”。

### 约束

- 不让 returned 项永久绕过任务截止。
- AI 预打回仍只是建议,不允许直接重提。
- 返修窗口过期后只锁定,不释放 item,避免破坏人工审核链路。
## 2026-05-31 打回返修页按钮语义不清且答题页缺少返修提交入口

### 现象

人工复审打回项列表按钮显示“修改并重提”,但点击后只是进入编辑页;进入答题页后,如果该 returned 题不是任务最后一题,底部主操作按钮不显示,红框区域为空,用户无法直观看到“重新提交”入口。

### 根因

答题页主按钮显示条件仍沿用普通批量提交逻辑:仅最后一题显示提交按钮。返修重提已经是单题提交语义,但 UI 没有为 returned 返修场景单独放开主按钮。

### 处理

1. 打回项列表中人工复审打回的动作文案改为“立即修改”。
2. 返修题答题页主按钮改为“重新提交”。
3. `returned + resubmitDeadline` 场景下无论是否最后一题都显示主按钮。
4. 点击“重新提交”继续走单题提交接口并进入 AI 预审或人工审核流。

### 约束

- AI 预打回仍不可操作。
- 普通未打回任务仍保持最后一题提交和批量提交逻辑。



## 2026-05-31 返修提交按钮位置不符合预期且普通入口也显示

### 现象

人工复审打回项进入答题页后,“重新提交”按钮出现在顶部操作区,而不是答题卡底部上一题和下一题之间的红框位置。同时页面只根据 `returned + resubmitDeadline` 判断是否显示返修提交,导致从“我的任务”等普通入口进入同一个 returned assignment 时也会显示“重新提交”。

### 根因

前端缺少“从打回项立即修改进入”的入口上下文,把返修提交当成 assignment 状态的全局按钮处理。答题卡底部导航也只有上一题和下一题两个按钮位置,没有中间操作区。

### 处理

1. `/labeler/returned` 点击“立即修改”时通过 React Router state 传入 `entry='returned-rework'` 和当前 assignmentId。
2. `AnswerPage` 只有在当前 returned assignment 与入口 state 匹配时才显示“重新提交”。
3. “重新提交”移动到 `.answer-pager` 中间区域,顶部不再展示 returned 返修提交按钮。
4. 上一题、下一题和进度点跳转不携带返修入口 state,避免其他题继承该按钮。

### 约束

- 后端返修窗口和 `POST /api/assignments/{assignmentId}/submit` 状态流不变。
- 页面刷新后入口 state 丢失,需要重新从打回项进入才显示返修提交入口。



## 2026-05-31 Reviewer 工作台题号误用 annotationId 且返修历史审计不可见

### 现象

Reviewer AI 工作台详情标题红框处显示 `88 · 测试agent`,但该数字实际是 `annotationId`,不是任务内题目序号。Labeler 答题页同一题显示为第 15 题,两端题序不一致。返修后 Reviewer 工作台右侧“处理日志 / 审计”只显示第二轮 AI 预审和待人工复审,第一轮 AI 预审和人工打回链路没有展示。

### 根因

Reviewer `AnnotationToReviewResponse` 没有返回任务内题序字段,前端只能展示 `annotationId`。`reviewTimeline` 后端原先只按当前 latest annotation 构造两阶段时间线,没有查询同一 assignment 的历史 revision。

### 处理

1. `AnnotationToReviewResponse` 新增 `itemIndex`,前端主标题改为“第 N 题 · 任务名”。
2. `reviewTimeline` 改为 assignment 级多轮审计,展示每轮 AI 预审和人工复审。
3. 左侧卡片 AI 分数旁显示“第 N 轮”胶囊。
4. `/api/ai-review/jobs` 同步返回 `itemIndex/itemTotal`,AI Reviewer Job 队列可展示真实题序。

### 约束

- 不修改状态机或 annotation revision 生成逻辑。
- `annotationId` 仍作为裁决接口入参和调试标识保留,不再作为用户主标题题号。



## 2026-05-31 AI Reviewer 处理日志只显示当前轮且日志撑高容器

### 现象

AI Reviewer 预审队列选中返修后的第二轮 job 时,右侧“处理日志 / 审计”只显示当前 job 的 queue / llm / verdict 或 error,第一轮 AI 预审历史不可见。日志条目变多时也会直接撑高右侧卡片,不便查看。

### 根因

前端原先使用当前 `AiReviewJob` 字段临时拼接时间线,没有向后端查询同一 assignment 下的历史 annotation revision。日志容器也没有独立滚动区域。

### 处理

1. 新增 `GET /api/ai-review/jobs/{jobId}/timeline`,通过 job 定位 assignment,返回同一题所有非 voided revision 的 AI job 生命周期事件。
2. AI Reviewer 预审队列选中 job 后请求真实 timeline,优先渲染后端多轮日志。
3. timeline 请求失败时只提示错误并展示当前 job 最小日志,不伪造历史轮次。
4. “处理日志 / 审计”增加内部滚动容器,避免日志数量撑高页面。
5. 移除该页面列表接口失败后的样例数据降级。

### 约束

- 不新增数据库表、不改变 AI 预审状态机。
- 这里只展示 AI job 生命周期,人工复审历史仍在 Reviewer 工作台审计时间线中查看。



## 2026-05-31 AI Reviewer 预审题目卡片缺少轮次标识

### 现象

AI Reviewer 预审队列左侧题目卡片只显示任务、题号、AI 决策、分数和时间。返修重提后同一题会进入第二轮 AI 预审,但卡片没有显示“第几轮”,用户需要点进日志才能判断当前 job 属于首轮还是返修轮。

### 根因

`GET /api/ai-review/jobs` 没有返回当前 job 关联 annotation 的 `revision_no`,前端只能展示题目序号,无法展示审核轮次。

### 处理

1. `AiReviewJobResponse` 新增 `roundNo`,来源于 `annotations.revision_no`。
2. AI Reviewer 预审队列左侧所有题目卡片显示“第 N 轮”胶囊。
3. 右侧详情头部同步显示轮次胶囊,保持选中项与详情一致。
4. 缺少 `roundNo` 时不展示轮次兜底,避免误导。

### 约束

- 不修改状态机、不新增数据库表或迁移。
- 轮次口径固定为 annotation revision,不是人工复审次数。



## 2026-05-31 AI 预审高分 REJECT 且评分对象混淆

### 现象

AI 预审出现 `totalScore=97.75` 但 `decision=REJECT` 的结果。按当前规则阈值,例如 `PASS=90`、`NEED_HUMAN=70`,该分数应对应 `PASS`,但日志显示为建议打回。

### 根因

1. 规则 Prompt 没有明确要求分数只评价“标注员提交的标注答案质量”,模型可能把原始回答质量和标注员判断质量混在一起。
2. 后端 complete 接口保留 LLM 自裁决语义,直接保存 LLM 回传的 decision,不会按阈值覆盖结果。
3. Agent 固定追加提示只说明 0~100 和加权总分,没有强制 decision 与阈值一致。

### 处理

1. Agent 固定追加提示补充评分对象和阈值一致性要求。
2. Agent 默认 Prompt 改为审核标注答案质量,不是评价原始数据或模型回答本身。
3. Owner 规则编辑页新建规则默认填入更清晰的 Prompt 模板。
4. Owner 规则编辑页提示 Prompt 评分对象和 decision/阈值一致性。
5. 后端发现 `totalScore` 与 `decision` 按阈值明显不一致时记录 warning,但不覆盖 LLM 输出。

### 约束

- 最终 decision 仍由 LLM 输出,本轮不改为后端强制派生。
- 不新增数据库表或迁移。
- 既有规则记录不会被自动改写,需要在 Owner 规则页手动替换 Prompt 后保存。
## 2026-05-31 续期任务最后提交被历史不可编辑 assignment 卡住

### 现象

Owner 将已截止任务续期发布后，Labeler 领取剩余题并标注到最后一题，点击普通“提交”时后端返回 `ASSIGNMENT_NOT_EDITABLE`，页面表现为续期后的剩余题无法完成提交。

### 根因

最后一题普通提交走 `POST /api/tasks/{taskId}/assignments/submit` 整任务批量提交。续期任务下，同一 Labeler 在同一 task 中可能既有历史 `submitted / ai_reviewing / reviewing / accepted` assignment，也有本轮新领取的 `claimed` assignment。原后端批量提交遍历该 Labeler 在任务下所有非 voided assignment，并对每条调用可编辑校验，遇到历史已提交或审核中题目就报 `ASSIGNMENT_NOT_EDITABLE`。

### 处理

1. 批量提交只处理 `claimed` 和返修窗口内 `returned`。
2. `submitted`、`ai_reviewing`、`reviewing`、`accepted`、`exported` 在批量提交中直接跳过。
3. `returned` 返修窗口过期仍返回 `RETURN_REWORK_EXPIRED`。
4. `claimed` 缺草稿或答案不完整仍返回 `BATCH_SUBMIT_INCOMPLETE`。
5. 本次没有新题需要提交时返回成功，`submittedCount=0`，前端提示“没有新的题目需要提交，已提交的题目正在审核中”。

### 约束

- 不重复提交已进入审核流或已通过的历史 annotation。
- 不改变截止结算、续期发布、返修窗口和单题提交状态机。
## 2026-05-31 单 Agent AI 审核只能串行执行

### 现象

AI Reviewer Job 队列中即使存在多条 pending job，单个 Agent 实例也只会一次领取并处理一条。页面通常只能看到一条 `running`，其余 job 需要等待当前 LLM 调用完成后才会继续执行。

### 根因

后端 `claim-next` 和 `runToken` 已经具备多 worker 安全领取与回写基础，但 Agent 端原先使用单个 `@Scheduled pollOnce()`，每次同步 `claimNext -> LLM review -> complete/fail`，没有内置 worker 池。

### 处理

1. 模型配置表新增 `worker_concurrency`，默认 3，合法范围 1-10。
2. 模型配置接口和 runtime 配置返回 `workerConcurrency`。
3. Agent 启动时读取后端 active 模型配置中的并发数，创建固定数量后台 worker。
4. 无 active 模型配置时，Agent 使用 `LABELHUB_AGENT_CONCURRENCY` 兜底。
5. 后端领取 pending job 使用 `FOR UPDATE SKIP LOCKED`，降低并发领取时的锁等待。
6. 前端模型配置页支持设置和展示“Agent 并发数”。

### 约束

- 并发数修改后需要重启 Agent 生效。
- 不引入消息队列，不改变 AI job 状态机和 runToken 回写校验。
## 2026-06-01 模型配置页面多配置接口未落地

### 现象

模型配置页面前端已经调用 `/api/ai-review/model-configs` 多配置接口，但后端只实现了旧的 `/api/ai-review/model-config` 单 active 配置接口。页面加载时新接口失败，进入“兼容模式 · 旧接口”，导致新建、复制、启用、删除等多配置能力不是真实闭环。

### 根因

前端多配置管理页面先于后端完整接口完成，后端缺少列表、创建、指定配置更新、删除和激活接口，只能通过旧单配置接口读写当前 active 配置。

### 处理

1. 新增 `/api/ai-review/model-configs` 多配置 Controller。
2. 补齐列表、新建、更新、删除、启用接口。
3. 复用 `ai_model_configs.status` 表示 active / inactive。
4. 创建配置要求 API Key；更新配置可留空沿用原密钥。
5. active 配置禁止删除，避免 Agent runtime 配置被误删。

### 约束

- 旧 `/api/ai-review/model-config` 兼容接口继续保留。
- 不新增数据库表，不改变 Agent runtime 读取 active 配置的语义。

## 2026-06-01 模型配置推理强度枚举与产品期望不一致

### 现象

模型配置页和后端原先保留了 `minimal / low / medium / high`，但当前产品期望是 `low / medium / high / xhigh`。历史配置里如果还存在 `minimal`，编辑页也会显示得不统一。

### 处理

1. 前端枚举收敛为 `low / medium / high / xhigh`。
2. 后端保存校验同步收敛到新四档。
3. 历史 `minimal` 在读回时兼容映射为 `low`，避免老数据影响编辑。

### 约束

- 不新增数据库迁移。
- 旧数据里的 `minimal` 仅做兼容读取，不再作为新保存值。

## 2026-06-01 Owner 数据看板除题目反馈外依赖样例数据

### 现象

Owner `/owner/dashboard` 除「题目反馈」外的主看板接口此前只有前端调用层和样例兜底。后端不可达或接口未实现时,页面会展示 `sampleDashboard` 并显示演示模式,Owner 容易误以为这些 KPI、任务进度、审核分布和争议数据是真实统计。

### 根因

数据看板前端先完成视觉和 API 预留,但后端缺少 `/api/dashboard/overview`、`task-progress`、`review-distribution`、`labeler-performance`、`submission-timeline`、`recent-activities`、`role-breakdown`、`disputes` 这些聚合接口。

### 处理

1. 新增 Owner Dashboard 聚合 Controller / Service / Repository。
2. 所有接口仅统计当前登录 Owner 名下、未软删除任务。
3. 查询前触发任务截止结算,避免过期任务状态滞后。
4. 前端移除主看板 `sampleDashboard` 和演示模式 Tag。
5. 主接口失败时显示 Alert + 重试,不再回落样例数据。
6. 争议卡接入真实 7/14/30 日数据,并展示 `samplingRatio` 与 `consistencyRate`。

### 约束

- 本轮不新增数据库表或迁移。
- 当前聚合为实时 SQL 直查;后续数据量大时需要补充报表索引或汇总表。
