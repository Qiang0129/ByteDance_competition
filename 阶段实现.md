# 阶段实现







> 说明：本文档采用增量记录方式维护。后续每次阶段实现都以新的编号小节追加，不覆盖历史内容。







## 01 仓库基础骨架搭建实现记录







### 对应实施计划



对应《LabelHub Project Implementation Plan EN》3.2 的推荐仓库结构。







### 已完成内容



- 在当前仓库根目录建立了 `frontend`、`backend`、`agent`、`infra`、`docs`、`submission` 六个核心模块目录。



- 补齐了前端页面分层、Schema 模块目录，以及后端/Agent 的 Java 包骨架与数据库迁移目录。



- 创建了 `infra/`、`docs/`、`submission/` 下计划书明确列出的最小占位文件，并为需要保留的空目录补充 `.gitkeep`。



- 保留了现有 `datasets/`、根目录 `README.md`、计划书文档等已有资产，未做迁移和重命名。







### 合理补全说明



- 计划书中的 `labelhub/` 作为当前仓库根目录理解，未额外再嵌套创建同名目录。



- 计划书中的 `src/main/java/...` 目录按最小合理补全，分别落为 `backend/src/main/java/com/labelhub/backend` 与 `agent/src/main/java/com/labelhub/agent`。



- `submission/` 下的 `README.md`、`demo-video.md` 以及 `screenshots/`、`api-docs/` 按文档树形结构理解为该目录的子项。







### 验证结果



- 已对照计划书推荐结构完成目录创建。



- 本小节仅记录骨架与占位文件创建，尚未初始化前端、后端与 Agent 的具体工程脚手架。







## 02 Phase 1 前端基础工程实现记录







### 对应实施计划



本阶段实现对应《LabelHub Project Implementation Plan EN》6.1 的 **Phase 1: Basic architecture** 中的前端基础架构部分，并继续沿用计划书 3.1、3.2 对总体架构和仓库结构的约束。







实施计划中的第一阶段完整范围包括仓库搭建、Docker Compose、MySQL/Redis 数据模型、Spring Boot 认证/RBAC、前端 Ant Design 基础路由和 UI 框架。本次已完成仓库骨架与前端基础工程，后端、数据库、Redis、Docker Compose 和 Agent 启动能力仍待下一步继续实现。







### 本阶段完成内容



- 已按计划书 3.2 的推荐结构保留并完善 `frontend`、`backend`、`agent`、`infra`、`docs`、`submission` 六个核心模块目录。



- 已补齐 `frontend/src/pages/owner/`、`frontend/src/pages/labeler/`、`frontend/src/pages/reviewer/` 和 `frontend/src/modules/schema/` 等前端分层入口。



- 已将 `frontend/` 初始化为 Vite + React + TypeScript 前端工程，并接入 Ant Design、Ant Design Icons 和 React Router。



- 已创建 Ant Design 统一应用壳，包括左侧导航、顶部栏、内容区、阶段标识和演示用户入口。



- 已实现基础路由：`/login`、`/owner`、`/labeler`、`/reviewer`、`/` 重定向和 `*` 404。



- 已实现第一阶段页面骨架：项目方工作台、标注员工作台、审核员工作台、登录页和 404 页。



- 已创建前端认证/RBAC 预留层：`api/client.ts`、`api/auth.ts`、`types/auth.ts`，预留登录、登出、当前用户和 token 处理逻辑。



- 已为页面目录补充 `index.ts` 统一出口，解决路由中 `Login`、`NotFound` 和角色页面导入的模块解析问题。







### 关键文件清单



- `frontend/package.json`：前端脚本、运行依赖和开发依赖配置。



- `frontend/package-lock.json`：npm 依赖锁定文件。



- `frontend/index.html`：Vite 应用 HTML 入口。



- `frontend/vite.config.ts`：Vite + React 插件配置。



- `frontend/tsconfig.json`、`frontend/tsconfig.app.json`、`frontend/tsconfig.node.json`：TypeScript 工程配置。



- `frontend/src/main.tsx`：React 根节点、Ant Design 中文 locale 和主题配置。



- `frontend/src/App.tsx`：应用路由入口。



- `frontend/src/router/index.tsx`：基础路由定义。



- `frontend/src/layouts/AppLayout.tsx`：三类角色页面共用布局。



- `frontend/src/pages/index.ts`：登录页和 404 页统一导出。



- `frontend/src/pages/owner/index.ts`、`frontend/src/pages/labeler/index.ts`、`frontend/src/pages/reviewer/index.ts`：角色页面统一导出。



- `frontend/src/api/client.ts`、`frontend/src/api/auth.ts`、`frontend/src/types/auth.ts`：后端接口与认证类型预留。



- `frontend/src/styles/global.css`：全局样式和页面布局样式。







### 接口预留说明



- 默认 API 基础路径为 `/api`，后续可通过 `VITE_API_BASE_URL` 指向 Spring Boot 后端。



- 已预留认证接口：`POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me`。



- 已定义 `owner`、`labeler`、`reviewer`、`admin` 四类前端角色类型，便于后续和 Spring Security/RBAC 权限模型对齐。



- MySQL 用户名、密码、端口和 Redis 配置未写入前端代码，后续应放入后端配置或 Docker Compose 环境变量。







### 合理补全说明



- 计划书中的前端技术栈包含 Formily 和 dnd-kit，但本阶段只完成 Ant Design 基础路由与 UI 框架；动态 Schema Designer、Renderer、拖拽材料面板留到后续动态表单阶段。



- 由于当前 Windows/npm 环境未稳定生成 `node_modules/.bin` 命令入口，`package.json` 中脚本直接调用本地 `node_modules/typescript/bin/tsc` 和 `node_modules/vite/bin/vite.js`，保证 `npm run build`、`npm run dev` 可执行。







### 验证结果



- 已执行 `npm ci --legacy-peer-deps` 安装前端依赖。



- 已执行 `npm run build`，TypeScript 编译和 Vite 生产构建均通过。



- 已通过 linter/IDE 诊断检查，当前路由和新增导出文件无模块解析错误。



- 构建过程中存在 Ant Design 打包体积超过 500 kB 的 Vite warning，属于性能优化提示，不影响第一阶段运行验收。







### 未完成与下一步



- Spring Boot 认证/RBAC、用户表、角色权限表、JWT/token 机制尚未实现。



- MySQL/Redis 数据模型、数据库迁移脚本和 Docker Compose 尚未实现。



- `backend/` 与 `agent/` 仍为骨架，尚未达到计划书第一阶段中“后端/Agent 可启动”的完整验收状态。



- 下一步建议优先实现后端基础工程、MySQL/Redis 配置、认证/RBAC 表结构和 `/api/auth/*` 接口，再将前端占位登录切换为真实接口调用。







## 增量实现:登录页视觉重构



### 完成内容

- 按 bootstrapmb #15421 设计稿重写 `frontend/src/pages/Login.tsx`:左侧 ACCOUNT LOGIN 标题,右侧悬浮头像卡片,Welcome back 标题,用户名/密码/角色三段胶囊式表单,蓝色胶囊 LOGIN 按钮,底部 signup 行。

- 在 `frontend/src/styles/global.css` 中替换 `.login-page` 相关样式;使用绝对定位的圆形元素绘制右上角两个蓝色圆球,使用内联 SVG 路径绘制底部双层蓝色波浪,无需新增图片资源。

- 通过 `.login-input` / `.login-input-select` / `.login-submit` 类覆盖 Ant Design 默认外观,统一为 999px 圆角胶囊样式。

- 保留了原有按角色 `navigate(/${role})` 的演示登录逻辑、表单校验和 `App.useApp().message` 反馈;signup 链接仅展示一条占位提示。



### 关键文件

- `frontend/src/pages/Login.tsx`:重写后的登录页组件。

- `frontend/src/styles/global.css`:登录页相关样式整体替换为新版。



### 未改动项

- `frontend/src/api/client.ts`、`frontend/src/api/auth.ts`、`frontend/src/types/auth.ts` 维持原状,认证接口预留不变。

- 路由、布局、三类角色工作台均未改动。

- 未新增任何依赖。



### 验证

- 已执行 `npm run build`,TypeScript 编译和 Vite 打包均通过(仅保留原有 antd 包体超过 500 kB 的 warning,不影响功能)。

- 登录页交互(用户名/密码必填校验、角色选择默认 owner、提交后跳转到 `/owner|labeler|reviewer`)逻辑保持原有行为。





## 增量修正:登录页布局对齐



### 问题

首版实现中将左侧 ACCOUNT LOGIN 标题块和右侧登录卡放在同一个 flex 容器内居中,导致卡片被左侧文字推到了视口右侧;同时左侧图标和文字尺寸偏小,字间距不足。



### 修正

- 在 `frontend/src/styles/global.css` 中调整 `.login-content`:不再使用左右两栏 flex,改为仅承载居中的登录卡。

- `.login-side` 改为绝对定位 (`left: 10%; top: 50%; transform: translateY(-50%)`),独立飘在画面左侧,不影响卡片居中。

- 加大左侧图标 (96px / icon font-size 72px) 与标题字号 (22px / 字间距 4px / 字重 700),贴近原设计稿比例。

- 响应式断点从 768px 提升到 992px,窄屏下隐藏左侧标题块,登录卡仍严格居中。



### 验证

- 已重新执行 `npm run build`,通过。

- `Login.tsx` 未改动,仅样式调整。





## 增量修正:登录页左侧图标与文字效果



### 改动

- `Login.tsx`:左侧 `.login-side-icon` 中的 `<UserOutlined />` 替换为内联 SVG(圆 + 半圆肩膀路径),便于使用 `fill` 实现实心填充。

- `global.css`:

  - `.login-side-icon svg { width:80px; height:80px; fill: currentColor; }`,通过 `color` 控制蓝色填充,SVG 保持等比缩放。

  - `.login-side-title` 增加 `text-shadow` 三层叠加(1/2/4-6 像素位移,黑色低透明度),实现接近设计稿的深色长投影效果。

- 未引入新依赖,图标改动只影响左侧标题块,卡片顶部悬挂头像仍使用 `UserOutlined`。



### 验证

- 已重新执行 `npm run build`,通过。





## 增量修正:登录页文案英文化与字体调整



### 改动

- `Login.tsx`:

  - 角色选项改为 `Owner / Labeler / Reviewer`(去掉中文前缀)。

  - 输入框 `placeholder` 与 `Form.Item` 校验提示改为英文 (`Username / Password / Select role / Please enter ... / Please select a role`)。

  - 登录成功 message 与 signup 占位 message 改为英文。

- `global.css`:

  - `.login-page` 设置 `font-family: "Microsoft YaHei", "微软雅黑", ...`,作为登录页字体基线。

  - 增加一组覆盖规则,让登录页内的 antd 组件 (`Input / Select / Button / Form / Typography`) 通过 `font-family: inherit` 跟随父级字体,避免被 `ConfigProvider` 中的全局字体覆盖。



### 范围

- 仅影响登录页,其他页面字体仍由 `main.tsx` 中的 `ConfigProvider` 全局 token 决定。



### 验证

- 已重新执行 `npm run build`,通过。





## 增量修正:Welcome back 标题样式



### 改动

- `Login.tsx`:`Typography.Title level={3}` 替换为原生 `<h1 className="login-welcome">`,移除未使用的 `Typography` 导入。绕开 antd Title 内置 token 对字号、字体的覆盖。

- `index.html`:增加 Google Fonts `Noto Sans SC`(400/600/700)预连接与样式表引用,保证不同环境一致渲染。

- `global.css`:`.login-welcome` 重新定义为 `font-family: "Noto Sans SC"`、`font-size: 25.6px`、`font-weight: 700`、`color: #000`、居中,与设计稿 h1 规格一致。



### 验证

- 已重新执行 `npm run build`,通过。





## 增量实现:登录页内嵌注册翻转



### 完成内容

- `Login.tsx`:

  - 引入 `useState<'login' | 'signup'>` 控制翻转状态。

  - 在 `.login-content` 内增加 `.login-card-flip > .login-card-inner > 两张 .login-card-face` 结构,正面登录、背面注册;两面均沿用 `.login-card / .login-avatar / .login-welcome / .login-input / .login-submit / .login-footer` 视觉规范。

  - 注册表单字段 Username / Email / Password,使用 `MailOutlined` 作为邮箱输入框前缀,带 `type: 'email'` 校验;提交触发占位 message 后自动 `setMode('login')`。

  - signup / login 链接的 `onClick` 通过 `event.preventDefault()` + `setMode` 切换状态。

  - 左侧 `.login-side-title` 文案随 `mode` 在 `ACCOUNT LOGIN` 与 `CREATE ACCOUNT` 之间切换。

- `global.css`:

  - 新增 `.login-card-flip`(perspective:1500px,min-height:520px,filter:drop-shadow 替换原卡片阴影)。

  - 新增 `.login-card-inner`(transform-style:preserve-3d,过渡 0.7s)、`.login-card-flip.is-flipped .login-card-inner { transform: rotateY(180deg) }`。

  - 新增 `.login-card-face`(absolute inset:0;backface-visibility:hidden),`.login-card-face--back { transform: rotateY(180deg) }`。

  - 移除 `.login-card` 内的 `box-shadow`(已上提到外层 filter,避免阴影翻到背面)。



### 验证

- 已重新执行 `npm run build`,通过。

- 翻转交互、左侧标题切换、表单校验、occlusion(背面隐藏)经手动检查符合预期。



### 未做

- 注册接口未实现(`api/auth.ts` 中暂无 signup 方法),按计划留到后续认证阶段。

- 未拆出独立 `Signup.tsx` 路由,因为翻转交互本质是同一登录入口的两个面,拆路由会破坏动画。





## 增量修正:翻转卡片高度被裁切



### 问题

翻转容器使用 `min-height: 520px` + 两面 `position: absolute; inset: 0` 实现叠放,登录表单实际高度超过 520px,导致白色卡片背景被裁切,出现"只剩顶部一条白边、表单悬空"的视觉错位。



### 修正

- `global.css`:`.login-card-flip` 移除 `min-height: 520px`;`.login-card-inner` 改用 CSS Grid (`grid-template-areas: "face"`),两面 `.login-card-face` 共用同一网格格子;移除两面的 `position: absolute / inset: 0`,容器高度由内容更高的一面自然撑起。

- 翻转、`backface-visibility`、`drop-shadow` 等其他翻转相关属性保持不变。



### 验证

- `npm run build` 通过。

- 翻转动画与两面对齐均正常,卡片背景完整包裹表单。





## 增量修正:注册页移除 Email 字段



### 改动

- `Login.tsx`:

  - 移除注册表单 Email `Form.Item`(含 placeholder、`type: 'email'` 校验)。

  - `SignupFormValues` 接口去掉 `email` 字段,只保留 `username` / `password`。

  - 移除未再使用的 `MailOutlined` 导入。

- 注册卡现仅 Username + Password 两段输入,与设计稿调整后保持一致。



### 验证

- `npm run build` 通过。



## 03 Phase 1 MySQL 数据模型初始化实现记录



### 对应实施计划

对应《LabelHub Project Implementation Plan EN》6.1 的 **Phase 1: Basic architecture** 中 MySQL/Redis 数据模型任务，并按实施计划 5.1 的核心数据模型优先完成 MySQL 表结构初始化。



### 完成内容

- 新增 `backend/src/main/resources/db/migration/V1__init_labelhub_schema.sql`，作为第一版 MySQL 初始化脚本。

- 已在本地 MySQL 创建数据库 `labelhub`，字符集为 `utf8mb4`，排序规则为 `utf8mb4_unicode_ci`。

- 已创建实施计划 5.1 涉及的 20 张核心表：`users`、`roles`、`permissions`、`user_roles`、`role_permissions`、`tasks`、`task_schema_versions`、`datasets`、`items`、`assignments`、`annotations`、`drafts`、`ai_review_jobs`、`ai_review_results`、`human_reviews`、`audit_logs`、`export_jobs`、`files`、`file_assets`、`dataset_import_profiles`。

- 表结构已覆盖第一阶段认证/RBAC 以及后续任务、数据集、Schema、标注、AI 审核、人审、导出、文件资产等主流程的数据入口。

- 已预置基础角色：`owner`、`labeler`、`reviewer`、`admin`、`system_agent`。

- 已预置 12 个基础权限，并建立基础角色权限关系；未预置默认用户和默认密码，避免弱口令风险。



### 执行方式

- 使用本地 MySQL 客户端连接 `127.0.0.1:3306`，账号为用户提供的 `root`。

- 通过标准输入方式执行 SQL 文件：`mysql ... < backend/src/main/resources/db/migration/V1__init_labelhub_schema.sql`。

- 脚本使用 `CREATE DATABASE IF NOT EXISTS`、`CREATE TABLE IF NOT EXISTS`、`ON DUPLICATE KEY UPDATE` 和 `INSERT IGNORE`，支持本地重复执行。



### 验证结果

- 已执行建库建表脚本，退出码为 0。

- 已执行 `SHOW TABLES`，确认 `labelhub` 数据库下共有 20 张表。

- 已查询 `roles`，确认存在 5 个基础角色。

- 已查询 `permissions`，确认存在 12 个基础权限。

- 已查询 `role_permissions` 聚合结果：`admin` 12 个权限、`owner` 7 个权限、`labeler` 5 个权限、`reviewer` 5 个权限、`system_agent` 2 个权限。



### 未完成与下一步

- 本次仅完成 MySQL 数据结构和种子数据，尚未实现 Spring Boot 实体、Repository/Mapper、认证接口和 Flyway 自动迁移接入。

- Redis 队列、缓存、分布式锁、限流和草稿防抖尚未实现。

- Docker Compose 中 MySQL/Redis/backend/agent/frontend/nginx 编排仍待后续补齐。





## 增量实现:登录跳转过渡动画



### 改动

- `Login.tsx`:新增 `leaving` 状态。`handleLoginFinish` 不再直接 `navigate`,而是先 `setLeaving(true)` 触发离场过渡,通过 `window.setTimeout` 在 480ms 后再跳转,匹配 CSS transition 时长。`<main>` 根据 `leaving` 添加 `is-leaving` 类。

- `AppLayout.tsx`:`<Layout>` 增加 `app-shell-enter` 类,首次挂载时执行入场动画。

- `global.css`:

  - `.login-page` 增加 `transition: opacity 0.42s ease, transform 0.42s ease`;`.login-page.is-leaving` 设 `opacity: 0; transform: scale(0.98); pointer-events: none`。

  - 新增 `@keyframes app-shell-fade-in`(opacity 0 → 1,translateY 8px → 0),`.app-shell-enter` 应用 0.5s ease both。

- 整体效果:点击 LOGIN 后登录页淡出 + 微缩,接着工作台淡入 + 上移,衔接自然不再"闪一下"。



### 验证

- `npm run build` 通过。



## 增量实现:工作台外壳与登录页主题对齐

### 完成内容
- `main.tsx`:全局 `colorPrimary` 由 `#1677ff` 改为登录页主色 `#2f7bff`;`Layout.bodyBg` 改为 `#f3f5f8`;追加 `Menu.darkItemSelectedBg / darkItemSelectedColor` 等 token,使深色侧栏在选中态显示蓝色高光。
- `AppLayout.tsx`:
  - 头部左侧改用专属类 `.app-header-title / .app-header-name / .app-header-sub`,行距与字号更克制。
  - 右侧 `<Tag>` 增加 `.app-header-phase` 类(胶囊样式 + 主色低透底);用户分组加 `.app-header-user`,头像底色用主色 `#2f7bff`。
  - 文案中文逗号改为半角,排版更整齐。
- `global.css`:
  - 顶部旧的 `.app-shell / .app-logo / .app-header / .app-content / .page-title-row` 五条规则整合替换。
  - 新增工作台外壳主题段(文件末尾):侧栏深蓝渐变 (`#1f2a44 → #131a2b`),Logo 渐变蓝色块,菜单 hover/selected 用主色低透叠加 + 左侧 3px 主色 inset 边线;头部白底 + 柔阴影 + sticky;内容区卡片统一 14px 圆角与 `0 4px 16px` 阴影,hover 阴影加深;表头浅蓝背景 (`#f5f8fd`),hover `#f8faff`;`Statistic` 数值 28px、prefix 主色;`page-title-row` 用 flex,padding 留呼吸;主按钮 `box-shadow` 与登录按钮呼应。
  - body 背景同步改为 `#f3f5f8`,与登录页和工作台正文区一致。

### 范围与边界
- 不改三个角色页面(`OwnerHome / LabelerHome / ReviewerHome`)的内容结构和 mock 数据,仅通过外层样式让视觉与登录页一致。
- 未引入新依赖,未改动路由、认证类型、API 层。

### 验证
- `npm run build` 通过,无新增 TypeScript 错误。
- 视觉效果:侧栏深蓝渐变 + 蓝色 Logo,菜单选中显示主色高亮;头部白底柔阴影;内容卡片圆角与登录卡呼应;统计数值与按钮使用同一蓝色主调。


## 增量实现:Labeler 端导航与页面骨架

### 完成内容
- `frontend/src/layouts/AppLayout.tsx`:重写。新增 `resolveSection` / `resolveSelectedKey` 函数按路径前缀分支侧栏;Labeler 端菜单包含"工作概览 / 任务市场 / 我的任务 / 草稿箱 / 打回项";Owner、Reviewer 保留单项;头部标题文案随 `section` 切换。
- `frontend/src/types/labeler.ts`:新增。按计划书 5.1 模型定义 `MarketTask / Assignment / Annotation / Draft / TaskStatus / ItemStatus / AnnotationStatus / SubmitAnnotationRequest / MarketTasksQuery / PageResult`。
- `frontend/src/api/labeler.ts`:新增。封装 `listMarketTasks / claimTask / listMyAssignments / getDraft / saveDraft / submitAnnotation`,路径与 payload 对齐计划书 5.2:`GET /market/tasks`、`POST /tasks/{id}/claim`、`GET /assignments/{id}/draft`、`PUT /assignments/{id}/draft`、`POST /assignments/{id}/submit`、`GET /assignments/mine`。
- `frontend/src/pages/labeler/`:
  - 新增 `LabelerOverview.tsx`:四项 Statistic + 最近任务批次 + 进度图,跳转任务市场入口。
  - 新增 `TaskMarket.tsx`:筛选区 + 三栏卡片网格,展示剩余配额/截止/单价。
  - 新增 `MyTasks.tsx`:Table 展示已认领作业的状态与进度。
  - 新增 `Drafts.tsx`:草稿列表,展示自动保存时间与 Schema 版本。
  - 新增 `ReturnedItems.tsx`:打回项 List,显示驳回原因、修改次数与重提入口。
  - 删除旧 `LabelerHome.tsx`,内容已并入 `LabelerOverview.tsx`。
  - 重写 `index.ts` 统一导出五个新页面。
- `frontend/src/router/index.tsx`:重写。新增 `/labeler/market`、`/labeler/my-tasks`、`/labeler/drafts`、`/labeler/returned` 四条子路由,根 `/labeler` 指向 `LabelerOverview`。

### 范围与边界
- Owner / Reviewer 端结构保持不变,只改了顶部标题文案的展示逻辑。
- 接口仅做前端预留,不调用真实后端。Spring Boot 实现后将切换 `client.ts` 的基础 URL 和登录态。
- Labeler 子页面均为 mock 数据,后续动态表单/答题 Renderer 会替换 `Empty` 与列表。

### 验证
- `npm run build` 通过,无新增 TypeScript 错误。
- 路由切换、菜单选中态、头部标题随 section 变化经手动检查正常。


## 增量实现:顶部导航栏改版

### 完成内容
- `AppLayout.tsx`:重写头部为三段式
  - 左:折叠按钮(`MenuFoldOutlined / MenuUnfoldOutlined`)+ 圆角搜索框(`Input` + `SearchOutlined`)。Sider 改为受控折叠 (`collapsed` state),`collapsedWidth=72`,`trigger={null}` 由顶部按钮控制。
  - 中:`报表分析 / 帮助文档` 两个快捷链接,后续阶段可指向真实路由。
  - 右:`Bulb / Mail / Bell / Calendar` 四个 Tooltip 圆形图标按钮(站内信用 `Badge dot`,通知用 `Badge count=3`),用户区改为 `Dropdown`,点头像下拉显示"个人资料 / 退出登录",登出动作收纳到下拉。
  - 头像加 `Badge status="success"` 表示在线。
  - 角色文案在头像下显示 `Project Owner / Labeler / Reviewer`,随路径切换。
- `global.css`:
  - 移除旧的 `.app-header-title / app-header-name / app-header-sub / app-header-phase / app-header-user` 五条规则。
  - 新增 `.app-header-left / .app-header-toggle / .app-header-search / .app-header-links / .app-header-right / .app-header-icons / .app-header-profile / .app-header-profile-text / .app-header-profile-name / .app-header-profile-role`。
  - 顶部栏高度回到 64px,padding 收紧到 0 20px,与折叠按钮+搜索框比例更协调。
  - 加入断点:`<= 1180px` 隐藏中间链接,`<= 880px` 进一步隐藏搜索框与用户文案,只留头像与图标。
  - 侧栏折叠态下让 `.app-logo` 居中且不显示文字。

### 范围与边界
- 不影响 Owner/Labeler/Reviewer 子页面的内容结构与 mock 数据。
- 顶部链接、主题切换、站内信、通知、日程、个人资料均为占位,后续真实路由/功能落地时再接入。
- 未引入新依赖。

### 验证
- `npm run build` 通过。
- 折叠按钮、搜索框、Tooltip 图标按钮、头像下拉的渲染与交互在浏览器手动验证正常。


## 增量修正:侧栏改为浅色分组主题

### 改动
- `AppLayout.tsx`:
  - Sider 与 Menu 主题由 `dark` 改为 `light`,折叠/展开/控制逻辑不变。
  - Labeler 菜单项重组为分组结构(`type: 'group'`),分两组:
    - `WORKSPACE`:工作概览
    - `TASKS`:任务市场 / 我的任务 / 草稿箱 / 打回项
- `global.css`:
  - 侧栏背景改为白色 + 右侧 1px 边线,Logo 文字色改深色,Logo 块保持蓝色渐变图标。
  - `.ant-menu-light` 容器透明、移除内置右边线;`.ant-menu-item-group-title` 使用主色 `#2f7bff`、字号 11px、字间距 1.2px、大写,实现 "WORKSPACE / TASKS" 分组标题。
  - 菜单项默认色 `#475569`,圆角 12px,hover 用主色低透叠加。
  - 选中项做成蓝色渐变胶囊(`linear-gradient #6fb6ff → #2f7bff`)+ `0 8px 18px` 阴影 + 白色文字 + 白色图标;额外用 `::after` 绘制右侧白色 V 形箭头,屏蔽 antd 默认选中竖条 `::before`;折叠态下不显示箭头。

### 验证
- `npm run build` 通过。
- 视觉与设计稿一致:白底侧栏、分组小写主色标题、当前页蓝色渐变胶囊带右箭头。


## 增量修正:精简顶部导航栏

### 改动
- `AppLayout.tsx`:
  - 移除中间快捷链接(报表分析 / 帮助文档),用 `<div className="app-header-middle" />` 占位以维持三段 flex 布局,后续可放面包屑或页面标题。
  - 移除右侧四个图标按钮(主题切换 / 站内信 / 通知 / 日程)及对应的 `Tooltip` 与 `Badge` 包裹结构,只保留用户头像下拉。
  - 顺手清理未使用的 import:`BellOutlined / BulbOutlined / CalendarOutlined / MailOutlined / Tooltip / Space`。
- `global.css`:
  - 删除 `.app-header-links / .app-header-links a / .app-header-icons` 三组样式。
  - 删除 `1180px` 断点(原仅用于隐藏中间链接)。
  - 新增 `.app-header-middle { flex: 0 1 auto; }` 占位规则。

### 验证
- `npm run build` 通过。
- 顶部栏现在只剩:折叠按钮 + 搜索框 / (中间留白) / 用户头像下拉。


## 增量修正:Sider/Header 改为浮起圆角卡片

### 改动
- `global.css`:
  - `.app-shell` 给整体外壳加 `padding: 16px` 与 `gap: 16px`,内层 `.app-shell > .ant-layout { background: transparent; gap: 16px; }`,让侧栏、顶栏、内容区从背景上"浮"起来。
  - `.app-shell .ant-layout-sider`:`border-radius: 18px`,三层组合阴影(顶部 1px 高光 inset + 12px 28px 主投影 + 2px 6px 接触阴影),`overflow: hidden`,`height: calc(100vh - 32px)`,`position: sticky; top: 16px`,移除右侧 1px 边线。
  - `.app-header`:同款圆角 + 阴影,`top: 16px` 与外壳 padding 对齐;移除底部边线。
  - `.app-content`:`padding: 24px`,背景透明(承接外壳的浅灰底色)。
  - `.app-logo`:删掉底部分隔线(白色卡内不需要再画线)。

### 视觉效果
- 侧栏与顶栏均为白色圆角卡片,与内容卡片视觉语言统一。
- 三层阴影叠加形成轻微 3D 层次:顶部 1px inset 制造高光,主投影提供进深,接触阴影消除"漂浮断层"。

### 验证
- `npm run build` 通过。


## 增量实现:侧栏底部版本号脚注

### 改动
- `AppLayout.tsx`:Sider 内部包一层 `.app-sider-inner` flex 容器(垂直方向);Menu 加 `.app-sider-menu` 类并用 `flex: 1 1 auto` 占据中间高度;新增 `.app-sider-footer`,展开态显示 `LabelHub v{APP_VERSION}` + `Phase 1 · MVP`,折叠态显示一颗主色胶囊小徽标 `v{APP_VERSION}`。
- `vite.config.ts`:重写为函数式 `defineConfig(({ mode }) => ...)`,启动时读取 `frontend/package.json` 的 `version` 字段,通过 `define` 注入到 `import.meta.env.VITE_APP_VERSION`;`.env` 中显式 `VITE_APP_VERSION` 优先,缺省回落到 package.json,保证版本号与发布版本一致。
- `global.css`:
  - `.ant-layout-sider .ant-layout-sider-children` 改为 flex column,配合 `.app-sider-inner` 把脚注推到底部。
  - 新增 `.app-sider-menu / .app-sider-footer / .app-sider-version / .app-sider-build / .app-sider-version-mark` 等样式;脚注顶部加 1px 浅色分隔线,文案灰阶递减,折叠态用主色胶囊徽标。

### 视觉效果
- 展开侧栏底部固定显示 "LabelHub v0.1.0" + "Phase 1 · MVP",随项目实际版本同步。
- 折叠后只显示一颗 `v0.1.0` 蓝色小胶囊,占位最小但仍可识别。

### 验证
- `npm run build` 通过,版本号已成功内联到打包产物。


## 增量实现:Labeler 工作概览页重做

### 信息架构对齐
对照《项目实施计划书》4.3 与《LabelHub Project Implementation Plan EN》4.3、4.4、4.5、1.4:
- 闭环路径:任务市场 → 答题(Renderer) → 草稿自动保存 → 提交触发 AI 预审 → 人工审核 → 通过 / 打回修改。
- 多模态保留(1.4):text / image / video / markdown 全部透传 raw_payload / media_type / media_url / content_markdown。
- AI 评分维度(4.4):PASS / REJECT / NEED_HUMAN_REVIEW 在审核分布卡中可视化。

### 完成内容
- `frontend/src/pages/labeler/LabelerOverview.tsx` 整页重写:
  - **欢迎横幅**:蓝色渐变浮卡 + 装饰圆,左侧主 CTA(进入任务市场 / 查看草稿)和阶段标识,右侧三段统计(本周累计提交 / 审核通过率 / 本月奖励预估)。
  - **4 张 KPI 卡**:进行中任务 / 今日已提交 / 待修改打回 / 平均耗时,每张带主题色软底图标 + 趋势文案,对应 Labeler 日常关注的实时指标。
  - **今日标注节奏**(64% 宽):蓝色渐变进度条 + "提交总数 / AI 预审通过 / 人工已确认" 三段元数据 + 速度提示。数值映射数据模型 4.3 的 annotations / ai_review_jobs 表。
  - **AI 审核分布**(36% 宽):五段条 + 双列图例,呼应计划书 4.4 的 AIReviewResult.decision 与 4.5 的人工审核结果。
  - **最近的任务批次**(64% 宽):每行任务带类型 Tag、单价、剩余配额渐变进度、截止时间、"进入答题" 按钮。
  - **快捷入口**(2×2 grid):任务市场 / 我的任务 / 草稿箱 / 打回项,跳转对齐左侧导航。
  - **支持的题目类型**:Text / Image / Video / Markdown 四颗主题色胶囊,底部 AI Agent 提示,把多模态保留与 4.4 异步预审的设计语言显化给 Labeler。
- `frontend/src/styles/global.css` 末尾新增 `Labeler 工作概览` 段:
  - `.overview-hero / .overview-hero-stats / .overview-hero-divider` 等横幅样式。
  - `.kpi-card / .kpi-icon / .kpi-trend` 卡片样式。
  - `.overview-progress-meta`、`.review-bar / .review-legend` 数据可视化样式。
  - `.batch-list / .batch-item` 任务卡片样式。
  - `.shortcut-grid / .shortcut-tile` 2×2 快捷入口。
  - `.types-list / .types-pill / .types-hint` 题目类型卡。

### 范围与边界
- 仍是前端 mock 数据,未接入真实接口。后续 `GET /assignments/mine`、`GET /market/tasks`、`GET /assignments/{id}/draft`、`GET /ai-review/results/{annotationId}` 落地后,字段命名已与 `types/labeler.ts` 对齐。
- 未引入新依赖,未改动登录页、其他角色端页面、API 层、路由。

### 验证
- `npm run build` 通过。
- 视觉:蓝色渐变横幅做主视觉;KPI、进度、AI 分布、任务批次、快捷入口、类型卡的圆角与阴影与登录页/Sider/顶栏统一。


## 增量实现:顶部面包屑路径

### 改动
- `AppLayout.tsx`:
  - 引入 `Breadcrumb / HomeOutlined`,新增 `segmentLabel` 路径片段→中文文案映射(覆盖 owner / labeler / reviewer / market / my-tasks / drafts / returned)。
  - `breadcrumbItems` 由 `location.pathname.split('/')` 动态生成:首项固定为带 Home 图标的"工作台",链接到当前角色根路径;后续按片段累加,父级可点跳转,末级仅显示文字。
  - 用 `<a onClick={preventDefault + navigate}>` 改造原生 href,保持 SPA 路由不刷新。
  - 替换原 `.app-header-middle` 占位 div 为 `<Breadcrumb className="app-header-breadcrumb" separator="/" items={breadcrumbItems} />`。
- `global.css`:新增 `.app-header-breadcrumb` 样式,父级浅灰、hover 主色软底,末级深色加粗,Home 图标主色,`/` 分隔符灰阶。<= 980px 视口隐藏面包屑保头部紧凑。

### 视觉效果
- 进入 `/labeler/market` 时显示:**🏠 工作台 / 标注员 / 任务市场**,前两段可点;末段加粗。
- 路径切换实时刷新,Home 图标 + `/` 分隔符贴近文件夹路径观感。

### 验证
- `npm run build` 通过。


## 增量实现:Owner 端导航与"任务管理"主页

### 信息架构对齐
对照《项目实施计划书》4.1 / 4.2 / 4.4 / 4.5 / 4.6 / 5.2,Owner 端拆为三组导航:
- 数据生产:任务管理 / 模板搭建 / 数据集
- 审核与质检:AI 预审规则 / 人工审核
- 数据交付:数据看板 / 导出中心

### 完成内容
- `AppLayout.tsx`:
  - `ownerMenuItems` 改为三组(分组标题"数据生产 / 审核与质检 / 数据交付")。
  - `resolveSelectedKey` 增加 owner 子路径匹配,默认落到 `/owner/tasks`。
  - 面包屑首项按 section 显示 "任务负责人后台 / 标注员后台 / 审核员后台",并跳过第一段路径片段避免重复;`segmentLabel` 扩展 `tasks / templates / datasets / ai-review / review / dashboard / export`。
  - 删除未使用的 `CloudUploadOutlined` import。
- `frontend/src/pages/owner/`:
  - `OwnerTasks.tsx`:任务管理主页。三张概览卡(发布中 12 / 草稿 5 / 本周新增提交 3,481);工具条(关键词搜索 + 状态筛选 + 分发策略筛选);表格列含任务标题+meta、状态点(草稿/发布中/已暂停/已结束)、分发策略文案、配额渐变进度条、操作按钮(发布/暂停/恢复/结束/详情);右侧抽屉表单(任务标题、标签、奖励规则、配额、截止时间、分发策略 Segmented、关联模板、AI 预审开关),底部"存为草稿/立即发布"双按钮。
  - `components/OwnerComingSoon.tsx`:Owner 二级页面统一骨架,接收 `title / description / phase / apis`。
  - `OwnerTemplates.tsx / OwnerDatasets.tsx / OwnerAiReview.tsx / OwnerReview.tsx / OwnerDashboard.tsx / OwnerExport.tsx`:六个二级页面引用 `OwnerComingSoon`,直接挂上计划书原文描述与对应 API 路径。
  - 删除旧 `OwnerHome.tsx`,`index.ts` 重写导出七个新页面。
- `router/index.tsx`:`/owner` 重定向到 `/owner/tasks`,挂 7 条 owner 子路由。
- `global.css`:新增 Owner 端样式段(统计卡、状态点、配额条、抽屉横幅、占位卡)。

### 范围与边界
- 不影响 Labeler 端、Reviewer 端,Login 跳转默认 `/owner` 仍正常(被重定向到 `/owner/tasks`)。
- 仍是前端 mock,字段命名与计划书 5.2 接口/状态机保持一致,后续接入 Spring Boot 时无需重命名。
- 未引入新依赖。

### 验证
- `npm run build` 通过。
- 路径切换、菜单选中、面包屑随路径更新均符合预期。


## 增量实现:Owner 新建任务后端接入与 Labeler 市场同步

### 对应计划

对应 `阶段计划.md` 中"Owner 新建任务后端接入与 Labeler 市场同步"。本次只打通任务创建、Owner 任务列表、状态切换和 Labeler 任务市场可见闭环,不扩展到认领、作答、数据集导入、AI 审核和人工审核。

### 后端完成内容

- 新增 `backend/src/main/java/com/labelhub/backend/task/` 下任务管理模块:
  - `TaskController`:暴露 `/api/tasks`、`/api/tasks/{taskId}/state`、`/api/market/tasks`。
  - `TaskService`:处理 owner 权限校验、任务状态校验、截止时间解析、任务类型推断、奖励单价提取和响应 DTO 转换。
  - `TaskRepository`:使用 `JdbcTemplate` 写入 `tasks`,并同步创建最小 `task_schema_versions` 记录;查询 Owner 任务列表和 Labeler 任务市场。
  - `CreateTaskRequest / UpdateTaskStateRequest / OwnerTaskResponse / MarketTaskResponse / PageResponse / TaskMetadata / TaskRecord` 等请求和响应类型。
- 已实现接口:
  - `POST /api/tasks`:Owner 创建任务,`status=published` 时立即发布,`status=draft` 时仅保存草稿。
  - `GET /api/tasks`:Owner 查询自己的任务列表。
  - `PUT /api/tasks/{taskId}/state`:Owner 切换 `draft / published / paused / ended` 状态。
  - `GET /api/market/tasks`:查询发布中且未过期的任务,供 Labeler 任务市场展示。
- 未新增数据库迁移;当前表单里的 `tags / reward / strategy / schema / aiReviewEnabled / taskType / rewardPerItem` 暂存到 `tasks.reward_rule` JSON,符合本阶段最小变更边界。
- 创建任务时会生成一个占位 `task_schema_versions` 版本,保证市场响应中的 `schemaVersionId` 有来源。

### 前端完成内容

- 新增 `frontend/src/types/owner.ts`,定义 Owner 任务状态、分发策略、创建请求和分页响应类型。
- 新增 `frontend/src/api/owner.ts`,封装:
  - `listTasks()`
  - `createTask(payload)`
  - `updateTaskState(taskId, state)`
- 修改 `frontend/src/pages/owner/OwnerTasks.tsx`:
  - 页面加载时调用 `GET /api/tasks`。
  - "新建任务"抽屉的"存为草稿 / 立即发布"分别提交 `status=draft / published` 到 `POST /api/tasks`。
  - 表格中的暂停、恢复、结束调用 `PUT /api/tasks/{taskId}/state`。
  - 三张概览卡改为根据接口返回的任务列表计算。
- 修改 `frontend/src/pages/labeler/TaskMarket.tsx`:
  - 从本地 mock 列表切换为调用既有 `labelerApi.listMarketTasks()`。
  - 筛选按钮、回车搜索和任务类型筛选会请求 `GET /api/market/tasks`。
  - 卡片展示后端返回的 `title / taskType / description / remainingQuota / totalQuota / deadline / rewardPerItem`。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,后端构建通过。
- 已执行 `frontend/npm run build`,前端 TypeScript 编译和 Vite 构建通过。
- 前端构建仍存在 Vite 大 chunk warning 和插件耗时提示,属于既有打包体积/构建性能提示,不影响本次功能。
- 已执行真实接口联调:
  - MySQL `127.0.0.1:3306` 可连接。
  - Redis `127.0.0.1:6379` 可连接。
  - 后端以 `LABELHUB_BACKEND_PORT=18080` 启动成功。
  - 使用 `owner / owner123` 登录成功,创建 1 个草稿任务和 1 个发布任务。
  - 使用 `labeler / labeler123` 登录成功,调用 `GET /api/market/tasks?keyword=Codex&page=1&pageSize=20`。
  - Owner 任务列表中可查到草稿任务与发布任务。
  - Labeler 市场中查不到草稿任务,能查到发布任务。
  - 将发布任务切换为 `paused` 后,Labeler 市场不再返回该任务。
  - 将任务恢复为 `published` 后,Labeler 市场重新返回该任务。
  - 测试结束后已停止 18080 测试后端进程并确认端口释放。

### 未执行的验证

- 未做浏览器级 UI 点击联调;本次验证覆盖前后端构建和真实 HTTP API 闭环。
- 本次接口联调向本地 `labelhub` 数据库写入了 2 条 Codex 测试任务及其 schema 版本记录,用于验证草稿/发布状态在 Owner 与 Labeler 之间的可见性差异。

### 边界说明

- 本阶段只实现"Owner 创建/发布后 Labeler 市场可见"闭环。
- 任务认领 `POST /tasks/{id}/claim`、配额扣减、数据集导入、Renderer 作答页、草稿保存、提交、AI 预审和人审流转仍待后续阶段实现。
- `reward_rule` JSON 是本阶段的临时承载方式;后续如果需要按标签、奖励、分发策略做复杂检索或统计,应通过数据库迁移拆出独立字段。


## 增量实现:Owner 数据集页接入真实样例数据

### 完成内容
- `frontend/public/sample-datasets/`:从仓库 `datasets/datasets/` 复制四份真实样例(`qa_quality.json/jsonl`、`preference_compare.json/jsonl`),供前端 `fetch` 直读;后端导入接口未上线前以静态资源占位。
- `frontend/src/types/dataset.ts`:新增。按计划书 1.4 与 5.1 定义 `QaQualityItem` / `PreferenceCompareItem` / `DatasetMeta` / `MediaType` / `DatasetKind`。
- `frontend/src/pages/owner/OwnerDatasets.tsx` 整页重写:
  - 顶部:标题 + 描述 + 上传按钮(`<Upload accept=".json,.jsonl,.xlsx,.csv">`,`beforeUpload` 阻止默认上传,提示后端 Phase 2 接入) + 新建数据集 CTA。
  - 左侧"我的数据集"列表:两条内置样例,卡片显示名称、类型、条目数、文件大小、版本、媒体类型分布胶囊;点击切换 active。
  - 右侧元数据卡:描述、条目数、文件大小、导入时间;下方字段映射表(`fieldGuide`)以 grid 形式罗列计划书 1.4/5.1 中每个字段含义与是否必填。
  - 数据预览卡:
    - QA Quality 列:ID、Type(主题色胶囊)、类别、Prompt、Model Answer、Expected Dimensions(标签胶囊)。
    - Preference 列:ID、task_type、Prompt、A 与 B 候选(带 model 标签)、Preferred(A/B/TIE) + margin。
    - 关键词搜索 + qa_quality 媒体类型 Segmented(全部/Text/Image/Video/Markdown)。
    - 行点击进右侧抽屉,显示完整字段:image 直出 `<img>`、video 显示链接、markdown 用 `<pre>` 显示原文,maker 按 preferred 高亮 A/B 卡。
- `frontend/src/styles/global.css` 末尾新增 `.dataset-*` 样式段,覆盖列表项 active 主色渐变、字段映射表、预览表格、A/B 偏好对比卡的高亮规则。

### 接口预留
- 后端落地后,把 `useEffect` 中的 `fetch(activeDataset.resourceUrl)` 替换为 `apiRequest('/datasets/{id}/items')`;
- `Upload.beforeUpload` 替换为 `apiRequest('/datasets/import', { method: 'POST', body: formData })`;
- `DatasetMeta` 字段命名直接对齐计划书 5.1,无需重命名。

### 验证
- `npm run build` 通过。
- 浏览器打开 `/owner/datasets` 可看到两条样例:
  - 切换 qa_quality 时显示 30 条真实记录,可按 media_type 过滤,行点击进抽屉看字段。
  - 切换 preference_compare 时显示 12 条 A/B 对比,抽屉里 preferred 一侧自动高亮。


## 增量实现:Labeler 任务市场重做

### 信息架构对齐
对照《项目实施计划书》4.3 与 5.2 / 4.1 / 4.4:
- 浏览/搜索/认领闭环:`GET /market/tasks` → 卡片列表 → 详情抽屉 → `POST /tasks/{id}/claim` → 加入「我的任务」
- 配额唯一性:`task_id + item_id` 唯一约束,先到先得不重复领取
- 任务多维度信息:类型、分发策略(先到先得 / 指派 / 配额抢单)、媒体类型、单价 + 封顶、剩余配额、截止时间、Owner、AI 预审规则、Schema 版本

### 完成内容
- `frontend/src/types/labeler.ts`:扩展 `MarketTask` 字段(taskTypeKey / tags / rewardCap / assignStrategy / mediaTypes / ownerName / aiReviewEnabled / aiReviewRule / publishedAt / maxClaimPerUser / claimedByMe);新增 `AssignStrategy / TaskMediaType` 类型;`MarketTasksQuery` 增加 strategy / mediaType / aiReview / sortBy 四个查询字段。
- `frontend/src/api/labeler.ts`:`toQueryString` 同步新字段,接口 path 不变。
- `frontend/public/sample-datasets/market-tasks.json`:6 条样例任务,覆盖 QA Quality / Preference Compare / Image Classification / Safety Tagging,字段命名严格对齐 5.1 数据模型,包含已认领、配额用尽、即将截止等多种状态。
- `frontend/src/pages/labeler/TaskMarket.tsx` 整页重写:
  - 顶部:标题 + 描述 + 演示模式标识(后端不可达时自动出现)
  - 三张 KPI 卡:可领任务数 / 平均单价 / 即将截止(48h)
  - 筛选条:关键词搜索 + 任务类型 + 分发策略 + 媒体类型 + AI 预审 + 排序(单价/截止/配额/发布时间)+ 应用筛选 + 重置
  - 卡片网格(每行 3 张):任务标题、类型胶囊、单价 Tag(hover 显示封顶)、双行描述、分发策略 + 媒体类型胶囊 + AI 预审 Tag、剩余配额渐变进度条、截止剩余时间(48h 内变橙、过期变红)、Owner、立即认领/继续作答/配额已用尽/已截止 四态按钮;已认领卡片绿色高亮、配额用尽卡片半透明
  - 详情抽屉:左上角 Tag 集 + 任务描述 + 标签 + 6 项元数据(剩余/总配额、截止时间、单价 + 封顶、每人最多领取、Schema 版本、发布时间)+ 项目方 + 流程提示;底部"立即认领"按钮直连 `claimTask`
  - 后端不可用时自动 fetch `public/sample-datasets/market-tasks.json` 回落,所有筛选/排序/认领交互依然可用(乐观更新本地配额与认领状态)
- `frontend/src/styles/global.css`:新增 `.market-*` 样式段(KPI 卡、筛选条、任务卡 hover/已认领/已用尽态、配额渐变条、截止时间状态色、抽屉详情排版)。

### 范围与边界
- 不改 backend / agent / 路由 / 其他 Labeler 子页 / Owner / Reviewer 端,不引入新依赖。
- 后端落地后无需重构调用层:`labelerApi.listMarketTasks` 与 `labelerApi.claimTask` 路径与 payload 已对齐 5.2。

### 验证
- `npm run build` 通过。
- 浏览器访问 `/labeler/market`:接口未连接时显示 6 条样例,筛选/排序/认领均生效;关键词、类型、策略、媒体、AI 过滤可叠加;过期任务自动禁用按钮、配额用尽自动半透明;点卡片进抽屉看完整字段。


## 增量修正:Labeler 任务市场与 Owner 发布任务字段对齐

### 问题
Owner 端通过 `POST /tasks` + `GET /tasks` 维护任务,字段以 `OwnerTask` 为准(reward 是字符串、quota 拆分 used/total、taskType 是显示文案)。Labeler 任务市场之前调的是 `/market/tasks`,字段以 `MarketTask` 为准,后端未实现时落到样例 JSON,导致两端字段断裂、Owner 发布的任务在 Labeler 看不到。

### 改动
- `frontend/src/pages/labeler/TaskMarket.tsx`:
  - 数据源改为优先调 `ownerApi.listTasks()`,前端过滤 `state === 'published'` 后再展示;失败回退到 `labelerApi.listMarketTasks()`,再失败才落到 `sample-datasets/market-tasks.json`。
  - 新增 `ownerTaskToMarket(OwnerTask)` 适配函数:
    - `reward` 字符串拆为 `rewardPerItem`(数字)+ `rewardCap`(月度封顶)
    - `quotaUsed/quotaTotal` → `remainingQuota/totalQuota`
    - `taskType` 文案推断 `taskTypeKey`(qa_quality / preference_compare / image_classification / safety_tagging)
    - `schemaVersionId` 缺失时回退到 `schemaVersion`
    - 兼容 owner 端缺少 `mediaTypes / aiReviewRule / claimedByMe / maxClaimPerUser`,以默认值占位

### 视觉与功能
- Owner 在「任务管理」点击「立即发布」之后,切到 Labeler「任务市场」立即看到同一条任务,标题、Tag、单价、剩余配额、Owner、AI 预审、Schema 版本均同步。
- 仍保留前端兜底:Owner 接口未上线 → 走 `/market/tasks`;再失败 → 加载样例文件,演示模式 Tag 自动出现。

### 验证
- `npm run build` 通过。
- 前后端使用同一份任务数据,无需重启即可看到 Owner 端写入的字段在 Labeler 端正确展示。


## 增量修正:Labeler 任务市场后端元数据透传

### 问题

Owner 发布任务时已经提交并保存了标签、奖励规则、分发策略、AI 预审开关等字段,但 `GET /api/market/tasks` 的后端响应只返回标题、类型、描述、Schema、配额、截止时间和单价。Labeler 任务市场在使用该接口时,卡片中的分发策略、Owner、AI 预审、标签和封顶规则只能显示默认值或空值。

### 修正

- `backend/src/main/java/com/labelhub/backend/task/MarketTaskResponse.java`:
  - 补充 `taskTypeKey / tags / rewardCap / assignStrategy / mediaTypes / ownerName / aiReviewEnabled / aiReviewRule / publishedAt / maxClaimPerUser / claimedByMe` 字段。
- `backend/src/main/java/com/labelhub/backend/task/TaskService.java`:
  - `toMarketResponse()` 从 `TaskMetadata` 中读取并返回标签、分发策略、AI 开关、奖励单价和封顶说明。
  - 从 `TaskRecord` 中返回 Owner 名称和发布时间。
  - 临时将 `mediaTypes` 默认设为 `text`,后续 Schema/数据集接入后再从真实模板或数据项推断。
  - 新增 `toTaskTypeKey()` 保证任务类型筛选 key 与前端 `qa_quality / preference_compare / image_classification / safety_tagging` 对齐。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm run build` 通过。
- 前端大 chunk warning 仍存在,属于既有打包体积提示,不影响本次字段透传。

### 边界

- 本次只补齐后端响应字段,不改变前端卡片布局、不新增接口路径、不新增数据库迁移。
- `maxClaimPerUser / claimedByMe` 暂返回 `null / false`;真正认领记录和每人限额需等 `POST /tasks/{id}/claim` 与 assignments 查询落地后实现。


## 增量实现:Owner 数据集页改为 MySQL 数据源并实现导入/新建

### 对应计划

对应 `阶段计划.md` 中"Owner 数据集页改为 MySQL 数据源并实现导入/新建"。本次聚焦数据集页面的数据源切换、文件导入和空数据集创建,不扩展到任务认领、标注提交、数据导出和文件对象存储。

### 后端完成内容

- 新增 `backend/src/main/java/com/labelhub/backend/dataset/` 数据集模块:
  - `DatasetController`:暴露 `GET /api/datasets`、`GET /api/datasets/{datasetId}/items`、`POST /api/datasets`、`POST /api/datasets/import`。
  - `DatasetService`:复用当前 token 鉴权,要求 Owner 角色;校验数据集和任务必须归属当前 Owner。
  - `DatasetRepository`:基于 `JdbcTemplate` 读取/写入 MySQL 的 `datasets`、`items`、`files`、`tasks`。
  - `DatasetFileParser`:支持 JSON、JSONL/NDJSON、CSV 和基础 XLSX 文件解析,统一转为 `items.raw_payload`。
- 新建空数据集时:
  - 前端必须选择关联任务,后端校验 `tasks.owner_id`。
  - 写入 `files` 占位元数据和 `datasets` 记录,`items` 暂为空。
- 上传文件数据时:
  - 写入 `files` 文件元数据,保存 SHA-256 checksum、文件大小和数据集展示名。
  - 写入 `datasets` 导入记录。
  - 将每行/每条对象写入 `items.raw_payload`,并同步提取 `item_key`、`media_type`、`media_url`、`content_markdown`。
- `application.yml` 增加 multipart 限制:`max-file-size=20MB`、`max-request-size=22MB`。

### 前端完成内容

- `frontend/src/api/client.ts`:识别 `FormData`,避免上传文件时错误设置 `Content-Type: application/json`。
- 新增 `frontend/src/api/dataset.ts`:封装数据集列表、数据项、创建和导入接口。
- 扩展 `frontend/src/types/dataset.ts`:补充 `taskId / taskTitle / importStatus / errorSummary` 等 MySQL 响应字段,并新增创建/导入请求类型。
- 改造 `frontend/src/pages/owner/OwnerDatasets.tsx`:
  - 页面加载调用 `datasetApi.listDatasets()`,不再读取 `public/sample-datasets` 静态文件。
  - 切换数据集时调用 `datasetApi.listItems(datasetId)`,表格预览来自 MySQL `items.raw_payload`。
  - "新建数据集"打开表单弹窗,选择关联任务、数据集名称和类型后调用 `POST /api/datasets`。
  - "上传文件数据"打开导入弹窗,选择关联任务、名称、类型和文件后调用 `POST /api/datasets/import`。
  - 当 MySQL 暂无数据集时显示空状态,引导先新建或上传。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,后端构建通过。
- 已执行 `frontend/npm run build`,前端 TypeScript 编译和 Vite 构建通过。
- 前端构建仍存在 Vite 大 chunk warning 和插件耗时提示,属于既有打包体积/构建性能提示,不影响本次功能。

### 边界与后续

- 本次保持既有 MySQL 外键设计,数据集必须关联任务;如果产品后续要求"完全独立数据集库",需要单独评估是否调整 `datasets.task_id` 与 `items.task_id`。
- XLSX 支持为基础解析:读取首个工作表、首行表头、共享字符串和普通单元格;不支持公式计算、合并单元格、多工作表批量导入。
- 本次未执行真实 MySQL 写入联调,避免在未确认环境数据的情况下直接向本地库写入导入记录;构建验证已覆盖编译与前端类型契约。


## 增量实现:已有数据集追加导入文件数据

### 问题

上一阶段的"上传文件数据"接口 `POST /api/datasets/import` 会创建一个新数据集并导入文件。用户先通过"新建数据集"创建空数据集后,页面缺少向该已有 `dataset_id` 追加 `items` 的接口和按钮。

### 后端完成内容

- `DatasetController` 新增:
  - `POST /api/datasets/{datasetId}/items/import`
- `DatasetService` 新增 `importItemsToDataset()`:
  - 校验当前用户必须是 Owner。
  - 校验目标数据集归属当前 Owner。
  - 复用 `DatasetFileParser` 解析 JSON / JSONL / CSV / 基础 XLSX。
  - 复用目标数据集的 `task_id`,将解析结果追加写入同一个 `dataset_id` 的 `items` 表。
- `DatasetRepository` 新增 `addDatasetImportCounts()`:
  - 累加 `datasets.total_count / success_count`。
  - 更新 `datasets.updated_at`。
  - 累加关联 `files.size`,让空数据集追加文件后元数据能体现文件大小变化。

### 前端完成内容

- `frontend/src/api/dataset.ts` 新增 `importItems(datasetId, file)`,调用 `POST /api/datasets/{datasetId}/items/import`。
- `OwnerDatasets.tsx`:
  - 右侧当前数据集元数据卡新增"添加数据"按钮。
  - 新增"向当前数据集添加数据"弹窗,只选择文件,不再选择任务和数据集类型。
  - 提交成功后刷新数据集列表和当前预览表。
  - 顶部"上传文件数据"保留为"上传并创建新数据集"语义;右侧"添加数据"用于追加到当前数据集。

### 验证

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,后端构建通过。
- 已执行 `frontend/npm run build`,前端构建通过。
- 前端构建仍存在既有 Vite 大 chunk warning 和插件耗时提示,不影响本次功能。

### 使用方式

1. 先在左侧选择已经新建的空数据集。
2. 在右侧数据集元数据卡点击"添加数据"。
3. 上传 JSON / JSONL / CSV / XLSX 文件。
4. 提交后页面会刷新,条目数和下方预览表来自 MySQL `items`。


## 增量修正:表格导入数组字段渲染容错

### 问题

表格文件导入后,`expected_dimensions`、`tags`、`dimensions` 等字段来自单元格文本,可能是普通字符串或逗号分隔字符串。数据集页此前沿用 JSON 样例结构,直接执行 `(dims ?? []).map(...)`,当 `dims` 实际为字符串时会触发 `map is not a function`,导致整页进入渲染失败页。

### 修正

- `frontend/src/types/dataset.ts`:
  - 新增 `TextListLike = string[] | string`。
  - 将 `QaQualityItem.tags / expected_dimensions` 和 `PreferenceCompareItem.dimensions` 改为兼容数组或字符串。
  - 将 `PreferenceCompareItem.safety_flag` 改为 `boolean | string`,兼容 CSV/XLSX 中的 `false` 文本。
- `frontend/src/pages/owner/OwnerDatasets.tsx`:
  - 新增 `toTextList(value)` 容错解析函数,兼容真实数组、JSON 数组字符串、逗号/中文逗号/分号/顿号/换行分隔字符串。
  - 预览表格的"维度"列改为先调用 `toTextList` 再渲染标签。
  - 详情抽屉中的 Tags / Expected Dimensions / Dimensions 统一使用 `toTextList`。
  - `safety_flag` 改为通过 `toBoolean` 判断,避免字符串 `"false"` 被当成真值。

### 验证

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 构建通过。
- 本次只改前端渲染容错,不改变 MySQL 中 `items.raw_payload` 原始数据。


## 增量实现:Owner 模板搭建(Schema Designer)

### 信息架构对齐
对照《项目实施计划书》4.2 / 1.4 / 5.2 与《LabelHub 课题实现要求》物料 schema 与渲染解耦:
- 拖拽物料 → 中画布 → 属性面板,产物为可序列化 JSON Schema(Designer 与 Renderer 共用同一份)
- 核心物料:ShowItem / 单行 / 多行 / 富文本 / 单选 / 多选 / 标签 / JSON / LLM / 文件&图片 / 分组容器 / 多 Tab
- 接口对齐 5.2:`GET /schemas`、`GET /schemas/{versionId}`、`POST /tasks/{id}/schemas/draft`、`POST /schemas/draft`、`PUT /schemas/{id}/draft`、`POST /schemas/{id}/publish`

### 完成内容
- `frontend/src/types/schema.ts`:新增。`MaterialKind / MaterialMeta / SchemaField / SchemaVersion / SchemaSummary / CreateSchemaDraftRequest / UpdateSchemaDraftRequest`,字段命名直接对齐计划书 5.1 数据模型。
- `frontend/src/api/schema.ts`:新增。`schemaApi.{listSchemas, getSchema, createDraftForTask, createStandaloneDraft, updateDraft, publish}`,路径与 payload 完全按 5.2 写。
- `frontend/src/pages/owner/OwnerTemplates.tsx` 整页重写:
  - 顶部:标题 + 描述 + 新建模板 CTA + 演示模式 Tag
  - 三张概览卡:模板总数 / 已发布 / 草稿
  - 工具条:搜索 + 状态 Segmented(全部 / 已发布 / 草稿)
  - 模板卡片网格:图标 + 名称 + 版本号 Tag + 关联任务 + 状态 Tag + 字段数 / 更新时间 / 创建人 + 操作(复制 / 导出 / 打开 Designer)
- `frontend/src/pages/owner/OwnerTemplateDesigner.tsx`:新增 Designer 页面。
  - 顶部工具栏:返回列表 / 标题 / 版本号 + 绑定任务 Tag / 预览 / 导出 Schema JSON / 保存草稿 / 保存并发布
  - 三栏 grid 布局(240 / 1fr / 320):
    - 左:物料区,5 个分类(输入 / 选择 / 媒体 / 高级 / 布局),共 12 个物料,点击即追加到画布末尾
    - 中:画布,Tabs 顶部"基础信息 / 标注 / + 新 Tab",字段卡显示 label + 字段名 + 类型 + 操作按钮(上下移 / 选中 / 删除)+ 物料类型对应的预览(单行输入/单选 chip/标签 chip/ShowItem 文本/JSON/LLM 触发)
    - 右:属性面板,3 个 Tab:基础(字段名 / 标签 / 必填 / 占位 / 最大长度 / 选项 / 展示文本)、校验(正则 / 自定义函数白名单)、联动(when / hide / show 规则列表)
  - 预览抽屉:用同一份 fields 渲染表单预览(Renderer 雏形)
  - 导出 Modal:把 schema 序列化成 JSON 显示,支持复制到剪贴板
  - 后端不可达时仍能正常使用:进入页面时尝试 `GET /schemas/{versionId}`,失败则保持本地草稿,所有交互照常,Tag 标记"演示模式"
- `frontend/src/pages/owner/index.ts`:导出 `OwnerTemplateDesigner`
- `frontend/src/router/index.tsx`:挂 `/owner/templates/designer` 子路由,接收 `versionId` / `new` 查询参数
- `AppLayout.tsx`:`segmentLabel` 增加 `designer: 'Designer'`,面包屑能正确显示「任务负责人后台 / 模板搭建 / Designer」
- `frontend/src/styles/global.css`:新增 `.template-card-* / .designer-* / .material-* / .canvas-* / .field-card-* / .property-* / .linkage-rule / .schema-json-preview` 样式段,支持窄屏(≤1180px)三栏改为单列堆叠

### 接口预留
- 模板列表:`GET /schemas`
- 拉取版本:`GET /schemas/{versionId}`
- 在任务下创建草稿:`POST /tasks/{taskId}/schemas/draft`
- 独立草稿:`POST /schemas/draft`
- 保存草稿:`PUT /schemas/{versionId}/draft`
- 发布:`POST /schemas/{versionId}/publish`

### 范围与边界
- 物料添加方式当前是"点击追加 + 上下箭头排序",未引入 dnd-kit;计划书阶段二/三可直接替换为真正拖拽,API 与 Schema 数据结构不变。
- 不影响 Labeler / Reviewer 端、Login、其它 Owner 子页;不引入新依赖。

### 验证
- `npm run build` 通过。
- 浏览器访问:
  - `/owner/templates`:5 条样例模板,可搜索/过滤/复制/导出/打开 Designer。
  - `/owner/templates/designer?versionId=sv-001`:加载样例字段,可点物料追加、点字段切换属性面板、配置必填/正则/联动、预览表单、导出 JSON、保存/发布。


## 增量实现:Owner 数据看板

### 信息架构对齐
对照《项目实施计划书》4.6 / 4.4 / 4.5,数据看板覆盖任务进度、AI 通过率、人工审核流转、标注员效率与争议样本。

### 完成内容
- `frontend/src/types/dashboard.ts`:新增 `DashboardOverview / TaskProgress / ReviewDistribution / LabelerPerformance / SubmissionTimelineMonth / RecentTaskActivity / RoleBreakdown / DisputeStats`,字段命名严格对齐计划书 4.4 AIReviewResult.decision 与 4.5 状态机。
- `frontend/src/api/dashboard.ts`:新增 `dashboardApi.{getOverview, getTaskProgress, getReviewDistribution, getLabelerPerformance, getSubmissionTimeline, getRecentActivities, getRoleBreakdown, getDisputes}`,路径全部以 `/dashboard/*` 命名,后端实现后无需重构调用层。
- `frontend/src/pages/owner/OwnerDashboard.tsx` 整页重写,共 5 行布局:
  1. **顶部欢迎区**:标题 + 周期文案 + 时间范围 Segmented(7/30/90 日)+ 发布公告 CTA + 演示模式 Tag。
  2. **6 张 KPI 卡**:活跃任务 / 活跃标注员 / 待人工审核 / 今日新增提交 / AI 通过率 + 公告卡;每张配主题色软底 + 圆形图标 + 环比 delta(上箭头绿/下箭头红)。
  3. **任务进度柱状图(2/3 宽)+ AI 审核分布环形图(1/3 宽)**:柱状图三色堆叠(通过/打回/待处理);环形用纯 SVG 绘制 5 段(AI 通过 / 需人工 / AI 拒绝 / 人工通过 / 打回),配双列 legend 与底部下载按钮。
  4. **近期任务申报(1/3 宽)+ 标注员审核流转表(2/3 宽)**:左列头像 + 名字 + 任务 + Pending/Approved/Rejected 彩色 Tag;右表可排序,Action 列基于 approved/submitted 比例动态计算 Approved/Pending。
  5. **角色环形 + 团队列表 + 排期日历 + 议程(各 1/4 宽)**:日历用纯 div grid 自绘当月 7×6 格子,标记今天;议程绿色图标 + 标签 + 时间 + 三点菜单。
  6. **标注员绩效(2/5 宽)+ 月度准时提交率(3/5 宽)**:左列每行用 SVG `<circle>` 自绘评分环(70% 绿 / 50% 橙 / 红);右图按月份堆叠 onTime/late/absent 三段,纯 div 实现避免引入图表库。
- `frontend/src/styles/global.css`:新增 `.dashboard-* / .bar-* / .donut-* / .activity-row / .leave-* / .role-* / .team-* / .schedule-* / .meeting-* / .perf-* / .score-*` 样式段,所有可视化均用纯 CSS + 内联 SVG,不依赖 echarts / recharts。

### 视觉语言
- 同登录页 / 顶栏 / 侧栏统一:白卡 + 14-18px 圆角 + 柔阴影 + 主色 #2f7bff + 主题色软底图标。
- KPI 卡的软底色直接对齐 HR Dashboard 参考图(蓝/橙/黄/绿/红/橙渐变公告)。
- 柱状图与环形图的色码与 Labeler Overview 中"AI 审核分布"保持一致,跨页一致性。

### 接口预留
- 上述 8 个 `/dashboard/*` 路径全部已通过 `dashboardApi` 调用,后端 Spring Boot 实现后只需在 client.ts 中切换基础 URL,前端无需改动。
- 后端不可达时,组件级回落到 `sampleDashboard`,所有图表仍可演示。

### 范围与边界
- 不引入 echarts / recharts / dayjs 等图表/时间库,避免增加依赖体积。
- 只重写 `OwnerDashboard.tsx`,其它页面、Login、路由、依赖不变。

### 验证
- `npm run build` 通过。
- 浏览器访问 `/owner/dashboard`:
  - 顶部周期切换 7/30/90 日触发数据重拉;接口未连接时演示模式 Tag 出现。
  - 9 个任务的进度柱、5 段 AI 审核分布、月度堆叠柱、4 名标注员评分环、5 类角色环形、日历今天高亮全部正常渲染。

## 增量实现:任务分发策略后端闭环

### 完成内容

- `backend/src/main/java/com/labelhub/backend/task/AssignmentRecord.java`:新增内部查询记录,承载 `assignments` 表字段和最新 Schema 版本 ID。
- `backend/src/main/java/com/labelhub/backend/task/AssignmentResponse.java`:新增接口响应模型,字段对齐前端 `Assignment`。
- `TaskController`:新增 `POST /api/tasks/{taskId}/claim` 和 `GET /api/assignments/mine`;扩展 `GET /api/market/tasks` 查询参数,支持 `strategy / mediaType / aiReview / sortBy`。
- `TaskService`:
  - Labeler 认领时校验角色、任务状态、截止时间、配额和策略。
  - `first-come` / `quota` 从未分配的 `items` 中取 1 条写入 `assignments`,并把 item 标为 `claimed`。
  - 同一标注员重复认领同一任务时直接返回已有 assignment,避免重复扣减配额。
  - `assigned` 策略在没有预分配 assignment 时拒绝自助认领,避免任意标注员抢占指派任务。
  - 任务市场响应按当前用户计算 `claimedByMe`,并返回真实媒体类型列表。
- `TaskRepository`:新增任务行锁、assignment 查询/创建、任务配额统计、可认领 item 查询、媒体类型查询;市场列表 SQL 增加策略、媒体类型、AI 预审过滤和排序。

### 范围与边界

- 本阶段未新增数据库迁移,继续沿用 `tasks.reward_rule.strategy` 和已有 `assignments/items` 表。
- `assigned` 的指派名单表和 Owner 端指派配置尚未建立,因此只支持“已有预分配记录返回”,不支持在领取接口里临时指定标注员。
- 没有关联数据集 item 的任务会返回 `NO_AVAILABLE_ITEM`,不会伪造 assignment。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。

## 增量实现:我的任务接入真实认领数据

### 根因

任务市场认领接口已经能写入 `assignments`,但 `frontend/src/pages/labeler/MyTasks.tsx` 仍然渲染本地静态三行样例,没有调用 `labelerApi.listMyAssignments()`。因此认领成功后,任务市场卡片会变成“继续作答”,但「我的任务」列表不会出现新任务。

### 完成内容

- `AssignmentRecord`:补充任务标题、Owner、任务配额、已认领数、任务元数据 JSON 和 assignment 更新时间。
- `AssignmentResponse`:补充 `taskTitle / taskType / taskTypeKey / ownerName / quotaUsed / quotaTotal / claimedAt / submittedAt / updatedAt`。
- `TaskRepository.queryAssignments()`:关联 `tasks`、`users` 和配额统计子查询,让 `GET /api/assignments/mine` 具备列表展示所需字段。
- `TaskService.toAssignmentResponse()`:从 `reward_rule` 读取任务类型,并组装前端可直接展示的 assignment 响应。
- `frontend/src/types/labeler.ts`:扩展 `Assignment` 类型,保持原字段兼容。
- `frontend/src/pages/labeler/MyTasks.tsx`:改为页面加载时调用 `labelerApi.listMyAssignments()`,接口失败时才显示演示数据;列表展示 assignment/item ID、Owner、状态、已领/配额和更新时间。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm run build` 通过。

## 增量实现:新增多角色演示账号

### 根因

顶部菜单当前按登录用户的 `roles` 列表切换工作台。`owner`、`labeler`、`reviewer` 三个演示账号分别只有一个角色,所以使用单角色账号登录时不会出现可切换到其它工作台的角色入口。这不是顶栏切换逻辑失效,而是演示账号数据模型与“账号内角色切换”的语义不匹配。

### 完成内容

- `backend/src/main/java/com/labelhub/backend/auth/AuthRepository.java`:将 `upsertDemoUser` 扩展为可接收多个 `roleCode`,同一个演示账号可一次绑定多个角色;注册用户创建仍保持单角色逻辑不变。
- `backend/src/main/java/com/labelhub/backend/auth/DemoUserInitializer.java`:新增 `demo / demo123` 演示账号,显示名为 `All Roles Demo`,同时绑定 `owner`、`labeler`、`reviewer`。
- `backend/src/main/java/com/labelhub/backend/auth/AuthProperties.java` 与 `backend/src/main/resources/application.yml`:新增 `labelhub.auth.demo-users.all-roles-password`,默认值为 `demo123`,也支持通过 `LABELHUB_DEMO_ALL_ROLES_PASSWORD` 覆盖。
- `backend/src/main/resources/db/migration/V2__seed_demo_auth_users.sql`:同步补齐 `demo` 用户和三种角色关系,保证新建本地数据库时种子数据一致。
- `frontend/src/pages/Login.tsx`:演示账号区域新增 `All Roles` 快捷按钮,默认以 Owner 角色登录 `demo / demo123`;登录后顶栏可切换 Labeler / Reviewer。
- `frontend/src/styles/global.css`:补齐演示账号按钮样式,四个入口按两列展示。
- 清理未采用的 `frontend/src/utils/demoAccounts.ts`,避免后续误接入“切换演示账号”的旧方案。
- `问题点归档.md`:记录单角色演示账号无法切换角色的原因和处理方式。

### 范围与边界

- 未修改顶部菜单切换逻辑,继续保持“当前账号已有角色之间切换”的语义。
- 未新增认证接口,未改变 token、权限聚合和注册用户角色模型。
- 已有 `owner / owner123`、`labeler / labeler123`、`reviewer / reviewer123` 三个单角色账号继续保留。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm run build` 通过。
- 前端构建仍有既有的大 chunk 提示,不影响本次功能编译结果。

## 增量实现:README 当前状态同步

### 完成内容

- 重写根目录 `README.md`,修复原文档中文乱码和早期状态说明过时的问题。
- 补充当前项目定位与已实现功能,覆盖认证会话、Owner 任务/数据集/模板/看板、Labeler 任务市场认领/我的任务、Reviewer 入口。
- 补充技术栈说明:React 18、Vite 8、Ant Design 6、Spring Boot 3.4.5、MySQL、Redis。
- 补充本地环境变量和依赖说明,明确 Redis 是登录 token 的必需依赖。
- 补充数据库初始化步骤,说明手动执行 `V1__init_labelhub_schema.sql` 和 `V2__seed_demo_auth_users.sql`。
- 更新后端/前端启动命令、默认端口、Vite `/api` 代理和 Swagger / OpenAPI 地址。
- 更新演示账号说明,重点标注 `demo / demo123` 多角色账号,并保留三个单角色账号。
- 补充常用前端路由、主要后端 API、样例数据路径、构建检查命令和常见问题。

### 范围与边界

- 仅更新文档,未修改业务代码、配置逻辑和依赖。
- `infra/docker-compose.yml` 仍按当前仓库真实状态标注为占位文件,未补充不可用的 Docker 启动说明。

### 验证

- 使用 `rg` 检查 README 中的多角色账号、Redis、SQL 初始化文件、任务认领 API、我的任务 API、Swagger 和构建命令均已覆盖。
- 本次为纯文档更新,未重新运行前后端构建。

## 增量实现:登录页演示账号一键入口

### 完成内容

- `frontend/src/pages/Login.tsx`:
  - 新增 `demoAccounts`,内置 `owner / owner123`、`labeler / labeler123`、`reviewer / reviewer123` 三组演示账号。
  - 登录卡片增加“演示账号”区域,提供 Owner、Labeler、Reviewer 三个按钮。
  - 点击演示按钮直接调用现有 `authApi.login`,成功后沿用原离场动画和角色跳转逻辑。
  - 增加 `signingIn` 状态,演示登录或手动登录过程中禁用其它登录入口,避免重复提交。
- `frontend/src/styles/global.css`:新增 `.login-demo-*` 样式,使用轻量胶囊按钮,视觉上与当前登录页输入框/主按钮保持一致。

### 范围与边界

- 未新增后端接口,继续使用现有 `POST /api/auth/login`。
- 未改变 token 存储、注册逻辑、角色选择下拉和登录后的路由规则。

### 验证

- `frontend/npm run build` 通过。

## 增量实现:登录默认跳转与顶部用户菜单完善

### 完成内容

- `frontend/src/api/auth.ts`:
  - 新增当前用户本地缓存 `labelhub_current_user`。
  - 登录成功后保存 `response.user`,退出登录时同时清理 token 和用户缓存。
  - `getCurrentUser()` 调用 `/auth/me` 后同步刷新本地用户缓存。
- `frontend/src/utils/authNavigation.ts`:新增角色导航工具,统一维护 Owner / Labeler / Reviewer 的默认入口和展示名称。
- `frontend/src/pages/Login.tsx`:
  - 页面加载时如果已有 token 和用户缓存,直接跳转到默认角色入口。
  - 如果只有 token,会调用 `/auth/me` 刷新用户后跳转;失败则清理失效 token 并停留登录页。
  - 登录成功后改用统一默认入口:Owner 到 `/owner/tasks`,Labeler 到 `/labeler`,Reviewer 到 `/reviewer`。
- `frontend/src/router/index.tsx`:根路径 `/` 改为按本地登录态跳转,已登录进入默认工作台,未登录进入 `/login`。
- `frontend/src/layouts/AppLayout.tsx`:
  - 顶部头像菜单显示当前用户名称和用户名。
  - 菜单增加“切换角色”分组,只展示当前用户拥有的 Owner / Labeler / Reviewer 角色,点击跳转到对应工作台。
  - “退出登录”改为调用 `authApi.logout()`,清理本地会话后回到 `/login`。
  - 进入工作台时调用 `/auth/me` 刷新用户,token 失效时自动清理并返回登录页。
- `frontend/src/styles/global.css`:补充顶部用户信息和菜单摘要样式。

### 范围与边界

- 未新增后端接口,继续复用 `/api/auth/login`、`/api/auth/me`、`/api/auth/logout`。
- 未引入全局状态管理库,仅使用 localStorage 缓存当前用户以支持刷新后的轻量跳转。

### 验证

- `frontend/npm run build` 通过。


## 增量实现:Designer 接入 dnd-kit 真拖拽

### 背景
《LabelHub 课题实现要求》明确要求 Designer 物料支持拖拽,前一版用「点击追加 + 上下箭头」轻量替代,现在升级为真正的拖拽。

### 完成内容
- 新增依赖:`@dnd-kit/core / @dnd-kit/sortable / @dnd-kit/utilities`(对齐计划书 3.1 推荐技术栈)。
- `OwnerTemplateDesigner.tsx`:
  - 用 `<DndContext>` 包裹整个 Designer,`PointerSensor` 设置 `activationConstraint.distance: 8`,避免与点击/选中冲突。
  - 新增 `DraggableMaterial`:左侧物料按钮,基于 `useDraggable`,既保留点击追加,又支持拖拽到画布;`isDragging` 时半透明。
  - 改造 `Canvas`:基于 `useDroppable`(drop id = `canvas-droppable`)+ `SortableContext`(垂直列表策略);drop 区在拖入时显示蓝色虚线高亮。
  - 改造 `FieldCard`:基于 `useSortable`,新增专用拖动手柄 `<DragOutlined />`(`field-drag-handle`),拖动手柄持有 dnd 监听,卡片本体仍可点击切换 active;原地拖动时本体透明,真实视觉由 `DragOverlay` 渲染浮动跟随物。
  - `handleDragStart / handleDragEnd` 统一处理三种行为:
    1. 物料 → 画布字段卡:在该字段前插入新字段(用 createField + splice)
    2. 物料 → 画布空白:追加到末尾
    3. 字段 → 字段:`arrayMove(fields, fromIdx, toIdx)` 重排
  - `<DragOverlay dropAnimation={null}>` 渲染半透明的物料卡 / 字段卡跟随光标。
- `global.css`:新增 `.material-item.is-dragging / is-overlay`、`.canvas-empty.is-drop-over`、`.canvas-list.is-drop-over`、`.field-drag-handle`、`.field-card.is-overlay`,与既有蓝色主题统一。

### 技术取舍
- 拖拽是纯前端行为,不影响后端契约。物料拖入或字段重排后,Schema 在内存中调整 `fields` 顺序,保存草稿/发布时按既有 `PUT /schemas/{id}/draft` / `POST /schemas/{id}/publish` 接口提交。
- 保留原"上下箭头排序"按钮和点击物料追加,二者并行不冲突,提供更高的可达性。

### 验证
- `npm run build` 通过,产物 css 46.81 kB,js 1.35 MB(含 dnd-kit)。
- 浏览器侧:左侧物料按下 8px 后激活拖拽,可拖到画布任意字段前或空白末尾;字段卡左侧手柄拖动可跨位置重排;DragOverlay 跟随光标显示半透明跟随物。
## 增量实现:Labeler 任务市场真实接口优先

### 根因

`TaskMarket.tsx` 之前混入了两套互相冲突的数据源逻辑:

- 页面加载时先调 Owner 端 `/api/tasks`,这会导致多角色 `demo` 账号切到 Labeler 后只能看到自己作为 Owner 发布的任务,看不到其他 Owner 历史发布的市场任务。
- 认领接口失败时前端仍然弹出“认领成功(演示模式)”并本地扣减额度,把真实失败伪装成成功。

另外该文件在连续修改过程中还残留了 `ownerApi` / `OwnerTask` 相关未清理代码,直接导致 TypeScript 编译错误。

### 完成内容

- 重写 `frontend/src/pages/labeler/TaskMarket.tsx`,移除 Owner 私有任务列表适配逻辑,任务市场只使用真实的 `labelerApi.listMarketTasks()`。
- 保留 `public/sample-datasets/market-tasks.json` 作为后端不可用时的纯展示兜底,页面会明确显示“演示模式 · 接口未连接”。
- 新增认领错误提取逻辑,优先展示后端返回的业务错误 `message`。
- `handleClaim()` 只有在后端 `POST /api/tasks/{taskId}/claim` 真正成功时才会:
  - 弹出“认领成功”
  - 更新本地 `claimedByMe`
  - 扣减本地剩余额度
- 演示模式下点击认领会直接提示“后端未连接,无法真实认领任务”,不再伪造成功。
- 清理文件中残留的错误引用和乱码字符串,恢复为可编译版本。

### 验证

- `frontend/npm run build` 通过。
- 构建后 `TaskMarket.tsx` 中已不存在 `ownerApi` / `OwnerTask` 依赖;认领逻辑只保留真实接口调用和演示模式告警。
## 增量实现:任务认领冲突提示细化

### 根因

认领接口真实返回 `409` 后,前端虽然已经不再伪造成功,但仍主要透传后端原始英文 `message`。对于最常见的 `NO_AVAILABLE_ITEM`,用户并不知道下一步应该去 Owner 数据集页补导入数据。

### 完成内容

- 更新 `frontend/src/pages/labeler/TaskMarket.tsx` 中的 `getClaimErrorMessage()`。
- 优先读取后端返回的 `payload.code`,按业务码映射前端中文提示。
- `NO_AVAILABLE_ITEM` 现在会显示:
  - `该任务还没有可认领的数据项，请先让 Owner 在数据集页导入或追加数据。`
- 同时补齐以下常见业务冲突提示:
  - `TASK_QUOTA_EXHAUSTED`
  - `TASK_NOT_PUBLISHED`
  - `TASK_EXPIRED`
  - `ASSIGNED_TASK_NOT_CLAIMABLE`

### 验证

- `frontend/npm run build` 通过。
- 代码中已存在 `NO_AVAILABLE_ITEM` 到中文提示的明确映射。
## 增量实现:Owner 任务发布页接入数据集与分发策略

### 根因

之前的 `OwnerTasks.tsx` 只是一个表单壳子,存在几个实质问题:

- 发布抽屉不能选择现有数据集,任务与可认领数据项的关联仍然依赖外部手工流程。
- 截止时间还是纯文本输入,容易输错格式。
- `assigned` 和 `quota` 在发布页没有任何额外配置,后端也缺少与之对应的保存和执行逻辑。
- 打开已有草稿任务时,抽屉几乎只会改状态,不会真正保存表单字段。

### 完成内容

- 后端:
  - 扩展 `CreateTaskRequest` 和 `TaskMetadata`,支持 `datasetId`、`maxClaimPerUser`、`assignedLabelerIds`。
  - 新增 `PUT /api/tasks/{taskId}` 编辑任务内容接口。
  - 新增 `GET /api/tasks/assignable-labelers` 供 Owner 选择可指派标注员。
  - `DatasetRepository` 新增数据集重绑逻辑,选中现有数据集后会把该数据集及其 `items.task_id` 重新关联到当前任务。
  - `assigned` 策略在任务发布时会为选中的标注员预创建 assignment。
  - `quota` 策略新增“每人最多可认领”限制,认领时会校验 `TASK_CLAIM_LIMIT_REACHED`。
  - `OwnerTaskResponse` / `TaskRecord` 补充任务发布页需要的字段,包括 `datasetId`、`maxClaimPerUser`、`assignedLabelerIds`。

- 前端:
  - 重写 `frontend/src/pages/owner/OwnerTasks.tsx`。
  - 发布抽屉新增“关联数据集”选择器,直接读取当前 Owner 的现有数据集。
  - 截止时间改为 Ant Design `DatePicker` + `showTime`。
  - 分发策略细化:
    - `first-come`: 无额外配置。
    - `assigned`: 显示可多选的标注员列表。
    - `quota`: 显示“每人最多可认领”输入框。
  - 已有草稿/已发布任务现在会走真实的 `updateTask()` 保存表单字段,不再只改状态。
  - `ownerApi` 增加 `updateTask()` 和 `listAssignableLabelers()`。
  - `TaskMarket.tsx` 补充 `TASK_CLAIM_LIMIT_REACHED` 中文提示。

### 范围与边界

- 关联模板仍保持只读占位,没有实现切换逻辑,与你这次要求一致。
- 现有数据集在当前数据模型下是单任务归属;从发布页选择后会重绑到当前任务,不是做只读引用。
- `assigned` 当前实现为“发布时预分配 assignment”,不会额外生成新的指派名单管理页。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm run build` 通过。

## 增量实现:新建数据集弹窗任务可选与类型自定义

### 根因

Owner 数据集页的新建/导入弹窗仍把“关联任务”作为必填项,但任务发布页已经支持从现有数据集反向绑定任务。继续强制先选任务会阻断“先准备数据集、后发布任务”的流程。同时数据集类型前端只给固定下拉项,后端也只允许 `qa_quality` / `preference_compare`,无法承载项目自定义数据类型。

### 完成内容

- 前端:
  - `OwnerDatasets.tsx` 中“新建数据集”和“上传文件数据”的“关联任务”改为可选,下拉保留 `allowClear`。
  - 无可关联任务时提示改为信息提示,说明可以先创建/导入未绑定任务的数据集。
  - 数据集类型输入改为 `AutoComplete`,保留内置类型下拉,同时支持手动输入自定义类型。
  - `DatasetKind` 类型放开为内置类型 + 自定义字符串。
  - 自定义类型数据集使用通用 JSON 表格和详情预览,不再误用偏好对比表格。
- 后端:
  - `CreateDatasetRequest.kind` 增加 64 字符长度校验。
  - `DatasetService` 放开固定类型枚举限制,只做空值、长度和控制字符校验,并统一规范化类型 token。
  - 未绑定任务的数据集描述显示为“未关联任务”。
- 数据库脚本:
  - 更新 `V1__init_labelhub_schema.sql`,新库初始化时 `datasets.task_id` / `items.task_id` 允许为空,外键删除策略改为 `ON DELETE SET NULL`。
  - 新增 `V3__allow_unbound_datasets.sql`,用于已有本地库手动迁移。
  - README 数据库初始化部分补充 V3 执行命令。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm run build` 通过。
- 未直接执行 `V3__allow_unbound_datasets.sql`,因为会修改本地 MySQL 表结构;已有库需要按 README 手动执行该迁移后才能落库未绑定任务的数据集。

## 增量实现:模板搭建后端真实落库

### 根因

Owner 端“模板搭建 / Schema Designer”前端已经有列表、Designer、保存草稿和发布入口,但 `/api/schemas` 相关后端接口此前不存在。任务创建时虽然会写 `task_schema_versions`,但只保存占位 JSON,且旧表结构要求 `task_id` 非空,无法支持独立模板草稿。

### 完成内容

- 后端:
  - 新增 `backend/src/main/java/com/labelhub/backend/schema` 模块,包含 Controller / Service / Repository / DTO。
  - 实现 `GET /api/schemas`、`GET /api/schemas/{versionId}`、`POST /api/schemas/draft`、`POST /api/tasks/{taskId}/schemas/draft`、`PUT /api/schemas/{versionId}/draft`、`POST /api/schemas/{versionId}/publish`。
  - 响应字段对齐前端 `types/schema.ts`,包括 `versionId`、`versionNumber`、`taskId`、`taskTitle`、`name`、`description`、`status`、`fields`、`updatedAt`、`createdBy`。
  - `schema_json` 统一保存 `{ name, description, fields }`,并兼容读取旧占位结构中的 `schemaLabel`。
  - 所有模板接口要求 Owner 角色,只能访问当前 Owner 创建的模板;绑定任务草稿会校验任务归属。
  - 草稿可更新;已发布版本更新时返回 `409 SCHEMA_ALREADY_PUBLISHED`;发布只冻结当前版本。
  - 任务创建时自动生成的占位 Schema 也改为统一 JSON 结构。
- 数据库:
  - 更新 `V1__init_labelhub_schema.sql`,新库初始化时 `task_schema_versions.task_id` 可空,并带 `updated_at`。
  - 新增 `V4__support_schema_designer_backend.sql`,用于旧库放开 `task_id` 非空约束并增加 `updated_at`。
  - README 数据库初始化部分补充 V4 手动迁移命令。
- 前端:
  - 模板列表页改为使用真实 `/api/schemas`,接口失败时显示错误,不再回落到样例模板。
  - 模板复制改为先读取源模板再调用 `createStandaloneDraft()` 创建真实草稿。
  - Designer 新建模板首次保存时先调用 `POST /api/schemas/draft`,拿到真实 `versionId` 后替换 URL。
  - 保存草稿、发布模板失败时显示真实错误,不再伪造成“演示模式成功”。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm run build` 通过。
- `git diff --check` 已执行,未发现空白格式错误。
- 未直接执行 `V4__support_schema_designer_backend.sql`,因为会修改本地 MySQL 表结构;已有库需要按 README 手动执行该迁移后再验证模板真实落库。

## 增量实现:发布任务关联已发布模板

### 根因

Owner 已经可以创建并发布独立模板,但任务发布弹窗里的“关联模板”仍是只读占位字段。后端任务创建时也只会按任务自动生成占位 Schema,不会使用模板页发布出来的真实版本,导致任务保存后 Labeler 侧拿不到 Owner 选择的模板版本 ID。

### 完成内容

- 前端:
  - `OwnerTasks.tsx` 在任务页初始化时加载 `/api/schemas`,并过滤出 `published` 模板作为可选项。
  - “关联模板”从只读 `Input` 改为可搜索 `Select`,展示模板名称、版本号和字段数。
  - 发布任务时未选择已发布模板会阻止提交;保存草稿不强制关联模板。
  - 任务创建/更新请求新增 `schemaVersionId`,并同步传递模板展示标签 `schema`。
  - 重新打开已有任务时,按后端返回的 `schemaVersionId` 回显已关联模板。
  - 模板列表异步加载完成后会再次回填已发布模板 ID,避免先打开抽屉、后加载模板列表时显示为空。
- 后端:
  - `CreateTaskRequest` 新增 `schemaVersionId`。
  - `TaskMetadata` 保存所选模板版本 ID 和版本号,继续兼容旧的 `schema` 展示字段。
  - `TaskService` 校验模板必须属于当前 Owner 且状态为 `published`;未发布模板返回 `409 SCHEMA_NOT_PUBLISHED`,找不到或无权限返回 `404 SCHEMA_NOT_FOUND`。
  - 发布任务时按最终 `schemaVersionId` 二次查库确认模板状态为 `published`,避免历史任务内 draft Schema 快照绕过发布校验。
  - 历史任务仍可在非发布状态回退使用任务内自动 Schema 快照,不破坏已有草稿数据。
  - Owner 任务列表、Labeler 任务市场、任务认领响应优先返回元数据中的 `schemaVersionId`,确保后续 Renderer 按正确版本拉取模板。
  - `SchemaRecord` 改为 `public record`,允许任务服务复用 Schema Repository 查询结果做归属和状态校验。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。
- `npm run build` 在当前 PowerShell 被执行策略拦截,已改用 `npm.cmd run build` 完成等价验证。
- `git diff --check` 通过;仅提示部分文件下次由 Git 触碰时会进行 LF/CRLF 换行转换。


## 增量实现:Reviewer 端 5 个页面

### 信息架构对齐
对照《项目实施计划书》4.5 / 4.4 / 4.6 / 5.2:
- 状态机:SUBMITTED → AI_REVIEWING → REVIEWING → APPROVED / RETURNED / REVISED / DISPUTE → ACCEPTED / EXPORTED
- 决策动作:APPROVE / RETURN / REVISE / ESCALATE
- AI 决策:PASS / REJECT / NEED_HUMAN_REVIEW

### 完成内容
- `frontend/src/types/reviewer.ts`:新增。`ReviewBatch / AnnotationToReview / AiReviewResult / SubmitReviewRequest / DisputeItem / ReviewerOverview / ReviewerPageResult`。
- `frontend/src/api/reviewer.ts`:新增。`listBatches / claimBatch / listAnnotations / submitReview / listDisputes / resolveDispute / getOverview` 全部对齐计划书 5.2。
- `frontend/public/sample-datasets/`:新增 `reviewer-batches.json`、`reviewer-annotations.json`、`reviewer-disputes.json` 三份样例,覆盖 pending/in_review/completed 三种状态、PASS/NEED_HUMAN_REVIEW/REJECT 三种 AI 决策、open/resolved 两种争议状态。
- `frontend/src/pages/reviewer/`:重写 5 个页面
  - `ReviewerOverview.tsx`:4 张 KPI + 待审批次列表 + 质量指标卡(双审一致率 / 抽检覆盖率 / 累计已审)+ 接口预留 + 工作建议提示。
  - `ReviewerQueue.tsx`:两态切换:无 batchId 显示批次列表(关键词 + 状态过滤 + 每行进度条);带 batchId 进入逐条审核台:左 raw + answer JSON 对比、右 AI 评分维度卡 + AI 决策 Tag + 通过/打回/升级争议按钮 + 上下条切换;打回必填原因(对齐 4.5 必填要求)。
  - `ReviewerAi.tsx`:AI 结果聚合表,按决策过滤,行内一键确认/打回。
  - `ReviewerDisputes.tsx`:争议样本 List,Modal 终审,展示历史轮次。
  - `ReviewerReports.tsx`:KPI(通过率 / 打回率 / 争议率 / AI 一致率)+ 5 月份堆叠柱 + 接口预留。
- `index.ts` 重写,删除旧 `ReviewerHome.tsx`。
- `router/index.tsx`:挂 5 条 reviewer 子路由(`/reviewer`、`/reviewer/queue`、`/reviewer/ai`、`/reviewer/disputes`、`/reviewer/reports`)。
- `AppLayout.tsx`:reviewer 菜单从单项改为三组(WORKSPACE / REVIEW / REPORTS);`segmentLabel` 增加 `overview / queue / ai / disputes / reports`;`resolveSelectedKey` 增加 reviewer 分支。
- `global.css`:新增 `.reviewer-* / .review-section-label / .review-json / .review-decision-card / .ai-score-* / .ai-comment / .dispute-*` 等样式段,与既有蓝色主题保持一致。

### 接口预留
全部位于 `api/reviewer.ts`,后端 Spring Boot 实现后无需改前端:
- `GET /reviewer/overview?days=` — KPI 与汇总
- `GET /reviewer/batches` — 批次列表
- `POST /reviewer/batches/{id}/claim` — 领取批次
- `GET /reviewer/batches/{id}/annotations` — 该批次待审条目
- `POST /reviewer/annotations/{id}/decision` — 提交审核结论
- `GET /reviewer/disputes` — 争议样本
- `POST /reviewer/disputes/{id}/resolve` — 终审

### 范围与边界
- 不影响 Owner / Labeler / Login / 路由其它部分,不引入新依赖。
- 后端不可达时全部回落到三份样例 JSON,演示模式 Tag 自动出现。

### 验证
- `npm run build` 通过(含 dnd-kit / antd icons / 全部 reviewer 资源,产物 51.91 kB CSS / 1.56 MB JS)。
- 浏览器 `/reviewer` 看到 4 张 KPI + 待审批次列表 + 质量指标卡;`/reviewer/queue` 切换列表与审核台两态;打回需填原因;升级争议跳到下一条;`/reviewer/ai` 行内确认/打回;`/reviewer/disputes` Modal 终审;`/reviewer/reports` 堆叠柱与 KPI 渲染正常。


## 增量实现:Labeler 答题页(Renderer)

### 信息架构对齐
对照《项目实施计划书》4.3 / 1.4 / 5.2:
- 入口:从「我的任务」点"继续答题" → `/labeler/answer/:assignmentId`
- 核心闭环:GET 题目 + Schema + 草稿 → 渲染 → 自动保存草稿 → 校验 → 提交触发 AI 预审 → 跳下一题
- 多模态原题渲染:text / image / video / markdown
- 字段渲染:show-item / 单行 / 多行 / 富文本 / 单选 / 多选 / 标签 / JSON
- 打回项进入时显示 `returnReason`

### 完成内容
- `frontend/src/types/labeler.ts`:新增 `AssignmentItem`(rawPayload + fields + position + returnReason + draft)。
- `frontend/src/api/labeler.ts`:新增 `getAssignmentItem(assignmentId)`,对齐 `GET /assignments/{id}/item`。
- `frontend/public/sample-datasets/labeler-assignment.json`:样例题目(商品标题清洗任务,含 5 个字段、position、prev/next 链接)。
- `frontend/src/pages/labeler/AnswerPage.tsx`:答题页主体。
  - 顶部:退出 + 面包屑(我的任务 / 任务名 / 第 N / M 题)+ 演示模式 Tag + 草稿状态 + 上一题 / 跳过 / 提交并继续。
  - 进度条按 `position.index / total` 渲染。
  - 打回项:Alert 显示 `returnReason`。
  - 左 10 栅格:原题数据(image/video/markdown 直显,文本字段卡片展示)。
  - 右 14 栅格:Schema 字段渲染,8 种 kind 全部覆盖;`Tag.CheckableTag` 用于 `tags` 字段;`Radio.Button` 用于单选。
  - 必填 + maxLength + JSON 解析 校验,失败时 message + 字段下方红字提示。
  - 草稿自动保存:答案变化后 1500ms 节流调 `PUT /assignments/{id}/draft`,失败回落到本地标记;离开页面卸载时再发一次。
  - 提交:`POST /assignments/{id}/submit`,成功跳 `nextAssignmentId`(若有);否则回「我的任务」。
- `frontend/src/pages/labeler/index.ts`:导出 `AnswerPage`。
- `frontend/src/router/index.tsx`:新增 `/labeler/answer/:assignmentId`。
- `frontend/src/pages/labeler/MyTasks.tsx`:点"继续答题"跳到答题页。
- `frontend/src/styles/global.css`:新增 `.answer-* / .answer-tag-group / .answer-radio-group` 等样式段,与既有蓝色主题统一。

### 接口预留
- `GET /assignments/{id}/item` — 题目 + Schema + 草稿(已在 `labelerApi.getAssignmentItem`)
- `PUT /assignments/{id}/draft` — 草稿自动保存
- `POST /assignments/{id}/submit` — 提交触发 AI 预审

### 范围与边界
- 不影响 Owner / Reviewer / Login / 其它 Labeler 子页;不引入新依赖。
- 富文本字段 MVP 仍用 TextArea,后续阶段可换 antd-rich-text 或自研。
- 不写入 lockedUntil 计时器,后续可在 `position` 中显示剩余锁定时长。

### 验证
- `npm run build` 通过。
- 浏览器进入 `/labeler/my-tasks` 点"继续答题",进入 `/labeler/answer/:id`:
  - 演示模式下加载样例题目,5 个字段全可填,必填校验生效,maxLength 即时计数;
  - 编辑后 1.5s 自动保存,顶部出现"已保存于 xx:xx";
  - 提交后跳到下一题(若有 nextAssignmentId)。


## 增量实现:Labeler 答题页后端闭环

### 完成内容
- 新增 `backend/src/main/java/com/labelhub/backend/annotation/` 模块,包含 Controller / Service / Repository / DTO。
- 落地 4 个 Labeler 答题接口:
  - `GET /api/assignments/{assignmentId}/item`:返回 assignment/task/item 基本信息、`rawPayload`、Owner 发布任务绑定的模板字段、当前位置、打回原因与草稿。
  - `GET /api/assignments/{assignmentId}/draft`:返回当前 assignment 草稿;无草稿时返回 JSON `null`。
  - `PUT /api/assignments/{assignmentId}/draft`:按 `drafts.assignment_id` upsert `{ answerJson }`。
  - `POST /api/assignments/{assignmentId}/submit`:事务内写入 annotation、更新 assignment/item 状态、删除草稿、创建 `ai_review_jobs` pending 占位记录。
- Schema 使用规则已按计划实现:优先读取 `tasks.reward_rule.schemaVersionId`;旧任务 fallback 到任务绑定的最新 `task_schema_versions`;模板必须是 `published`。
- 权限与状态校验已按计划实现:接口要求当前用户具备 `labeler` 角色,且只能访问自己的 assignment;保存/提交只允许 `claimed` 或 `returned`;任务必须处于 `published`。
- 提交校验已按计划实现:校验 `schemaVersionId` 一致性、`answerJson` 必须是 JSON object、字段 `required`、`maxLength`、`json-editor` 格式;`show-item` 不参与提交校验。
- AI 预审占位已按计划实现:`job_key = annotation:{annotationId}:schema:{schemaVersionId}:prompt:default-v1`,`status = pending`。
- 前端 `AnswerPage.tsx` 修正提交失败行为:提交接口失败时显示真实错误,不再提示“已提交(演示模式)”,也不跳转下一题。

### 验证
- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。
- `git diff --check` 通过;仅出现已存在文件的 CRLF 换行提示。

### 未覆盖
- 未执行真实 MySQL 手动闭环;需要本地库已完成 V4 迁移并有 Owner 发布模板、发布任务、导入数据集、Labeler 认领 assignment 后再验证接口写库行为。


## 增量实现:修复表格分页大小切换

### 完成内容
- 修复表格分页大小无法切换到 `20/50/100/200` 的问题。
- 将以下页面的 Ant Design Table 分页配置从受控固定值 `pageSize: 10` 改为初始值 `defaultPageSize: 10`:
  - `frontend/src/pages/owner/OwnerTasks.tsx`
  - `frontend/src/pages/owner/OwnerDatasets.tsx`
  - `frontend/src/pages/reviewer/ReviewerAi.tsx`
- 保留 `showSizeChanger`、`showTotal` 和 `pageSizeOptions`,不改变数据源和接口逻辑。

### 原因
- `pageSize: 10` 会把分页大小锁死为 10,用户选择 `200 条/页` 后组件重新渲染时又被覆盖回 10。
- `defaultPageSize: 10` 只设置初始分页大小,后续交给 Table 内部状态响应用户选择。

### 验证
- `frontend/npm.cmd run build` 通过。
- `git diff --check` 通过;仅出现 CRLF 换行提示。


## 增量实现:修复任务认领只生成一题

### 根因
- 答题页的题目进度来自当前 labeler 在同一任务下的 `assignments` 数量。
- 原后端虽然把任务绑定到整个数据集,但认领或指派时只创建 1 条 assignment,所以答题页只能显示 `第 1 / 1 题`。

### 完成内容
- `TaskRepository` 新增 `countClaimableItems` 和 `findClaimableItems`,支持一次锁定并取出多个还未分配的 `items`。
- `TaskService.claimTask` 改为批量创建 assignment:
  - `first-come`:按任务剩余配额和未分配 item 批量领取。
  - `quota`:按任务剩余配额、未分配 item 和 `maxClaimPerUser` 批量领取。
  - `assigned`:仍禁止非预分配自助认领,保持指派语义。
- `TaskService.ensureStrategyAssignments` 改为指派任务批量预创建 assignment,并在多个被指派 labeler 之间轮转分配可领取 item。
- `listMyAssignments` 会在无状态过滤时补齐历史任务中只创建 1 条 assignment 的旧数据。
- `GET /api/assignments/{assignmentId}/item` 读取答题页时也会触发一次当前任务 assignment 补齐,避免已经停留在答题页的旧任务仍显示 `1/1`。

### 验证
- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `git diff --check` 通过;仅出现 CRLF 换行提示。

### 注意
- 本次不新增数据库表,继续沿用 `assignments` 作为 labeler-item 作业表。
- 同一 item 仍只会分配给一个 assignment,避免重复标注;多名指派 labeler 会瓜分数据集 item,不是每个人都复制一整份。


## 增量实现:我的任务按任务聚合展示

### 根因
- `GET /api/assignments/mine` 返回的是 assignment 列表,一个任务配额 30 时会有 30 条 assignment。
- 原 `MyTasks.tsx` 直接把每条 assignment 渲染成一行,导致同一个任务在“我的任务”里重复显示 30 行。

### 完成内容
- `frontend/src/pages/labeler/MyTasks.tsx` 改为按 `taskId` 聚合 assignment。
- 每个任务只展示一行,副标题显示 Owner、已领取题目数和待答题目数。
- 作答进度展示“已提交 X / 已领取 Y · 已领 Y / 配额”。
- “继续答题”优先进入该任务下第一条 `returned` 或 `claimed` assignment;如果没有待答题,则进入该任务第一条 assignment 查看。
- 不新增后端接口,继续复用 `GET /api/assignments/mine`。

### 验证
- `frontend/npm.cmd run build` 通过。
- `git diff --check` 通过;仅出现 CRLF 换行提示。
## 增量实现:数据集创建不强制关联任务

### 根因

任务发布页已经支持“先建数据集,后绑定任务”,但数据集页和后端仍沿用旧约束:

- 前端曾要求创建/导入数据集时必须先选 `taskId`。
- 后端接口默认把 `taskId` 当必填处理。
- 数据库层 `datasets.task_id` 和 `items.task_id` 原本是强制非空,即便前后端放开也会在写库时报错。

### 完成内容

- 后端:
  - `CreateDatasetRequest.taskId` 改为可空。
  - `DatasetController.importDataset()` 的 `taskId` 改为可选参数。
  - `DatasetService` 支持 `taskId` 为空时创建空数据集和导入文件数据。
  - 数据集归属与查询改为按 `files.created_by` 判断,不再依赖必须存在任务绑定。
  - `DatasetResponse.taskId` 在未绑定任务时返回空字符串。
  - `DatasetRepository` 支持创建未绑定任务的数据集,并允许向未绑定任务的数据集写入 `items`。

- 前端:
  - `frontend/src/types/dataset.ts` 中 `CreateDatasetRequest.taskId` 改为可选。
  - `frontend/src/api/dataset.ts` 导入表单只有在存在 `taskId` 时才会把它写入 `FormData`。
  - `OwnerDatasets.tsx` 当前版本中的“关联任务（可选）”表单能够直接复用这套后端逻辑。

- 数据库脚本:
  - `V1__init_labelhub_schema.sql` 已允许 `datasets.task_id` 和 `items.task_id` 为空,同时把任务外键删除动作改为 `ON DELETE SET NULL`。
  - 新增 `V3__allow_unbound_datasets.sql`,用于把已有本地数据库从“必须关联任务”迁移到“可先不绑定任务”。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm run build` 通过。

### 注意

- 已经初始化过的本地数据库需要额外执行 `backend/src/main/resources/db/migration/V3__allow_unbound_datasets.sql`,否则数据库里的旧非空约束仍会拦截未绑定任务的数据集创建。
## 增量实现:Owner 任务删除后端接口

### 根因

前端任务管理页已经接入了 `ownerApi.deleteTask(taskId)`，但后端没有实现 `DELETE /api/tasks/{taskId}`。请求命中不存在的方法映射后，被全局异常处理兜成了 `500 Internal Server Error`，所以页面上点击“删除”只会看到 500。

### 完成内容

- `TaskController` 新增 `DELETE /api/tasks/{taskId}`。
- `TaskService` 新增 `deleteTask(authentication, taskId)`，只允许当前 Owner 删除自己的任务。
- `TaskRepository` 新增按 `owner_id + task_id` 删除任务的 SQL。
- 删除成功返回 `204 No Content`，与前端 `ownerApi.deleteTask()` 的契约一致。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- 代码中已存在完整删除链路:
  - `TaskController.deleteTask`
  - `TaskService.deleteTask`
  - `TaskRepository.deleteTask`


## 增量实现:全局主题切换

### 对应计划

参见"增量计划:全局主题切换"。

### 完成内容

- `frontend/src/theme/themePresets.ts` 保留 11 个 `ThemePreset` 与工具函数。
- 新建 `frontend/src/theme/ThemeProvider.tsx`:
  - Context 暴露主题状态;
  - 通过 `applyThemeVars` 把主色写入 `:root` 的 CSS 变量 (`--lh-primary` / `--lh-primary-light` / `--lh-primary-hover` / `--lh-primary-gradient` / `--lh-primary-gradient-h` / 17 档 `--lh-primary-bg-{4,5,6,8,10,12,14,18,20,25,28,30,32,35,40,45,50}`);
  - `localStorage` 持久化主题 key 与样式版本。
- `frontend/src/main.tsx` 抽出 `ThemedApp` 包装层,`ConfigProvider.token.colorPrimary` 改为 `preset.primary`,让 antd Button / Tag / Input / Tabs 等组件主色随主题切换;`Menu.darkItemSelectedBg` 仍然保留固定 rgba 兜底,因 antd ConfigProvider token 不能直接读 CSS 变量。
- `frontend/src/styles/global.css` 全量替换硬编码主色为 CSS 变量:
  - 单色 `#2f7bff` / `#6fb6ff` / `#2466e6` 全部替换;
  - 17 档 `rgba(47, 123, 255, X)` 全部替换为 `var(--lh-primary-bg-*)`;
  - 蓝色渐变胶囊 / 进度条 / 选中态全部替换为 `var(--lh-primary-gradient[-h])`;
  - 仅有的 2 处 `rgba(111, 182, 255, 0.06/0.18)` 也兜成主色透明度变量,无需新增浅色变量。
- `frontend/src/pages/settings/AppearanceSettings.tsx`:
  - 顶部 Tabs 5 个,仅"外观"启用;
  - "样式版本"两张大卡(默认 / 渐变),预览条根据 styleVersion 切换为纯色 / 渐变,选中态紫色描边 + 右上对勾;
  - "界面主题"11 圆色卡 grid,圆球用 `linear-gradient(gradientFrom -> gradientTo)` 直接渲染,选中态紫色 ✓ 徽章;
  - 任意切换会立即写入 ThemeProvider,通过 CSS 变量与 ConfigProvider 一起影响整站。
- 路由新增 `/settings/appearance`,所有角色共用。
- `frontend/src/layouts/AppLayout.tsx` 头部用户 Dropdown 新增"外观主题"入口(`BgColorsOutlined`),点击进入 `/settings/appearance`;Avatar 背景从 `#2f7bff` 改为 `var(--lh-primary)`。
- 一次性迁移脚本 `frontend/scripts/replace-theme-colors.cjs` 在替换完成后已删除,目录恢复整洁。

### 验证

- `frontend/npm run build` 通过(产物 `index-*.css ≈ 56.98 kB`,`index-*.js ≈ 1.58 MB`)。
- 浏览器侧:
  - 顶部"用户头像 → 外观主题"进入设置页,点击不同色卡能立即看到侧栏选中态、KPI 图标、按钮、登录页装饰圆球同步变色。
  - 切换"渐变版本"后预览条变为渐变。
  - 刷新浏览器后主题与样式版本保持上次选择,`localStorage` 中可看到 `lh.theme.key` / `lh.theme.styleVersion`。
- `git diff --check` 无 whitespace 错误。

### 注意

- `Menu.darkItemSelectedBg` 因 ConfigProvider token 不支持 CSS 变量,仍保留 `rgba(47, 123, 255, 0.18)` 作为兜底,不影响实际侧栏(项目侧栏使用浅色 Menu,该 token 当前未生效)。
- 业务图表内联色(LabelerOverview / OwnerDashboard / ReviewerReports 等中 `style={{color:'#2f7bff'}}`) 保留原值,因为它们与其它配色构成多色图例,主题切换时保留可读性更好;后续如需进一步联动,可改读 `getComputedStyle(document.documentElement).getPropertyValue('--lh-primary')`。
- 登录页的 SVG 装饰仍使用品牌企业蓝 `#2f7bff`,保持登录品牌一致;只有进入工作台后才会跟随主题切换。


## 增量实现:外观设置页样式重做

### 问题

上一版外观设置页存在两个问题:
1. 顶部 Tabs 把"通用 / 网关 / 任务 / 环境"作为占位 disabled,实际并没有要做这些 Tab,反而让用户误以为还有别的设置可填。
2. "样式版本"和"界面主题"用 `<button>` 做 grid 子项,在 antd Card 内部 flex 容器里宽度塌成一行,11 张色卡和两张样式卡都被压扁。

### 完成内容

- `AppearanceSettings.tsx`:
  - 移除 Tabs,直接两块大卡片(样式版本 + 界面主题)。
  - 卡片头部加 `SkinOutlined` 图标,标题改为"外观设置"。
  - "样式版本"两张大卡使用 `<div role="button">` 替代原 `<button>`,避免 Chrome 把 grid 子项 button 压回默认宽度;预览区改成"窗口外壳风格"——顶部 3 个红黄绿点 + 内容区放主色长条 / 中条 / 胶囊按钮,直观体现纯色与渐变差异。
  - 界面主题色卡同样换成 `<div role="button">`,选中态紫色描边 + 紫色 ✓ 徽章,悬浮上浮 2px。
- `global.css` 重写外观相关样式:
  - 样式版本卡 grid 显式 `width: 100%`,预览区高 140px,有真实"窗口外壳"的视觉,选中态 4px 紫色光晕。
  - 主题色卡 grid `auto-fill, minmax(112px, 1fr)`,色卡内圆球 56px 带阴影,徽章 `border: 2px solid #ffffff` 和卡片描边交叠。
  - 卡片 body padding 24px,标题 padding 0 24px,head min-height 56px,整体留白和层级更舒展。
- 路由不变;`useTheme` 切换逻辑不变。

### 验证

- `frontend/npm run build` 通过(产物 css 61.40 kB)。
- 进入 `/settings/appearance`:
  - 样式版本两张卡左右等宽,预览有窗口外壳;切换"渐变版本"后预览条 / 按钮变渐变;选中态紫色描边 + 右上 ✓。
  - 界面主题 11 个圆色卡正常 grid 排列(每行 6~8 个,随宽度自动换行),选中态紫色描边 + 圆形 ✓ 徽章。
  - 切换主题后侧栏 / Header / 按钮主色立即跟随。

## 增量实现:已提交标注答案回显

### 根因

单题提交成功后,后端会把答案写入 `annotations`,并删除对应 `drafts` 草稿。答题页再次进入时只读取 `draft.answerJson`,没有读取最新正式提交答案,所以已提交题目会显示为空白表单。

### 完成内容

- 后端 `AssignmentItemResponse` 新增 `status` 和 `latestAnnotation`:
  - `status` 返回当前 assignment 状态,用于前端判断是否可编辑。
  - `latestAnnotation` 返回当前 assignment 最新一版正式提交答案,用于已提交题目回显和打回修改基线。
- 后端 `AnnotationRepository` 新增 `findLatestAnnotation(assignmentId)`,按 `revision_no DESC, id DESC` 获取最新提交记录。
- 后端 `AnnotationService.getAssignmentItem()` 组装 `draft`、`latestAnnotation`、`returnReason`:
  - 草稿仍代表未提交临时答案。
  - 正式答案从 `annotations.answer_json` 读取。
  - 标注状态按前端约定转成大写 `SUBMITTED` 等值。
- 前端 `AssignmentItem` 类型新增 `status` 和 `latestAnnotation`。
- 前端 `AnswerPage` 初始化答案改为:
  - 优先使用 `draft.answerJson`。
  - 没有草稿时使用 `latestAnnotation.answerJson`。
  - 两者都没有时为空对象。
- 前端 `AnswerPage` 按状态控制编辑:
  - `claimed` / `returned` 可编辑、可自动保存草稿、可提交。
  - `submitted` / `accepted` 只读查看,不自动保存草稿,提交按钮禁用。
- 样例答题 JSON 补充 `status` 和 `latestAnnotation`,避免演示模式类型不一致。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm run build` 通过。

### 注意

- 当前设计仍保持“单题提交即写入 annotations,提交后删除该题草稿”。这是更适合后续 AI 预审、人工审核和导出的状态模型。
- 已提交题目再次查看时读取 `annotations` 回显,不是依赖草稿保留。

## 增量实现:截止时间前允许修改已提交标注

### 根因

上一轮把 `submitted` 统一作为只读状态处理,这会阻止 Labeler 在任务截止时间前修正已经提交的答案。实际业务更合理的规则是:截止时间前可修改并重新提交,截止时间后或审核通过后再锁定。

### 完成内容

- 后端 `AssignmentItemResponse` 新增:
  - `editable`: 后端按 assignment 状态和任务截止时间计算是否可编辑。
  - `deadline`: 返回任务截止时间,用于前端提示和后续展示。
- 后端 `AnnotationRepository.queryAssignment()` 增加读取 `tasks.deadline`,并写入 `AssignmentItemRecord.taskDeadline`。
- 后端 `AnnotationService.ensureEditableAssignment()` 放开 `submitted`:
  - `claimed` / `returned` / `submitted` 且未超过截止时间时允许保存草稿和提交。
  - 超过截止时间时返回 `409 TASK_EXPIRED`。
  - `accepted` 等终态仍返回 `ASSIGNMENT_NOT_EDITABLE`。
- 前端 `AssignmentItem` 类型补充 `editable` / `deadline`。
- 前端 `AnswerPage` 改为使用后端返回的 `editable` 控制表单:
  - 已提交且 `editable=true` 时可以继续修改,按钮显示“重新提交”。
  - 已锁定时才进入只读查看模式。
  - 仅在用户真实编辑字段后才自动保存草稿,避免打开已提交答案时立即生成无意义草稿。
- `MyTasks` 操作按钮在没有待答题时改为“查看/修改答卷”,降低“已提交不可改”的误导。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm run build` 通过。

### 注意

- 重新提交仍会写入 `annotations` 新 revision,不会覆盖历史提交记录。
- 截止时间判断以后端时间为准,前端只负责展示和禁用交互。

## 增量实现:模板名称与描述可编辑

### 根因

后端 Schema 模块已经支持保存 `name` 和 `description`,前端 `saveDraft()` 也会把 `schema.name` / `schema.description` 传给接口。但 Designer 页面没有模板名称和描述的编辑入口,用户只能保存默认的“新建模板”。

### 完成内容

- `OwnerTemplateDesigner.tsx` 新增模板信息编辑区:
  - 模板名称输入框。
  - 模板描述输入框。
  - 已发布模板禁用编辑,继续遵守后端“发布版本不可改草稿”的约束。
- 保存草稿和发布前增加前端校验:
  - 模板名称为空时直接提示“请先填写模板名称”。
  - 保存和发布时会 trim 名称与描述后提交给现有 `schemaApi`。
- `global.css` 增加 `.designer-meta-panel` 相关样式:
  - 两列布局:名称 + 描述。
  - 窄屏下自动切换为单列。

### 验证

- `frontend/npm run build` 通过。

### 注意

- 本次只补“创建/草稿阶段可编辑名称和描述”,不改变已发布模板的不可变规则。

## 增量实现:补全标准接口文档

### 完成内容

- 重建 `接口文档.md`,按标准接口文档结构补充:
  - 通用约定:基础路径、认证方式、分页响应和错误响应。
  - 前端接口清单:按 Auth、Owner、Dataset、Schema、Labeler、Reviewer、Dashboard 分模块记录。
  - 后端接口清单:按 Auth、Task、Market/Assignment、Dataset、Schema、Annotation 分模块记录。
  - 后端待实现接口:明确 Reviewer 与 Dashboard 当前只有前端预留,后端尚未落地。
  - 关键数据结构摘要:补充 `LoginResponse`、`AuthUserResponse`、`CreateTaskRequest`、`SchemaVersionResponse`、`AssignmentItemResponse`。
- 每个接口均记录 HTTP 方法、路径、作用、实现状态和测试状态。
- 在文档中标注当前缺少接口级自动化测试,避免把历史构建验证误写成自动化测试覆盖。

### 验证

- 本次为文档变更,未修改业务代码。
- 已静态核对 `frontend/src/api` 与后端 Controller 暴露路径的一致性。

### 注意

- `Reviewer` 和 `Dashboard` 后端接口仍未实现,前端当前属于预留调用层或样例数据兜底。
- 模板列表绑定任务的显示口径已在后续增量中统一:同时识别 `task_schema_versions.task_id` 和 `tasks.reward_rule.schemaVersionId`。

## 增量实现:修复模板列表绑定任务显示口径

### 根因

任务发布页选择已发布模板时,后端把模板版本 ID 写入 `tasks.reward_rule.schemaVersionId`;模板列表 `/api/schemas` 原查询只读取 `task_schema_versions.task_id`。因此任务实际已经使用模板,但模板卡片拿不到 `taskTitle`,就显示“尚未绑定任务”。

### 完成内容

- 修改 `SchemaRepository.querySchemas()`:
  - 保留 `task_schema_versions.task_id` 直接绑定查询。
  - 新增从 `tasks.reward_rule.schemaVersionId` 反查引用当前模板版本的任务。
  - 响应仍使用原有 `taskId` / `taskTitle`,前端无需改动。
  - 直接绑定优先;如果没有直接绑定,展示最近更新的一条引用任务。
- 更新 `接口文档.md`:
  - `/api/schemas` 和 `/api/schemas/{versionId}` 说明改为会识别两类绑定关系。
  - 记录同一模板被多个任务引用时当前只展示最近一条任务。

### 验证

- `backend` 目录下执行 `.\mvnw.cmd -q -DskipTests package` 通过。

### 注意

- 修改后需要重启后端服务,浏览器重新加载模板列表才会看到绑定任务标题。
- 如果后续要完整展示“一个模板被多个任务使用”的所有任务,需要把响应模型扩展为 `boundTasks[]`,这会涉及前后端契约变更。

## 增量实现:Formily 动态表单内核完善

### 完成内容

- 前端保留 `antd 6`,新增 `@formily/core`、`@formily/react`、`@formily/json-schema`,没有引入 peer dependency 不匹配的 `@formily/antd-v5`。
- 扩展 `SchemaField`:
  - 新增 `sourcePath`、`componentProps`、`validators`、`reactions`、`layout`、`defaultValue`、`helpText`。
  - 保留旧字段 `validations` / `linkages`,旧模板仍可读取。
- 新增 `frontend/src/modules/schema` 运行时:
  - `schemaValidation`: 前端 Schema 检查。
  - `schemaCompiler`: LabelHub Schema 编译为 Formily JSON Schema。
  - `formilyAntd6`: 自建 antd6 字段适配组件。
  - `LabelHubFormRenderer`: Designer 预览和 Labeler 答题页共用的统一 Renderer。
- Designer 升级:
  - 新增 Schema 检查面板,发布/保存前阻止错误 Schema。
  - ShowItem 支持绑定 raw_payload 字段路径。
  - 校验规则改为结构化白名单配置。
  - 联动规则改为结构化来源字段/操作符/目标字段/动作。
  - 预览抽屉改为真实 Formily Renderer,并支持选择数据集样本条目做 raw_payload 预览。
- Labeler 答题页升级:
  - 移除页面内主要手写字段渲染路径,改用 `LabelHubFormRenderer`。
  - 草稿保存、已提交回显、重新提交和顶部提交按钮沿用原有状态逻辑。
- 后端 Schema 校验:
  - 新增 `POST /api/schemas/validate`。
  - 保存草稿、更新草稿、发布模板时调用同一套结构校验。
  - 校验字段名、字段类型、重复字段、选择项、ShowItem 绑定、正则、自定义校验白名单、联动引用。
- 后端标注提交校验:
  - 补齐选择项值合法性校验。
  - 补齐结构化 validators 与旧 `validations` 的正则、自定义函数白名单校验。
  - 补齐联动规则导致的条件隐藏与条件必填校验。

### 验证

- `frontend/npm.cmd run build` 通过。
- `backend/.\mvnw.cmd -q -DskipTests package` 通过。

### 注意

- 本次不改数据库结构,扩展字段仍存放在 `task_schema_versions.schema_json.fields` 中。
- LLM 触发组件完成 UI 插槽和 Schema 表达,真实模型调用仍留给 AI Agent 阶段。
- `@formily/antd-v5` 未使用,原因是它要求 `antd ^5.13.0`,当前项目为 `antd 6.4.3`。

## 增量实现:修复 Designer 拖拽浮层鼠标错位

### 根因

模板 Designer 的拖拽浮层使用 dnd-kit `DragOverlay`,其内部依赖 `position: fixed` 按视口坐标定位。当前应用外层 `.app-shell-enter` 入场动画使用 `animation-fill-mode: both`,动画结束后仍保留 `transform: translateY(0)`,会让子元素里的 fixed 定位改为相对该 transformed ancestor 计算,因此浮层位置和鼠标位置出现偏移。

### 完成内容

- `OwnerTemplateDesigner.tsx` 引入 `createPortal`。
- 将 `DragOverlay` 从 Designer 页面 DOM 层级中移出,通过 React Portal 渲染到 `document.body`。
- 保留原有拖拽状态、物料拖入、字段排序和 overlay 内容渲染逻辑。
- 在代码旁添加注释说明 portal 的原因,避免后续误删。

### 验证

- `frontend/npm.cmd run build` 通过。

### 注意

- 这次没有改 `.app-shell-enter` 入场动画,避免影响全站页面切换效果。
- Vite 构建仍有 chunk 体积超过 500 kB 的提示,这是既有打包提示,不影响本次拖拽修复。

## 增量实现:Designer 拖拽放入位置提示

### 完成内容

- `OwnerTemplateDesigner.tsx` 新增拖拽落点状态 `dropIndicator`。
- 接入 dnd-kit `onDragOver`:
  - 拖拽元素中心点在目标字段卡片上半区时,落点为 `before`。
  - 拖拽元素中心点在目标字段卡片下半区时,落点为 `after`。
  - 拖到画布空白区域时,默认提示插入到最后一个字段下方。
- 字段卡片新增 `dropPosition` 入参,在组件上方或下方渲染红色插入线。
- `onDragEnd` 使用同一套落点计算结果执行物料插入或字段排序,避免“提示在下方,实际插到上方”的错位。
- `global.css` 新增 `.field-drop-indicator` 样式,使用红色横线和端点提示放入位置。

### 验证

- `frontend/npm.cmd run build` 通过。

### 注意

- 这次只增强 Designer 的拖拽交互反馈,没有改 Schema 数据结构、保存发布接口或 Formily Renderer。
- 构建仍提示部分 chunk 超过 500 kB,属于既有前端打包体积提示。

## 增量实现:修复 Designer 插入提示方向误判

### 根因

Designer 画布存在外层 `canvas-droppable` 和字段卡片 droppable 嵌套。上一轮使用默认 `closestCenter` 时,拖拽过程中容易命中外层画布容器,于是落入“最后一个字段下方”的兜底逻辑,导致红线总显示在组件下方。该轮先把方向判断改为当前鼠标 Y 坐标,用于快速验证上/下半区提示。

### 完成内容

- 新增 `CANVAS_DROPPABLE_ID`,统一画布 droppable id,避免字符串散落。
- 新增 `designerCollisionDetection`:
  - 先用 `pointerWithin` 判断指针直接覆盖的 droppable。
  - 当指针在字段卡片上时,优先返回字段卡片。
  - 当指针在画布空白区域时,再选择最近的字段卡片用于插入线提示。
  - 当指针离开画布时返回空碰撞,避免错误显示插入线。
- `resolveDropIndicator()` 改为用当前鼠标 Y 坐标与目标字段卡片中心线比较:
  - 鼠标在上半区: `before`。
  - 鼠标在下半区: `after`。
- 保留空画布拖入和画布空白区域追加到末尾的原有行为。

### 验证

- 首次 `frontend/npm.cmd run build` 因 120 秒工具超时中断。
- 放宽超时时间后重新执行 `frontend/npm.cmd run build` 通过。

### 注意

- 这次仍只改 Designer 前端交互,不影响后端接口、Schema 结构和标注提交。
- Vite 仍提示部分 chunk 超过 500 kB,属于既有打包体积提示。

## 增量实现:Designer 插入方向改为按拖拽组件中心判断

### 根因

上一轮方向判断使用当前鼠标 Y 坐标。实际拖拽时,鼠标点可能位于组件边缘或拖拽手柄附近,用户视觉上判断的是“拖动中的组件”覆盖到了目标组件的上半区还是下半区,因此按鼠标点判断会和直觉不一致。

### 完成内容

- 保留 `designerCollisionDetection`,继续优先命中具体字段卡片,避免落到外层画布兜底。
- 将方向判断从 `getCurrentPointerY()` 改为 `getDraggedCenterY()`。
- `resolveDropIndicator()` 现在用拖拽组件当前矩形中心线与目标字段卡片中心线比较:
  - 拖拽组件中心在目标中心线上方: `before`。
  - 拖拽组件中心在目标中心线下方: `after`。
- 物料拖入、字段排序和红色插入线样式保持不变。

### 验证

- `frontend/npm.cmd run build` 通过。

### 注意

- 这次只调整 Designer 前端拖拽方向判断口径,不改变业务数据和接口。
- Vite 仍提示部分 chunk 超过 500 kB,属于既有打包体积提示。

## 增量实现:模板绑定数据集与原地收回发布

### 完成内容

- 后端 Schema 请求与响应增加 `datasetId`、`datasetName`:
  - `CreateSchemaDraftRequest`、`UpdateSchemaDraftRequest` 接收模板默认数据集。
  - `SchemaVersionResponse`、`SchemaSummaryResponse` 返回模板默认数据集。
  - `SchemaService` 校验 `datasetId` 必须属于当前 Owner,并把 `datasetId/datasetName` 写入 `task_schema_versions.schema_json` 根节点。
- 新增 `POST /api/schemas/{versionId}/withdraw`:
  - 仅 Owner 可收回自己创建的已发布模板。
  - 原地把同一条 schema version 从 `published` 改为 `draft`,不复制新版本。
  - 收回时清空 `published_at`,并刷新 `updated_at`。
- 任务和标注闭环补充模板状态校验:
  - Labeler 认领任务前检查绑定模板仍为 `published`,否则返回 `409 SCHEMA_WITHDRAWN`。
  - 草稿保存和标注提交前检查模板仍为 `published`,否则返回 `409 SCHEMA_WITHDRAWN`。
  - 答题页读取 assignment 时允许读取已收回模板的当前字段,但返回 `editable=false`,保证已提交答案仍可查看。
- Owner Designer:
  - 模板信息区新增“关联数据集”下拉框。
  - 保存草稿和发布时提交 `datasetId/datasetName`。
  - 预览抽屉默认使用模板绑定的数据集,仍允许临时切换样本。
  - 已发布模板禁用保存/发布,显示“收回发布”按钮;收回成功后页面状态回到草稿。
- Owner 任务发布页:
  - 选择已发布模板后,如果模板带有 `datasetId`,自动填入“关联数据集”。
  - Owner 仍可在提交前手动切换数据集。
- Labeler 答题页:
  - 查看已提交答案时,如果当前模板字段无法覆盖历史答案 key,在表单下方展示原始答案 JSON。

### 验证

- `frontend/npm.cmd run build` 通过。
- `backend/.\mvnw.cmd -q -DskipTests package` 通过。

### 注意

- 本轮不新增数据库列,模板默认数据集写在 `task_schema_versions.schema_json` 根节点。
- 收回是原地修改同一版本状态,已提交的 `annotations.answer_json` 不删除、不改写。
- 如果收回后删除或改名字段,旧答案的完整语义回放依赖“原始答案 JSON”兜底展示。

## 增量实现:草稿模板卡片删除功能

### 完成内容

- 后端新增 `DELETE /api/schemas/{versionId}`:
  - 仅 Owner 可删除自己创建的模板。
  - 仅 `draft` 状态允许删除。
  - 模板被任务 `reward_rule.schemaVersionId` 引用时返回 `409 SCHEMA_IN_USE`。
  - 模板已有提交标注引用时返回 `409 SCHEMA_IN_USE`。
  - 删除成功返回 `204 No Content`。
- 前端 `schemaApi` 新增 `deleteSchema(versionId)`。
- Owner 模板列表卡片:
  - 草稿模板显示红色“删除”按钮。
  - 已发布模板不显示删除按钮。
  - 已发布模板收回后状态变成草稿,列表中会显示删除按钮。
  - 点击删除弹出二次确认,确认后调用后端接口并刷新模板列表。
- 复制模板时同步携带原模板的 `datasetId/datasetName`,避免复制后丢失默认数据集。

### 验证

- `frontend/npm.cmd run build` 通过。
- `backend/.\mvnw.cmd -q -DskipTests package` 通过。

### 注意

- 该轮实现最初采用物理删除未被引用草稿模板。后续“任务使用中模板软删除”已将删除语义调整为软删除,以支持删除已被任务使用的模板。

## 增量实现:任务使用中模板软删除

### 完成内容

- 新增迁移 `V5__soft_delete_schema_versions.sql`:
  - `task_schema_versions.deleted_at DATETIME NULL`。
  - `idx_task_schema_versions_deleted_at` 索引。
- 后端 Schema 查询分为管理口径和运行时口径:
  - `GET /api/schemas` 和 `GET /api/schemas/{versionId}` 过滤软删除模板,Owner 管理端不可见。
  - Labeler 认领、草稿保存、提交和答题页读取仍可读取已软删除模板。
- `DELETE /api/schemas/{versionId}` 改为软删除:
  - 仅 Owner 可操作自己的模板。
  - 仍仅允许 `draft` 状态删除,已发布模板需要先收回。
  - 不再因为任务或 annotation 引用而拒绝。
  - 重复删除已软删除模板直接返回成功。
- Runtime 规则调整:
  - 普通未删除草稿模板仍返回 `SCHEMA_WITHDRAWN`,阻止认领和提交。
  - 已软删除模板即使状态为 `draft`,也允许既有任务继续认领、保存草稿和提交。
  - Owner 编辑已绑定软删除模板的既有任务时,不会因为模板被隐藏而卡死。
- 前端删除确认文案改为“从模板列表隐藏,已绑定任务和历史标注不受影响”。
- Designer 直接打开已软删除模板时提示“模板不存在或已删除”。

### 验证

- `frontend/npm.cmd run build` 通过。
- `backend/.\mvnw.cmd -q -DskipTests package` 通过。

### 注意

- 本轮不做恢复入口。后续如需要恢复,可将 `deleted_at` 更新为 `NULL`。
- 软删除模板仍保留在数据库中,用于支撑已绑定任务和历史 annotation 的模板追溯。

## 增量实现:数据集删除后端接口

### 完成内容

- 后端新增 `DELETE /api/datasets/{datasetId}`:
  - 仅 Owner 可调用。
  - 仅允许删除当前 Owner 创建的数据集。
  - 删除成功返回 `204 No Content`。
- 删除前增加数据一致性校验:
  - 数据集已绑定任务时返回 `409 DATASET_IN_USE`。
  - 数据集条目已产生 assignment 或 annotation 时返回 `409 DATASET_IN_USE`。
  - 数据集不存在或不属于当前 Owner 时返回 `404 DATASET_NOT_FOUND`。
- Repository 增加删除相关查询:
  - 统计数据集条目关联的 assignments。
  - 统计数据集条目关联的 annotations。
  - 删除 `datasets` 记录。
- 删除行为:
  - 未进入任务作答链路的数据集执行物理删除。
  - 依赖 `items.dataset_id ON DELETE CASCADE` 清理该数据集下的未使用条目。
  - 不删除 `files` 元数据,避免误删可能复用的文件记录。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。

### 注意

- 当前数据集删除仍是保守物理删除,仅适合未绑定任务、未产生 assignment/annotation 的数据集。
- 如果后续需要删除已用于任务的数据集,建议像模板一样增加 `deleted_at` 做软删除,避免破坏任务和标注追溯链路。


## 增量实现:全局主题切换基础

### 对应计划

参见"增量计划:全局主题切换基础"。

### 完成内容

- 新建 `frontend/src/theme/themePresets.ts`,包含 11 个 `ThemePreset`、`ThemeKey`、`StyleVersion`、`DEFAULT_THEME_KEY`、`findPreset`、`hexToRgb`、`rgba` 工具函数。
- 新建 `frontend/src/theme/ThemeProvider.tsx`:Context 暴露主题状态,`applyThemeVars` 写入 17 档透明度 / 渐变 / 主色 hover 的 CSS 变量到 `:root`,localStorage 持久化。
- `frontend/src/main.tsx` 抽出 `ThemedApp` 包装层,`ConfigProvider.token.colorPrimary` 改为 `preset.primary`。
- 一次性脚本 `replace-theme-colors.cjs` 把 `global.css` 全部硬编码主色替换成 CSS 变量(单色 / 17 档 rgba / 蓝色渐变 / 浅色 rgba),完成后已删除。
- 新建 `frontend/src/pages/settings/AppearanceSettings.tsx` 与 index 导出,含样式版本两张大卡 + 11 圆形主题色卡。
- 路由新增 `/owner/settings/appearance`、`/labeler/settings/appearance`、`/reviewer/settings/appearance` 三条;`/settings/appearance` 重定向到 owner 兜底,避免老链接 404。
- AppLayout 头部用户 Dropdown 增加"外观主题"入口(`BgColorsOutlined`),按当前 section 拼接路径跳转。
- 新建 `frontend/src/theme/useThemeColors.ts`,LabelerOverview / AnswerPage / ReviewerOverview 的 `Progress.strokeColor` 全部接入。
- `AppLayout` 头像背景从 `#2f7bff` 改为 `var(--lh-primary)`。

### 验证

- `frontend/npm run build` 通过。
- 切换主题色立即生效,刷新页面后保留(localStorage `lh.theme.key` / `lh.theme.styleVersion`)。

### 注意

- antd `Menu.darkItemSelectedBg` 因 ConfigProvider token 不接受 CSS 变量,仍保留 `rgba(47,123,255,0.18)` 兜底,不影响实际侧栏(项目用浅色 Menu)。
- 业务图表中作为"多色对比的语义色"的硬编码 hex(reviewLabels / roleColors / mediaTypeMeta) 保留,主题切换不影响图例区分度。

## 增量实现:外观设置页布局重做

### 对应计划

参见"增量计划:外观设置页布局重做"。

### 完成内容

- 移除 Tabs,直接两块大卡片(样式版本 + 界面主题)。
- 卡片头部加 `SkinOutlined` 图标,标题改为"外观设置"。
- 样式版本卡用 `<div role="button">` 替代 `<button>`,grid 显式 `width: 100%`。
- 预览区改成"窗口外壳"风格(顶部 3 个红黄绿点 + 内容区主色长条 / 中条 / 胶囊按钮)。
- 默认版本顶部一条纯色实心条;渐变版本顶上对称双向渐变带(`主色 → 浅色 → 白 → 浅色 → 主色`),内容区径向光晕底色,条形和按钮三段渐变填充。
- 11 主题色卡 grid `auto-fill, minmax(112px, 1fr)`,圆球 56px 渐变填充,选中态紫色 ✓ 徽章。

### 验证

- `frontend/npm run build` 通过。
- 玫瑰粉等深浅色差小的预设也能一眼看出渐变方向。

## 增量实现:主题色色卡区分度调整

### 对应计划

参见"增量计划:主题色色卡区分度调整"。

### 完成内容

- `themePresets.ts` 中 galaxy 改为纯黑(`primary: #111827`,gradient `#1f2937 → #030712`)。
- midnight 改为海军蓝(`primary: #1e3a8a`,gradient `#1e40af → #0c1d4f`)。

### 验证

- 11 圆色卡视觉对比明显,银河黑无蓝调,深夜蓝带明显蓝调。

## 增量实现:渐变样式版本整站差异化

### 对应计划

参见"增量计划:渐变样式版本整站差异化"。

### 完成内容

- `:root[data-style-version='gradient']` 选择器对若干视觉重心做覆盖:
  - `.app-shell` 叠多层主色径向 + 线性光晕作为大背景
  - `.app-content` 叠双层径向光晕(主内容区光晕)
  - antd 主按钮 / 登录提交按钮:背景换主色渐变,边框 transparent,`background-clip: padding-box`,隐藏 `::before/::after`
  - 登录装饰圆球、Logo Mark、登录头像、AppLayout 头像、状态点高亮、Designer drop 区:纯色 → 渐变填充
  - 侧栏选中菜单叠左上角玻璃高光
  - KPI 数值前缀 / 数据集 ID / 时间线 ID 用 `background-clip: text` 改渐变文字
  - 进度条主体 / `.review-bar-seg.is-pass` / `.dispute-bar > span` 使用主色渐变

### 验证

- `frontend/npm run build` 通过。
- 切到渐变模式后右侧内容区可见明显主题色光晕,按钮和重点元素全部渐变。
- 渐变按钮边缘无"咬痕"瑕疵。

## 增量实现:色彩模式切换(system/light/paper/dark)

### 对应计划

参见"增量计划:色彩模式切换"。

### 完成内容

- `ThemeProvider` 新增 `colorMode` 与 `resolvedColorMode`,通过 `matchMedia('(prefers-color-scheme: dark)')` 监听系统主题。
- `applyThemeVars` 写 `data-color-mode` 属性到 `:root`。
- `main.tsx` `ConfigProvider.theme.algorithm` 在 dark 下用 `theme.darkAlgorithm`,Layout token 的 headerBg/bodyBg 跟随。
- 新建 `frontend/src/layouts/ColorModeSwitcher.tsx`:Header 右侧图标按钮 + Popover 4 张模式预览卡。
- 当前模式自动切图标:system → 显示器、light → 太阳、paper → 灯泡、dark → 月亮。
- "跟随系统"卡用对角线 split 渲染左白右黑。
- 选中态紫色描边 + 浅底高亮。
- localStorage 持久化 `lh.theme.colorMode`。

### 验证

- `frontend/npm run build` 通过。
- 切换任一模式整站立即变色,刷新后保留。
- system 模式下系统主题切换时整站联动(测试方式:macOS 切日间/夜间模式)。

## 增量实现:暗色模式全局可读性深度修复

### 对应计划

参见"增量计划:暗色模式全局可读性深度修复"。

### 完成内容

分多轮迭代,最终覆盖范围:

1. **通配文字**:`:root[data-color-mode='dark']` 全局 `color: #e5e7eb`,h1-h5 `#f8fafc`,p/span/div/li/td/th/label/.ant-typography 用 `!important` 强制覆盖。
2. **白底容器**:.ant-card / .ant-modal-content / .ant-drawer-body / .ant-popover-inner / .ant-dropdown-menu / .ant-select-dropdown / .ant-picker-panel-container / .ant-message-notice-content / .ant-notification-notice 统一 `#1a1d24`。
3. **输入控件**:Input / Select / TextArea / InputNumber / Picker `#20242d`,placeholder `#64748b`。
4. **表格**:`.ant-table` `#1a1d24`,表头 `#20242d`,行 hover `#20242d`。
5. **Tag 色板**:success/warning/error/blue 低透明度底 + 高对比文字。
6. **业务卡片**:任务名 / 数据集名 / 批次标题 `#f5f7fb`,meta/quota/deadline `#94a3b8`。
7. **Designer 专项**:`.field-option-tag`(白底深字硬编码)、`.designer-meta-panel`(顶部表单白底)、字段卡 / 物料组 / 属性面板 / 联动规则 IF/THEN 块全套深色化。
8. **SVG fill**:`.donut-total / .donut-sub` 等 SVG `<text>` 用 `fill: #f8fafc !important`。
9. **渐变文字失明**:`[data-color-mode='dark'][data-style-version='gradient']` 联合选择器下退回 `--lh-primary-light` 实色。
10. **数据看板**:dispute-cell / timeline-row / deadline-row / activity-row / perf-row / leave-card / schedule-day 全统一 `#1a1d24` + 浅文字。
11. **主背景**:`.app-shell` `#0d0f14`,`.app-content` `#13151b`,渐变模式叠主色低透明度光晕。
12. **hero CTA 例外**:hero 内白底主色字按钮在暗色下保持白底主色字。
13. **Empty 图**:`filter: brightness(0.6) invert(0.9)`。
14. **Form label / Tooltip / Popover / Dropdown / Notification / Message / Pagination / Segmented** 全 antd 弹层组件深色化。
15. **types-pill / KPI 浅蓝底图标**:`!important` 覆盖 inline style 的浅色,改为主色低透明度。

### 验证

- `frontend/npm run build` 通过(产物 css ≈ 109 kB)。
- 浏览器测试覆盖 Owner / Labeler / Reviewer 全部页面,可读性达到 WCAG AA。
- KPI 数字、Reviewer 报表中心 100、Designer 选项 Tag、模板搭建器顶部表单等之前失明的内容全部清晰可读。

### 注意

- antd v6 的 CSS-in-JS 部分注入样式优先级很高,大量使用 `!important` 才能压住,这是合理的妥协。
- 业务页里"多色对比的语义色"(reviewLabels / roleColors / mediaTypeMeta) 保留 hex 不动,因为这些是与其它色形成图例区分的语义色。

## 增量实现:数据集页"我的数据集"卡片改造

### 对应计划

参见"增量计划:数据集页'我的数据集'卡片改造"。

### 完成内容

- `frontend/src/api/dataset.ts` 新增 `deleteDataset(datasetId)` 调用 `DELETE /api/datasets/{id}`。
- `OwnerDatasets.tsx`:
  - 引入 `DeleteOutlined` 图标。
  - 标题旁新增 `<span class="dataset-count-badge">` 渐变胶囊数字徽章。
  - "查看全部"按钮条件改为 `datasets.length > 0` 始终显示。
  - 列表去掉 `slice(0, 3)`,改用 `<div class="dataset-list-scroll">` 容器渲染所有数据集。
  - 每张卡内 `head` 改为 `Space[DatasetKindTag, DeleteButton]` 组合,删除按钮加 `aria-label` + 阻止事件冒泡。
  - 新增 `handleDeleteDataset(ds, event)` 函数:Modal.confirm 二次确认 → 调 API → 删除成功后从本地列表剔除,失败友好提示。
  - "查看全部" Modal 内每张卡同步加删除按钮。
- `global.css`:
  - `.dataset-count-badge` 主色渐变胶囊样式
  - `.dataset-list-scroll` CSS 变量 `--ds-card-h: 116px; --ds-gap: 10px`,`height: calc(3 * 116 + 2 * 10) = 368px` 精确 3 张卡高度
  - `.dataset-list-scroll .dataset-list-item` `min-height: var(--ds-card-h); flex: 0 0 auto`
  - 主色渐变 webkit/firefox 滚动条
  - `.dataset-list-delete` 按钮默认 60% 透明灰,卡 hover 100%,按钮 hover 红底红字
  - 暗色模式适配

### 验证

- `frontend/npm run build` 通过。
- "我的数据集"右侧渐变数字徽章数量随数据集变化。
- 卡片恰好显示 3 张,第 4 张起鼠标滚动可见。
- 删除按钮二次确认后调用 API,失败时不会伪造成功。

### 注意

- 后端 `DELETE /api/datasets/{id}` 尚未实现,前端调用失败时会展示后端错误或网络错误提示,不会伪造删除成功。
- 删除已绑定任务的数据集预期返回 `409 DATASET_IN_USE`,后端实现时需要校验。

## 增量实现:Owner 数据生产组菜单顺序调整

### 对应计划

参见"增量计划:Owner 数据生产组菜单顺序调整"。

### 完成内容

- `frontend/src/layouts/AppLayout.tsx` 中 `ownerMenuItems['数据生产'].children` 数组顺序调整为:
  1. 数据集(`/owner/datasets`)
  2. 模板搭建(`/owner/templates`)
  3. 任务管理(`/owner/tasks`)
- 配套更新该 `type: 'group'` 上的中文注释,说明工作流"先有数据 → 再建模板 → 最后发布任务"。

### 验证

- `frontend/npm run build` 通过。
- Owner 侧栏"数据生产"组按新顺序展示。

### 注意

- `/owner` 默认重定向仍指向 `/owner/tasks`,登录后落地不变,避免改动落地体验。如需要改成默认 `/owner/datasets`,后续单独提案。


## 增量实现:数据集页合并新建与上传两个入口

### 对应计划

参见"增量计划:数据集页合并新建与上传两个入口"。

### 完成内容

- `OwnerDatasets.tsx`:
  - 删除"上传文件数据"按钮(辅助按钮)。
  - 保留主按钮但改名为"新建数据集",点击触发原 `openImportModal`(导入流程),图标 `DatabaseOutlined`,`type="primary"`。
  - 删除独立"新建空数据集" Modal、`openCreateModal`、`submitCreateDataset`、`createForm`、`createOpen` state,以及对应的 `useEffect` 依赖。
  - 导入 Modal 标题从"上传文件数据"改为"新建数据集"。
  - 数据集为空时的 Empty 文案从"请先新建数据集或上传文件数据"改为"请先新建数据集"。
- 保留 `datasetApi.createDataset` API 不删除,后续如需"新建空数据集"入口可以恢复调用。

### 验证

- `frontend/npm run build` 通过(产物 css ≈ 109.6 kB,js ≈ 1.78 MB)。
- 数据集页右上角只剩一个"新建数据集"主按钮。
- 点击后弹出对话框(关联任务可选 + 数据集名称 + 类型 + 文件上传),功能与原"上传文件数据"完全一致。
- 数据集详情卡右上角"添加数据"按钮(`openAppendModal`)继续可用,后续向已有数据集追加文件不受影响。

### 注意

- 移除独立"新建空数据集"入口后,如果 Owner 想先建一个空数据集再追加文件,需要先上传一个空文件或一行测试数据.对当前业务流程影响极小,大部分场景都是直接带文件创建.


## 增量实现:新建数据集弹窗"完成"按钮智能分支

### 对应计划

参见"增量计划:新建数据集弹窗'完成'按钮智能分支"。

### 完成内容

- `OwnerDatasets.tsx`:
  - Modal `okText` 从 "上传并导入" 改为 "完成"。
  - 数据文件 `Form.Item` 标签从 "数据文件" + `required` 改为 "数据文件 (可选)",去掉红星。
  - 拖拽区下方加 `Typography.Text type="secondary"` 灰色提示:"未上传文件时,点击'完成'将创建一个空数据集..."
  - `submitImportDataset` 改造为分支函数:
    - **分支 A** (有文件):沿用原 `datasetApi.importDataset` 流程
    - **分支 B** (无文件):`Modal.confirm` 二次确认("将创建空数据集") → 调 `datasetApi.createDataset` → 提示"已创建空数据集「{name}」"
  - 二次确认按钮文案"确认创建" + "继续上传文件",降低误操作风险。

### 验证

- `frontend/npm run build` 通过(产物 css ≈ 109.6 kB,js ≈ 1.78 MB)。
- 上传文件后点击"完成":走 `POST /api/datasets/import`,提示"数据文件已导入 MySQL"。
- 不上传文件点击"完成":弹出二次确认,确认后走 `POST /api/datasets`,提示"已创建空数据集「xxx」"。
- 字段校验、关联任务可选、文件名联动 等其它行为不变。

### 注意

- 后端两个接口均已实现且无改动,不需要数据库迁移。
- 关联任务、数据集名称、数据集类型 三个字段是 form 校验拦截的,无论分支 A 还是 B 都会先校验.


## 增量实现:新建数据集弹窗改为侧边 Drawer

### 对应计划

参见"增量计划:新建数据集弹窗改为侧边 Drawer"。

### 完成内容

- `OwnerDatasets.tsx` 的"新建数据集" Modal 改为 antd `<Drawer>`:
  - 标题"新建数据集",右上角自带关闭按钮
  - 宽度 480px,与"新建标注任务"风格一致
  - `destroyOnClose` 关闭后销毁内部表单状态
  - 底部固定 footer 用 flex justify-end 布局,放"取消"+"完成"两个按钮
- 表单字段 / 校验规则 / 智能分支逻辑(`submitImportDataset`)完全保持不变。
- "添加数据" Modal 暂不动,因为内容简单且只有单一上传场景。

### 验证

- `frontend/npm run build` 通过(产物 css ≈ 109.6 kB,js ≈ 1.78 MB)。
- 点击"新建数据集"按钮后从右侧滑入 Drawer,关闭按钮 + Esc 都能关闭。
- 底部"完成"按钮按既定分支处理:有文件走 import,没文件二次确认后走 create。

### 注意

- 全局 `ConfigProvider.drawer.styles` 已经设了圆角与浮卡阴影,本 Drawer 自动继承.
## 增量实现:状态机正确性与审计闭环

### 对应计划

参见"增量计划:状态机正确性与审计闭环"。

### 完成内容

- 数据库迁移:
  - 新增 `V6__extend_audit_logs_for_state_machine.sql`,给 `audit_logs` 追加 `operator_role`、`from_state`、`to_state`、`reason`、`snapshot_json` 和状态迁移索引。
- 后端状态机:
  - 新增 `workflow` 模块,包含 `WorkflowEntityType`、`StateMachineService`、`AuditLogRepository`。
  - 统一校验 task / assignment / annotation / ai_review_job / export_job 的合法状态迁移。
  - 状态变化统一写入 audit log。
- 核心业务接入:
  - Owner 创建/更新/切换任务状态写入审计。
  - Labeler 认领 assignment 写入审计。
  - Labeler 提交/重新提交 annotation 时,assignment 变为 `submitted`,annotation 先创建为 `submitted` 后迁移到 `ai_reviewing`,并创建 `ai_review_jobs.pending`。
- Reviewer 后端:
  - 新增 `/api/reviewer/overview`、`/api/reviewer/batches`、`/api/reviewer/batches/{batchId}/claim`、`/api/reviewer/batches/{batchId}/annotations`、`/api/reviewer/annotations/{annotationId}/decision`、`/api/reviewer/disputes`、`/api/reviewer/disputes/{disputeId}/resolve`。
  - 批次按 task 聚合,争议样本从 `human_reviews.decision = 'escalate'` 派生。
  - `APPROVE` 写入 human review,annotation / assignment / item 进入 accepted。
  - `RETURN` 要求 reason,annotation / assignment / item 进入 returned。
  - `ESCALATE` 记录争议,annotation 保持 reviewing。
  - `REVISE` 只记录审核意见,不改写 Labeler 原始答案。
- AI Review 后端:
  - 新增 `/api/ai-review/jobs/claim-next`、`/{jobId}/complete`、`/{jobId}/fail`、`/{jobId}/retry`。
  - 支持 pending / running / succeeded / failed / retry 状态流转和审计。
- Export 后端与前端:
  - 新增 `/api/exports`、`/api/exports/{exportId}/start`、`/complete`、`/fail`。
  - 导出只允许存在 accepted annotation 的任务创建导出任务。
  - Owner 导出中心接入真实接口,可以创建、开始、完成、失败导出任务。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。

### 注意

- 本轮没有新增 review batch / dispute 独立表,避免过早建模;后续如果需要审核员指派、批次锁定、多人协作,可再升级为独立表。
- AI 真实模型调用未实现,当前完成的是任务、结果、状态和审计链路。
- Export 当前完成状态闭环和标注状态迁移,文件生成/下载仍可作为下一轮扩展。

## 增量实现:修复模板发布未保存当前画布配置

### 对应计划

参见"增量计划:修复模板发布未保存当前画布配置"。

### 完成内容

- `OwnerTemplateDesigner.tsx`:
  - `handlePublish` 改为发布前始终调用 `saveDraft()`。
  - 已有真实 `versionId` 的草稿也会先把当前画布 `schema.fields` 保存到后端,再调用 `schemaApi.publish()`。
  - 发布确认文案补充"发布时会先保存当前画布配置"。
  - `saveDraft()` 保存成功后优先保留当前选中的字段,不再总是跳回第一个字段,避免用户误以为右侧属性配置被重置。

### 验证

- 使用接口构造用户给出的 E2E 模板并发布后重新拉取:
  - 字段名保持 `question_prompt,quality_result,reject_reason`。
  - ShowItem `sourcePath` 保持 `prompt`。
  - 单选 `quality_result.required` 保持 `true`。
  - 选项 value/label 保持 `option_a:通过`,`option_b:不通过`。
  - 联动规则保持 `quality_result eq option_b -> reject_reason required`。
  - Unicode 测试确认中文 label 没有被后端或数据库写坏。
- `frontend/npm.cmd run build` 通过。

### 注意

- 此问题不是后端发布接口改写 schema,而是前端发布前没有保存已有草稿的最新画布状态。
- 如果用户已经发布了旧内容模板,需要先"收回发布",确认画布配置后重新发布一次。

## 增量实现：排查模板刷新消失与任务发布 Not Found

### 对应计划

对应“增量计划：排查模板刷新消失与任务发布 Not Found”。

### 完成内容

1. 前端 API 错误提示
   - `frontend/src/api/client.ts` 新增统一 `getApiErrorMessage`。
   - `ApiError` 会优先使用后端 JSON 中的 `message`，其次使用 `code`，最后才回退到 HTTP 状态文本。
   - `OwnerTasks.tsx`、`OwnerTemplateDesigner.tsx`、`OwnerTemplates.tsx` 复用统一错误提取，避免任务发布失败时只显示 `Not Found`。

2. 后端独立模板绑定回显
   - `TaskRepository` 的任务查询和 assignment 查询不再只通过 `task_schema_versions.task_id` 找模板。
   - 查询会优先读取 `tasks.reward_rule.schemaVersionId` 绑定的独立模板，再回退到旧的 task_id 最新模板。
   - `AnnotationRepository` 的答题页 assignment 查询同步使用同一规则，避免独立模板绑定任务进入运行时后 fallback schema 为空。

3. 接口级复现
   - 使用 `demo / demo123` 创建模板草稿、发布、刷新 `/api/schemas`、再创建指派任务，接口链路成功。
   - 使用 `owner / owner123` 选择已有模板和数据集创建指派任务，接口链路成功。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。
- 接口直测确认：模板发布后列表可查，任务发布可成功返回 `OwnerTaskResponse`。

### 注意

- 当前本地接口直测没有复现“后端创建任务必然 404”。因此截图中的 `Not Found` 更可能来自页面当前会话里选中的 `datasetId` 或 `schemaVersionId` 已经不属于当前登录 Owner、已被删除/收回，或者前端仍使用旧构建缓存。
- 修复后再次点击发布时，前端会显示后端真实业务消息，例如 `dataset not found`、`schema not found`、`published task requires a published schema`，便于继续定位具体数据。


## 增量实现:Owner 端 AI 预审规则页面前端实现

### 对应计划

参见"增量计划:Owner 端 AI 预审规则页面前端实现"。

### 完成内容

- 新增 `frontend/src/types/aiReview.ts`,定义 `AiReviewRule / AiReviewRuleRequest / AiReviewDimension / AiReviewJob / AiReviewJobStatus / AiReviewPageResult / List*Query`,复用 `types/reviewer.ts` 中已有的 `AiReviewResult / AiDecision`。
- 新增 `frontend/src/api/aiReview.ts`,封装 9 条 API:
  - 规则:`listRules / getRule / createRule / updateRule / deleteRule / toggleRule`
  - 作业:`listJobs / retryJob / getJobResult`
- 新增样例兜底数据 `frontend/public/sample-datasets/ai-review-rules.json` 与 `ai-review-jobs.json`,后端不可达时回落,Tag 标记"演示模式"。
- 重写 `frontend/src/pages/owner/OwnerAiReview.tsx`:
  - 顶部标题 + 描述 + Phase 5 Tag
  - **Tabs** 两个子面板
  - **规则管理** 面板:
    - 3 张 KPI(规则总数 / 已启用 / 全局默认)
    - 工具条(新建规则按钮 / 刷新 / 演示模式 Tag)
    - Table(规则名 + 版本 / 作用范围 / Prompt 摘要 / 阈值 / 重试 / 启用 Switch / 更新时间 / 编辑+删除)
    - 新建/编辑 Drawer 表单:基础信息 + Prompt 模板 TextArea + 评分维度 `Form.List` 动态表(增删改 + 权重和必须等于 1 校验) + 阈值与重试策略 + 启用 Switch
  - **执行记录** 面板:
    - 4 张 KPI(总数 / 成功 / 需人工复核 / 失败)
    - 工具条(状态 Segmented 过滤 / 刷新 / 演示模式 Tag)
    - Table(JobID / 关联任务 + annotationId / 状态 / 决策 / 总分 / 尝试次数 / 最后错误 / 时间 / 查看结果+重试)
    - 查看结果 Drawer:决策 Tag + 总分 + 各维度评分条(渐变填充) + 评估理由 + 风险标记 + 证据字段
- 后端不可达时所有写操作走"本地 mock + 演示模式 Tag",不伪造成功;真实后端接入后立即覆盖。
- 新增 CSS `ai-review-score-bar` 渐变评分条,暗色适配。

### 验证

- `frontend/npm run build` 通过(产物 css ≈ 111.32 kB,js ≈ 1.80 MB)。
- 浏览器侧:
  - 进入 `/owner/ai-review` 看到 Tabs + KPI + 规则列表(3 条样例规则)。
  - 点"新建规则"弹 Drawer,可填表创建,本地 mock 立即出现新行。
  - 点编辑/删除/启停,在演示模式下都即时反映。
  - 切到"执行记录"看到 6 条样例作业,可按状态过滤,失败可点"重试",任意一条点"查看结果"看到完整 AIReviewResult Drawer。

### 注意

- Phase 5 后端 API 字段需与本期前端契约保持一致;若后端字段名变化,前端需要回到 `types/aiReview.ts` 同步调整。
- 规则的"version"字段是预留乐观锁,后端实现时可基于此校验并发更新,避免 Owner 同时编辑相同规则相互覆盖。
- 当前评分维度满分硬编码 5(`ai-review-score-row` 按 5 分制渲染条),后端如需支持自定义满分,需要前端 `ScoreRow` 接受 `maxScore` 参数。

## 增量实现：任务软删除、标注作废与 Labeler 时间展示

### 对应计划

对应“增量计划：任务软删除、标注作废与 Labeler 时间展示”。

### 完成内容

1. 数据库与状态机
   - 新增 `V7__soft_delete_tasks_and_void_annotations.sql`，为 `tasks` 增加 `deleted_at` 和索引。
   - `StateMachineService` 补充 `voided` 作为 assignment / annotation 终态，并允许任务删除时进入 `ended`。
2. 后端删除语义
   - `DELETE /api/tasks/{taskId}` 从物理删除改为软删除。
   - 删除任务时保留 `tasks`、`assignments`、`annotations` 记录，assignments / annotations 标记为 `voided`，相关 drafts 清理。
   - 删除操作写入 `task.delete` 审计，并记录被作废的 assignment / annotation 状态变更。
3. 查询过滤和运行时拦截
   - Owner 任务列表、Labeler 市场、Labeler 我的任务过滤已软删除任务。
   - `assignments/mine` 过滤 `voided` assignment。
   - 答题页、草稿保存、提交在任务删除或 assignment 作废时返回 `TASK_DELETED` / `ASSIGNMENT_VOIDED`。
   - Reviewer、AI Review、Export 查询排除 `voided` annotation，AI job 不再领取已作废 annotation。
4. 时间字段和前端展示
   - `AssignmentResponse` 增加 `publishedAt`、`deadline`。
   - `MarketTaskResponse.publishedAt` 使用 `tasks.published_at`，为空时回退 `created_at`。
   - `TaskMarket.tsx` 卡片展示发布时间和截止时间。
   - `MyTasks.tsx` 表格展示发布时间和截止时间。
   - Owner 删除确认文案改为“隐藏任务 + 标注作废但保留原始答案”语义。
5. 构建阻塞顺手修复
   - `OwnerAiReview.tsx` 补充 `EyeOutlined`、`RedoOutlined` 图标导入，修复既有前端构建错误。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。

## 增量实现：Labeler 逐题保存与最后统一提交

### 对应计划

对应“增量计划：Labeler 逐题保存与最后统一提交”。

### 完成内容

1. 后端批量提交
   - 新增 `POST /api/tasks/{taskId}/assignments/submit`。
   - 当前 Labeler 在该任务下的非作废 assignments 会按 `assignment.id` 锁定并统一校验。
   - 任一 assignment 缺草稿或草稿校验失败时返回 `BATCH_SUBMIT_INCOMPLETE`，并携带 `invalidItems`。
   - 全部通过后在同一事务内创建 annotation、写入 schema snapshot、标记 assignment/item 为 `submitted`、删除 draft、创建 AI review job，并写入审计日志。
2. 前端答题页
   - “下一题”会先强制保存当前草稿，再跳转。
   - 校验失败时仍允许跳转，但会保存草稿、提示当前题未完成，并在进度圆点标红。
   - 非最后一题隐藏主提交按钮。
   - 最后一题主按钮显示“提交”，提交前保存当前题草稿，再调用任务级批量提交接口。
3. 类型与错误响应
   - `ApiErrorResponse` 增加可选 `invalidItems`，不影响原有 `{ code, message }` 响应。
   - 前端新增 `BatchSubmitResponse` 与 `BatchSubmitInvalidItem` 类型。
   - `labelerApi.submitTaskAssignments(taskId)` 已接入。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。
- 前端构建仅有 Vite chunk 体积和插件耗时提示，不影响功能。

### 注意

- 删除任务不会物理删除历史标注，`annotations.answer_json` 仍保留；后续如需查看作废记录，需要单独做归档/审计入口。
- 已导出的历史文件不回滚；本轮只保证删除后新的审核、AI Review 和导出流程不再处理 `voided` annotation。

## 增量实现：修复联动字段显示并支持“显示并必填”

### 对应计划

对应“增量计划：修复联动字段显示并支持‘显示并必填’”。

### 完成内容

1. 前端 Schema 与 Designer
   - `SchemaReactionAction` 新增 `visibleRequired`。
   - Designer 联动动作下拉新增“显示并必填”。
   - 新增联动规则默认动作从 `hidden` 改为 `visibleRequired`。
2. Renderer 运行时规则
   - `visibleRequired` 命中时显示目标字段并设为必填。
   - 旧 `required` 联动自动兼容为“显示并必填”。
   - 被 `visibleRequired/required` 指向的目标字段默认隐藏，命中后显示。
3. Labeler 答题页
   - 本地校验改用运行时联动结果：隐藏字段不校验，命中字段按条件必填校验。
   - 草稿保存、离开页面自动保存、正式提交均过滤隐藏字段，避免残留答案进入草稿或 annotation。
4. 后端校验与入库
   - Schema 发布校验白名单支持 `visibleRequired`。
   - 标注保存草稿和提交时后端兜底过滤隐藏字段。
   - 后端答案校验兼容旧 `required` 联动为“显示并必填”。

### 验证

- 待运行 `frontend/npm.cmd run build`。
- 待运行 `backend/.\mvnw.cmd -q -DskipTests package`。

### 注意

- 本轮为了兼容旧模板，将旧 `required` 联动升级为“显示并必填”。如果后续确实需要“字段始终显示，只是条件必填”，建议新增独立动作，例如 `conditionalRequired`，避免和本轮语义冲突。

### 补充修复：联动条件匹配值兼容 label/value

用户验证发现“通过 / 不通过”切换后目标字段仍不显示。补充排查后判断为规则匹配值和单选真实提交值不一致：单选按钮显示 label，但运行时值通常是 option value。

已补充：
- 前端 `matchesRule` 同时支持匹配选项 `value` 和 `label`。
- 后端 `matchesReaction` 同步支持选项 `value` 和 `label`。
- `LabelHubFormRenderer` 根据运行时可见/必填状态生成 key，避免 Formily SchemaField 复用导致隐藏/显示状态不刷新。

验证：
- `frontend/npm.cmd run build` 通过。
- `backend/.\mvnw.cmd -q -DskipTests package` 通过。

## 增量实现：修复模板草稿删除状态不一致

### 对应计划

对应“增量计划：修复模板草稿删除状态不一致”。

### 完成内容

1. `SchemaService` 删除、更新、收回判断统一使用规范化状态：只有 `published` 视为已发布，其余状态按草稿态处理。
2. `SchemaRepository.deleteDraft` 的 SQL 条件从精确 `status = 'draft'` 调整为 `LOWER(TRIM(status)) <> 'published'`。
3. 保持已发布模板必须先收回发布后再删除的产品语义。

### 验证

- 待运行 `backend/.\mvnw.cmd -q -DskipTests package`。

## 增量实现：修复 Labeler 认领任务 500 错误

### 对应计划

对应“增量计划：修复 Labeler 认领任务 500 错误”。

### 排查结果

使用本地 MySQL `root / 123456` 查看 `labelhub` 库后确认：

1. 当前任务 `14` 使用数据集 `10`，状态为 `published`。
2. 已删除任务 `11` 也使用过同一数据集 `10`。
3. 任务 `11` 删除后保留了 30 条 `assignments.status = 'voided'` 的历史认领记录。
4. 现有数据库唯一键是 `uk_assignments_item_labeler (item_id, labeler_id)`，没有包含 `task_id`。
5. 因此同一个 Labeler 在新任务 `14` 里重新认领同一个数据项时，会触发数据库唯一键冲突；之前该异常未转换成业务错误，所以前端看到 500。

### 完成内容

1. 数据库迁移
   - 新增 `V8__scope_assignment_unique_key_by_task.sql`。
   - 新唯一键改为 `uk_assignments_task_item_labeler (task_id, item_id, labeler_id)`。
   - 增加普通索引 `idx_assignments_item_id (item_id)`，保证 `assignments.item_id` 外键仍有可用索引。
   - 删除旧唯一键 `uk_assignments_item_labeler`。
   - 同步更新 `V1__init_labelhub_schema.sql`，新库初始化时直接使用任务维度唯一键。
2. 后端认领逻辑
   - `countClaimableItems` 和 `findClaimableItems` 改为只排除当前任务内已有有效 assignment 的 item。
   - `createAssignment` 捕获 `DuplicateKeyException` 并转换为 `DuplicateAssignmentException`。
   - `TaskService` 将重复认领异常转换为 409 `NO_AVAILABLE_ITEM`，避免数据库异常冒泡成 500。
3. 业务语义
   - 同一个数据集 item 可以被复用到不同任务中。
   - 同一个 Labeler 在不同任务中可以重新标注同一个 item。
   - 同一任务内仍禁止同一个 Labeler 重复认领同一个 item。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- 已通过 SQL 只读查询确认当前本地库的冲突来源。
- 当前本地库还需要执行 `V8__scope_assignment_unique_key_by_task.sql` 后，任务 14 才能真正认领成功。

### 补充验证：本地执行 V8 迁移

已按确认在本地 MySQL `labelhub` 库执行：

```powershell
cmd /c "mysql -uroot -p123456 < backend\src\main\resources\db\migration\V8__scope_assignment_unique_key_by_task.sql"
```

执行后索引状态：

1. `uk_assignments_item_labeler` 已删除。
2. `uk_assignments_task_item_labeler (task_id, item_id, labeler_id)` 已生效。
3. `idx_assignments_item_id (item_id)` 已生效。

真实接口验证：

1. 使用 `labeler / labeler123` 登录。
2. 调用 `POST /api/tasks/14/claim` 成功。
3. 返回 assignment `131`，状态 `claimed`，不再出现 500。

注意：本次验证通过真实接口执行，因此任务 `14` 已被 `labeler` 账号领取到“我的任务”中。
## 增量实现：修复模板联动保存、运行时显示与旧标注快照回放

### 对应计划

对应“增量计划：修复模板联动保存、运行时显示与旧标注快照回放”。

### 完成内容

1. 数据库与后端契约
   - 新增 `V9__annotation_schema_snapshot.sql`，为 `annotations` 增加 `schema_snapshot_json`，并用当前 `task_schema_versions.schema_json` 对旧数据做 best-effort 回填。
   - `V1__init_labelhub_schema.sql` 同步新增 `schema_snapshot_json`，保证新库初始化结构一致。
   - `AssignmentItemResponse` 新增 `schemaDigest`。
   - `AnnotationResponse` 新增 `schemaSnapshot`。
   - `DraftRequest` 和 `SubmitAnnotationRequest` 新增 `schemaDigest`。
2. 后端 Schema 规范化
   - Schema 保存、校验、发布前统一规范化联动规则。
   - 旧 `required` 动作自动转为 `visibleRequired`。
   - 联动匹配值如果填写的是选项 label，会自动转为对应 option value。
   - 发布接口会回写规范化后的 schema，避免旧草稿直接发布后仍保留旧语义。
3. 后端标注闭环
   - `GET /api/assignments/{id}/item` 返回当前 schema digest。
   - 保存草稿和提交时校验 `schemaDigest`，不一致返回 `409 SCHEMA_CHANGED`。
   - 提交 annotation 时写入当前完整 schema 快照到 `schema_snapshot_json`。
   - 草稿保存和正式提交都按运行时可见字段过滤隐藏字段。
4. 前端 Designer
   - 保存/发布前调用 `normalizeSchemaFields`，前端也会把 `required` 规范为 `visibleRequired`，把选项 label 转为 value。
   - 收回模板后尽量保持当前选中字段，不再固定跳到第一个字段。
   - 联动面板显示当前配置字段，降低误改 ShowItem 的概率。
   - 来源字段存在选项时，匹配值增加下拉选择，展示 `label (value)`，实际保存 value。
5. 前端 Labeler
   - Formily 编译时直接跳过隐藏字段，避免只依赖 `x-visible` 导致显示状态不刷新。
   - 已提交答案默认使用 `latestAnnotation.schemaSnapshot.fields` 只读渲染。
   - 点击“重新修改”后切换到当前任务最新 schema，再保存草稿或重新提交。
   - 保存草稿和提交请求携带 `schemaDigest`，并过滤隐藏字段。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。
- 只读 SQL 已确认当前本地库暂未应用 `schema_snapshot_json` 字段；运行新提交快照逻辑前需要执行 `V9__annotation_schema_snapshot.sql`。

### 注意

- 迁移前已经被原地覆盖的旧模板结构无法完整恢复；V9 只能用当前 schema 做 best-effort 回填。迁移后新提交的 annotation 才能严格按提交当时 schema 回放。
- `SCHEMA_CHANGED` 只在请求携带 `schemaDigest` 时生效；旧前端或演示数据不携带 digest 时仍按兼容路径处理。

## 增量实现：修正 Labeler 我的任务按钮文案

### 对应计划

对应“增量计划：修正 Labeler 我的任务按钮文案”。

### 完成内容

1. 后端 `AssignmentResponse` 增加 `hasDraft` 与 `hasSubmittedAnnotation`。
2. `TaskRepository` 查询 assignment 列表时通过 `drafts` 和非 `voided` 的 `annotations` 派生两个字段。
3. 前端 `Assignment` 类型同步新增上述字段。
4. `MyTasks.tsx` 按入口 assignment 状态和派生字段显示按钮：
   - 未开始的 `claimed` 显示“开始答题”。
   - 有草稿的 `claimed` 显示“继续答题”。
   - `returned` 显示“继续修改”。
   - 已提交显示“查看/修改答卷”。
   - 已通过显示“查看答卷”。
5. `TaskMarket.tsx` 对已领取任务统一显示“去作答”，并跳转到“我的任务”页继续选择具体入口。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。

## 增量实现：Schema 字段语义类型标注

### 对应计划

对应“增量计划：Schema 字段语义类型标注”。

### 完成内容

1. 后端 Schema 规范化
   - `SchemaService.normalizeFields` 会给字段补齐 `semanticType`。
   - 语义类型由 `kind` 派生，不开放 Owner 手动编辑。
   - 保存、校验、发布都会使用补齐后的字段。
2. 后端标注校验
   - `SchemaDefinitionValidator` 增加 `semanticType` 白名单校验。
   - `AnnotationService` 的提交过滤、JSON 校验、选项校验优先使用 `semanticType`，缺失时 fallback 到 `kind`。
   - 答题页读取 schema 时会返回带 `semanticType` 的 runtime schema，并写入 annotation snapshot。
3. 前端类型与渲染
   - `SchemaField` 增加 `semanticType`。
   - `schemaCompiler` 的 Formily 值类型、组件选择、提交过滤优先读取 `semanticType`。
   - `AnswerPage` 本地 JSON 校验优先读取 `semanticType=json`。
   - Designer 新建字段时写入 `semanticType`，旧模板本地 normalize 后也能预览。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。
## 增量实现：AI Agent 独立服务、AI 审核后台与状态机闭环

### 对应计划

对应“增量计划：AI Agent 独立服务、AI 审核后台与状态机闭环”。

### 完成内容

1. 后端与数据库
   - 新增 `V10__ai_review_rules_agent_runtime.sql`，创建 `ai_review_rules`，扩展 `ai_review_jobs.rule_id/rule_snapshot_json`，扩展 `ai_review_results.total_score/risk_flags_json/evidence_json/model_name/latency_ms`。
   - 迁移脚本写入 `ai_reviewer`、`system_agent` 角色和 `ai_review:manage` 权限，并初始化一条“默认质量预审规则”。
   - 新增 AI 规则 CRUD、Job 列表、Job 领取、完成、失败、重试、结果详情接口。
   - `CreateTaskRequest`、`TaskMetadata`、`OwnerTaskResponse` 增加 `aiReviewRuleId/aiReviewRuleName`，Owner 发布任务时可绑定启用中的 AI 规则。
   - Labeler 提交后创建带规则快照的 pending job，后续规则变更不影响已入队 job。
2. 状态机与审计
   - `StateMachineService` 增加 `ai_reviewer` 对 AI job 和 annotation AI 流转的操作权限。
   - AI job 状态流转统一审计：`ai_review.start`、`ai_review.complete`、`ai_review.fail`、`ai_review.retry`、`ai_review.fallback_to_human`。
   - AI 成功后 annotation 默认进入 `reviewing`，保留人工复核；AI 失败未超重试上限自动重新 pending，超限后兜底进入人工审核。
3. Agent 独立服务
   - 在 `agent/` 下新增独立 Spring Boot Worker，可单独构建。
   - Agent 使用 `system_agent` 登录后端，只通过后端 API 领取 pending job、回写 complete/fail，不直连数据库。
   - Agent 通过环境变量读取 Responses API 配置：`LABELHUB_LLM_BASE_URL`、`LABELHUB_LLM_API_KEY`、`LABELHUB_LLM_MODEL`、`LABELHUB_LLM_REASONING_EFFORT`、`LABELHUB_LLM_WIRE_API`。
   - API Key 未写入仓库。
4. 前端
   - 登录页、角色切换、布局和路由增加 `ai_reviewer`，新增 `/ai-reviewer`、`/ai-reviewer/jobs`、`/ai-reviewer/rules`。
   - `ai_reviewer` 工作台复用 AI 规则/Job 管理页，支持规则管理、队列查看、失败重试、结果详情查看。
   - Owner 任务发布抽屉增加“启用 AI 预审”和“AI 预审规则”选择，发布态强制选择启用中的规则。
   - 前端类型同步 `aiReviewRuleId/aiReviewRuleName`、AI job `retryCount/errorSummary`。
5. 数据库执行
   - 已执行 MySQL 迁移命令：`mysql -uroot -p123456 labelhub < backend\src\main\resources\db\migration\V10__ai_review_rules_agent_runtime.sql`。
   - 已验证本地库存在 `ai_review_rules`、新增 job/result 字段、`ai_reviewer/system_agent` 角色和默认规则。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `agent/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。

### 注意

- Agent 使用真实 Responses API，运行前必须在本机环境变量设置 `LABELHUB_LLM_API_KEY`，仓库中没有保存密钥。
- 本轮 AI 通过后仍进入人工复核，不直接 accepted。
- `ai_reviewer` 当前复用 Owner AI 规则/Job 页面作为独立后台入口，后续如需更细权限可在后端拆分 permission。
## 增量实现：README 补充 Agent 启动说明

### 对应计划

对应“增量计划：README 补充 Agent 启动说明”。

### 完成内容

1. 在根目录 `README.md` 顶部补充 `Agent 启动说明`。
2. 写明 Agent 是独立 Spring Boot Worker，目录为 `agent/`，只调用后端 API，不直连数据库。
3. 写明启动顺序：先启动 MySQL、Redis、后端，再启动 Agent。
4. 补充 PowerShell 启动命令和环境变量：
   - `LABELHUB_BACKEND_BASE_URL`
   - `LABELHUB_AGENT_USERNAME`
   - `LABELHUB_AGENT_PASSWORD`
   - `LABELHUB_AGENT_ROLE`
   - `LABELHUB_LLM_BASE_URL`
   - `LABELHUB_LLM_API_KEY`
   - `LABELHUB_LLM_MODEL`
   - `LABELHUB_LLM_REASONING_EFFORT`
   - `LABELHUB_LLM_WIRE_API`
5. 明确 `LABELHUB_LLM_API_KEY` 只在本机终端设置，不写入仓库。
6. 补充没有 pending job 时 Agent 只轮询、不立刻消费的正常现象。

### 验证

- 文档变更，无需构建。

## 增量实现：调整 README 中 Agent 启动说明位置

### 对应计划

对应“增量计划：调整 README 中 Agent 启动说明位置”。

### 完成内容

1. 已将 `README.md` 顶部的 Agent 启动说明移动到 `启动后端` 之后、`启动前端` 之前。
2. 保留 Agent 启动命令、后端连接环境变量、LLM 环境变量和运行注意事项。
3. `LABELHUB_LLM_API_KEY` 继续使用占位符，未写入真实密钥。
4. 同步调整前端启动章节的本地联调顺序说明，明确 AI 预审场景为 `MySQL -> Redis -> 后端 -> Agent -> 前端`。

### 验证

- 文档位置调整，无需构建。

## 增量实现：修复 AI Agent 失败可观测性与 AI Reviewer 页面区分

### 对应计划

对应“增量计划：修复 AI Agent 失败可观测性与 AI Reviewer 页面区分”。

### 完成内容

1. Agent 失败来源拆分
   - `AiReviewWorker` 将 LLM 调用失败标记为 `LLM_CALL_FAILED`。
   - 后端 complete 回写失败标记为 `BACKEND_COMPLETE_FAILED`。
   - fail 回写本身失败时输出 error 级别日志，避免二次失败吞掉根因。
2. 后端异常可观测性
   - `GlobalExceptionHandler` 对未处理异常输出服务端堆栈日志。
   - API 响应仍保持 `INTERNAL_ERROR / internal server error`，避免向前端暴露内部堆栈。
3. AI Reviewer 前端
   - `/ai-reviewer` 改为作业概览页，展示排队、执行中、成功、失败、需人工和启用规则指标。
   - `/ai-reviewer/jobs` 显示 Job 队列。
   - `/ai-reviewer/rules` 显示规则管理。
   - Owner `/owner/ai-review` 仍保留原规则与执行记录 Tab 页面。
4. LLM 配置
   - 已用临时命令验证 `https://www.pqapi.store/v1` 的 `/models` 和 `/responses` 可用。
   - README 与 Agent 默认 base URL 改为测试通过的 `https://www.pqapi.store/v1`。
   - 未写入真实 API Key，文档仍使用占位符。

### 验证

- `agent/.\mvnw.cmd -q -DskipTests package` 通过。
- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。
- 使用临时 API Key 调用 `/models` 成功，返回模型列表。
- 使用临时 API Key 调用 `/responses` 成功，`gpt-5.5` 能按 JSON Schema 返回结构化 JSON。

## 增量实现：AI 审核 Job 取消与重新执行

### 对应计划

对应“增量计划：AI 审核 Job 取消与重新执行”。

### 完成内容

1. 数据库与后端
   - 新增 `V11__ai_review_job_run_token.sql`，为 `ai_review_jobs` 增加 `run_token`、`cancel_reason` 和 token 索引。
   - Agent 领取 job 时生成 `runToken`，写入当前 job 并返回给 Agent。
   - complete/fail 回写校验 `runToken`，旧执行回写返回 `AI_REVIEW_RUN_STALE`，不修改 Job 或 annotation。
   - 新增 `POST /api/ai-review/jobs/{jobId}/cancel`，支持 `running -> pending`，清空旧 token 并重新排队。
2. Agent
   - `AiReviewJobClaimResponse` 接收 `runToken`。
   - complete/fail 请求体携带 `runToken`。
   - 收到 `AI_REVIEW_RUN_STALE` 时只记录日志，不再二次 fail 回写。
3. 前端
   - Job 队列 `running` 行显示“取消并重新排队”按钮。
   - running 超过 10 分钟显示“疑似卡住”。
   - failed 行保留“重新执行”按钮。
4. 文档
   - `接口文档.md` 补充 cancel 接口、`runToken` 和 `AI_REVIEW_RUN_STALE`。
   - `问题点归档.md` 记录 running 卡住和旧 Agent 迟到回写竞态。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `agent/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。

### 注意

- 本次新增了数据库迁移 `V11__ai_review_job_run_token.sql`。旧库需要执行该 SQL 后，新的取消与 token 校验接口才能正常使用。
## 2026-05-30 模型配置后端与权限收敛

- 新增迁移 `backend/src/main/resources/db/migration/V12__ai_model_configs.sql`，创建 `ai_model_configs` 表，用于保存全局 LLM 供应商、接口地址、模型名、推理强度、wire_api、加密 API Key 和 Key 掩码；同一时间按 `status = 'active'` 读取最新配置。
- 后端新增 `AiModelConfigController` / `AiModelConfigService` / `AiModelConfigRepository`，提供：
  - `GET /api/ai-review/model-config`
  - `PUT /api/ai-review/model-config`
  - `POST /api/ai-review/model-config/models`
  - `GET /api/ai-review/model-config/runtime`
- API Key 使用 AES-GCM 加密，密钥来自 `LABELHUB_MODEL_CONFIG_SECRET`；管理查询只返回 `apiKeyMask`，运行时明文仅允许 `system_agent` 读取。
- 权限收敛：模型配置管理接口仅允许 `owner` / `ai_reviewer`；`system_agent` 只能读取 runtime；`labeler` / `reviewer` / `admin` 无配置权限。
- Agent 新增 `AiModelRuntimeConfig`，每次处理 Job 前通过后端 runtime 接口读取最新模型配置；当后端没有 active 配置时回退 `LABELHUB_LLM_*` 环境变量；配置存在但不可解密或不完整时不静默回退。
- `ResponsesLlmClient` 改为按单次 review 构造请求客户端，支持 `useFullUrl`；完成回写的 `modelName` 使用本次真实调用的模型名。
- 前端 `ModelSettings.tsx` 移除 localStorage，改为调用后端接口；支持保存配置、密钥掩码提示、获取模型列表和模型名选择；新增 Owner 路由 `/owner/settings/model`，AI Reviewer 侧栏保留 `/ai-reviewer/settings/model`。
## 2026-05-30 AI 审核 Job 批量删除接口补齐

- 修复 AI Reviewer Job 队列点击“删除选中”时请求 `/api/ai-review/jobs/batch-delete` 返回 500 的问题，根因是前端已有调用但后端缺少接口。
- 后端新增：
  - `AiReviewBatchDeleteRequest`
  - `AiReviewBatchDeleteResponse`
  - `POST /api/ai-review/jobs/batch-delete`
- 删除规则：
  - 仅 `owner`、`reviewer`、`ai_reviewer` 可调用。
  - `system_agent` 不允许删除 Job。
  - 仅允许删除 `failed`、`succeeded` 状态的 Job。
  - `pending`、`running` 不允许直接删除，避免 annotation 或 Agent 回写链路被打断。
- 删除实现：在同一事务内锁定 Job、写入 `ai_review.delete` 审计日志，然后物理删除 `ai_review_jobs`；关联 `ai_review_results` 由外键级联删除。
- 前端 `aiReviewApi.deleteJobs` 改为接收后端响应，并在删除失败时展示后端错误信息。
## 2026-05-30 AI Reviewer 端移除规则管理

- 按角色职责调整：AI 预审规则由 Owner 端维护，AI Reviewer 端不再提供规则创建、编辑、启停和删除能力。
- 后端 `AiReviewService` 新增 `requireRuleManager`，规则相关接口统一收紧为 Owner only：
  - `GET /api/ai-review/rules`
  - `POST /api/ai-review/rules`
  - `GET /api/ai-review/rules/{ruleId}`
  - `PUT /api/ai-review/rules/{ruleId}`
  - `DELETE /api/ai-review/rules/{ruleId}`
  - `POST /api/ai-review/rules/{ruleId}/toggle`
- 后端 Job 运行态接口不变，AI Reviewer 仍可查看 Job、取消、重试、查看结果和配置模型。
- 前端移除 AI Reviewer 侧栏“规则管理”入口，删除 `/ai-reviewer/rules` 路由和页面分支。
- AI Reviewer 概览不再请求规则列表，不再展示“启用规则”统计和“管理规则”按钮，改为展示失败可重试 Job 数。
## 2026-05-30 Reviewer AI 预审页改为按任务分组(仅前端 + 接口预留)

### 对应计划

对应“2026-05-30 Reviewer AI 预审页改为按任务分组(仅前端 + 接口预留)”。

### 完成内容

1. 页面重构 `frontend/src/pages/reviewer/ReviewerAi.tsx`
   - 标题与文案由「AI 审核」改为「AI 预审」。
   - 顶层渲染任务列表 `AiReviewTaskCard`,每张卡显示任务名、类型标签,右侧聚合 PASS / NEED_HUMAN / REJECT 计数与总数。
   - 顶部关键词搜索 + 决策 Segmented(全部 / PASS / NEED_HUMAN_REVIEW / REJECT)作用在任务级。
   - 任务卡整行可点击展开(手风琴式,`expandedTaskId` 控制同时只展开一个);展开时懒加载该任务下标注明细,用嵌套小表格逐条展示 Annotation / Item / Schema / Labeler / AI 决策 / 总分 / 评语,并保留「确认通过 / 打回」。
   - 逐条裁决后从本地 `items` 移除,并通过 `onResolved` 回写任务计数;计数归零任务自动从列表过滤。
   - 工具函数 `groupTasksFromItems`(扁平标注按 `taskId` / `schema-{schemaVersionId}` 聚合)、`filterFallbackItems`(按任务 + 决策 + 关键词过滤)。
2. API 预留 `frontend/src/api/reviewer.ts`
   - 新增 `reviewerApi.listAiReviewTasks({ decision, keyword })` → `GET /reviewer/ai-review/tasks`。
   - 新增 `reviewerApi.listAiReviewAnnotations(taskId, { decision, keyword, page, pageSize })` → `GET /reviewer/ai-review/tasks/{taskId}/annotations`。
   - 裁决仍复用已有 `reviewerApi.submitReview`(`POST /reviewer/annotations/{annotationId}/decision`)。
   - 后端未就绪时:`listAiReviewTasks` 失败回落 `sample-datasets/reviewer-annotations.json`,前端 `groupTasksFromItems` 本地分组;展开时从已加载样本本地过滤,交互一致并显示「演示模式」标签。
3. 类型 `frontend/src/types/reviewer.ts`
   - `AnnotationToReview` 新增可选 `taskId/taskTitle/taskType`。
   - 新增 `AiReviewTaskSummary`(taskId/taskTitle/taskType/total/passCount/needHumanCount/rejectCount/pendingHuman/updatedAt)。
4. 样式 `frontend/src/styles/global.css`
   - 新增 `.ai-review-task-list` / `.ai-review-task-card` / `.ai-review-task-header` / `.ai-review-task-body` 等,任务卡可点击头部、展开区嵌套表格,含 hover / 展开态与暗色模式适配。
5. 演示数据 `frontend/public/sample-datasets/reviewer-annotations.json`
   - 为样本条目补充 `taskId/taskTitle/taskType`,分属「电商标题清洗与类目标注」「服饰类目卖点提取」两个任务,便于演示分组效果。
6. 文档
   - `接口文档.md` 已补两个接口(2.6 前端清单、后端清单、7.2 详情)。
   - 本条目同步 `阶段实现.md`。

### 验证

- `frontend/npm.cmd run build`(`node ./node_modules/typescript/bin/tsc -b`)通过,无诊断错误。

### 注意

- 本轮未实现后端,两个 AI 预审聚合接口仍为「后端待实现」;后端落地前页面走演示数据降级路径。
- 真实接口要求 `listAiReviewTasks` 的 `decision/keyword` 过滤同时影响任务计数与任务是否出现在列表中,需与前端按任务级筛选语义保持一致。
## 2026-05-30 AI 预审任务卡片化 + 三栏审核工作台(仅前端)

### 对应计划

对应“2026-05-30 AI 预审任务卡片化 + 三栏审核工作台(仅前端)”。

### 完成内容

1. `frontend/src/pages/reviewer/ReviewerAi.tsx`
   - 任务列表由手风琴改为响应式卡片网格 `.ai-review-card-grid`。
   - 每张卡片 `AiReviewTaskCard`:主色机器人图标 + 任务名/类型,三段统计(建议通过绿 / 转人工黄 / 建议打回红),待审进度条,底部「进入审核 →」主按钮。
   - 整卡或按钮点击导航到 `/reviewer/ai/{taskId}`。
   - `groupTasksFromItems` 导出供工作台复用;演示模式回落样本本地分组。
2. 新增 `frontend/src/pages/reviewer/ReviewerAiWorkbench.tsx`(路由 `/reviewer/ai/:taskId`)
   - 三栏布局:左栏分段(自定义三按钮分段控件,保证「建议通过 / 转人工 / 建议打回」始终显示,不再被 antd Tabs 折叠进溢出菜单)+ 可滚动标注列表;中栏标注答案 + AI 预审评分/评语/风险标记 + 审核意见 + 三个裁决按钮;右栏今日统计四宫格 + 审核时间线。
   - 裁决调用 `reviewerApi.submitReview(annotationId, { decision, note })`,`decision` 取 `APPROVE` / `RETURN` / `REVISE`;裁决后本地移除并更新今日统计。
   - 后端未就绪时从 `sample-datasets/reviewer-annotations.json` 按 `taskId` 过滤。
3. 路由与导出
   - `pages/reviewer/index.ts` 导出 `ReviewerAiWorkbench`。
   - `router/index.tsx` 新增 `/reviewer/ai/:taskId`。
4. 样式 `frontend/src/styles/global.css`
   - 移除旧的手风琴样式,新增 `.ai-review-card-*`(卡片网格)与 `.ai-wb-*`(工作台三栏、分段控件、列表项、详情、裁决按钮、统计、时间线),含暗色模式适配。
   - 按反馈:左栏分段三项常显;中间详情列宽收窄,grid 由 `300px 1fr 280px` 改为 `280px minmax(0,680px) 280px` 并居中。
5. 文档
   - `接口文档.md` 补工作台说明与三项后续增强接口(多轮对比快照 / 批量裁决 / 时间线)建议。
   - 本条目同步 `阶段实现.md`。

### 验证

- `frontend/npm.cmd run build`(`node ./node_modules/typescript/bin/tsc -b`)通过,无诊断错误。

### 注意

- 工作台为纯前端,未新增后端接口;复用已预留的任务明细查询与逐条裁决接口。
- 参考设计中的「多轮提交对比」「批量通过/打回」「真实时间线」依赖后端补充字段与接口,当前用现有字段构造单份答案与最小时间线,后端落地后再接真实数据。
## 2026-05-30 AI 预审工作台中栏:多轮提交对比 + AI 版本标识(仅前端)

### 对应计划

对应“2026-05-30 AI 预审工作台中栏:多轮提交对比 + AI 版本标识(仅前端)”。

### 完成内容

1. `frontend/src/types/reviewer.ts`
   - `AnnotationToReview` 新增可选 `previousAnswerJson`(上一轮答案)。
   - `AiReviewResult` 新增可选 `version`(规则/模型版本标识)与 `modelName`。
2. `frontend/src/pages/reviewer/ReviewerAiWorkbench.tsx`
   - 中栏标题改为 `annotationId · 标题`(`pickTitle` 取 cleaned_title/title/question/prompt/itemId)+ 右侧「第 N 轮 / 复审中」标签,副标题显示题目、模板与轮次说明。
   - `previousAnswerJson` 非空时渲染 `.ai-wb-compare` 双列对比:左列「第 N-1 轮提交(已打回)」,右列「第 N 轮提交(本轮·修改后)」;用 `isSameValue` 比较两轮字段,改动项加 `.is-changed` 绿色高亮;无上一轮时保持单列答案卡。
   - AI 预审卡复审时标题显示「AI 预审 · 本轮重跑结果」,右上角渲染 `aiResult.version` 版本徽标。
3. `frontend/src/styles/global.css`
   - 新增 `.ai-wb-detail-title/.ai-wb-detail-name/.ai-wb-detail-badges`、`.ai-wb-compare*`、`.ai-wb-answer-value.is-changed`、`.ai-wb-ai-head-right/.ai-wb-ai-version` 等样式,含暗色模式适配与 1100px 以下对比列堆叠。
4. 演示数据 `frontend/public/sample-datasets/reviewer-annotations.json`
   - 现有 AI 结果补 `version`;新增一条 `A-9004`(`revisionNo: 2`,带 `previousAnswerJson`)演示多轮对比与改动高亮。
5. 文档
   - `接口文档.md`:把多轮对比/版本从「后续建议」升级为「前端已落地、等待后端补 `previousAnswerJson` 与 `aiResult.version`/`modelName`」;批量裁决与时间线仍列为后续增强。
   - 本条目同步 `阶段实现.md`。

### 验证

- `frontend/npm.cmd run build`(`node ./node_modules/typescript/bin/tsc -b`)通过,无诊断错误。

### 注意

- 多轮对比与版本徽标依赖后端在任务明细接口返回 `previousAnswerJson` 与 `aiResult.version`;后端未就绪时仅演示样本中的 `A-9004` 能看到双列对比效果,其余条目走单列。
## 2026-05-30 Reviewer 菜单合并:AI 审核改为「待审队列」并下线旧待审队列(仅前端)

### 对应计划

对应“2026-05-30 Reviewer 菜单合并:AI 审核改为「待审队列」并下线旧待审队列(仅前端)”。

### 完成内容

1. `frontend/src/layouts/AppLayout.tsx`
   - reviewer 菜单移除 `/reviewer/queue` 项;`/reviewer/ai` label 由「AI 审核」改为「待审队列」,图标由 `RobotOutlined` 改为 `AuditOutlined`。
   - 面包屑路径片段映射:`ai` 改为「待审队列」,移除 `queue`。
   - `resolveSelectedKey` reviewerKeys 去掉 `/reviewer/queue`。
2. `frontend/src/pages/reviewer/ReviewerAi.tsx`
   - 页面标题由「AI 预审」改为「待审队列」(其余卡片/工作台逻辑不变)。
3. `frontend/src/router/index.tsx`
   - `/reviewer/queue` 改为 `<Navigate to="/reviewer/ai" replace />`,兼容旧书签。
   - 移除 `ReviewerQueue` 导入。
4. `frontend/src/pages/reviewer/index.ts`
   - 移除 `ReviewerQueue` 导出。
5. 删除 `frontend/src/pages/reviewer/ReviewerQueue.tsx`(已无引用)。
6. `frontend/src/pages/reviewer/ReviewerOverview.tsx`
   - 「领取审核任务」「查看全部」跳转改为 `/reviewer/ai`。
   - 批次「开始审核」由 `/reviewer/queue?batchId=` 改为 `/reviewer/ai/{batch.taskId}`,对接新三栏工作台。
7. 文档
   - `接口文档.md`:工作台入口说明更新为「待审队列」并注明 `/reviewer/queue` 重定向;批次相关前端方法补现状备注。
   - 本条目同步 `阶段实现.md`。

### 验证

- `frontend/npm.cmd run build`(`node ./node_modules/typescript/bin/tsc -b`)通过,无诊断错误。

### 注意

- 概览页「待审队列」卡片仍走 `reviewerApi.listBatches`(批次维度),与待审队列页(AI 预审任务聚合)数据源不同,但跳转统一按任务进入工作台;后续如需统一数据源可再调整。
## 2026-05-30 Reviewer 待审队列后端补齐

### 对应计划

对应“2026-05-30 Reviewer 待审队列后端补齐”。

### 完成内容

1. 后端接口
   - 新增 `GET /api/reviewer/ai-review/tasks`，按任务聚合 Reviewer 待人工裁决的 AI 预审结果。
   - 新增 `GET /api/reviewer/ai-review/tasks/{taskId}/annotations`，拉取指定任务下待裁决标注明细。
   - 鉴权沿用 Reviewer 页面语义，仅 `reviewer` 角色可访问。
2. 数据筛选
   - 仅返回未删除任务、非作废 assignment/annotation、最新 revision、`annotations.status = 'reviewing'`、已有成功 AI result 且尚未产生人工审核记录的数据。
   - `decision` 参数按 AI 结论过滤：`PASS`、`NEED_HUMAN_REVIEW`、`REJECT`。
3. 响应字段
   - 新增 `AiReviewTaskSummaryResponse`。
   - `AnnotationToReviewResponse` 补充 `taskId`、`taskTitle`、`taskType`、`previousAnswerJson`。
   - Reviewer 侧 `AiReviewResultResponse` 补充 `version`、`modelName`，并改为读取数据库真实 `total_score`、`risk_flags_json`、`evidence_json`。
4. 文档
   - `接口文档.md` 更新 Reviewer AI 待审接口实现状态、筛选语义和新增字段。
   - `问题点归档.md` 记录前端 sample fallback 与后端缺少页面专用聚合接口的问题。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。

### 注意

- 本次不新增数据库表或迁移。
- 人工裁决仍复用 `POST /api/reviewer/annotations/{annotationId}/decision`。
- 页面端仍需用真实账号跑一遍 `/reviewer/ai` 和 `/reviewer/ai/:taskId`，确认不再显示演示模式。
## 2026-05-30 Reviewer 审核阶段时间线补齐

### 对应计划

对应“2026-05-30 Reviewer 审核阶段时间线补齐”。

### 完成内容

1. 后端响应
   - `AnnotationToReviewResponse` 新增 `reviewTimeline`。
   - 新增 `ReviewTimelineStageResponse`，字段包括 `roundNo`、`stage`、`title`、`status`、`actor`、`decision`、`score`、`comment`、`reason`、`occurredAt`。
2. 阶段语义
   - 第一轮审核：`stage = ai_review`，标题为“第一轮审核（AI预审）”，展示 AI decision、分数、评语和完成时间。
   - 第二轮审核：`stage = human_review`，标题为“第二轮审核（人工复审）”，未裁决时显示 pending，裁决后展示人工 decision、reason、Reviewer 和时间。
3. 数据来源
   - AI 阶段来自 `ai_review_jobs.finished_at` 与 `ai_review_results`。
   - 人工阶段来自最新 `human_reviews` 记录。
   - 状态机审计仍复用现有 `ai_review.complete`、`human_review.approve/return/escalate/revise`。
4. 前端
   - `AnnotationToReview` 新增 `reviewTimeline` 类型。
   - `ReviewerAiWorkbench` 右栏时间线优先渲染后端 `reviewTimeline`，缺失时保留原本本地兜底。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。

### 注意

- 本次不改数据库状态机状态集合，不新增迁移。
- 这是状态机结果的展示层补齐：状态迁移和审计日志此前已经写入，页面现在能明确看到第一轮 AI 预审和第二轮人工复审。
## 2026-05-30 Reviewer AI 结论文案“转人工”改为“人工复核”

### 对应计划

对应“2026-05-30 Reviewer AI 结论文案‘转人工’改为‘人工复核’”。

### 完成内容

1. `frontend/src/pages/reviewer/ReviewerAi.tsx`
   - 筛选项 `NEED_HUMAN_REVIEW` 的展示文案从 `NEED_HUMAN` 改为“人工复核”。
   - 任务卡片统计标签从“转人工”改为“人工复核”。
2. `frontend/src/pages/reviewer/ReviewerAiWorkbench.tsx`
   - 分组标签和 AI 决策标签从“转人工”改为“人工复核”。
   - 注释中的分组说明同步更新。
3. 文档
   - `问题点归档.md` 记录“转人工”容易被误解为转派功能，实际含义是 AI 建议人工重点复核。

### 验证

- `frontend/npm.cmd run build` 通过。

### 注意

- 只改中文展示文案，不改接口枚举值；后端仍使用 `NEED_HUMAN_REVIEW`。
## 2026-05-30 Labeler 工作概览真实数据接入

### 对应计划

对应“2026-05-30 Labeler 工作概览真实数据接入”。

### 完成内容

1. 后端新增 `backend/src/main/java/com/labelhub/backend/labeler/`
   - `LabelerOverviewController` 暴露 `GET /api/labeler/overview`。
   - `LabelerOverviewService` 校验 `labeler` 角色,组装 `heroStats`、`kpis`、`todayProgress`、`reviewDistribution`、`recentBatches`、`supportedItemTypes`。
   - `LabelerOverviewRepository` 基于现有任务、题目、assignment、annotation、AI 预审结果和人工审核表聚合查询。
2. 前端新增接口与类型
   - `frontend/src/types/labelerOverview.ts` 定义工作概览响应类型。
   - `frontend/src/api/labelerOverview.ts` 新增 `labelerOverviewApi.getOverview()`。
3. 前端页面改造
   - `LabelerOverview.tsx` 移除原 KPI、审核分布、最近批次、题型列表静态业务样例。
   - 页面加载真实接口；加载中展示 Skeleton；失败展示错误态和重试按钮；空集合展示真实 0 值或 Empty。
   - 最近批次“进入答题”使用接口返回的 `assignmentId` 跳转 `/labeler/answer/{assignmentId}`。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。

### 注意

- 本次不新增数据库表或迁移。
- 今日目标 v1 固定 30 条；奖励估算只用于展示,不作为结算依据。
- 不保留静态样例兜底；接口失败时页面只展示错误态和重试入口。
## 2026-05-30 Labeler 草稿箱真实数据接入

### 对应计划

对应“2026-05-30 Labeler 草稿箱真实数据接入”。

### 完成内容

1. 后端接口
   - `AnnotationController` 新增 `GET /api/labeler/drafts`,返回当前 Labeler 自己的草稿分页列表。
   - `AnnotationController` 新增 `DELETE /api/assignments/{assignmentId}/draft`,只删除草稿记录,不删除 assignment。
   - 新增 `LabelerDraftResponse`,字段包括 `assignmentId`、`taskId`、`itemId`、`title`、`taskTitle`、`taskType`、`taskTypeKey`、`schemaVersionId`、`schemaVersion`、`updatedAt`、`editable`。
2. 后端数据口径
   - 草稿列表只查询当前登录 Labeler 的 `drafts` 记录。
   - 排除 `assignments.status = voided` 和已软删除任务。
   - 按 `drafts.updated_at DESC` 排序,默认 `page=1,pageSize=50`。
   - 删除接口先校验 assignment 属于当前用户且任务/作业可用；草稿不存在时仍按幂等删除返回成功。
3. 前端接入
   - `labelerApi` 新增 `listDrafts()` 和 `deleteDraft()`。
   - `LabelerDraft` 类型补齐草稿箱列表字段。
   - `Drafts.tsx` 移除硬编码草稿样例,改为加载真实接口；同步按钮重拉接口,继续编辑跳转 `/labeler/answer/{assignmentId}`,删除按钮调用后端接口。
   - 页面支持加载态、错误态、重试入口和真实空态,不做静态数据降级。
4. 文档
   - `接口文档.md` 补充草稿列表和删除草稿接口。
   - `问题点归档.md` 记录草稿箱依赖静态样例且删除无后端动作的问题及处理。

### 验证

- `backend/.\mvnw.cmd -q -DskipTests package` 通过。
- `frontend/npm.cmd run build` 通过。

### 注意

- 本次不新增数据库表或迁移。
- 删除草稿不会影响任务领取状态；用户仍可从我的任务或答题入口重新生成草稿。



## 增量实现:Owner 端「人工审核」页面落地

### 对应阶段计划

对应「增量计划:Owner 端「人工审核」页面落地」。仅前端实现,接口预留,与既有 owner 端页面风格统一,后端未就绪时回落到样例 JSON 并显示演示模式 Tag。

### 完成内容

- 新增 `frontend/src/types/ownerReview.ts`,字段命名严格对齐计划书 4.5 / 4.6 / 5.1:
  - `ReviewStage` (initial / second / final / sampling)。
  - `OwnerReviewOverview` 总览 KPI(含 `pendingAnnotations / todayApproved / todayReturned / todayDisputes / samplingCoverage / consistencyRate / returnRate / reviewerWorkloads`)。
  - `OwnerReviewTaskRow` 任务级进度行 + `ReviewStageProgress` 三阶段进度。
  - `OwnerReviewAnnotation` 条目明细 + `ReviewAuditLogEntry` 审计日志。
  - `OwnerReviewPageResult / OwnerReviewTasksQuery / OwnerReviewAuditQuery` 通用分页与查询参数。
  - `OwnerReviewBatchDecisionRequest / Response` 预留批量裁决 payload。
- 新增 `frontend/src/api/ownerReview.ts`,封装 5 个 `/reviews/*` 预留接口:
  - `GET /reviews/overview`
  - `GET /reviews/tasks`
  - `GET /reviews/tasks/{taskId}/annotations`
  - `GET /reviews/audit-log`
  - `POST /reviews/batch-decision`
- 新增 4 份样例数据,放在 `frontend/public/sample-datasets/`,后端不可达时回落:
  - `owner-review-overview.json`(KPI + 3 名审核员工作负载)。
  - `owner-review-tasks.json`(4 个任务级三阶段进度,覆盖文本清洗 / 信息抽取 / QA Quality / Preference Compare)。
  - `owner-review-annotations.json`(5 条条目明细,覆盖审核中 / 已通过 / 已打回 / 已修订 / 争议中 5 种状态)。
  - `owner-review-audit-log.json`(6 条审计日志,覆盖 approve / return / escalate / revise / ai_review.complete)。
- 重写 `frontend/src/pages/owner/OwnerReview.tsx`,不再使用 `OwnerComingSoon`:
  - 顶部 KPI 4 张:待审条目数(主色) / 进行中争议(品红色) / 抽检覆盖率 / 双审一致率与返工率。
  - 主区左侧(`xl={16}`):工具条(关键词搜索 + 阶段 Segmented + 状态 Select + 刷新)+ 任务进度表(7 列:任务、三阶段进度条、通过/打回/进行中、抽检/争议、审核员、SLA/更新、操作)。
  - 三阶段进度条:三段堆叠条按 reviewed+pending 占比分配宽度,鼠标悬停显示该阶段 reviewed/total / approved / returned 详情;下方一行 legend 显示三阶段 reviewed/total。
  - 主区右侧(`xl={8}`):审核员当日负载卡(列表项:姓名 / 待审 / 今日已审 / 一致率环形进度) + 审计日志卡(antd Timeline,12 条上限,按操作者角色过滤)。
  - 审计日志 timeline 颜色按 action 语义自动着色:`approve / accept` 绿、`return / reject` 红、`escalate / dispute` 紫、`system_agent` 灰、其他蓝。
  - `prettifyAction` 把 `annotation.review.approve` 等机器名转成「审核通过」等中文,Owner 阅读更直接。
  - 任务详情抽屉:三阶段汇总卡(reviewedPct + 已审/待审/打回数) + 条目明细表(7 列:Annotation / Item & 标注员 / 当前阶段 / 状态 / AI 预审 / 最近裁决 / 更新)。
  - 后端不可达时:并行 fetch 三份样例 JSON 占位,顶部加「演示模式 · 后端未就绪」Tag,所有筛选 / 抽屉 / 角色过滤交互保持可演示。
- 在 `frontend/src/styles/global.css` 末尾追加 owner-review 局部样式段:
  - `.owner-review-kpi-row` / `.owner-review-kpi-meta` 顶部 KPI 卡微调。
  - `.owner-review-stage-bar` / `-track` / `-seg` (三段:initial 蓝 / second 紫 / final 绿,is-pending 用斜纹) / `-legend`。
  - `.owner-review-meta-tag.is-deadline / .is-sampling` SLA 与抽检率胶囊。
  - `.owner-review-workload-list / -item / -name / -meta` 审核员负载列表。
  - `.owner-review-audit-card / -row / -action / -meta / -reason` 审计日志时间线行,reason 用左侧黄色边线 + 浅黄底突出。
  - `.owner-review-stage-cards / -card / -title / -value / -meta` 抽屉内三阶段汇总卡。
  - 暗色模式适配:沿用现有 `data-color-mode='dark'` 选择器复写背景与边框透明度,不引入新 token。

### 关键文件

- `frontend/src/types/ownerReview.ts` 新建。
- `frontend/src/api/ownerReview.ts` 新建。
- `frontend/src/pages/owner/OwnerReview.tsx` 整页重写。
- `frontend/src/styles/global.css` 末尾追加 `owner-review-*` 样式段,不改既有规则。
- `frontend/public/sample-datasets/owner-review-{overview,tasks,annotations,audit-log}.json` 新建。

### 接口预留说明

- 路径前缀 `/reviews`(对齐计划书 5.2 该模块原始路径),与 Reviewer 端 `/reviewer/*` 区分语义:
  - Owner 端 `/reviews/*` 是横向跟踪 + 审计,只读为主。
  - Reviewer 端 `/reviewer/*` 是纵向逐条裁决,写为主。
- 后端落地时,Spring Boot 只需新增 `OwnerReviewController + Service + Repository` 一组,响应字段直接复用 `audit_logs / annotations / human_reviews / assignments` 表的现有字段,无需新增表。
- `POST /reviews/batch-decision` 留接口未在 UI 上提供入口,等后端确认权限边界(Owner 是否允许批量替代 Reviewer 决策)后再决定要不要在表格里加批量操作。

### 范围与边界

- 仅前端实现,不动后端代码、数据库迁移、Reviewer 端、AppLayout、路由、认证 / 权限。
- 不引入新依赖,不修改既有 `owner-stat-*` / `owner-toolbar` / `owner-table-card` / `OwnerComingSoon` 等公共组件,其他 owner 占位页继续使用 `OwnerComingSoon`。
- 演示数据用于前端可演示,后端真实数据上线后会替换,调用层无需重构。

### 验证结果

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 打包均通过。
- 修复一处 TS2783 spread 重复键告警(`{ stage, ...s, total }` -> `{ ...s, total }`,`pickStage` 已经把 `stage` 写入返回值)。
- 既有 Vite 大 chunk warning(1.9 MB)和 plugin timing 提示属于全仓库历史告警,与本期改动无关。
- 浏览器手动校验:
  - `/owner/review` 默认进入演示模式(后端未实现 `/reviews/*`),顶部 Tag「演示模式 · 后端未就绪」自动出现。
  - KPI 4 张 / 任务进度表 / 审核员负载 / 审计日志四块同屏布局正常,样式与既有 owner 端页面一致。
  - 关键词搜索 / 阶段 Segmented / 状态 Select / 审计日志角色 Select 四组筛选可叠加生效。
  - 点击任务行右侧眼睛图标弹出 ≤90% 视口宽抽屉,显示三阶段汇总卡 + 条目明细表。

### 未完成与下一步

- 后端 `/reviews/*` 5 个接口尚未实现,后续 Phase 5 后端阶段一并落地。
- 批量裁决 UI 待权限边界确认后再决定要不要加。
- 抽检比例 / 一致率的计算口径需要后端在 `/reviews/overview` 中明确(目前样例 JSON 给的是模拟值)。
- 审计日志 12 条上限是 UI 折中,后端落地后建议在卡片右上提供「查看更多」跳转到独立审计日志页或 Modal,本期未实现,留给数据看板阶段统一处理。

## 2026-05-30 README Agent 启动与模型配置说明修正

### 对应计划

对应“2026-05-30 README Agent 启动与模型配置说明修正”。

### 完成内容

1. README 常用环境变量新增 `LABELHUB_MODEL_CONFIG_SECRET`。
2. 数据库初始化说明补充 V5-V12 迁移命令,覆盖 Agent Job、runToken 和 Web 端模型配置表。
3. 后端启动命令补充 `LABELHUB_MODEL_CONFIG_SECRET`,并说明该密钥用于加密/解密 Web 端保存的模型 API Key。
4. Agent 启动说明改为两种方式：
   - Web 端已有 active 模型配置时,Agent 只配置后端连接与 system_agent 登录变量。
   - 没有 Web 端 active 模型配置时,再配置 `LABELHUB_LLM_*` 作为兜底。
5. 常见保存失败场景补充说明：提示 `LABELHUB_MODEL_CONFIG_SECRET is required to store or read api keys` 时,需要设置该变量并重启后端。

### 验证

- 本次仅修改 README 文档,未运行构建。
- 已通过 `rg` 检查 README 中 `LABELHUB_MODEL_CONFIG_SECRET`、V12 迁移和 Agent 模型配置说明均已出现。



## 增量实现:答题页填完自动跳下一题

### 对应阶段计划

对应「增量计划:答题页填完自动跳下一题」。仅前端,接入既有草稿自动保存与校验逻辑,不动后端、不动状态机、不引入新依赖。

### 完成内容

- `frontend/src/pages/labeler/AnswerPage.tsx`:
  - 引入 `Switch / Tooltip`(antd 已有,无新增依赖)。
  - 新增常量:`AUTO_ADVANCE_DELAY_MS = 1500`,`AUTO_ADVANCE_STORAGE_KEY = 'labelhub:answer:auto-advance'`。
  - 新增工具函数 `loadAutoAdvancePref / saveAutoAdvancePref`,与既有 `loadSplitPercent / saveSplitPercent` 同样模式持久化,默认开启。
  - 新增 state:`autoAdvance` (开关偏好) / `autoAdvanceHint` (跳前 hint 文案)。
  - 新增 ref:`advancedAssignmentsRef = useRef<Set<string>>(new Set())`,记录本会话已自动跳过的 assignment,避免回到上一题再次触发。
  - 新增 effect:在 `autoAdvance && canEdit && dirty && nextAssignmentId && !已跳过过 && 校验通过` 时,延迟 1.5 秒触发,先 `saveCurrentDraft(true)` 再 `navigate(/labeler/answer/{next})`;延迟内组件卸载或依赖变化会清除 timer 并重置 hint。
  - 跳题前再调用一次 `collectValidationErrors()`,避免延迟期间用户把字段改回不合法状态仍被跳走。
  - 顶栏草稿状态提示左侧新增「填完自动下一题」`Switch + Tooltip`,胶囊样式;切换时同步写入 `localStorage`。
  - 顶栏草稿状态提示新增分支:`autoAdvanceHint` 命中时优先展示「已自动保存,即将进入下一题」,优先级在「保存中 / 已保存于」之上。
- `frontend/src/styles/global.css`:在 `.answer-draft-tip` 之后追加 `.answer-auto-advance / .answer-auto-advance-label` 两条样式,胶囊浅主色底配合既有顶栏视觉。

### 关键文件

- `frontend/src/pages/labeler/AnswerPage.tsx` 局部增量。
- `frontend/src/styles/global.css` 末尾局部追加。

### 范围与边界

- 不改后端、不改 API 层、不动状态机、不动现有「上一题/下一题/进度点跳转/退出/提交」按钮逻辑。
- 不引入新依赖。
- 不改其他角色端。
- 用户主动操作优先级最高:手动点「下一题」立即切题,不重复跳;退出 Modal 仍正常确认。
- 边界场景:已锁定 / 查看模式 / 末题 / 必填缺失 / 格式错误时不触发自动跳;回到上一题修改时也不会再次自动跳。

### 验证结果

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 打包通过(仍只剩既有 1.9 MB chunk 警告与本期无关)。
- 已通过 `getDiagnostics` 检查,新增代码无 TS 错误。
- 浏览器手动校验:
  - 开启开关时填完单选 / 多选 / 文本必填项后,顶栏出现「已自动保存,即将进入下一题」,1.5 秒后切到下一题。
  - 必填字段留空 / 文本超 maxLength / JSON 格式错误时不触发自动跳。
  - 关闭开关后只保留草稿自动保存,不再切题。
  - 回到上一题再修改不会被再次自动跳。
  - 顶栏「下一题」按钮、「上一题」按钮、进度点跳转、退出 Modal、提交按钮在自动跳延迟期间均可正常工作。

### 未完成与下一步

- 暂未提供切题过渡动画,仅靠顶栏文字提示;若后续用户反馈需要更明显的视觉反馈,可在切题前给右侧表单卡加一段淡出动画。
- 自动跳题延迟时长目前与草稿防抖一致(1500ms),如果后续遇到草稿保存比预期慢的场景,可考虑在 effect 中改为「等草稿落库 Promise 完成再跳」,而不是固定 setTimeout。



## 增量实现:答题页节奏分级(离散字段即时,连续字段短防抖)

### 对应阶段计划

对应「增量计划:答题页节奏分级(离散字段即时,连续字段短防抖)」。仅前端,接入既有 `resolveSemanticType`,不改 Renderer / API 层 / 后端。

### 完成内容

- `frontend/src/pages/labeler/AnswerPage.tsx`:
  - 把原本统一的 `DRAFT_DEBOUNCE_MS = 1500` 删除,自动跳题延迟 `AUTO_ADVANCE_DELAY_MS = 1500` 删除。
  - 新增四档常量:
    - `DRAFT_TYPING_DEBOUNCE_MS = 400`:连续字段草稿节流;
    - 离散字段草稿走 `0ms` 立即保存,不再防抖;
    - `AUTO_ADVANCE_FAST_MS = 200`:离散字段答完跳题延迟;
    - `AUTO_ADVANCE_TYPING_MS = 800`:连续字段答完跳题缓冲。
  - 新增 `lastChangeIsDiscreteRef = useRef<boolean>(true)`:
    - 在 `LabelHubFormRenderer.onChange` 里调用 `isDiscreteFieldChange(renderFields, answer, nextAnswer)` 后赋值;
    - 草稿 effect 与自动跳题 effect 在选 `setTimeout` 延迟前读取该 ref。
  - 新增 helper `isDiscreteFieldChange(fields, prev, next)`:
    - 用浅 diff 找出 prev 与 next 间所有变更 key;
    - 对每个 key 通过 `resolveSemanticType` 取语义类型,任一命中 `single_choice / multi_choice / tags / file` 集合即返回 true;
    - 找不到变更字段或全是连续字段时返回 false,效果等价于按节流路径走。
  - 新增 `DISCRETE_SEMANTIC_TYPES` 常量集中维护离散语义类型枚举,后续如果引入新材料(下拉 select / Boolean 开关 / 评分等)只需追加该集合,不必改两处 effect。
  - 自动跳题开关 Tooltip 文案改为「选择类字段(单选/多选/标签)点完即保存并跳下一题;输入类字段(文本/JSON)填完后约 0.8 秒自动跳。」与新延迟描述对齐。
  - 顶栏「已自动保存,即将进入下一题」hint 与之前一致,只是出现时间随类型变化(选择题瞬现瞬消)。

### 关键文件

- `frontend/src/pages/labeler/AnswerPage.tsx` 局部增量,无新文件。
- `frontend/src/styles/global.css` 不变。

### 范围与边界

- 不动 `LabelHubFormRenderer`、不动 schema 编译器与校验器、不动 API 层、不动后端、不动状态机。
- 不引入新依赖。
- 离散字段集合目前枚举为 `single_choice / multi_choice / tags / file`;`text / json / display / layout` 走连续路径。
- 用户主动点击「上一题/下一题/进度点跳转/退出/提交」按钮逻辑保持不变。

### 验证结果

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 打包通过。
- 已通过 `getDiagnostics`,无 TS 错误。
- 浏览器手动校验(开启开关):
  - 单选/多选/tags 字段:点完顶栏短暂闪「草稿保存中」→「即将进入下一题」→ ~200ms 切题。
  - 文本/JSON 字段:停止输入 ~400ms 看到草稿落库提示,~800ms 切题。
  - 必填缺失或 JSON 不合法时不切题。
  - 同题既有单选又有文本时,改单选立即跳;只改文本走节流路径。
  - 关闭开关后只保留草稿自动保存,完全不切题。

### 未完成与下一步

- 当前 hint 在选择题路径下出现时间极短(可能不到 100ms 就被切题打断),后续如果用户反馈"看不到提示",可考虑给 hint 加最小展示时长 150ms。
- 如果后续引入下拉 `select / boolean switch / 评分` 等新材料,需要把对应 semantic 加入 `DISCRETE_SEMANTIC_TYPES`。
- 草稿自动保存当前是 `setTimeout + 接口请求`,极端情况下"立即保存"的请求可能与"切题前保存"的请求并发到达后端,后端 upsert 逻辑已经能容忍乱序,本期不再加客户端串行化。



## 增量实现:答题页含文字输入字段时禁用自动跳题

### 对应阶段计划

对应「增量计划:答题页含文字输入字段时禁用自动跳题」。仅前端,基于既有 `resolveSemanticType` / `MaterialKind` 派生,不改 Renderer / API / 后端。

### 完成内容

- `frontend/src/pages/labeler/AnswerPage.tsx`:
  - 新增模块级常量与 helper:
    - `TEXT_INPUT_KINDS = new Set(['text-single', 'text-multi', 'rich-text', 'json-editor'])`,集中维护文字输入物料枚举。
    - `isTextInputLikeField(field)`:优先读 `semanticType in ('text','json')`,否则按 `kind in TEXT_INPUT_KINDS` 兜底,与既有"优先读 semanticType"约定一致。
  - 在组件内派生:
    - `hasTextInputField = useMemo(() => renderFields.some(isTextInputLikeField), [renderFields])`。
    - `effectiveAutoAdvance = autoAdvance && !hasTextInputField`,作为本题"自动跳题是否真正生效"的派生开关。
  - 自动跳题 effect 守卫从 `if (!autoAdvance) return` 改为 `if (!effectiveAutoAdvance) return`;依赖数组中 `autoAdvance` 同步替换为 `effectiveAutoAdvance`。
  - 顶栏 Switch UI:
    - `disabled={hasTextInputField}` + `checked={effectiveAutoAdvance}`(注意是派生值而非原始偏好,避免显示"开着但实际不生效"的歧义)。
    - 外层 `<span>` 类名根据禁用态切换 `is-disabled`,Tooltip 文案区分两种情况:
      - 禁用时:「本题含文字输入字段,标注员可能需要思考,自动跳下一题已禁用,请手动点击「下一题」。」
      - 启用时:沿用上一轮文案。
    - 用户偏好 `autoAdvance` 本身不被覆盖,切到纯选择题时仍按偏好生效。
- `frontend/src/styles/global.css`:`.answer-auto-advance` 之后追加 `.answer-auto-advance.is-disabled`(背景灰化)与 `.answer-auto-advance.is-disabled .answer-auto-advance-label`(文字灰化),与 antd Switch 禁用态视觉对齐。

### 关键文件

- `frontend/src/pages/labeler/AnswerPage.tsx` 局部增量。
- `frontend/src/styles/global.css` 末尾段微调。

### 范围与边界

- 不改 Renderer / 后端 / API / schema 编译器 / schema 校验器。
- 不引入新依赖。
- 不动现有「上一题/下一题/进度点跳转/退出/提交」按钮逻辑。
- `show-item / file-upload / 媒体` 类字段不算文字输入,不影响开关。
- `MaterialKind` 后续新增 Markdown / 代码编辑器等文字输入物料时,需追加到 `TEXT_INPUT_KINDS`;若后端统一收敛到 `semanticType in ('text','json')` 则前端无需扩展。

### 验证结果

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 打包通过(只剩 1.9 MB chunk warning 与本期无关)。
- `getDiagnostics` 无 TS 错误。
- 浏览器手动校验:
  - 纯选择题(只有「通过/不通过」单选):开关可切换,点选 ~200ms 自动跳下一题,行为与上一轮一致。
  - 含「问题说明」多行文本的题:开关被强制关闭并灰化禁用,即使用户上一题手动开过本题也不会自动跳;手动点「下一题」仍正常工作。
  - 切回纯选择题:开关恢复用户偏好状态,自动跳重新生效。
  - 文字输入字段的草稿自动保存仍按 400ms 节流落库,与禁用自动跳互不影响。

### 未完成与下一步

- 当前判定基于"题目是否含文字输入字段",而非"用户当前正在编辑哪种字段"。后续如果遇到"题目里既有选择又有大量文本,但用户只改了选择"的复杂场景,可以考虑改成"按本次会话内是否触碰过文字输入字段"做更细的开关控制。本期不做,因为复杂题需要思考整体已经手动控制更安全。



## 增量实现:移除答题页「填完自动下一题」功能

### 对应阶段计划

对应「增量计划:移除答题页「填完自动下一题」功能」。仅前端,只删不加,完全保留草稿自动保存(按字段类型分级)。

### 完成内容

- `frontend/src/pages/labeler/AnswerPage.tsx`:
  - 删除常量:`AUTO_ADVANCE_FAST_MS / AUTO_ADVANCE_TYPING_MS / AUTO_ADVANCE_STORAGE_KEY`。
  - 删除函数:`loadAutoAdvancePref / saveAutoAdvancePref`。
  - 删除 state:`autoAdvance` / `autoAdvanceHint`。
  - 删除 ref:`advancedAssignmentsRef` / `hasEditedRef`。
  - 删除派生:`hasTextInputField` / `effectiveAutoAdvance`。
  - 删除 useEffect:「答完自动跳下一题」整段。
  - 删除 UI:顶栏 Switch + Tooltip,以及 `autoAdvanceHint` 分支;`applyItem` 中 `hasEditedRef.current = false` 重置语句;`onChange` 中 `hasEditedRef.current = true` 标记语句。
  - 删除 helper:`isTextInputLikeField` 与常量 `TEXT_INPUT_KINDS`。
  - 移除 antd import 中已不再使用的 `Switch / Tooltip`。
  - 保留:`DRAFT_TYPING_DEBOUNCE_MS`(连续字段节流 400ms)、`lastChangeIsDiscreteRef`(本次变更是否离散)、`isDiscreteFieldChange / DISCRETE_SEMANTIC_TYPES`,均只服务于草稿保存的离散/连续分级,跳题相关被剔除后这些代码继续保持业务正确性。
  - 保留:草稿自动保存 useEffect 不变,「离散立即(0ms) / 连续节流(400ms)」分级仍然生效。
- `frontend/src/styles/global.css`:删除 `.answer-auto-advance / .answer-auto-advance.is-disabled / .answer-auto-advance-label` 三条样式。CSS 总体积回到 138.77 kB(与新增前一致)。

### 关键文件

- `frontend/src/pages/labeler/AnswerPage.tsx` 局部回退,无新文件。
- `frontend/src/styles/global.css` 局部回退,无新文件。

### 范围与边界

- 不动后端、不动 API、不动状态机、不动既有「上一题/下一题/进度点跳转/退出/提交」按钮逻辑、不引入新依赖。
- localStorage 中旧偏好键 `labelhub:answer:auto-advance` 残留值不再读取,无副作用,不主动清理。
- 不删除「问题点归档」中关于"dirty 守卫导致自动跳题被静默取消"的记录,作为同类场景设计参考保留。

### 验证结果

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 打包通过(只剩既有 1.9 MB chunk warning 与本期无关)。
- `getDiagnostics` 无 TS 错误。
- 已用 grep 全文扫描确认 `autoAdvance / hasTextInputField / advancedAssignments / hasEditedRef / isTextInputLikeField / TEXT_INPUT_KINDS / AUTO_ADVANCE / answer-auto-advance` 等所有相关标识符在 AnswerPage.tsx 与 global.css 中都已无残留。
- 浏览器手动校验:
  - 顶栏不再出现「填完自动下一题」开关与跳题 hint。
  - 单选 / 多选 / tags / file 字段:点完顶栏短暂闪「草稿保存中」→「已保存于 hh:mm:ss」,无后续切题。
  - 文本 / JSON 字段:停止输入约 400ms 看到草稿保存,行为与上一轮一致。
  - 用户手动点击「下一题」「上一题」「进度点跳转」「退出」「提交」按钮均正常,前置草稿保存仍生效。

### 未完成与下一步

- 此项功能正式下线,后续如需要再做"批量裁决式自动推进"(更适用于 Reviewer 端逐条确认),需要重新评估场景与交互边界,不在 Labeler 答题页复用本期方案。



## 2026-05-30 Labeler 打回项真实数据接入

### 对应阶段计划

对应「2026-05-30 Labeler 打回项真实数据接入」。本轮按计划书 4.5 的“Reviewer 查看 AI 结果并通过或打回;Labeler 查看打回原因、修改并重提”闭环实现,同时把 AI `REJECT` 明确为待人工复审的预审建议。

### 完成内容

- 后端新增 `GET /api/labeler/returned-items`:
  - 文件:`LabelerReturnedItemsController / Service / Repository / LabelerReturnedItemResponse`。
  - 权限:必须登录且具备 `labeler` 角色,否则返回现有 `ApiException(FORBIDDEN)` 风格。
  - 查询参数:`source=all|human_return|ai_pre_reject`,默认 `all`;`page` 默认 1;`pageSize` 默认 20,最大 100。
  - 人工复审打回口径:当前 Labeler 的 latest annotation,assignment 已 `returned`,latest human review 的 decision 为 `return/returned/reject/rejected`。
  - AI 预打回口径:latest annotation 最新 AI result 为 `REJECT`,annotation 仍 `reviewing`,没有人工 review,assignment 未 `returned/accepted/voided`。
  - 同一条 latest annotation 已人工打回时只显示人工复审打回,不重复显示 AI 预打回。
- 前端接入:
  - `frontend/src/types/labeler.ts` 新增 `ReturnedItemSource`、`ReturnedItemSourceCode`、`LabelerReturnedItem`。
  - `frontend/src/api/labeler.ts` 新增 `labelerApi.listReturnedItems(query)`。
  - `frontend/src/pages/labeler/ReturnedItems.tsx` 移除硬编码样例,改为真实接口加载。
  - 页面增加“全部 / 人工复审打回 / AI预打回”筛选、加载骨架、错误 Alert + 重试、真实 Empty 空态。
  - 人工复审打回显示 Reviewer、轮次和正式原因,按钮“修改并重提”跳转 `/labeler/answer/{assignmentId}`。
  - AI 预打回显示 AI 结论、评分、评语、风险标记和证据,按钮“等待人工复审”禁用。
- 文档:
  - `接口文档.md` 增加前端方法和后端接口说明。
  - `问题点归档.md` 记录原静态样例和 AI/人工语义混淆问题。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过;仅保留既有 chunk 大小 warning。

### 范围与边界

- 不新增数据库表或迁移。
- 不修改 Reviewer 裁决接口,只消费现有 `ai_review_results` 与 `human_reviews`。
- 不保留任何静态业务样例作为降级数据;接口失败只展示错误态和重试入口。



## 增量实现:答题页入口定位 + 翻页按钮位置

### 对应阶段计划

对应「增量计划:答题页入口定位 + 翻页按钮位置」。仅前端,不动后端、API、状态机、不引入新依赖。

### 完成内容

- `frontend/src/pages/labeler/AnswerPage.tsx`:
  - 新增 `entryRedirectedTaskRef = useRef<string | null>(null)`,记忆已自动定位过的任务 ID。
  - 新增「自动定位到第一道未完成题」useEffect,触发条件:
    - `item` 已加载;
    - `item.position.prevAssignmentId` 不存在(即用户进入的是该任务首题);
    - `item.taskId` 与 ref 不同(同任务只跳一次);
    - `position.statuses / assignmentIds` 长度匹配且当前题状态为 `completed`;
    满足时在 `statuses` 顺序里找最早的 `empty`,退而求其次找 `incomplete`,用 `navigate(/labeler/answer/{id}, { replace: true })` 静默跳过去,history 不留痕。其余分支(中途题、当前题未完成、全部完成、缺字段)写入 ref 后放行。
  - 顶栏右侧 Space 移除「上一题」「下一题」按钮,只保留:草稿状态提示 + (末题/已提交时显示的)「提交 / 重新修改」按钮。
  - 右侧填写卡 `Card` 内,`Space` 中在 `LabelHubFormRenderer + rawAnswerFallback` 之后追加 `.answer-pager` 导航条:`<上一题>` 左 + `<下一题>` 右,下一题用 `type="primary" ghost` 主色描边款,与提交按钮(实色)做视觉区分,避免误点。
  - `handlePrev / handleSkip` 函数本身不动,只是绑定到新位置的按钮。
- `frontend/src/styles/global.css`:
  - `.answer-draft-tip` 之后新增 `.answer-pager` 样式段:`display: flex; justify-content: space-between; gap: 12px; padding-top: 16px; border-top: 1px solid #f0f2f7;`。
  - 媒体查询 `@media (max-width: 720px)` 折行成纵向,两按钮各占满行。
- `frontend/src/pages/labeler/MyTasks.tsx` 未改动,既有 `resolveEntryAssignment` 优先 `returned > claimed > 第 0 个` 的逻辑保留,与本期 AnswerPage 内重定位形成两层保护:外层挑入口、内层兜底。

### 关键文件

- `frontend/src/pages/labeler/AnswerPage.tsx` 局部增量。
- `frontend/src/styles/global.css` 局部新增。

### 范围与边界

- 不改后端、不改 API、不改 `position` 字段结构、不动状态机。
- 不引入新依赖,不动 Renderer。
- 自动定位仅在"该任务首题入口"触发,中途题不强行弹走,保留用户主动浏览意图。
- 同任务只跳一次,用 ref 记忆;切到另一个任务时根据新 `taskId` 重置判定。

### 验证结果

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 打包通过。
- `getDiagnostics` 无 TS 错误。
- 浏览器手动校验:
  - 进入已经做过 1~5 题的任务首题入口:页面静默跳到第 6 题(首个 empty)。
  - 任务全部已完成:停留在第 1 题,可继续浏览/修改。
  - 进度点跳到已完成题:不再被弹回。
  - 上一题/下一题按钮在右侧填写卡底部呈左右两端分布,提交按钮仍在顶栏右侧。
  - 窄屏拖到 < 720px:导航条按钮折行成纵向,各占满行。
  - 草稿自动保存继续按字段类型分级生效。

### 未完成与下一步

- 自动定位用 `replace` 模式不留 history,后退按钮直接回到 MyTasks 入口,这是有意为之。如果后续有人反馈"误点查看已完成题后想直接后退到第 1 题",可以把 `replace` 改为 `false`,但会污染 history。当前以"少干扰"为优先。
- 进度条上的"完成态着色"还是依赖 `position.statuses`,后端打回 / 修订后状态变化要保证及时刷新,否则定位可能定位到一个已经被打回但状态没更新的题。本期不动后端字段。



## 增量实现:答题页翻页按钮间距 + 报告问题入口

### 对应阶段计划

对应「增量计划:答题页翻页按钮间距 + 报告问题入口」。仅前端,接口预留,后端未就绪时降级演示模式。

### 完成内容

- `frontend/src/types/labeler.ts`:
  - 新增 `ReportIssueCategory` 联合类型,枚举 6 个分类。
  - 新增 `ReportIssueRequest { category, description }` 与 `ReportIssueResponse { reportId, receivedAt }`。
- `frontend/src/api/labeler.ts`:
  - 在 import type 中追加 `ReportIssueRequest / ReportIssueResponse`。
  - 在 `submitTaskAssignments` 之后追加 `reportIssue(assignmentId, payload)`,路径 `/assignments/{id}/issues`,与既有 `/assignments/{id}/draft` 风格一致。
- `frontend/src/pages/labeler/AnswerPage.tsx`:
  - antd 导入新增 `Form / Select`,`@ant-design/icons` 新增 `FlagOutlined`。
  - 类型导入新增 `ReportIssueCategory / ReportIssueRequest`。
  - 组件状态新增 `reportOpen / reportSubmitting / reportForm` 与两个 helper:
    - `openReportDialog`:重置表单默认值并打开 Modal。
    - `submitReport`:`reportForm.validateFields` → `labelerApi.reportIssue` → 成功提示 / 演示模式回退,完整 try/catch。
  - 顶栏「已保存于…」右侧、提交按钮左侧插入 `<Button icon={FlagOutlined} className="answer-report-btn">报告问题</Button>`,仅 `canEdit` 时渲染,避免对锁定题做无效报告。
  - 页面底部、最外层 `</Space>` 之前插入 `<Modal>`:
    - 标题区显示当前 `assignmentId / itemId`(`Typography.Text code`)。
    - `Form`:分类 `Select`(6 项含中文说明)+ 描述 `TextArea`(5~500 字符校验, showCount)。
    - `okText="提交报告"`,`confirmLoading={reportSubmitting}`,提交期间 `maskClosable=false` 防止误关。
- `frontend/src/styles/global.css`:
  - `.answer-pager` 调整为 `margin-top: 24px; padding-top: 20px;`,与最后一个字段拉开总计约 44px 呼吸距离。
  - 在 `.answer-draft-tip` 之后新增 `.answer-report-btn` 样式:默认 `#e2e6ec` 灰边 + `#64748b` 字色,hover 变 `#f59e0b` 橙色边 + `#d97706` 字色,与主流程蓝色按钮错开。

### 关键文件

- `frontend/src/types/labeler.ts` 末尾追加。
- `frontend/src/api/labeler.ts` 局部增量。
- `frontend/src/pages/labeler/AnswerPage.tsx` 局部增量。
- `frontend/src/styles/global.css` 局部新增。

### 范围与边界

- 不动后端、不动 audit_logs 表结构、不动其他角色端、不引入新依赖。
- 报告问题不影响 annotation 状态机,与 draft / submit / decision 互不干扰。
- 后端不可达时不阻断答题流程,演示模式弹本地成功提示;真实接口上线后自然走真实路径。
- 已锁定 / 查看模式下隐藏「报告问题」按钮,避免对锁定题做无效报告。

### 验证结果

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 打包通过。
- `getDiagnostics` 无 TS 错误。
- 浏览器手动校验:
  - 进入可编辑题:顶栏「已保存于…」右侧出现灰色「报告问题」按钮,hover 变橙;点击弹出 Modal。
  - Modal 校验:不选分类无法提交;描述 < 5 字符提示「至少 5 个字符」;5~500 字符正常提交。
  - 后端未实现:接口失败 → 弹「问题已记录(演示模式)」,Modal 关闭。
  - 已锁定题:报告问题按钮隐藏。
  - 翻页按钮上方留出 24+20=44px 间距,不再贴住最后一个字段。

### 未完成与下一步

- 后端 `POST /assignments/{id}/issues` 待落地,落地时建议:
  1. 新增 `issues` 表(或复用 audit_logs `entity_type='issue'`),字段:`assignment_id / task_id / labeler_id / category / description / status / created_at`;
  2. 同步写一条 `audit_logs.entity_type='issue'`,便于 Owner 端聚合查看;
  3. Owner 端审核 / 看板补一个 issue 列表入口。
- 多语言支持:本期分类标签是中文硬编码,后续如果支持英文界面需要走 i18n。



## 增量实现:答题页翻页按钮固定在填写卡底部

### 对应阶段计划

对应「增量计划:答题页翻页按钮固定在填写卡底部」。本轮只修复布局定位,不改后端、不改答题状态机、不改保存 / 提交 / 跳转逻辑。

### 完成内容

- `frontend/src/pages/labeler/AnswerPage.tsx`:
  - 右侧填写卡新增 `answer-form-card` class,把布局约束限定在填写卡,不影响左侧原题卡。
  - 新增 `.answer-form-content` 容器包住 `LabelHubFormRenderer` 和 raw answer fallback。
  - `.answer-pager` 从表单 `Space` 中移出,作为填写卡 body 的底部兄弟节点。
- `frontend/src/styles/global.css`:
  - `.answer-form-card` 设置最小高度,保证短题也有稳定底部区域。
  - `.answer-form-card.ant-card` 本体改为纵向 flex,避免 Ant Card body 只按内容高度撑开。
  - `.answer-form-card.ant-card > .ant-card-body` 设置 `flex: 1 1 auto` 并改为纵向 flex,让 body 吃满卡片剩余高度。
  - `.answer-pager` 改为 `margin: auto -22px 0; padding: 20px 22px 0;`,用 `margin-top:auto` 吸附到底部,不再跟随短表单字段上浮。
  - 保留窄屏下按钮纵向铺满。

### 验证结果

- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过;仅保留既有 chunk 大小 warning。

### 范围与边界

- 顶栏仍不展示「上一题/下一题」。
- 只修复按钮固定底部问题,不调整报告问题入口和分页行为。



## 增量实现:导出中心页面升级

### 对应阶段计划

对应「增量计划:导出中心页面升级」。仅前端,后端已有接口不动,新增预留接口 2 个。

### 完成内容

- `frontend/src/types/export.ts`:
  - `ExportJob` 扩展字段:`taskTitle / exportedCount / fileSizeBytes / downloadUrl / mappingJson / createdBy`,均为可选,后端 succeeded 后填充。
  - 新增 `ExportOverview` 类型:`totalJobs / succeededJobs / failedJobs / monthlyExportedItems / monthlyFileSizeBytes`。
- `frontend/src/api/export.ts`:
  - 新增 `getOverview()`:GET `/exports/overview`,后端待实现。
  - 新增 `getDownloadUrl(exportId)`:拼接 `/exports/{id}/download` URL,前端用 `window.open` 触发浏览器下载。
  - 补充完整 JSDoc 注释,列出 7 个接口路径。
- `frontend/src/pages/owner/OwnerExport.tsx` 整页重写:
  - 顶部 KPI 4 张:导出总数(主色) / 成功率 / 本月导出条目 / 本月文件大小;后端 `/exports/overview` 未实现前用 `useMemo` 从列表本地计算回落。
  - 工具条:任务 Select(showSearch) + 格式 Select + 创建导出按钮 + 竖线分隔 + 关键词 Input + 状态 Segmented(全部/待执行/导出中/已完成/失败) + 刷新按钮。
  - 表格 8 列:导出 ID(code) / 任务(标题+ID) / 格式(蓝色胶囊) / 状态(含图标) / 进度(antd Progress) / 条目数&文件大小 / 时间(创建+更新) / 操作(详情+流转+下载)。
  - 操作列:查看详情(EyeOutlined) + 开始(PlayCircleOutlined) + 完成(CheckCircleOutlined) + 失败(CloseCircleOutlined + Popconfirm) + 下载(DownloadOutlined)。
  - 详情抽屉(Drawer 520px):Descriptions 展示全字段 + 错误详情卡(红底) + 字段映射 JSON 预览 + succeeded 时底部下载按钮。
  - 演示模式:后端不可达时 `usingFallback=true`,顶部 Tag 提示,列表为空但不报错。
  - 本地筛选:`keyword` 搜索任务名/导出 ID + `statusFilter` 状态过滤,`useMemo` 派生 `visibleJobs`。
  - `formatBytes` 工具函数:B / KB / MB 三档格式化。
- 不新增 CSS 样式段,完全复用既有 `owner-stat-card / owner-toolbar / owner-table-card / page-stack / page-title-row` 通用样式。

### 关键文件

- `frontend/src/types/export.ts` 扩展。
- `frontend/src/api/export.ts` 扩展。
- `frontend/src/pages/owner/OwnerExport.tsx` 整页重写。

### 范围与边界

- 不动后端(已有 5 个接口不改),不动路由,不动其他页面,不引入新依赖。
- `GET /exports/overview` 后端待实现,前端先本地计算;后端落地后直接替换 `useMemo` 为 API 调用。
- `GET /exports/{id}/download` 后端待实现,前端用 `window.open(url)` 触发;后端落地后返回文件流或 302 重定向。
- 字段映射(`mappingJson`)当前创建导出时未在 UI 上提供配置入口(只传空),后续如需要可在工具条加「高级选项」折叠面板。

### 验证结果

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 打包通过。
- `getDiagnostics` 无 TS 错误。
- 浏览器手动校验:
  - 后端可达时:创建导出 → pending 行出现 → 点开始 → running + 进度条动画 → 点完成 → succeeded + 100% 绿色进度条。
  - 关键词搜索 + 状态 Segmented 可叠加筛选。
  - 点眼睛图标弹出详情抽屉,Descriptions 展示全字段;succeeded 底部有下载按钮。
  - 后端不可达时:顶部「演示模式 · 后端未就绪」Tag 出现,列表为空。

### 未完成与下一步

- 后端 `GET /exports/overview` 待实现:建议在 `ExportController` 加一个聚合查询,按当前 Owner 的 export_jobs 表统计。
- 后端 `GET /exports/{id}/download` 待实现:建议返回 `Content-Disposition: attachment` + 文件流,或 302 到 OSS 签名 URL。
- 字段映射 UI 入口:后续如需要,在创建导出工具条加「高级选项」折叠面板,让 Owner 配置输出字段名映射。
- 导出完成后 annotation 状态迁移到 `exported`:后端 `POST /exports/{id}/complete` 已实现该逻辑,前端无需额外处理。



## 增量实现:AI 审核后台「预审队列」三栏工作台

### 对应阶段计划

对应「增量计划:AI 审核后台「预审队列」三栏工作台」。仅前端,复用现有后端接口,不引入新依赖。

### 完成内容

- 新增 `frontend/src/pages/aiReviewer/AiPreReviewQueue.tsx`:
  - 顶部:标题 + 服务状态 Tag(绿色「服务在线」) + 演示模式 Tag。
  - KPI 4 张:作业总数(主色) / 成功 / 需人工处理 / 失败,从 `GET /ai-review/jobs` 列表本地计算。
  - 筛选条:状态 Segmented(全部/排队中/执行中/成功/失败) + 关键词 Input + 刷新按钮。
  - 三栏主体(`ai-queue-layout` flex):
    - 左栏(`ai-queue-left` 320px):Job 列表,每项(`ai-queue-item`)显示任务名 + 状态 Tag + annotationId + 决策 Badge(颜色按 PASS/NEED_HUMAN/REJECT) + 分数 + 时间 + 错误摘要(红色左边线);点击选中高亮(`is-active`)。
    - 中栏 + 右栏(`ai-queue-detail` flex:1):
      - 顶部 header:annotationId + 任务名 + 提交时间 + 规则名 + AI 建议 Tag(含图标和分数)。
      - 中栏(Col lg=12):提交内容 JSON 预览(`ai-queue-json` 代码块) + 维度评分进度条(颜色按分数分档:≥80 绿 / ≥60 橙 / <60 红) + 综合分(独立一行,顶部分隔线)。
      - 右栏(Col lg=12):AI 评语卡(决策标签 + comment + risk_flags 标签) + Prompt 模板卡(黄底代码块,右上角规则名 Tag) + 处理日志 Timeline(queue → llm → verdict/error,颜色按结论着色)。
  - 未选中时右侧显示 Empty 提示。
  - 选中 Job 后调用 `GET /ai-review/results/{annotationId}` 获取详细评分;失败时用 Job 自身字段构造最小 result 回落。
  - 后端不可达时回落到 `ai-review-jobs.json` 样例,顶部「演示模式」Tag。
- `frontend/src/router/index.tsx`:新增 `<Route path="/ai-reviewer/queue" element={<AiPreReviewQueue />} />`。
- `frontend/src/layouts/AppLayout.tsx`:
  - `aiReviewerMenuItems` 新增 `{ key: '/ai-reviewer/queue', icon: <ProjectOutlined />, label: '预审队列' }`。
  - `resolveSelectedKey` 的 `aiReviewerKeys` 数组新增 `/ai-reviewer/queue`。
- `frontend/src/styles/global.css` 末尾追加 `.ai-queue-*` 样式段:
  - `.ai-queue-layout`:flex gap:16px min-height:520px。
  - `.ai-queue-left`:320px 白底圆角卡,内含 header + 可滚动列表。
  - `.ai-queue-item`:圆角 10px,hover 浅主色底,active 主色边框 + 阴影。
  - `.ai-queue-detail`:flex:1 白底圆角卡,内含 header + Row 两列。
  - `.ai-queue-card`:内部子卡片,无阴影,浅边框。
  - `.ai-queue-json`:monospace 代码块,浅灰底,max-height 200px 可滚动。
  - `.ai-queue-prompt`:monospace 代码块,黄底,max-height 240px 可滚动。
  - `.ai-queue-score-row`:flex 对齐评分行。
  - `.ai-queue-verdict`:AI 建议标签容器。
  - 响应式:≤991px 三栏改为上下堆叠,左栏 max-height 300px。
  - 暗色模式:背景/边框透明度适配。

### 关键文件

- `frontend/src/pages/aiReviewer/AiPreReviewQueue.tsx` 新建。
- `frontend/src/router/index.tsx` 局部增量。
- `frontend/src/layouts/AppLayout.tsx` 局部增量。
- `frontend/src/styles/global.css` 末尾追加。

### 范围与边界

- 不新增后端接口,完全复用 `GET /ai-review/jobs` 与 `GET /ai-review/results/{annotationId}`。
- 不引入新依赖,不动其他页面。
- Prompt 模板当前为前端硬编码示例文案;后端落地后可从 `AiReviewRule.promptTemplate` 动态获取(需要在 Job 响应中补充 `promptSnapshot` 字段)。
- 处理日志 Timeline 从 Job 字段(`createdAt / startedAt / finishedAt / errorSummary / retryCount`)构造最小版本;后端如果补充 `GET /ai-review/jobs/{id}/timeline` 可直接替换。

### 验证结果

- 已执行 `frontend/npm run build`,TypeScript 编译和 Vite 打包通过。
- `getDiagnostics` 无 TS 错误。
- 浏览器手动校验:
  - 进入 `/ai-reviewer/queue`:KPI 4 张 + 筛选条 + 三栏布局完整。
  - 左栏列表按状态/关键词筛选生效,点击某条后右侧展示详情。
  - 中栏:维度评分进度条颜色按分数分档;综合分独立一行。
  - 右栏:AI 评语 + Prompt 模板(黄底) + Timeline 审计日志。
  - 窄屏(< 992px)三栏改为上下堆叠。
  - 后端不可达时回落到样例数据,顶部「演示模式」Tag。

### 未完成与下一步

- Prompt 模板动态化:后端在 Job 响应中补充 `promptSnapshot` 字段后,前端替换硬编码文案。
- 处理日志详细化:后端补充 `GET /ai-review/jobs/{id}/timeline` 后,前端替换 `buildTimeline` 构造逻辑。
- 左栏列表虚拟滚动:当前 200 条上限足够,如果后续 Job 量级到千级需要引入虚拟列表。
- 操作按钮(重试/取消):当前三栏工作台是只读查看,操作仍在 Job 队列页;后续可在详情面板顶部加操作按钮。



## 2026-05-30 Owner 数据看板「题目反馈」真实闭环

### 对应阶段计划

对应「2026-05-30 Owner 数据看板『题目反馈』真实闭环」。本轮新增 `issues` 表,实现 Labeler 上报与 Owner 看板只读查看,不做处理/忽略动作。

### 后端实现

- 新增 Flyway 迁移 `backend/src/main/resources/db/migration/V13__assignment_issues.sql`:
  - 创建 `issues` 表:`assignment_id / task_id / item_id / labeler_id / category / description / status / created_at / updated_at`。
  - 建立 `(task_id, created_at)`、`(labeler_id, created_at)`、`(status, created_at)` 索引。
  - 外键关联 `assignments / tasks / items / users`。
- `AnnotationController` 新增 `POST /api/assignments/{assignmentId}/issues`。
- `AnnotationService.reportIssue`:
  - 校验当前用户是 Labeler。
  - 校验 assignment 属于当前 Labeler、非 `voided`、任务未软删除。
  - 校验 category 为 `data_error / schema_mismatch / media_broken / duplicate / sensitive / other`。
  - 校验 description 去空白后长度 5~500。
  - 写入 `issues.status = open`。
  - 同步写 `audit_logs`,action 为 `assignment.issue_reported`,reason 为 description,snapshot 包含 issue/category/task/item 信息。
- 新增 `DashboardIssueFeedbackController / Service / Repository / IssueFeedbackResponse`:
  - `GET /api/dashboard/issue-feedback`。
  - 仅 Owner 可访问。
  - 只查当前 Owner 名下且未软删除任务的反馈。
  - 支持 `page / pageSize / status(open|all)`,按 `issues.created_at DESC, issues.id DESC` 分页。

### 前端实现

- `frontend/src/types/labeler.ts`:把 `ReportIssueResponse` 改为真实后端响应字段 `issueId / assignmentId / taskId / itemId / category / description / status / createdAt`。
- `frontend/src/pages/labeler/AnswerPage.tsx`:
  - 「报告问题」提交失败时显示真实错误。
  - 移除“问题已记录(演示模式)”假成功兜底,失败后抽屉不关闭。
- `frontend/src/types/dashboard.ts`:新增 `DashboardPageResult / IssueFeedback / IssueFeedbackStatus`。
- `frontend/src/api/dashboard.ts`:新增 `listIssueFeedback(params)`。
- `frontend/src/pages/owner/OwnerDashboard.tsx`:
  - 顶部按钮和 KPI 行右侧卡片均改为「题目反馈」。
  - 首次进入请求 `GET /api/dashboard/issue-feedback?page=1&pageSize=5&status=open`,展示待查看反馈数量。
  - 点击「查看反馈」打开 Drawer,展示任务、题目、Labeler、分类、描述、提交时间。
  - Drawer 支持 Skeleton/List loading、Alert + 重试、Empty 空态。
  - 不使用静态样例作为题目反馈降级数据。
- `frontend/src/styles/global.css`:补充题目反馈卡和 Drawer 列表样式,并适配暗色模式。

### 接口文档

- `接口文档.md` 第 10 节已从「报告问题预留接口」更新为「题目反馈真实闭环接口」:
  - `POST /api/assignments/{assignmentId}/issues`
  - `GET /api/dashboard/issue-feedback`
  - 权限、参数、响应字段、错误语义和前端失败态均已记录。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过。
- Vite 输出 chunk 体积超过 500 kB 的提示,属于既有打包体积警告,不影响本轮接口与页面功能。

### 后续建议

- 后续如要闭环处理,可新增 Owner 更新状态接口,例如 `PATCH /api/dashboard/issue-feedback/{issueId}` 支持 `resolved / ignored`,并保留状态流转审计。

## 2026-05-31 AI 预审评分体系统一为 100 分制

### 对应阶段计划

对应「2026-05-31 AI 预审评分体系改为 100 分制」。本轮把 AI 预审规则、Agent 输出约束、前端展示和样例数据从旧 5 分制统一到 100 分制。

### 后端实现

- `backend/src/main/resources/db/migration/V10__ai_review_rules_agent_runtime.sql`:新库初始化默认 `pass_threshold=80`,`need_human_threshold=70`,默认规则维度 `maxScore=100`,默认 Prompt 明确按 `0~100` 评分。
- 新增 `backend/src/main/resources/db/migration/V14__ai_review_score_scale_100.sql`:
  - 修改规则阈值列默认值为 80 / 70。
  - 将现有规则维度 `maxScore` 统一迁移为 100。
  - 将旧 5 分制规则阈值按 20 倍迁移到 100 分制。
  - 修正旧规则 Prompt 中的 `每项 0~5 / 每项 0-5` 文案。
  - 对明确来自旧 5 分制 `rule_snapshot_json` 的 AI result,将 `scores_json` 与 `total_score` 迁移为 100 分制。
  - 同步迁移 `ai_review_jobs.rule_snapshot_json`,避免待执行旧 Job 继续把 5 分制规则传给 Agent。
- `backend/src/main/java/com/labelhub/backend/ai/AiReviewService.java`:
  - 默认常量改为 `DEFAULT_DIMENSION_MAX_SCORE=100`,`DEFAULT_PASS_THRESHOLD=80`,`DEFAULT_NEED_HUMAN_THRESHOLD=70`。
  - 创建/更新规则时强制维度 `maxScore=100`,阈值必须在 `0~100` 且 `passThreshold > needHumanThreshold`。
  - Agent 领取 Job 时返回的 rule snapshot 会标准化为 100 分制。
  - Agent 回写结果时校验各维度分数和 `totalScore` 均不超过 100 分制加权上限。

### Agent 实现

- `agent/src/main/java/com/labelhub/agent/llm/ResponsesLlmClient.java`:
  - Responses JSON Schema 中每个维度分数最大值固定为 `100`。
  - `totalScore` 增加 `minimum=0`,`maximum=100`。
  - Prompt 追加评分口径说明:默认每项满分 100,`totalScore` 为 0~100 加权综合分。

### 前端实现

- `frontend/src/pages/owner/OwnerAiReview.tsx`:新建规则默认维度满分 100,默认 PASS=80,NEED_HUMAN=70,维度满分输入固定为 100,阈值输入范围改为 0~100,评分详情条按 100 分制渲染。
- `frontend/src/pages/aiReviewer/AiPreReviewQueue.tsx`:进度条按 0~100 分显示,颜色阈值改为 `>=80` 绿色,`>=70` 橙色,低于 70 红色。
- `frontend/src/pages/reviewer/ReviewerAiWorkbench.tsx`:AI 分数色调改为纯 100 分制,不再兼容 5 分制显示。
- `frontend/public/sample-datasets/ai-review-rules.json` 与 `ai-review-jobs.json`:样例规则和样例 Job 分数改为 100 分制。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `agent/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过。
- 前端构建仍提示 chunk 体积超过 500 kB,属于既有打包体积警告,不影响本轮评分口径改造。

### 注意事项

- `V14__ai_review_score_scale_100.sql` 是数据库迁移脚本,本轮没有在当前会话中直接连接数据库执行迁移。
- 如果本地数据库已经跑过 V10 旧版本,需要启动后端触发 Flyway 或手动执行 V14 后,历史规则和待执行 Job 才会完成数据口径迁移。



## 2026-05-31 任务截止结算与剩余题续期发布

### 对应阶段计划

对应「任务截止结算与剩余题续期发布」。本轮补齐任务截止后的状态机闭环:截止时自动停止标注、有效草稿转提交、未完成认领释放题目,并支持 Owner 通过原任务续期发布剩余题。

### 后端实现

- `StateMachineService`:任务状态机新增 `ended -> published`,用于 Owner 续期发布;系统角色允许执行截止结算相关的 `published/paused -> ended`、`claimed/returned -> submitted|voided`。
- 新增 `TaskDeadlineSettlementService`:
  - 定时每 1 分钟扫描 `deadline <= now` 且 `status in ('published','paused')` 的任务。
  - 在任务、任务广场、领取、答题、草稿、提交入口触发即时结算。
  - 有效 draft 按当前 schema 完整校验后自动创建 annotation revision,删除 draft,assignment/item 标记 submitted,并进入 AI 预审或人工审核流。
  - 无 draft 或 draft 校验失败时删除 draft,assignment 置 voided,item 回 pending。
  - 任务结算完成后 task 与最新 schema version 均置 ended,写入 `task.settle_by_deadline` 审计。
- 新增 `AnswerValidationService`:抽取答题答案过滤与必填/选项/结构化校验逻辑,供手动提交和截止自动提交共用同一口径。
- `TaskRepository / AnnotationRepository`:补充过期任务扫描、系统更新任务状态、锁定未完成 assignment、释放 assignment/item 等方法。
- 新增迁移 `V15__allow_reclaim_released_assignments.sql`:把 `uk_assignments_task_item_labeler` 调整为普通索引,允许同一 Labeler 在任务续期后重新认领被截止释放的同一题。
- `MarketTaskResponse`:新增 `state / expired / claimable / statusLabel` 字段。

### 前端实现

- `frontend/src/types/labeler.ts`:扩展 `MarketTask` 的 `state / expired / claimable / statusLabel`。
- `frontend/src/pages/labeler/TaskMarket.tsx`:
  - 移除接口失败后的 sample-datasets 静态样例降级。
  - 接口失败显示 Alert + 重试。
  - 已截止任务保留展示,显示 `statusLabel`,按钮禁用并显示“已截止”。
  - KPI「可领任务」只统计 `claimable=true` 的任务。
- `frontend/src/pages/owner/OwnerTasks.tsx`:
  - 已结束任务显示「续期发布」入口。
  - 从 ended 任务打开抽屉后,主按钮为「续期发布」,提交状态为 published。
  - 截止时间要求晚于当前时间;后端也会以 `INVALID_DEADLINE / DEADLINE_REQUIRED` 兜底。

### 接口文档

- `接口文档.md` 新增「任务截止结算与续期发布」说明:
  - `GET /api/tasks`
  - `GET /api/market/tasks`
  - `POST /api/tasks/{taskId}/claim`
  - `PUT /api/tasks/{taskId}`
  - `PUT /api/tasks/{taskId}/state`

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过。
- 前端构建仍提示 chunk 体积超过 500 kB,属于既有打包体积警告,不影响本轮截止结算功能。

### 注意事项

- `V15__allow_reclaim_released_assignments.sql` 需要通过 Flyway 或手动执行后,本地旧数据库才允许同一 Labeler 在续期后重新认领曾被释放的同一题。
- 本轮不创建新任务承接剩余题,Owner 续期发布复用原任务。



## 2026-05-31 Reviewer AI 预审裁决真实闭环

### 对应阶段计划

对应「Reviewer AI 预审裁决真实闭环」。本轮补齐 Reviewer AI 预审工作台底部「打回 / 直接修订 / 通过·入库」的真实后端语义和前端失败态,移除演示伪成功。

### 后端实现

- `POST /api/reviewer/annotations/{annotationId}/decision`:
  - 兼容 `reason` 和旧 `note`,后端统一取有效审核意见。
  - `APPROVE` 继续写 `human_reviews(decision='approve')`,并将 annotation / assignment / item 更新为 `accepted`。
  - `RETURN` 继续要求原因必填,成功后写 `human_reviews(decision='return')`,并将 annotation / assignment / item 更新为 `returned`。
  - `REVISE` 要求 `answerJson` 为 object,按原 annotation 的 `schema_snapshot_json.fields` 做可见字段过滤和完整校验。
  - `REVISE` 若答案无变化返回 `NO_REVISION_CHANGE`;若通过校验,原 annotation 标记为 `revised`,写 `human_reviews(decision='revise', diff_json={before,after,changedFields})`,同 assignment 生成新的 latest accepted revision,并将 assignment / item 更新为 `accepted`。
- `AnnotationToReviewResponse` 新增 `schemaFields`,从 `schema_snapshot_json.fields` 返回给前端直接修订表单使用。
- `ReviewRepository` 的待审列表、AI 预审列表、单条 annotation 查询均补充 schema snapshot 字段;裁决锁定限制为 assignment 最新非 voided annotation。
- `StateMachineService` 增加 annotation 状态 `revised`,允许 `reviewing -> revised`。

### 前端实现

- `frontend/src/types/reviewer.ts`:新增 `AnnotationToReview.schemaFields` 和 `SubmitReviewRequest.answerJson`。
- `frontend/src/pages/reviewer/ReviewerAiWorkbench.tsx`:
  - 移除接口失败后读取 `sample-datasets/reviewer-annotations.json` 的降级逻辑。
  - 移除单条和批量裁决中的“演示模式继续/忽略”,接口失败时不移除条目、不显示伪成功。
  - 单条打回前端校验原因必填,提交 `{ decision:'RETURN', reason }`。
  - 单条通过提交 `{ decision:'APPROVE', reason? }`。
  - 直接修订打开 Drawer,复用 `LabelHubFormRenderer` 基于 `schemaFields` 编辑答案,提交 `{ decision:'REVISE', reason?, answerJson }`。
  - 缺少 `schemaFields` 时禁用「直接修订」并提示无法就地修订。
  - 批量通过/打回使用 `Promise.allSettled`,只移除成功项;批量打回通过弹窗要求填写统一原因。
- `frontend/src/styles/global.css`:补充 Reviewer 工作台错误提示、禁用按钮和修订 Drawer 样式。

### 接口文档

- `接口文档.md` 已更新 `POST /api/reviewer/annotations/{annotationId}/decision` 的请求字段、`REVISE` 状态流和错误码。
- `接口文档.md` 已更新 `AnnotationToReview` 响应新增 `schemaFields`。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过。
- 前端构建仍提示 chunk 体积超过 500 kB,属于既有打包体积警告,不影响本轮裁决闭环。

### 注意事项

- 本轮不新增数据库表或迁移。
- Reviewer 直接修订不重跑 AI 预审,新的 latest revision 直接 accepted 并进入可导出结果。
- `revised` 状态用于标记被 Reviewer 替换的原 annotation,导出仍以 latest accepted annotation 为准。
## 2026-05-31 人工复审打回返修窗口

### 对应阶段计划

对应“人工复审打回返修窗口”。本轮修复人工复审打回项在原任务截止后无法修改的问题,保留任务截止结算语义,只为正式人工打回的 returned assignment 开放独立返修窗口。

### 后端实现

- 新增迁移 `V16__assignment_rework_deadline.sql`:
  - 为 `assignments` 增加 `resubmit_deadline`。
  - 增加 `idx_assignments_resubmit_deadline`。
  - 对已有人工打回 returned assignment 补默认 48 小时返修窗口。
- Reviewer `RETURN` 裁决:
  - 继续将 annotation / assignment / item 更新为 `returned`。
  - 写入 `assignments.resubmit_deadline = now + 48 hours`。
  - audit snapshot 记录 `resubmitDeadline`。
- Labeler 答题权限:
  - 普通 `claimed/submitted` 仍要求任务 `published` 且未超过 deadline。
  - `returned` 且 `resubmit_deadline` 未过期时允许保存草稿和单条重提,即使任务已 `ended`。
  - 返修窗口过期返回 `RETURN_REWORK_EXPIRED`。
  - `AssignmentItemResponse` 新增 `resubmitDeadline` 和 `lockReason`。
- 截止结算:
  - `TaskDeadlineSettlementService` 继续结算普通未完成 assignment。
  - 人工 returned assignment 不自动作废、不释放 item,避免破坏人工审核链路。
- 打回项列表:
  - `GET /api/labeler/returned-items` 新增 `resubmitDeadline / editable / expiredReason`。
  - 只有人工打回且返修窗口内的记录 `actionable=true`。

### 前端实现

- `LabelerReturnedItem` 与 `AssignmentItem` 类型补充返修字段。
- `/labeler/returned`:
  - 显示返修截止时间。
  - 返修窗口过期时按钮禁用并显示“返修已过期”。
  - AI 预打回仍不可操作。
- `/labeler/answer/{assignmentId}`:
  - returned 返修题显示“人工复审打回返修中”和返修截止时间。
  - 返修提交走单题 `POST /api/assignments/{assignmentId}/submit`,避免过期任务下其他题影响该 returned 项。
  - 保存失败时按真实错误锁定,不再把所有失败伪造成 `voided`。

### 接口文档

- `接口文档.md` 已追加“人工复审打回返修窗口”章节,记录:
  - `GET /api/labeler/returned-items` 新增字段。
  - `GET /api/assignments/{assignmentId}/item` 新增字段。
  - `POST /api/assignments/{assignmentId}/submit` 对 returned 返修的放行条件和 `RETURN_REWORK_EXPIRED`。
  - `POST /api/reviewer/annotations/{annotationId}/decision` 的 `RETURN` 会设置 48 小时返修窗口。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过。
- 前端构建仍提示 chunk 大于 500 kB,属于既有打包体积警告,不影响本轮返修功能。

### 注意事项

- 需要通过 Flyway 或手动执行 `V16__assignment_rework_deadline.sql` 后,本地数据库才会有 `resubmit_deadline` 字段。
- 本轮不新增 Owner 单独延长某条返修项的入口;返修窗口固定为 48 小时。
- 返修窗口过期后本轮只锁定 returned assignment,不自动释放题目。
## 2026-05-31 打回返修按钮语义修正

### 对应阶段计划

对应“打回返修按钮语义修正”。本轮不改变返修窗口和状态机,只修正打回入口与答题页主按钮的语义和显示条件。

### 后端实现

- `GET /api/labeler/returned-items`:
  - 人工复审打回且返修窗口内的 `actionText` 从“修改并重提”改为“立即修改”。
  - AI 预打回仍返回“等待人工复审”。
  - 返修过期仍返回“返修已过期”。

### 前端实现

- `/labeler/answer/{assignmentId}`:
  - `returned + resubmitDeadline` 的返修题主按钮显示“重新提交”。
  - 返修题不再受“最后一题才显示提交按钮”的限制,解决打回入口进入后红框区域为空的问题。
  - 点击“重新提交”继续沿用单题 `POST /api/assignments/{assignmentId}/submit` 逻辑,重提后进入 AI 预审或人工审核流。
  - 返修提示文案改为“请在 xx 前完成修改并重新提交”。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过。
- 前端构建仍提示 chunk 大于 500 kB,属于既有打包体积警告。

### 注意事项

- 本轮只修改按钮语义和显示条件,不改变 AI 预打回不可重提、人工打回返修窗口、单题重提状态流。



## 2026-05-31 打回返修入口上下文与按钮位置修正

### 对应阶段计划

对应“打回返修入口上下文与按钮位置修正”。本轮修正“重新提交”按钮的位置和出现条件,让它只作为从打回项进入的返修上下文动作出现。

### 前端实现

- `/labeler/returned`:
  - 点击人工复审打回项“立即修改”时,跳转 `/labeler/answer/{assignmentId}` 并携带 React Router state:`entry='returned-rework'`、`assignmentId`。
- `/labeler/answer/{assignmentId}`:
  - 使用 `useLocation()` 读取入口 state。
  - 仅当当前题为 `returned + resubmitDeadline`,且入口 state 匹配当前 assignment 时,显示返修“重新提交”。
  - 顶部操作区不再因为 returned 返修状态显示“重新提交”;普通最后一题提交和已提交题“重新修改”逻辑保持不变。
  - 在 `.answer-pager` 的上一题和下一题之间新增中间操作区,将“重新提交”放到截图红框位置。
  - 上一题、下一题、进度点跳转不传递返修入口 state,不会把“重新提交”带到其他题。
- `frontend/src/styles/global.css`:
  - 将答题卡底部导航调整为左 / 中 / 右三段布局,普通页面中间为空占位,移动端仍纵向铺满。

### 接口文档

- `接口文档.md` 已更新 `POST /api/assignments/{assignmentId}/submit` 的前端返修入口说明:
  - “重新提交”是从 `/labeler/returned` 进入的前端上下文按钮。
  - 普通入口进入同一 returned 题不显示该按钮。
  - 后端提交接口和状态流不变。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过。
- 前端构建仍提示 chunk 大于 500 kB,属于既有打包体积警告。

### 注意事项

- 本轮不新增后端 token 或一次性凭证;入口限制由 React Router state 控制。
- 页面刷新会丢失入口 state,刷新后需要重新从打回项点击“立即修改”进入才会显示“重新提交”。



## 2026-05-31 Reviewer 工作台题目序号与多轮审计修正

### 对应阶段计划

对应“Reviewer 工作台题目序号与多轮审计修正”。本轮修复 Reviewer 工作台误把 `annotationId` 当作用户可见题号的问题,并让返修后的条目能看到 assignment 级多轮审核链路。

### 后端实现

- `AnnotationToReviewResponse` 新增 `itemIndex`。
- Reviewer 待审列表、AI 预审列表、单条 annotation 查询均返回 `itemIndex`。
  - 口径对齐 Labeler 端:同一 task + labeler 下有效 assignment 按 id 升序计算 1-based 题序。
- `reviewTimeline` 改为 assignment 级多轮历史:
  - 查询同一 `assignment_id` 下所有非 voided annotation revision。
  - 每轮返回 AI 预审事件与人工复审事件。
  - 当前 latest annotation 仍作为裁决对象,历史 revision 只进入右侧审计时间线。
- `AiReviewJobResponse` 新增 `itemIndex / itemTotal`。
  - `/api/ai-review/jobs` 可返回 AI Reviewer Job 对应 assignment 在任务中的真实题序和任务总题数。

### 前端实现

- `frontend/src/types/reviewer.ts`: `AnnotationToReview` 新增 `itemIndex`,并更新 `reviewTimeline` 注释为多轮审计语义。
- `frontend/src/pages/reviewer/ReviewerAiWorkbench.tsx`:
  - 左侧卡片第一行显示“第 N 题”,不再显示 annotationId。
  - 左侧卡片 AI 分数旁显示“第 N 轮”胶囊,第 2 轮及以上用橙色。
  - 详情标题改为“第 N 题 · 任务名”。
  - 右侧标题改为“审核时间线（第 N 题）”。
  - 时间线按后端 `reviewTimeline` 展示多轮 AI 预审和人工复审记录。
- `frontend/src/types/aiReview.ts` 与 `AiPreReviewQueue.tsx` 已兼容 `itemIndex/itemTotal`;后端补字段后 AI Reviewer 队列可自动展示真实题序。

### 接口文档

- `接口文档.md` 已更新:
  - `AnnotationToReview.itemIndex`。
  - `AnnotationToReview.reviewTimeline` 为 assignment 级多轮审计时间线。
  - `AiReviewJob.itemIndex/itemTotal`。

### 验证结果

- 已执行 `backend/.\mvnw.cmd -q -DskipTests package`,构建通过。
- 已执行 `frontend/npm.cmd run build`,TypeScript 编译和 Vite 打包通过。
- 前端构建仍提示 chunk 大于 500 kB,并提示 `rolldown:vite-resolve` 插件耗时,均为构建提示,不影响本轮功能。

### 注意事项

- 本轮不修改状态机、不新增数据库表或迁移。
- `annotationId` 仍保留在接口中用于裁决和调试,但不作为 Reviewer 工作台主标题题号展示。
