# LabelHub 接口文档

更新日期：2026-05-27

## 1. 通用约定

### 1.1 基础路径

| 项目 | 约定 |
|---|---|
| 前端默认 API Base URL | `/api` |
| 前端可覆盖配置 | `VITE_API_BASE_URL` |
| 后端统一前缀 | `/api` |
| 默认请求格式 | `application/json` |
| 文件上传格式 | `multipart/form-data` |
| 认证方式 | `Authorization: Bearer <accessToken>` |

### 1.2 分页响应

后端分页接口统一返回：

```json
{
  "items": [],
  "page": 1,
  "pageSize": 20,
  "total": 0
}
```

### 1.3 错误响应

后端全局异常处理统一返回：

```json
{
  "code": "ERROR_CODE",
  "message": "error message"
}
```

### 1.4 状态说明

| 状态 | 含义 |
|---|---|
| 已实现 | 当前仓库已有前端调用方法或后端 Controller/Service 实现 |
| 前端已预留 | 前端 `api` 层已有方法,但后端尚未实现对应 REST endpoint |
| 后端待实现 | 后端当前没有对应 Controller endpoint |
| 已构建验证 | 对应阶段曾通过前端或后端构建验证,但不代表已有自动化接口测试 |
| 待补自动化测试 | 当前仓库未发现接口级自动化测试用例 |

## 2. 前端接口清单

前端接口位于 `frontend/src/api/`。`apiRequest` 会自动拼接 `/api` 前缀,并在存在 token 时自动携带 Bearer Token。

### 2.1 认证接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `authApi.login(payload)` | POST | `/auth/login` | 用户登录,写入 token 和当前用户信息 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `authApi.logout()` | POST | `/auth/logout` | 用户退出,清理后端会话和本地 token | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `authApi.getCurrentUser()` | GET | `/auth/me` | 获取当前登录用户与权限 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `authApi.register(payload)` | POST | `/auth/register` | 注册用户账号 | 已实现并对接后端 | 待页面正式联调,待补自动化测试 |

### 2.2 Owner 任务接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `ownerApi.listTasks()` | GET | `/tasks` | 获取当前 Owner 的任务列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.createTask(payload)` | POST | `/tasks` | 创建任务,支持数据集、模板和分发策略配置 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.updateTask(taskId, payload)` | PUT | `/tasks/{taskId}` | 更新任务基础信息、模板、数据集和分发策略 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.updateTaskState(taskId, state)` | PUT | `/tasks/{taskId}/state` | 修改任务状态,如发布、暂停、结束 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.deleteTask(taskId)` | DELETE | `/tasks/{taskId}` | 删除当前 Owner 名下任务 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.listAssignableLabelers()` | GET | `/tasks/assignable-labelers` | 获取可指派的 Labeler 列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |

### 2.3 数据集接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `datasetApi.listDatasets()` | GET | `/datasets` | 获取当前 Owner 的数据集列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.createDataset(payload)` | POST | `/datasets` | 新建空数据集,任务关联可选 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.importDataset(payload)` | POST | `/datasets/import` | 上传文件并创建数据集,任务关联可选 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.importItems(datasetId, file)` | POST | `/datasets/{datasetId}/items/import` | 向已有数据集追加导入数据项 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.listItems(datasetId)` | GET | `/datasets/{datasetId}/items` | 查看数据集条目原始内容 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.deleteDataset(datasetId)` | DELETE | `/datasets/{datasetId}` | 删除未绑定任务且未产生作业的数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |

### 2.4 模板 Schema 接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `schemaApi.listSchemas()` | GET | `/schemas` | 获取当前 Owner 的模板版本列表,包含任务引用和默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.getSchema(versionId)` | GET | `/schemas/{versionId}` | 获取指定模板版本完整字段定义、绑定任务和默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.validate(payload)` | POST | `/schemas/validate` | 校验模板字段结构,返回错误和警告列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.createStandaloneDraft(payload)` | POST | `/schemas/draft` | 创建不绑定任务的模板草稿,可保存默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.createDraftForTask(taskId, payload)` | POST | `/tasks/{taskId}/schemas/draft` | 在指定任务下创建模板草稿,可保存默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.updateDraft(versionId, payload)` | PUT | `/schemas/{versionId}/draft` | 保存模板草稿内容、名称、描述和默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.publish(versionId)` | POST | `/schemas/{versionId}/publish` | 发布模板版本供任务和 Renderer 使用 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.withdraw(versionId)` | POST | `/schemas/{versionId}/withdraw` | 收回已发布模板,同一版本回到草稿状态 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.deleteSchema(versionId)` | DELETE | `/schemas/{versionId}` | 软删除草稿模板,从模板列表隐藏且不影响已绑定任务 | 已实现并对接后端 | 已构建验证,待补自动化测试 |

### 2.5 Labeler 任务与答题接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `labelerApi.listMarketTasks(query)` | GET | `/market/tasks` | 浏览已发布且未过期的任务市场 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.claimTask(taskId)` | POST | `/tasks/{taskId}/claim` | 认领任务数据项,后端按策略生成 assignments | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.listMyAssignments(status)` | GET | `/assignments/mine` | 获取我的领取/提交/打回作业列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.getAssignmentItem(assignmentId)` | GET | `/assignments/{assignmentId}/item` | 获取答题页原题、Schema、草稿和最新提交答案 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.getDraft(assignmentId)` | GET | `/assignments/{assignmentId}/draft` | 获取指定 assignment 草稿 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.saveDraft(assignmentId, answerJson)` | PUT | `/assignments/{assignmentId}/draft` | 自动保存或覆盖草稿 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.submitAnnotation(assignmentId, payload)` | POST | `/assignments/{assignmentId}/submit` | 提交正式标注答案,生成 annotation revision | 已实现并对接后端 | 已构建验证,待补自动化测试 |

### 2.6 Reviewer 预留接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `reviewerApi.getOverview(rangeDays)` | GET | `/reviewer/overview` | 审核员工作概览 | 前端已预留,后端待实现 | 未联调 |
| `reviewerApi.listBatches(params)` | GET | `/reviewer/batches` | 待审批次列表 | 前端已预留,后端待实现 | 未联调 |
| `reviewerApi.claimBatch(batchId)` | POST | `/reviewer/batches/{batchId}/claim` | 审核员领取批次 | 前端已预留,后端待实现 | 未联调 |
| `reviewerApi.listAnnotations(batchId, params)` | GET | `/reviewer/batches/{batchId}/annotations` | 拉取批次内待审标注 | 前端已预留,后端待实现 | 未联调 |
| `reviewerApi.submitReview(annotationId, payload)` | POST | `/reviewer/annotations/{annotationId}/decision` | 提交单条人工审核结论 | 前端已预留,后端待实现 | 未联调 |
| `reviewerApi.listDisputes(params)` | GET | `/reviewer/disputes` | 获取争议样本列表 | 前端已预留,后端待实现 | 未联调 |
| `reviewerApi.resolveDispute(disputeId, payload)` | POST | `/reviewer/disputes/{disputeId}/resolve` | 解决争议样本 | 前端已预留,后端待实现 | 未联调 |

### 2.7 Dashboard 预留接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `dashboardApi.getOverview(range)` | GET | `/dashboard/overview` | 数据看板总览指标 | 前端已预留,后端待实现 | 未联调 |
| `dashboardApi.getTaskProgress()` | GET | `/dashboard/task-progress` | 任务进度统计 | 前端已预留,后端待实现 | 未联调 |
| `dashboardApi.getReviewDistribution(range)` | GET | `/dashboard/review-distribution` | 审核结果分布 | 前端已预留,后端待实现 | 未联调 |
| `dashboardApi.getLabelerPerformance(range)` | GET | `/dashboard/labeler-performance` | 标注员绩效统计 | 前端已预留,后端待实现 | 未联调 |
| `dashboardApi.getSubmissionTimeline(year)` | GET | `/dashboard/submission-timeline` | 年度提交趋势 | 前端已预留,后端待实现 | 未联调 |
| `dashboardApi.getRecentActivities()` | GET | `/dashboard/recent-activities` | 最近任务动态 | 前端已预留,后端待实现 | 未联调 |
| `dashboardApi.getRoleBreakdown()` | GET | `/dashboard/role-breakdown` | 用户角色分布 | 前端已预留,后端待实现 | 未联调 |
| `dashboardApi.getDisputes(days)` | GET | `/dashboard/disputes` | 争议样本统计 | 前端已预留,后端待实现 | 未联调 |

## 3. 后端接口清单

后端接口位于 `backend/src/main/java/com/labelhub/backend/**Controller.java`。除登录和注册外,默认需要 Bearer Token。

### 3.1 认证 Auth

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| POST | `/api/auth/login` | 公开 | `username`, `password`, `role?` | `LoginResponse` | 登录并签发访问 token | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/auth/register` | 公开 | `username`, `password`, `role?` | `AuthUserResponse` | 注册用户并绑定角色 | 已实现 | 待页面正式联调,待补自动化测试 |
| POST | `/api/auth/logout` | 登录用户 | `Authorization` header | `204 No Content` | 退出登录并清理 token 会话 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/auth/me` | 登录用户 | `Authorization` header | `CurrentUserResponse` | 获取当前用户和权限 | 已实现 | 已构建验证,待补自动化测试 |

### 3.2 Owner 任务 Task

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/tasks` | Owner | 无 | `PageResponse<OwnerTaskResponse>` | 获取当前 Owner 的任务列表 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/tasks` | Owner | `CreateTaskRequest` | `OwnerTaskResponse` | 创建任务,可绑定数据集和已发布模板 | 已实现 | 已构建验证,待补自动化测试 |
| PUT | `/api/tasks/{taskId}` | Owner | `CreateTaskRequest` | `OwnerTaskResponse` | 更新当前 Owner 名下任务 | 已实现 | 已构建验证,待补自动化测试 |
| DELETE | `/api/tasks/{taskId}` | Owner | `taskId` | `204 No Content` | 删除当前 Owner 名下任务 | 已实现 | 已构建验证,待补自动化测试 |
| PUT | `/api/tasks/{taskId}/state` | Owner | `state` | `OwnerTaskResponse` | 更新任务状态 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/tasks/assignable-labelers` | Owner | 无 | `AssignableLabelerResponse[]` | 获取可被指派的 Labeler 用户 | 已实现 | 已构建验证,待补自动化测试 |

注意：当前 `DELETE /api/tasks/{taskId}` 后端只校验任务归属,未限制只能删除草稿或已结束任务。如后续需要保留已发布任务审计链路,应补充状态校验或软删除。

### 3.3 Labeler 任务市场与领取

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/market/tasks` | 登录用户 | `keyword?`, `taskType?`, `strategy?`, `mediaType?`, `aiReview?`, `sortBy?`, `page?`, `pageSize?` | `PageResponse<MarketTaskResponse>` | 浏览可领取任务市场 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/tasks/{taskId}/claim` | Labeler | `taskId` | `AssignmentResponse` | 按分发策略认领任务数据项;模板被收回时返回 `409 SCHEMA_WITHDRAWN` | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/assignments/mine` | Labeler | `status?` | `PageResponse<AssignmentResponse>` | 获取当前 Labeler 的作业列表 | 已实现 | 已构建验证,待补自动化测试 |

### 3.4 数据集 Dataset

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/datasets` | Owner | 无 | `PageResponse<DatasetResponse>` | 获取当前 Owner 的数据集列表 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/datasets` | Owner | `name`, `kind`, `taskId?` | `DatasetResponse` | 创建空数据集,可暂不绑定任务 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/datasets/import` | Owner | `multipart: file`, `kind`, `name?`, `taskId?` | `DatasetResponse` | 上传文件并创建数据集 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/datasets/{datasetId}/items/import` | Owner | `multipart: file` | `DatasetResponse` | 向已有数据集追加导入条目 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/datasets/{datasetId}/items` | Owner | `datasetId` | `JsonNode[]` | 查看数据集条目原始 payload | 已实现 | 已构建验证,待补自动化测试 |
| DELETE | `/api/datasets/{datasetId}` | Owner | `datasetId` | `204 No Content` | 删除当前 Owner 的未使用数据集;已绑定任务或已有作业时拒绝 | 已实现 | 已构建验证,待补自动化测试 |

注意：数据集删除当前采用物理删除,仅允许删除未绑定任务且没有 assignment/annotation 的数据集。删除 `datasets` 会通过 `items.dataset_id ON DELETE CASCADE` 清理未使用条目,但不会删除 `files` 元数据。

### 3.5 模板 Schema

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/schemas` | Owner | 无 | `PageResponse<SchemaSummaryResponse>` | 获取当前 Owner 的模板版本列表,返回任务绑定信息和模板默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/schemas/{versionId}` | Owner | `versionId` | `SchemaVersionResponse` | 获取模板版本详情、绑定任务信息和模板默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/schemas/validate` | Owner | `name?`, `description?`, `fields` | `SchemaValidationResponse` | 校验模板字段结构,返回错误和警告列表 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/schemas/draft` | Owner | `name`, `description?`, `taskId?`, `datasetId?`, `datasetName?`, `fields` | `SchemaVersionResponse` | 创建独立模板草稿,可绑定默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/tasks/{taskId}/schemas/draft` | Owner | `name`, `description?`, `datasetId?`, `datasetName?`, `fields` | `SchemaVersionResponse` | 为任务创建模板草稿,可绑定默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| PUT | `/api/schemas/{versionId}/draft` | Owner | `name?`, `description?`, `taskId?`, `datasetId?`, `datasetName?`, `fields` | `SchemaVersionResponse` | 更新草稿模板和默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/schemas/{versionId}/publish` | Owner | `versionId` | `SchemaVersionResponse` | 发布模板版本 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/schemas/{versionId}/withdraw` | Owner | `versionId` | `SchemaVersionResponse` | 原地收回已发布模板,同一版本回到草稿状态 | 已实现 | 已构建验证,待补自动化测试 |
| DELETE | `/api/schemas/{versionId}` | Owner | `versionId` | `204 No Content` | 软删除草稿模板;非草稿拒绝,已绑定任务和历史标注不受影响 | 已实现 | 已构建验证,待补自动化测试 |

注意：任务发布页通过 `tasks.reward_rule.schemaVersionId` 引用已发布模板。`/api/schemas` 与 `/api/schemas/{versionId}` 现在会同时识别 `task_schema_versions.task_id` 直接绑定和 `tasks.reward_rule.schemaVersionId` 引用绑定;如果多个任务引用同一模板,当前响应展示最近更新的一条任务。模板默认数据集不新增数据库列,存放在 `task_schema_versions.schema_json` 根节点的 `datasetId/datasetName`。

删除规则：`DELETE /api/schemas/{versionId}` 只允许删除 `draft` 状态模板。已发布模板需要先调用 `withdraw` 收回为草稿。删除是软删除,写入 `task_schema_versions.deleted_at`;`GET /api/schemas` 和新任务模板下拉会隐藏该模板,但已绑定任务的认领、作答、提交和历史 annotation 查看仍可读取原模板。

### 3.6 标注答题 Annotation

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/assignments/{assignmentId}/item` | Labeler | `assignmentId` | `AssignmentItemResponse` | 获取答题页原题、Schema、位置、草稿和最新提交答案 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/assignments/{assignmentId}/draft` | Labeler | `assignmentId` | `DraftResponse` 或 `null` | 获取 assignment 草稿 | 已实现 | 已构建验证,待补自动化测试 |
| PUT | `/api/assignments/{assignmentId}/draft` | Labeler | `answerJson` | `DraftResponse` | 保存或更新草稿;模板被收回时返回 `409 SCHEMA_WITHDRAWN` | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/assignments/{assignmentId}/submit` | Labeler | `schemaVersionId`, `answerJson`, `draftVersion?` | `AnnotationResponse` | 提交标注答案并生成 annotation revision;模板被收回时返回 `409 SCHEMA_WITHDRAWN` | 已实现 | 已构建验证,待补自动化测试 |

### 3.7 后端待实现接口

| 模块 | HTTP | 路径 | 作用 | 当前状态 | 测试状态 |
|---|---|---|---|---|---|
| Reviewer | GET | `/api/reviewer/overview` | 审核工作概览 | 后端待实现 | 未联调 |
| Reviewer | GET | `/api/reviewer/batches` | 待审批次列表 | 后端待实现 | 未联调 |
| Reviewer | POST | `/api/reviewer/batches/{batchId}/claim` | 领取审核批次 | 后端待实现 | 未联调 |
| Reviewer | GET | `/api/reviewer/batches/{batchId}/annotations` | 批次内待审标注列表 | 后端待实现 | 未联调 |
| Reviewer | POST | `/api/reviewer/annotations/{annotationId}/decision` | 提交人工审核结论 | 后端待实现 | 未联调 |
| Reviewer | GET | `/api/reviewer/disputes` | 争议样本列表 | 后端待实现 | 未联调 |
| Reviewer | POST | `/api/reviewer/disputes/{disputeId}/resolve` | 解决争议样本 | 后端待实现 | 未联调 |
| Dashboard | GET | `/api/dashboard/overview` | 数据看板总览 | 后端待实现 | 未联调 |
| Dashboard | GET | `/api/dashboard/task-progress` | 任务进度统计 | 后端待实现 | 未联调 |
| Dashboard | GET | `/api/dashboard/review-distribution` | 审核结果分布 | 后端待实现 | 未联调 |
| Dashboard | GET | `/api/dashboard/labeler-performance` | 标注员绩效统计 | 后端待实现 | 未联调 |
| Dashboard | GET | `/api/dashboard/submission-timeline` | 提交趋势 | 后端待实现 | 未联调 |
| Dashboard | GET | `/api/dashboard/recent-activities` | 最近动态 | 后端待实现 | 未联调 |
| Dashboard | GET | `/api/dashboard/role-breakdown` | 角色分布 | 后端待实现 | 未联调 |
| Dashboard | GET | `/api/dashboard/disputes` | 争议统计 | 后端待实现 | 未联调 |

## 4. 关键数据结构摘要

### 4.1 登录响应 `LoginResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `accessToken` | string | Bearer Token |
| `tokenType` | string | 固定为 Bearer 类型 |
| `expiresIn` | number | token 有效期秒数 |
| `user` | object | 当前用户信息 |

### 4.2 用户信息 `AuthUserResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 用户 ID |
| `username` | string | 登录账号 |
| `displayName` | string | 展示名称 |
| `roles` | string[] | 角色编码,如 owner / labeler / reviewer |

### 4.3 任务创建 `CreateTaskRequest`

| 字段 | 类型 | 说明 |
|---|---|---|
| `title` | string | 任务标题,必填 |
| `tags` | string[] | 标签 |
| `reward` | string | 奖励规则展示字段 |
| `quota` | number | 任务配额 |
| `deadline` | string | 截止时间 |
| `datasetId` | string | 关联数据集 ID |
| `strategy` | string | 分发策略: `first-come` / `assigned` / `quota` |
| `maxClaimPerUser` | number | 配额抢单模式下每人上限 |
| `assignedLabelerIds` | string[] | 指派模式下的 Labeler ID |
| `schema` | string | 模板展示标签兼容字段 |
| `schemaVersionId` | string | 已发布模板版本 ID |
| `aiReviewEnabled` | boolean | 是否启用 AI 预审 |
| `description` | string | 任务描述 |
| `taskType` | string | 任务类型 |
| `status` | string | 任务状态 |

### 4.4 模板版本 `SchemaVersionResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `versionId` | string | 模板版本 ID |
| `versionNumber` | string | 版本号展示值,如 r1 |
| `taskId` | string | 直接绑定任务 ID,可空 |
| `taskTitle` | string | 绑定任务标题,可空 |
| `name` | string | 模板名称 |
| `description` | string | 模板描述 |
| `datasetId` | string | 模板默认关联数据集 ID,可空 |
| `datasetName` | string | 模板默认关联数据集名称,可空 |
| `status` | string | `draft` / `published` |
| `fields` | JsonNode | Renderer 使用的字段定义 |
| `updatedAt` | string | 更新时间 |
| `createdBy` | string | 创建人 |

### 4.5 答题页响应 `AssignmentItemResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `assignmentId` | string | 作业 ID |
| `taskId` | string | 任务 ID |
| `taskTitle` | string | 任务标题 |
| `itemId` | string | 数据项 ID |
| `status` | string | 作业状态 |
| `editable` | boolean | 当前是否可编辑 |
| `deadline` | string | 任务截止时间 |
| `schemaVersionId` | string | 生效模板版本 ID |
| `rawPayload` | JsonNode | 原始题目数据 |
| `fields` | JsonNode | 模板字段定义 |
| `position` | object | 上一题/下一题位置 |
| `returnReason` | string | 打回原因 |
| `draft` | object | 当前草稿 |
| `latestAnnotation` | object | 最新已提交答案 |

### 4.6 模板校验响应 `SchemaValidationResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `valid` | boolean | 是否无错误 |
| `errors` | SchemaDiagnostic[] | 阻止保存/发布的错误 |
| `warnings` | SchemaDiagnostic[] | 不阻止发布但需要关注的警告 |

`SchemaDiagnostic` 字段包括 `level`、`code`、`message`、`fieldName`。

## 5. 后续接口补齐建议

1. 优先实现 Reviewer 后端接口,使 AI 预审之后的人工审核闭环从前端 mock 变为真实数据。
2. 再实现 Dashboard 后端接口,复用现有任务、assignment、annotation、review 表做聚合统计。
3. 为认证、任务创建、任务认领、草稿保存、提交标注、模板发布补充接口级自动化测试。
4. 后续如需要完整展示“一个模板被多个任务使用”的关系,可将 Schema 响应从单个 `taskId/taskTitle` 扩展为 `boundTasks[]`。


## 6. 数据集 - 删除接口(后端预留)

### `DELETE /api/datasets/{datasetId}`

**说明**:删除数据集.前端已在 `frontend/src/api/dataset.ts` 中实现 `datasetApi.deleteDataset(id)`,等待后端实现.

**鉴权**:Owner 角色,且数据集创建者必须是当前 Owner.

**路径参数**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| datasetId | string | 是 | 数据集 ID |

**响应**:

- `204 No Content` - 删除成功(无 body)
- `401 Unauthorized` - 未登录
- `403 Forbidden` - 不是创建者
- `404 DATASET_NOT_FOUND` - 数据集不存在或不属于当前 Owner
- `409 DATASET_IN_USE` - 数据集已绑定任务或已有作业,需先解绑或删除任务

**前端调用**:

```ts
await datasetApi.deleteDataset(datasetId);
```

**前端处理**:

1. Modal.confirm 二次确认
2. 调用 API,成功后从本地 `datasets` 列表剔除并切换 `activeId` 到剩余第一项
3. 失败用 `message.error(err.message ?? '删除数据集失败...')`,不伪造成功


## 7. 状态机与审计闭环接口

### 7.1 状态机审计字段

本轮扩展 `audit_logs`,状态变化统一写入以下字段:

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| `entity_type` | string | `task` / `assignment` / `annotation` / `ai_review_job` / `export_job` |
| `entity_id` | number | 业务实体 ID |
| `operator_id` | number | 操作者用户 ID,系统任务可空 |
| `operator_role` | string | `owner` / `labeler` / `reviewer` / `system_agent` |
| `action` | string | 稳定动作名,如 `annotation.submit` |
| `from_state` | string | 迁移前状态 |
| `to_state` | string | 迁移后状态 |
| `reason` | string | 打回、失败、争议等原因 |
| `before_json` | json | 迁移前快照 |
| `after_json` | json | 迁移后快照 |
| `snapshot_json` | json | 状态机补充快照 |

### 7.2 Reviewer 接口

#### `GET /api/reviewer/overview`

**作用**:Reviewer 工作概览,返回待审批次、今日通过/打回/争议和累计审核量。

**鉴权**:Reviewer。

**Query**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `days` | number | 否 | 统计范围,默认 30 |

**实现状态**:已实现。

**测试状态**:已通过后端打包;页面构建通过。

#### `GET /api/reviewer/batches`

**作用**:按任务聚合生成 Reviewer 待审批次列表。

**鉴权**:Reviewer。

**Query**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `status` | string | 否 | `pending` / `in_review` / `completed` |
| `keyword` | string | 否 | 搜索任务名或任务 ID |
| `page` | number | 否 | 页码 |
| `pageSize` | number | 否 | 每页数量 |

**实现状态**:已实现。

**测试状态**:已通过后端打包;前端 Reviewer 队列构建通过。

#### `POST /api/reviewer/batches/{batchId}/claim`

**作用**:领取审核批次。当前不新增批次表,该接口返回对应 task 派生批次信息。

**鉴权**:Reviewer。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `GET /api/reviewer/batches/{batchId}/annotations`

**作用**:获取某个批次内待审 annotation。`batchId=all` 时返回全部。

**鉴权**:Reviewer。

**Query**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `decision` | string | 否 | 按最新人工审核结论过滤 |
| `page` | number | 否 | 页码 |
| `pageSize` | number | 否 | 每页数量 |

**实现状态**:已实现。

**测试状态**:已通过后端打包;前端 AI 审核页复用该接口构建通过。

#### `POST /api/reviewer/annotations/{annotationId}/decision`

**作用**:提交人工审核结论,并通过状态机更新 annotation / assignment / item 状态。

**鉴权**:Reviewer。

**Request Body**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `decision` | string | 是 | `APPROVE` / `RETURN` / `REVISE` / `ESCALATE` |
| `reason` | string | RETURN 时必填 | 打回或争议原因 |
| `note` | string | 否 | 备注 |
| `escalate` | boolean | 否 | 兼容字段 |

**状态变化**:

- `APPROVE`:annotation -> `accepted`,assignment -> `accepted`,item -> `accepted`。
- `RETURN`:annotation -> `returned`,assignment -> `returned`,item -> `returned`。
- `ESCALATE`:写入 `human_reviews`,annotation 保持 `reviewing`。
- `REVISE`:仅记录审核意见,不改写 Labeler 答案。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `GET /api/reviewer/disputes`

**作用**:获取争议样本列表。当前从 `human_reviews.decision = 'escalate'` 派生。

**鉴权**:Reviewer。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/reviewer/disputes/{disputeId}/resolve`

**作用**:终审处理争议样本。

**鉴权**:Reviewer。

**Request Body**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `resolution` | string | 是 | `approve` / `reject` |
| `note` | string | 否 | 终审备注 |

**实现状态**:已实现。

**测试状态**:已通过后端打包。

### 7.3 AI Review 接口

#### `POST /api/ai-review/jobs/claim-next`

**作用**:领取下一个 pending AI 审核任务,状态迁移为 `running`。

**鉴权**:Owner / Reviewer / System Agent。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/ai-review/jobs/{jobId}/complete`

**作用**:写入 AI 审核结果,job 进入 `succeeded`,annotation 进入 `reviewing`。

**鉴权**:Owner / Reviewer / System Agent。

**Request Body**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `scores` | json | 否 | AI 分项评分 |
| `decision` | string | 是 | `PASS` / `REJECT` / `NEED_HUMAN_REVIEW` |
| `comment` | string | 否 | AI 评语 |
| `promptSnapshot` | string | 否 | Prompt 快照 |
| `responseJson` | json | 否 | 模型原始响应 |

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/ai-review/jobs/{jobId}/fail`

**作用**:AI 审核任务标记失败,job 进入 `failed`,annotation 保持当前状态。

**鉴权**:Owner / Reviewer / System Agent。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/ai-review/jobs/{jobId}/retry`

**作用**:失败的 AI 审核任务重新进入 `pending`。

**鉴权**:Owner / Reviewer / System Agent。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

### 7.4 Export 接口

#### `POST /api/exports`

**作用**:Owner 创建导出任务。任务必须已有 accepted annotation。

**鉴权**:Owner。

**Request Body**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `taskId` | string | 是 | 任务 ID |
| `format` | string | 否 | `json` / `jsonl` / `csv` / `xlsx`,默认 json |
| `mappingJson` | json | 否 | 字段映射 |

**实现状态**:已实现。

**测试状态**:已通过后端打包;Owner 导出中心前端构建通过。

#### `GET /api/exports`

**作用**:Owner 查看自己的导出任务列表。

**鉴权**:Owner。

**实现状态**:已实现。

**测试状态**:已通过后端打包;Owner 导出中心前端构建通过。

#### `POST /api/exports/{exportId}/start`

**作用**:导出任务从 `pending` 进入 `running`。

**鉴权**:Owner。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/exports/{exportId}/complete`

**作用**:导出任务从 `running` 进入 `succeeded`,并把对应任务下 accepted annotation 标记为 `exported`。

**鉴权**:Owner。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/exports/{exportId}/fail`

**作用**:导出任务从 `running` 进入 `failed`,记录失败原因。

**鉴权**:Owner。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

## 8.4 错误响应展示与独立模板绑定说明

### 通用错误响应

后端业务异常统一返回 JSON：

```json
{
  "code": "SCHEMA_NOT_FOUND",
  "message": "schema not found"
}
```

前端 `ApiError` 现在优先展示 `message`，其次展示 `code`，最后才使用 HTTP 状态文本。这样任务发布失败时不会只看到 `Not Found`，而能看到具体业务原因。

### 任务与独立模板绑定

任务发布接口仍使用原请求字段 `schemaVersionId`，绑定关系写入 `tasks.reward_rule.schemaVersionId`。后端任务列表、assignment 列表和答题页运行时读取模板时，会优先使用该字段指向的独立模板；没有该字段时再回退到旧的 `task_schema_versions.task_id` 最新版本。

实现状态：已实现。
测试状态：已通过接口直测、后端打包和前端构建。

## 8.5 任务软删除与作废状态接口变更

### 前端接口变更

| 前端方法 | 对应后端接口 | 作用 | 变更点 | 实现状态 | 测试状态 |
| --- | --- | --- | --- | --- | --- |
| `ownerApi.deleteTask(taskId)` | `DELETE /api/tasks/{taskId}` | Owner 删除任务 | 从物理删除改为软删除；任务隐藏，assignment / annotation 标记 `voided` | 已实现 | 后端打包、前端构建通过 |
| `labelerApi.listMarketTasks(query)` | `GET /api/market/tasks` | Labeler 任务市场 | 后端过滤 `tasks.deleted_at IS NULL`；卡片展示 `publishedAt` 和 `deadline` | 已实现 | 前端构建通过 |
| `labelerApi.listMyAssignments(status)` | `GET /api/assignments/mine` | Labeler 我的任务 | 后端过滤已删除任务和 `voided` assignment；响应新增 `publishedAt`、`deadline` | 已实现 | 前端构建通过 |
| `labelerApi.getAssignmentItem(id)` | `GET /api/assignments/{assignmentId}/item` | 进入答题页 | 任务已删除返回 `TASK_DELETED`；assignment 已作废返回 `ASSIGNMENT_VOIDED` | 已实现 | 后端打包通过 |
| `labelerApi.saveDraft(id, answerJson)` | `PUT /api/assignments/{assignmentId}/draft` | 保存草稿 | 已删除 / 作废任务禁止保存 | 已实现 | 后端打包通过 |
| `labelerApi.submitAnnotation(id, payload)` | `POST /api/assignments/{assignmentId}/submit` | 提交标注 | 已删除 / 作废任务禁止提交 | 已实现 | 后端打包通过 |

### 后端接口变更

| 接口 | 鉴权 | 作用 | 关键响应/错误 | 实现状态 | 测试状态 |
| --- | --- | --- | --- | --- | --- |
| `DELETE /api/tasks/{taskId}` | Owner | 软删除任务并作废相关标注 | 成功返回 `204 No Content`；任务不存在返回 `TASK_NOT_FOUND` | 已实现 | 后端打包通过 |
| `GET /api/market/tasks` | 登录用户 | 查询可认领任务 | 不返回 `deleted_at IS NOT NULL` 的任务；`publishedAt` 使用真实发布时间 | 已实现 | 后端打包通过 |
| `GET /api/assignments/mine` | Labeler | 查询我的任务 | 不返回已删除任务和 `voided` assignment；新增 `publishedAt`、`deadline` | 已实现 | 后端打包通过 |
| `GET /api/assignments/{assignmentId}/item` | Labeler | 查询作答题目 | 删除任务返回 `TASK_DELETED`；作废 assignment 返回 `ASSIGNMENT_VOIDED` | 已实现 | 后端打包通过 |
| `PUT /api/assignments/{assignmentId}/draft` | Labeler | 保存草稿 | 删除任务返回 `TASK_DELETED`；作废 assignment 返回 `ASSIGNMENT_VOIDED` | 已实现 | 后端打包通过 |
| `POST /api/assignments/{assignmentId}/submit` | Labeler | 提交标注 | 删除任务返回 `TASK_DELETED`；作废 assignment 返回 `ASSIGNMENT_VOIDED` | 已实现 | 后端打包通过 |

### 响应字段补充

`GET /api/assignments/mine` 的 `AssignmentResponse` 新增字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `publishedAt` | string | 任务发布时间，优先取 `tasks.published_at`，为空时回退任务创建时间 |
| `deadline` | string | 任务截止时间，未设置时为空字符串 |

### 状态与错误码

| 类型 | 值 | 说明 |
| --- | --- | --- |
| assignment status | `voided` | 任务被 Owner 删除后，相关认领作废 |
| annotation status | `voided` | 任务被 Owner 删除后，相关标注作废，`answer_json` 保留 |
| error code | `TASK_DELETED` | 任务已被软删除，禁止认领、保存草稿或提交 |
| error code | `ASSIGNMENT_VOIDED` | assignment 已作废，禁止继续操作 |

实现状态：已实现。
测试状态：已通过 `backend/.\mvnw.cmd -q -DskipTests package` 和 `frontend/npm.cmd run build`。


## 7. AI 预审规则 / 作业 (后端 Phase 5 预留)

前端 `frontend/src/api/aiReview.ts` 已封装,样例兜底数据见 `public/sample-datasets/ai-review-rules.json`、`ai-review-jobs.json`。

### 7.1 规则管理

#### `GET /api/ai-review/rules`

**鉴权**:Owner 角色。

**Query**:
- `status`: `enabled | disabled`(可选)
- `taskId`: string(可选,过滤作用任务)
- `page`、`pageSize`: number(可选)

**响应**:`{ items: AiReviewRule[], page, pageSize, total }`

#### `POST /api/ai-review/rules`

**Body**:`AiReviewRuleRequest`

```ts
{
  name: string;
  scopeTaskId?: string;
  promptTemplate: string;
  dimensions: { key, label, weight, maxScore }[];
  passThreshold: number;
  needHumanThreshold: number;
  maxRetry: number;
  retryBackoffSec: number;
  status: 'enabled' | 'disabled';
}
```

**响应**:`AiReviewRule`(含后端生成的 `ruleId / version / updatedAt / createdBy`)

**校验要求**:
- `dimensions` 权重之和 = 1(允许 ±0.001 容差)
- `passThreshold > needHumanThreshold`
- `scopeTaskId` 必须属于当前 Owner 或为空

#### `GET /api/ai-review/rules/{ruleId}`

返回 `AiReviewRule`。

#### `PUT /api/ai-review/rules/{ruleId}`

Body 同 `POST`,后端基于 `version` 做乐观锁。冲突返回 `409 RULE_VERSION_CONFLICT`。

#### `DELETE /api/ai-review/rules/{ruleId}`

204 No Content。

#### `POST /api/ai-review/rules/{ruleId}/toggle`

Body:`{ status: 'enabled' | 'disabled' }`

返回更新后的 `AiReviewRule`。

### 7.2 作业管理

#### `GET /api/ai-review/jobs`

**Query**:`status / ruleId / taskId / startAt / endAt / page / pageSize`

**响应**:`{ items: AiReviewJob[], page, pageSize, total }`

`AiReviewJob` 字段:`jobId, annotationId, taskId, taskTitle, ruleId?, ruleName?, status, decision?, totalScore?, attempts, lastError?, createdAt, finishedAt?`

#### `POST /api/ai-review/jobs/{jobId}/retry`

将失败的作业重新入队。返回更新后的 `AiReviewJob`(`status` 通常变回 `pending`,`attempts` +1,`lastError` 清空)。

非 `failed` 状态返回 `409 JOB_NOT_RETRYABLE`。

#### `GET /api/ai-review/results/{annotationId}`

返回 `AiReviewResult`(对齐计划书 4.4):

```ts
{
  scores: { relevance?, accuracy?, format_compliance?, safety?, [key]: number };
  total_score: number;
  decision: 'PASS' | 'NEED_HUMAN_REVIEW' | 'REJECT';
  comment: string;
  risk_flags: string[];
  evidence: string[];
}
```

不存在时返回 `404 RESULT_NOT_FOUND`。
