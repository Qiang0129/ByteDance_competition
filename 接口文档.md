# LabelHub 接口文档

更新日期：2026-06-06

## 0. 文档职责与阅读边界

- 本文档只负责维护前后端接口契约、关键数据结构、调用约定和接口级补充说明。
- `README.md` 负责项目总览、系统能力、技术架构、运行方式和协作入口。
- `阶段计划.md` 负责记录阶段性工作为什么做、覆盖范围和验收口径。
- `阶段实现.md` 负责记录对应计划已经完成什么、如何验证以及残留事项。
- `问题点归档.md` 负责记录风险、缺陷、阻塞和待处理问题。
- 如果某项内容主要描述页面演进、阶段过程或主题归并，应写入阶段文档；如果主要描述请求路径、入参、出参、状态流转字段和兼容约定，应写入本文档。

### 0.1 阅读顺序

1. 先通过 `README.md` 了解项目能力与模块边界。
2. 再通过 `阶段计划.md` 与 `阶段实现.md` 了解某个主题的阶段背景、完成度和验证情况。
3. 最后在本文档查看接口路径、数据结构、状态说明和前后端调用细节。

## 1. 通用约定

### 1.1 基础路径

| 项目 | 约定 |
|---|---|
| 前端默认 API Base URL | `/api` |
| 前端可覆盖配置 | `VITE_API_BASE_URL` |
| 后端统一前缀 | `/api` |
| 默认请求格式 | `application/json` |
| 文件上传格式 | `multipart/form-data` |
| 认证方式 | `Authorization: Bearer <accessToken>` |

### 1.2 分页响应

后端分页接口统一返回：

```json
{
  "items": [],
  "page": 1,
  "pageSize": 20,
  "total": 0
}
```

### 1.3 错误响应

后端全局异常处理统一返回：

```json
{
  "code": "ERROR_CODE",
  "message": "error message"
}
```

### 1.4 状态说明

| 状态 | 含义 |
|---|---|
| 已实现 | 当前仓库已有前端调用方法或后端 Controller/Service 实现 |
| 前端已预留 | 前端 `api` 层已有方法,但后端尚未实现对应 REST endpoint |
| 后端待实现 | 后端当前没有对应 Controller endpoint |
| 已构建验证 | 对应阶段曾通过前端或后端构建验证,但不代表已有自动化接口测试 |
| 待补自动化测试 | 当前仓库未发现接口级自动化测试用例 |

## 2. 前端接口清单

前端接口位于 `frontend/src/api/`。`apiRequest` 会自动拼接 `/api` 前缀,并在存在 token 时自动携带 Bearer Token。

### 2.1 认证接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `authApi.login(payload)` | POST | `/auth/login` | 用户登录,写入 token 和当前用户信息 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `authApi.logout()` | POST | `/auth/logout` | 用户退出,清理后端会话和本地 token | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `authApi.getCurrentUser()` | GET | `/auth/me` | 获取当前登录用户与权限 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `authApi.register(payload)` | POST | `/auth/register` | 注册用户账号 | 已实现并对接后端 | 待页面正式联调,待补自动化测试 |

### 2.2 Owner 任务接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `ownerApi.listTasks()` | GET | `/tasks` | 获取当前 Owner 的任务列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.createTask(payload)` | POST | `/tasks` | 创建任务,支持数据集、模板和分发策略配置 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.updateTask(taskId, payload)` | PUT | `/tasks/{taskId}` | 更新任务基础信息、模板、数据集和分发策略 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.updateTaskState(taskId, state)` | PUT | `/tasks/{taskId}/state` | 修改任务状态,如发布、暂停、结束 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.deleteTask(taskId)` | DELETE | `/tasks/{taskId}` | 删除当前 Owner 名下任务 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `ownerApi.listAssignableLabelers()` | GET | `/tasks/assignable-labelers` | 获取可指派的 Labeler 列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |

### 2.3 数据集接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `datasetApi.listDatasets()` | GET | `/datasets` | 获取当前 Owner 的数据集列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.createDataset(payload)` | POST | `/datasets` | 新建空数据集,任务关联可选 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.importDataset(payload)` | POST | `/datasets/import` | 上传文件并创建数据集,任务关联可选 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.importItems(datasetId, file)` | POST | `/datasets/{datasetId}/items/import` | 向已有数据集追加导入数据项 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.listItems(datasetId)` | GET | `/datasets/{datasetId}/items` | 查看数据集条目原始内容 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `datasetApi.deleteDataset(datasetId)` | DELETE | `/datasets/{datasetId}` | 删除未绑定任务且未产生作业的数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |

### 2.4 模板 Schema 接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `schemaApi.listSchemas()` | GET | `/schemas` | 获取当前 Owner 的模板版本列表,包含任务引用和默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.getSchema(versionId)` | GET | `/schemas/{versionId}` | 获取指定模板版本完整字段定义、绑定任务和默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.validate(payload)` | POST | `/schemas/validate` | 校验模板字段结构,返回错误和警告列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.createStandaloneDraft(payload)` | POST | `/schemas/draft` | 创建不绑定任务的模板草稿,可保存默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.createDraftForTask(taskId, payload)` | POST | `/tasks/{taskId}/schemas/draft` | 在指定任务下创建模板草稿,可保存默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.updateDraft(versionId, payload)` | PUT | `/schemas/{versionId}/draft` | 保存模板草稿内容、名称、描述和默认数据集 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.publish(versionId)` | POST | `/schemas/{versionId}/publish` | 发布模板版本供任务和 Renderer 使用 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.withdraw(versionId)` | POST | `/schemas/{versionId}/withdraw` | 收回已发布模板,同一版本回到草稿状态 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `schemaApi.deleteSchema(versionId)` | DELETE | `/schemas/{versionId}` | 软删除草稿模板,从模板列表隐藏且不影响已绑定任务 | 已实现并对接后端 | 已构建验证,待补自动化测试 |

### 2.5 Labeler 任务与答题接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `labelerApi.listMarketTasks(query)` | GET | `/market/tasks` | 浏览已发布且未过期的任务市场 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.claimTask(taskId)` | POST | `/tasks/{taskId}/claim` | 认领任务数据项,后端按策略生成 assignments | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.listMyAssignments(status)` | GET | `/assignments/mine` | 获取我的领取/提交/打回作业列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.getAssignmentItem(assignmentId)` | GET | `/assignments/{assignmentId}/item` | 获取答题页原题、Schema、草稿和最新提交答案 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.getDraft(assignmentId)` | GET | `/assignments/{assignmentId}/draft` | 获取指定 assignment 草稿 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.saveDraft(assignmentId, answerJson)` | PUT | `/assignments/{assignmentId}/draft` | 自动保存或覆盖草稿 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.listDrafts(query)` | GET | `/labeler/drafts` | 获取当前 Labeler 草稿箱列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.deleteDraft(assignmentId)` | DELETE | `/assignments/{assignmentId}/draft` | 删除指定 assignment 草稿,不删除作业 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.listReturnedItems(query)` | GET | `/labeler/returned-items` | 获取人工复审打回和 AI 预打回列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.submitAnnotation(assignmentId, payload)` | POST | `/assignments/{assignmentId}/submit` | 提交正式标注答案,生成 annotation revision | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerApi.reportIssue(assignmentId, payload)` | POST | `/assignments/{assignmentId}/issues` | 上报题目数据 / 模板 / 多模态等异常 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `labelerOverviewApi.getOverview()` | GET | `/labeler/overview` | 获取 Labeler 工作概览聚合数据 | 已实现并对接后端 | 已构建验证,待补自动化测试 |

### 2.6 Reviewer 预留接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `reviewerApi.getOverview(rangeDays)` | GET | `/reviewer/overview` | 审核员工作概览 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `reviewerApi.listAiReviewTasks(params)` | GET | `/reviewer/ai-review/tasks` | AI 预审-按任务聚合的任务列表(含 PASS/NEED_HUMAN/REJECT 计数) | 已实现并对接后端 | 已构建验证,待页面联调 |
| `reviewerApi.listAiReviewAnnotations(taskId, params)` | GET | `/reviewer/ai-review/tasks/{taskId}/annotations` | AI 预审-展开任务时拉取该任务下已出 AI 结论的标注明细 | 已实现并对接后端 | 已构建验证,待页面联调 |
| `reviewerApi.listBatches(params)` | GET | `/reviewer/batches` | 待审批次列表(目前仅 Reviewer 概览页用;待审队列已改用 AI 预审任务聚合接口) | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `reviewerApi.claimBatch(batchId)` | POST | `/reviewer/batches/{batchId}/claim` | 审核员领取批次 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `reviewerApi.listAnnotations(batchId, params)` | GET | `/reviewer/batches/{batchId}/annotations` | 拉取批次内待审标注(旧待审队列页下线后前端暂未直接调用) | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `reviewerApi.submitReview(annotationId, payload)` | POST | `/reviewer/annotations/{annotationId}/decision` | 提交单条人工审核结论 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `reviewerApi.listDisputes(params)` | GET | `/reviewer/disputes` | 获取争议样本列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `reviewerApi.resolveDispute(disputeId, payload)` | POST | `/reviewer/disputes/{disputeId}/resolve` | 解决争议样本 | 已实现并对接后端 | 已构建验证,待补自动化测试 |

### 2.7 Dashboard 接口

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|
| `dashboardApi.getOverview(range)` | GET | `/dashboard/overview` | 数据看板总览指标 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `dashboardApi.getTaskProgress()` | GET | `/dashboard/task-progress` | 任务进度统计 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `dashboardApi.getReviewDistribution(params)` | GET | `/dashboard/review-distribution` | 审核结果分布,支持 `range` 或 `year` | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `dashboardApi.downloadReviewDistributionReport(year)` | GET | `/dashboard/review-distribution/report` | 下载年度审核分布 CSV 报告 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `dashboardApi.getLabelerPerformance(range)` | GET | `/dashboard/labeler-performance` | 标注员绩效得分与基础信息 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `dashboardApi.getSubmissionTimeline(year)` | GET | `/dashboard/submission-timeline` | 年度提交趋势 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `dashboardApi.getRecentActivities()` | GET | `/dashboard/recent-activities` | 最近任务动态 | 已删除 | 2026-06-05 Owner 数据看板移除“近期任务申报”容器后不再调用 |
| `dashboardApi.getRoleBreakdown()` | GET | `/dashboard/role-breakdown` | 用户角色分布 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `dashboardApi.getDisputes(days)` | GET | `/dashboard/disputes` | 争议样本统计 | 已实现并对接后端 | 已构建验证,待补自动化测试 |
| `dashboardApi.listIssueFeedback(params)` | GET | `/dashboard/issue-feedback` | Owner 数据看板题目反馈列表 | 已实现并对接后端 | 已构建验证,待补自动化测试 |

## 3. 后端接口清单

后端接口位于 `backend/src/main/java/com/labelhub/backend/**Controller.java`。除登录和注册外,默认需要 Bearer Token。

### 3.1 认证 Auth

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| POST | `/api/auth/login` | 公开 | `username`, `password`, `role?` | `LoginResponse` | 登录并签发访问 token | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/auth/register` | 公开 | `username`, `password`, `role?` | `AuthUserResponse` | 注册用户并绑定角色 | 已实现 | 待页面正式联调,待补自动化测试 |
| POST | `/api/auth/logout` | 登录用户 | `Authorization` header | `204 No Content` | 退出登录并清理 token 会话 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/auth/me` | 登录用户 | `Authorization` header | `CurrentUserResponse` | 获取当前用户和权限 | 已实现 | 已构建验证,待补自动化测试 |

### 3.2 Owner 任务 Task

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/tasks` | Owner | 无 | `PageResponse<OwnerTaskResponse>` | 获取当前 Owner 的任务列表 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/tasks` | Owner | `CreateTaskRequest` | `OwnerTaskResponse` | 创建任务,可绑定数据集和已发布模板 | 已实现 | 已构建验证,待补自动化测试 |
| PUT | `/api/tasks/{taskId}` | Owner | `CreateTaskRequest` | `OwnerTaskResponse` | 更新当前 Owner 名下任务 | 已实现 | 已构建验证,待补自动化测试 |
| DELETE | `/api/tasks/{taskId}` | Owner | `taskId` | `204 No Content` | 删除当前 Owner 名下任务 | 已实现 | 已构建验证,待补自动化测试 |
| PUT | `/api/tasks/{taskId}/state` | Owner | `state` | `OwnerTaskResponse` | 更新任务状态 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/tasks/assignable-labelers` | Owner | 无 | `AssignableLabelerResponse[]` | 获取可被指派的 Labeler 用户 | 已实现 | 已构建验证,待补自动化测试 |

注意：当前 `DELETE /api/tasks/{taskId}` 后端只校验任务归属,未限制只能删除草稿或已结束任务。如后续需要保留已发布任务审计链路,应补充状态校验或软删除。

### 3.3 Labeler 任务市场与领取

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/market/tasks` | 登录用户 | `keyword?`, `taskType?`, `strategy?`, `mediaType?`, `aiReview?`, `sortBy?`, `page?`, `pageSize?` | `PageResponse<MarketTaskResponse>` | 浏览可领取任务市场 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/labeler/overview` | Labeler | 无 | `LabelerOverviewResponse` | 获取 Labeler 工作概览聚合数据 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/labeler/drafts` | Labeler | `page?`, `pageSize?` | `PageResponse<LabelerDraftResponse>` | 获取当前 Labeler 草稿箱列表 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/labeler/returned-items` | Labeler | `source?`, `page?`, `pageSize?` | `PageResponse<LabelerReturnedItemResponse>` | 获取人工复审打回和 AI 预打回列表 | 已实现 | 已构建验证,待补自动化测试 |

#### GET /api/labeler/overview

- 权限：仅 `labeler` 角色。
- 用途：驱动 `/labeler` 工作概览页,不使用静态样例兜底。
- 响应结构：
```json
{
  "heroStats": {
    "weeklySubmitted": 0,
    "reviewPassRate": 0,
    "monthlyRewardEstimate": 0,
    "monthlyRewardDetails": [
      {
        "taskId": "1",
        "assignmentId": "10",
        "annotationId": "100",
        "taskTitle": "QA 质量评估",
        "itemId": "20",
        "itemIndex": 1,
        "itemTitle": "QA 质量评估 · 第 1 题",
        "acceptedAt": "2026-06-06 20:30",
        "rewardPerItem": 0.6
      }
    ]
  },
  "kpis": {
    "activeTasks": 0,
    "submittedToday": 0,
    "returnedItems": 0,
    "avgDurationSec": 0,
    "todayReward": 0
  },
  "todayProgress": {
    "target": 30,
    "submitted": 0,
    "aiPassed": 0,
    "humanConfirmed": 0,
    "percent": 0,
    "avgDurationSec": 0,
    "estimatedFinishTime": ""
  },
  "reviewDistribution": {
    "aiPass": 0,
    "aiNeedHuman": 0,
    "aiReject": 0,
    "humanPass": 0,
    "humanReturned": 0
  },
  "recentBatches": [
    {
      "taskId": "1",
      "assignmentId": "10",
      "title": "QA 质量评估",
      "description": "请按任务模板完成当前标注批次。",
      "taskType": "QA Quality",
      "taskTypeKey": "qa_quality",
      "remainingQuota": 10,
      "totalQuota": 30,
      "deadline": "2026-06-05 24:00",
      "rewardPerItem": 0.6,
      "updatedAt": "2026-05-30 19:30"
    }
  ],
  "supportedItemTypes": [
    { "key": "text", "label": "Text" }
  ]
}
```
- 数据口径：
  - `activeTasks`：当前 Labeler 非作废、状态为 `claimed/submitted/returned` 且任务未删除的 distinct task 数。
  - `submittedToday`：今日创建或提交的非作废 annotation 数。
  - `returnedItems`：当前 Labeler `assignments.status = returned` 数。
  - `avgDurationSec`：`assignments.claimed_at -> submitted_at` 的平均秒数,无提交时为 0。
  - `reviewPassRate`：accepted assignment / 已提交 assignment,无提交时为 0。
  - `monthlyRewardEstimate`：本月通过入库的最新有效 annotation 按任务 `reward_rule.rewardPerItem` 汇总,只作展示估算；通过时间优先取 `human_review.approve` / `human_review.revise.accept` 审计时间。
  - `monthlyRewardDetails`：本月通过入库奖励明细,按题返回任务、题号、通过时间和单价；与 `monthlyRewardEstimate` 使用同一口径,按 `acceptedAt DESC, annotationId DESC` 排序。
  - `todayProgress.target`：v1 固定为 30。
  - `reviewDistribution`：最近 7 日 AI 预审结果和人工审核结果分布。

#### GET /api/labeler/drafts

- 权限：仅 `labeler` 角色。
- 用途：驱动 `/labeler/drafts` 草稿箱页,不使用静态样例兜底。
- 查询参数：
  - `page`：页码,默认 1。
  - `pageSize`：每页条数,默认 50,最大 100。
- 响应结构：
```json
{
  "items": [
    {
      "assignmentId": "10",
      "taskId": "1",
      "itemId": "100",
      "title": "QA 质量评估 - 第 18 题",
      "taskTitle": "QA 质量评估",
      "taskType": "QA Quality",
      "taskTypeKey": "qa_quality",
      "schemaVersionId": "3",
      "schemaVersion": "v3",
      "updatedAt": "2026-05-30 19:30",
      "editable": true
    }
  ],
  "page": 1,
  "pageSize": 50,
  "total": 1
}
```
- 数据口径：
  - 只返回当前登录 Labeler 自己的草稿。
  - 只返回已存在 `drafts` 记录的数据。
  - 排除 `assignments.status = voided` 和已软删除任务。
  - 按 `drafts.updated_at DESC` 排序。

#### GET /api/labeler/returned-items

- 权限：仅 `labeler` 角色。
- 用途：驱动 `/labeler/returned` 打回项页,不使用静态样例兜底。
- 查询参数：
  - `source`：`all | human_return | ai_pre_reject`,默认 `all`。
  - `page`：页码,默认 1。
  - `pageSize`：每页条数,默认 20,最大 100。
- 响应结构：
```json
{
  "items": [
    {
      "source": "HUMAN_REVIEW_RETURN",
      "sourceLabel": "人工复审打回",
      "assignmentId": "10",
      "annotationId": "31",
      "taskId": "1",
      "itemId": "100",
      "title": "QA 质量评估 - 第 18 题",
      "taskTitle": "QA 质量评估",
      "taskType": "QA Quality",
      "taskTypeKey": "qa_quality",
      "schemaVersionId": "3",
      "revisionNo": 2,
      "updatedAt": "2026-05-30 19:30",
      "reviewerName": "Reviewer A",
      "reviewRoundNo": 1,
      "humanReason": "请补充 evidence 字段。",
      "aiDecision": "REJECT",
      "aiComment": "证据不足。",
      "aiTotalScore": 62.5,
      "aiRiskFlags": ["missing_evidence"],
      "aiEvidence": ["answer.evidence 为空"],
      "actionable": true,
      "actionText": "立即修改"
    }
  ],
  "page": 1,
  "pageSize": 20,
  "total": 1
}
```
- 数据口径：
  - `HUMAN_REVIEW_RETURN`：当前 Labeler 的 latest annotation 所属 assignment 已正式 `returned`,且 latest human review 的 decision 为 `return/returned/reject/rejected`；返修窗口内列表按钮显示“立即修改”,进入答题页后通过“重新提交”再次提交。
  - `AI_PRE_REJECT`：当前 Labeler 的 latest annotation 最新 AI result 为 `REJECT`,annotation 仍为 `reviewing`,尚无人工 review 裁决,assignment 未 `returned/accepted/voided`；该类只是 AI 预审建议,不可直接重提。
  - 同一 latest annotation 已经人工打回时只显示人工复审打回,不重复显示 AI 预打回。
  - 排除已软删除任务和作废 assignment。
- 典型错误：
  - `401 UNAUTHORIZED`：未登录或 token 无效。
  - `403 FORBIDDEN`：当前用户不具备 `labeler` 角色。
  - `400 INVALID_RETURNED_ITEM_SOURCE`：`source` 不属于 `all/human_return/ai_pre_reject`。
| POST | `/api/tasks/{taskId}/claim` | Labeler | `taskId` | `AssignmentResponse` | 按分发策略认领任务数据项;模板被收回时返回 `409 SCHEMA_WITHDRAWN` | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/assignments/mine` | Labeler | `status?` | `PageResponse<AssignmentResponse>` | 获取当前 Labeler 的作业列表 | 已实现 | 已构建验证,待补自动化测试 |

### 3.4 数据集 Dataset

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/datasets` | Owner | 无 | `PageResponse<DatasetResponse>` | 获取当前 Owner 的数据集列表 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/datasets` | Owner | `name`, `kind`, `taskId?` | `DatasetResponse` | 创建空数据集,可暂不绑定任务 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/datasets/import` | Owner | `multipart: file`, `kind`, `name?`, `taskId?` | `DatasetResponse` | 上传文件并创建数据集 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/datasets/{datasetId}/items/import` | Owner | `multipart: file` | `DatasetResponse` | 向已有数据集追加导入条目 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/datasets/{datasetId}/items` | Owner | `datasetId` | `JsonNode[]` | 查看数据集条目原始 payload | 已实现 | 已构建验证,待补自动化测试 |
| DELETE | `/api/datasets/{datasetId}` | Owner | `datasetId` | `204 No Content` | 删除当前 Owner 的未使用数据集;已绑定任务或已有作业时拒绝 | 已实现 | 已构建验证,待补自动化测试 |

注意：数据集删除当前采用物理删除,仅允许删除未绑定任务且没有 assignment/annotation 的数据集。删除 `datasets` 会通过 `items.dataset_id ON DELETE CASCADE` 清理未使用条目,但不会删除 `files` 元数据。

数据集类型归一化：

- `qa_quality`:问答质量评估。
- `preference_compare`:偏好对比标注（Pairwise Preference / RLHF）。
- 后端会将 `preference / preference_ab / preference_a_b / pairwise_preference / pairwise_preference_rlhf / rlhf / 偏好对比...` 归一为 `preference_compare`。
- 前端新建数据集抽屉提供 `preference_compare · 偏好对比标注（Pairwise Preference / RLHF）` 预设；手动输入上述别名时提交前也会归一为 `preference_compare`。

### 3.5 模板 Schema

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/schemas` | Owner | 无 | `PageResponse<SchemaSummaryResponse>` | 获取当前 Owner 的模板版本列表,返回任务绑定信息和模板默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/schemas/{versionId}` | Owner | `versionId` | `SchemaVersionResponse` | 获取模板版本详情、绑定任务信息和模板默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/schemas/validate` | Owner | `name?`, `description?`, `fields` | `SchemaValidationResponse` | 校验模板字段结构,返回错误和警告列表 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/schemas/draft` | Owner | `name`, `description?`, `taskId?`, `datasetId?`, `datasetName?`, `fields` | `SchemaVersionResponse` | 创建独立模板草稿,可绑定默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/tasks/{taskId}/schemas/draft` | Owner | `name`, `description?`, `datasetId?`, `datasetName?`, `fields` | `SchemaVersionResponse` | 为任务创建模板草稿,可绑定默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| PUT | `/api/schemas/{versionId}/draft` | Owner | `name?`, `description?`, `taskId?`, `datasetId?`, `datasetName?`, `fields` | `SchemaVersionResponse` | 更新草稿模板和默认数据集 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/schemas/{versionId}/publish` | Owner | `versionId` | `SchemaVersionResponse` | 发布模板版本 | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/schemas/{versionId}/withdraw` | Owner | `versionId` | `SchemaVersionResponse` | 原地收回已发布模板,同一版本回到草稿状态 | 已实现 | 已构建验证,待补自动化测试 |
| DELETE | `/api/schemas/{versionId}` | Owner | `versionId` | `204 No Content` | 软删除草稿模板;非草稿拒绝,已绑定任务和历史标注不受影响 | 已实现 | 已构建验证,待补自动化测试 |

注意：任务发布页通过 `tasks.reward_rule.schemaVersionId` 引用已发布模板。`/api/schemas` 与 `/api/schemas/{versionId}` 现在会同时识别 `task_schema_versions.task_id` 直接绑定和 `tasks.reward_rule.schemaVersionId` 引用绑定;如果多个任务引用同一模板,当前响应展示最近更新的一条任务。模板默认数据集不新增数据库列,存放在 `task_schema_versions.schema_json` 根节点的 `datasetId/datasetName`。

删除规则：`DELETE /api/schemas/{versionId}` 只允许删除 `draft` 状态模板。已发布模板需要先调用 `withdraw` 收回为草稿。删除是软删除,写入 `task_schema_versions.deleted_at`;`GET /api/schemas` 和新任务模板下拉会隐藏该模板,但已绑定任务的认领、作答、提交和历史 annotation 查看仍可读取原模板。

### 3.6 标注答题 Annotation

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/assignments/{assignmentId}/item` | Labeler | `assignmentId` | `AssignmentItemResponse` | 获取答题页原题、Schema、位置、草稿和最新提交答案 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/assignments/{assignmentId}/draft` | Labeler | `assignmentId` | `DraftResponse` 或 `null` | 获取 assignment 草稿 | 已实现 | 已构建验证,待补自动化测试 |
| PUT | `/api/assignments/{assignmentId}/draft` | Labeler | `answerJson` | `DraftResponse` | 保存或更新草稿;模板被收回时返回 `409 SCHEMA_WITHDRAWN` | 已实现 | 已构建验证,待补自动化测试 |
| DELETE | `/api/assignments/{assignmentId}/draft` | Labeler | `assignmentId` | `204 No Content` | 删除 assignment 草稿;不删除 assignment 或 annotation | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/assignments/{assignmentId}/submit` | Labeler | `schemaVersionId`, `answerJson`, `draftVersion?` | `AnnotationResponse` | 提交标注答案并生成 annotation revision;模板被收回时返回 `409 SCHEMA_WITHDRAWN` | 已实现 | 已构建验证,待补自动化测试 |
| POST | `/api/assignments/{assignmentId}/issues` | Labeler | `category`, `description` | `ReportIssueResponse` | 上报题目反馈并写入 `issues` + `audit_logs` | 已实现 | 已构建验证,待补自动化测试 |

#### DELETE /api/assignments/{assignmentId}/draft

- 权限：仅 `labeler` 角色,且 assignment 必须属于当前登录用户。
- 语义：只删除 `drafts` 表记录,不删除 assignment、annotation 或任务领取状态。
- 幂等：assignment 校验通过但草稿不存在时,仍返回 `204 No Content`。
- 典型错误：
  - `404 ASSIGNMENT_NOT_FOUND`：assignment 不存在或不属于当前 Labeler。
  - `409 TASK_DELETED`：任务已删除。
  - `409 ASSIGNMENT_VOIDED`：assignment 已作废。

### 3.7 Owner 数据看板 Dashboard

| HTTP | 路径 | 权限 | 主要入参 | 主要返回 | 作用 | 实现状态 | 测试状态 |
|---|---|---|---|---|---|---|---|
| GET | `/api/dashboard/overview` | Owner | `range=7d\|30d\|90d` | `DashboardOverviewResponse` | 数据看板总览 KPI | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/task-progress` | Owner | 无 | `DashboardItemsResponse<TaskProgressResponse>` | 最近更新的任务进度 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/task-progress-chart` | Owner | 无 | `DashboardItemsResponse<TaskProgressResponse>` | 任务进度图表数据 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/role-users` | Owner | `role=labeler\|reviewer` | `DashboardItemsResponse<DashboardRoleUserResponse>` | 查看 KPI 角色人数对应的人员列表 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/review-distribution` | Owner | `range=7d\|30d\|90d` 或 `year=YYYY` | `ReviewDistributionResponse` | AI 与人工审核分布 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/review-distribution/report` | Owner | `year?` | `text/csv` 文件 | 下载年度审核分布报告 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/labeler-performance` | Owner | `range=7d\|30d\|90d` | `DashboardItemsResponse<LabelerPerformanceResponse>` | 标注员绩效得分与基础信息 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/submission-timeline` | Owner | `year?` | `DashboardItemsResponse<SubmissionTimelineMonthResponse>` | 指定年份 12 个月准时/延迟/缺席统计 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/recent-activities` | Owner | 无 | `DashboardItemsResponse<RecentTaskActivityResponse>` | 最近 8 条任务相关审计动态 | 已删除 | 2026-06-05 Owner 数据看板移除“近期任务申报”容器后同步删除 |
| GET | `/api/dashboard/role-breakdown` | Owner | 无 | `DashboardItemsResponse<RoleBreakdownResponse>` | 按任务类型聚合去重 Labeler 数 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/disputes` | Owner | `days=7\|14\|30` | `DisputeStatsResponse` | 争议、抽检比例和 AI/人工一致率 | 已实现 | 已构建验证,待补自动化测试 |
| GET | `/api/dashboard/issue-feedback` | Owner | `page?`, `pageSize?`, `status?` | `PageResponse<IssueFeedbackResponse>` | 查看当前 Owner 名下任务的题目反馈 | 已实现 | 已构建验证,待补自动化测试 |

#### Owner Dashboard 通用口径

- 所有 `/api/dashboard/*` 接口仅允许 `owner` 角色访问。
- 每次查询前先执行任务截止结算,保证已过截止时间的任务先进入真实状态。
- 统计范围固定为当前登录 Owner 名下、`tasks.deleted_at IS NULL` 的任务。
- 无数据时返回 0 值对象或 `items: []`,前端不使用样例数据兜底。
- 查询参数错误:
  - `INVALID_DASHBOARD_RANGE`: `range` 不属于 `7d / 30d / 90d`。
  - `INVALID_DASHBOARD_YEAR`: `year` 超出 `2000..2100`。
  - `INVALID_DASHBOARD_DAYS`: `days` 不属于 `7 / 14 / 30`。
  - `UNSUPPORTED_ROLE`: `role` 不属于 `labeler / reviewer`。

#### `GET /api/dashboard/overview`

响应字段:

- `rangeStart / rangeEnd`:统计周期。
- `kpis.activeTasks`:Owner 名下 `published / paused` 任务数。
- `kpis.labelerCount`:全平台 active 且拥有 `labeler` 角色的去重用户数。
- `kpis.pendingReview`:latest annotation 状态为 `submitted / ai_reviewing / reviewing` 的数量。
- `kpis.reviewerCount`:全平台 active 且拥有 `reviewer` 角色的去重用户数。
- `kpis.aiPassRate`:周期内 AI `PASS / 全部 AI result`。
- `kpis.avgDurationSec`:assignment `claimed_at -> submitted_at` 平均秒数。
- `kpis.deltas`:有事件时间的指标按上一等长周期计算环比,缺少可靠历史快照的指标返回 0；`labelerCount / reviewerCount` 为全平台当前角色数量,不提供环比。

#### `GET /api/dashboard/role-users`

- 权限:Owner。
- 入参:
  - `role`:必填,仅支持 `labeler / reviewer`。
- 查询口径与 overview KPI 的 `labelerCount / reviewerCount` 完全一致:
  - `users.status = 'active'`
  - `users.deleted_at IS NULL`
  - 用户拥有入参指定的 `roles.role_code`
- 响应字段:
  - `id`:用户 ID。
  - `username`:登录账号。
  - `name`:展示名称。
  - `status`:用户状态。
  - `roles`:该用户拥有的全部角色 code;多角色账号会展示多个标签。

#### `GET /api/dashboard/task-progress`

返回最近更新的 12 个任务:

- `total`:优先 items 数,其次任务 quota,最后非 voided assignment 数。
- `approved`:assignment 状态 `accepted / exported`。
- `returned`:assignment 状态 `returned`。
- `pending`: `max(total - approved - returned, 0)`。

#### `GET /api/dashboard/task-progress-chart`

- 权限:Owner。
- 返回 Owner 数据看板“任务进度”容器图表数据。
- 响应结构与 `GET /api/dashboard/task-progress` 一致，继续复用 `TaskProgressResponse`。
- 当前口径与旧任务进度接口保持一致:
  - `total`:优先 items 数,其次任务 quota,最后非 voided assignment 数。
  - `approved`:assignment 状态 `accepted / exported`。
  - `returned`:assignment 状态 `returned`。
  - `pending`: `max(total - approved - returned, 0)`。
- 前端使用该接口渲染折线图 / 柱状图切换，旧 `task-progress` 接口暂时保留。

#### `GET /api/dashboard/review-distribution`

- 入参:
  - `range`:可选,取值 `7d / 30d / 90d`;未传 `year` 时使用。
  - `year`:可选,取值 `2000..2100`;传入时按该自然年统计,优先级高于 `range`。
- AI 分布来自周期内 `ai_review_results.decision`。
- 人工通过包含 `approve / approved / revise / revised`。
- 人工打回包含 `return / returned / reject / rejected`。
- 升级争议复用 Owner 争议样本统计口径，按周期内 `decision = escalate` 的去重 annotation 数统计。
- 响应字段包含 `aiPass / aiNeedHuman / aiReject / humanPass / humanReturned / humanDisputed`。

#### `GET /api/dashboard/review-distribution/report`

- 权限:Owner。
- 入参:`year?`,未传时使用当前年份,合法范围 `2000..2100`。
- 返回:`text/csv; charset=UTF-8`,文件名形如 `review-distribution-2026.csv`。
- CSV 列:`统计周期 / 分类 / 数量 / 占比`。
- CSV 行:AI 通过、需人工复核、AI 拒绝、人工通过、打回修改、升级争议、合计。

#### `GET /api/dashboard/labeler-performance`

- 按 Labeler 聚合绩效卡片需要的基础信息。
- `score = approved / submitted`,无提交时为 0。
- `role` 使用该 Labeler 参与最多的任务 `reward_rule.taskType`,缺失时为 `通用标注`。
- 响应字段为 `labelerId / name / role / avatar / score`。
- 返回提交量最高的 10 人。

#### `GET /api/dashboard/submission-timeline`

- 返回指定年份 12 个月,无数据月份也返回 0。
- `onTime`:提交时间不晚于任务 deadline,或任务无 deadline。
- `late`:提交时间晚于任务 deadline。
- `absent`:已过 deadline 且真实存在但无提交的 assignment。

#### `GET /api/dashboard/recent-activities`

- 该接口已于 2026-06-05 删除。
- 删除原因：Owner 数据看板已移除“近期任务申报”容器，前端不再需要最近任务动态列表。
- 对应后端 `DashboardController.getRecentActivities`、`DashboardService.getRecentActivities`、`DashboardRepository.listRecentActivities` 和 `RecentTaskActivityResponse` 已移除。

#### `GET /api/dashboard/role-breakdown`

- 按 Owner 任务 `reward_rule.taskType` 聚合去重 Labeler 数。
- 缺失任务类型归为 `通用标注`。

#### `GET /api/dashboard/disputes`

- `disputed`:周期内人工 review `decision = escalate` 的去重 annotation 数。
- `resolved`:发生 escalate 后,latest 人工裁决变为 approve / return / revise 的数量。
- `pending = disputed - resolved`。
- `samplingRatio = 周期内有人工 review 的 annotation / 周期内提交 annotation`。
- `consistencyRate`:AI PASS 对人工通过、AI REJECT 对人工打回;`NEED_HUMAN_REVIEW` 不进入一致率分母。

### 3.8 后端待实现接口

> Reviewer 和 Owner Dashboard 主要后端接口已完成补齐。本节暂不保留新的待实现 Dashboard 接口。

| 模块 | HTTP | 路径 | 作用 | 当前状态 | 测试状态 |
|---|---|---|---|---|---|
| - | - | - | - | 暂无 | - |

## 4. 关键数据结构摘要

### 4.1 登录响应 `LoginResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `accessToken` | string | Bearer Token |
| `tokenType` | string | 固定为 Bearer 类型 |
| `expiresIn` | number | token 有效期秒数 |
| `user` | object | 当前用户信息 |

### 4.2 用户信息 `AuthUserResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 用户 ID |
| `username` | string | 登录账号 |
| `displayName` | string | 展示名称 |
| `roles` | string[] | 角色编码,如 owner / labeler / reviewer |

### 4.3 任务创建 `CreateTaskRequest`

| 字段 | 类型 | 说明 |
|---|---|---|
| `title` | string | 任务标题,必填 |
| `tags` | string[] | 标签 |
| `reward` | string | 奖励规则展示字段 |
| `quota` | number | 任务配额 |
| `deadline` | string | 截止时间 |
| `datasetId` | string | 关联数据集 ID |
| `strategy` | string | 分发策略: `first-come` / `assigned` / `quota` |
| `maxClaimPerUser` | number | 配额抢单模式下每人上限 |
| `assignedLabelerIds` | string[] | 指派模式下的 Labeler ID |
| `schema` | string | 模板展示标签兼容字段 |
| `schemaVersionId` | string | 已发布模板版本 ID |
| `aiReviewEnabled` | boolean | 是否启用 AI 预审 |
| `description` | string | 任务描述 |
| `taskType` | string | 任务类型 |
| `status` | string | 任务状态 |

### 4.4 模板版本 `SchemaVersionResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `versionId` | string | 模板版本 ID |
| `versionNumber` | string | 版本号展示值,如 r1 |
| `taskId` | string | 直接绑定任务 ID,可空 |
| `taskTitle` | string | 绑定任务标题,可空 |
| `name` | string | 模板名称 |
| `description` | string | 模板描述 |
| `datasetId` | string | 模板默认关联数据集 ID,可空 |
| `datasetName` | string | 模板默认关联数据集名称,可空 |
| `status` | string | `draft` / `published` |
| `fields` | JsonNode | Renderer 使用的字段定义 |
| `updatedAt` | string | 更新时间 |
| `createdBy` | string | 创建人 |

### 4.5 答题页响应 `AssignmentItemResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `assignmentId` | string | 作业 ID |
| `taskId` | string | 任务 ID |
| `taskTitle` | string | 任务标题 |
| `itemId` | string | 数据项 ID |
| `status` | string | 作业状态 |
| `editable` | boolean | 当前是否可编辑 |
| `deadline` | string | 任务截止时间 |
| `schemaVersionId` | string | 生效模板版本 ID |
| `rawPayload` | JsonNode | 原始题目数据 |
| `fields` | JsonNode | 模板字段定义 |
| `position` | object | 上一题/下一题位置 |
| `returnReason` | string | 打回原因 |
| `draft` | object | 当前草稿 |
| `latestAnnotation` | object | 最新已提交答案 |

### 4.6 模板校验响应 `SchemaValidationResponse`

| 字段 | 类型 | 说明 |
|---|---|---|
| `valid` | boolean | 是否无错误 |
| `errors` | SchemaDiagnostic[] | 阻止保存/发布的错误 |
| `warnings` | SchemaDiagnostic[] | 不阻止发布但需要关注的警告 |

`SchemaDiagnostic` 字段包括 `level`、`code`、`message`、`fieldName`。

## 5. 后续接口补齐建议

1. 优先实现 Reviewer 后端接口,使 AI 预审之后的人工审核闭环从前端 mock 变为真实数据。
2. 再实现 Dashboard 后端接口,复用现有任务、assignment、annotation、review 表做聚合统计。
3. 为认证、任务创建、任务认领、草稿保存、提交标注、模板发布补充接口级自动化测试。
4. 后续如需要完整展示“一个模板被多个任务使用”的关系,可将 Schema 响应从单个 `taskId/taskTitle` 扩展为 `boundTasks[]`。


## 6. 数据集 - 删除接口(后端预留)

### `DELETE /api/datasets/{datasetId}`

**说明**:删除数据集.前端已在 `frontend/src/api/dataset.ts` 中实现 `datasetApi.deleteDataset(id)`,等待后端实现.

**鉴权**:Owner 角色,且数据集创建者必须是当前 Owner.

**路径参数**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| datasetId | string | 是 | 数据集 ID |

**响应**:

- `204 No Content` - 删除成功(无 body)
- `401 Unauthorized` - 未登录
- `403 Forbidden` - 不是创建者
- `404 DATASET_NOT_FOUND` - 数据集不存在或不属于当前 Owner
- `409 DATASET_IN_USE` - 数据集已绑定任务或已有作业,需先解绑或删除任务

**前端调用**:

```ts
await datasetApi.deleteDataset(datasetId);
```

**前端处理**:

1. Modal.confirm 二次确认
2. 调用 API,成功后从本地 `datasets` 列表剔除并切换 `activeId` 到剩余第一项
3. 失败用 `message.error(err.message ?? '删除数据集失败...')`,不伪造成功


## 7. 状态机与审计闭环接口

### 7.1 状态机审计字段

本轮扩展 `audit_logs`,状态变化统一写入以下字段:

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| `entity_type` | string | `task` / `assignment` / `annotation` / `ai_review_job` / `export_job` |
| `entity_id` | number | 业务实体 ID |
| `operator_id` | number | 操作者用户 ID,系统任务可空 |
| `operator_role` | string | `owner` / `labeler` / `reviewer` / `system_agent` |
| `action` | string | 稳定动作名,如 `annotation.submit` |
| `from_state` | string | 迁移前状态 |
| `to_state` | string | 迁移后状态 |
| `reason` | string | 打回、失败、争议等原因 |
| `before_json` | json | 迁移前快照 |
| `after_json` | json | 迁移后快照 |
| `snapshot_json` | json | 状态机补充快照 |

### 7.2 Reviewer 接口

#### `GET /api/reviewer/overview`

**作用**:Reviewer 工作概览,返回待审批次、今日通过/打回/争议和累计审核量。

**鉴权**:Reviewer。

**Query**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `days` | number | 否 | 统计范围,默认 30 |

**实现状态**:已实现。

**测试状态**:已通过后端打包;页面构建通过。

#### `GET /api/reviewer/ai-review/tasks`

**作用**:AI 预审页「按任务」聚合列表。返回每个任务下已出 AI 结论的标注计数,供 Reviewer 先按任务浏览、再展开逐条裁决。

**鉴权**:Reviewer。

**Query**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `decision` | string | 否 | 决策过滤:`PASS` / `NEED_HUMAN_REVIEW` / `REJECT`,缺省为全部 |
| `keyword` | string | 否 | 搜索任务名称 / 任务类型 / 任务 ID / annotation ID |
| `view` | string | 否 | 视图:`pending`(默认,仅未审条目)/ `reviewed`(当前 reviewer 已审过的条目)/ `all`(两者并集) |
| `page` | number | 否 | 页码 |
| `pageSize` | number | 否 | 每页数量 |

**响应**:`PageResponse<AiReviewTaskSummary>`,`AiReviewTaskSummary` 字段:

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| `taskId` | string | 任务 ID |
| `taskTitle` | string | 任务名称 |
| `taskType` | string? | 任务类型展示名 |
| `total` | number | 该任务下已出 AI 结论的标注总数 |
| `passCount` | number | 决策为 PASS 的条数 |
| `needHumanCount` | number | 决策为 NEED_HUMAN_REVIEW 的条数 |
| `rejectCount` | number | 决策为 REJECT 的条数 |
| `pendingHuman` | number | 待人工裁决的条数(`view=reviewed` 时为 0) |
| `reviewedCount` | number | 当前 Reviewer 已审过的条数(`view=pending` 时为 0) |
| `updatedAt` | string? | 最近更新时间 |

**说明**:
- `decision` / `keyword` 过滤会同时影响任务计数和任务是否出现在列表中。
- `view=pending`(兼容旧请求):仅未删除任务、非作废 assignment/annotation、最新 revision、`annotations.status = 'reviewing'`、已有成功 AI result 且尚未产生人工审核记录的 annotation。
- `view=reviewed`:仅当前 Reviewer 已经写过 `human_reviews` 的最新 revision,不限 annotation status。
- `view=all`:`pending ∪ reviewed`(全 Reviewer 维度并集口径,适合查阅整体进度)。

**实现状态**:已实现并对接后端,支持「待审/已完成/全部」三视图。

**测试状态**:后端打包通过;前端构建通过;页面已接入并提供视图切换。

#### `GET /api/reviewer/ai-review/tasks/{taskId}/annotations`

**作用**:展开某个任务时拉取该任务下已出 AI 结论的标注明细,逐条供 Reviewer 确认通过 / 打回。

**鉴权**:Reviewer。

**Path**:

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| `taskId` | string | 任务 ID |

**Query**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `decision` | string | 否 | 决策过滤,取值同上 |
| `keyword` | string | 否 | 搜索 annotation / item / 标注员 |
| `view` | string | 否 | 视图:`pending`(默认)/ `reviewed`(我已审)/ `all` |
| `page` | number | 否 | 页码 |
| `pageSize` | number | 否 | 每页数量 |

**响应**:`PageResponse<AnnotationToReview>`(结构与 `GET /api/reviewer/batches/{batchId}/annotations` 一致,补充 `taskId` / `taskTitle` / `taskType` / `itemIndex` / `previousAnswerJson` / `aiResult.version` / `aiResult.modelName` / `reviewTimeline`;`view=reviewed` 或 `all` 视图下额外回填 `humanReason` / `humanReviewedAt` / `humanReviewerName`)。

**说明**:
- `view=pending`(兼容旧请求):仅返回该任务下待人工裁决的 AI 预审结果,筛选条件同任务聚合接口。
- `view=reviewed`:仅返回当前 Reviewer 已审过的最新 revision,响应回填最近一轮人工裁决信息(`decision` / `humanReason` / `humanReviewedAt` / `humanReviewerName`),按 `human_reviews.created_at DESC` 排序;前端用于「查看历史」只读模式。
- `view=all`:并集口径,前端按是否有 `humanReviewedAt` 区分待审与已审。
- `decision` 按 AI 结论过滤,不是人工审核结论。
- 逐条裁决仍复用 `POST /api/reviewer/annotations/{annotationId}/decision`,无需新增写接口。

**实现状态**:已实现并对接后端,支持「待审/已完成/全部」三视图。

**测试状态**:后端打包通过;前端构建通过;页面已接入并提供视图切换。

> **待审队列审核工作台(前端页面 `/reviewer/ai/:taskId`)**:Reviewer 侧栏「待审队列」即原 AI 预审页(路由仍为 `/reviewer/ai`),在任务卡片点击「进入审核」进入三栏工作台。原独立「待审队列」菜单与 `/reviewer/queue` 路由已下线,`/reviewer/queue` 重定向到 `/reviewer/ai`。该页面数据完全复用上面两个接口与逐条裁决接口,没有新增后端 REST endpoint:
> - 拉取该任务标注:`GET /api/reviewer/ai-review/tasks/{taskId}/annotations`;
> - 逐条裁决:`POST /api/reviewer/annotations/{annotationId}/decision`,`decision` 取 `APPROVE`(通过入库)/`RETURN`(打回)/`REVISE`(直接修订),使用 `reason` 携带审核意见;后端兼容旧 `note` 字段。
> - 左栏按 AI 决策(`PASS` / `NEED_HUMAN_REVIEW` / `REJECT`)本地分组展示。
>
> 工作台「多轮提交对比」与「AI 版本标识」已在前端落地,后端已在 `GET /api/reviewer/ai-review/tasks/{taskId}/annotations` 的 `AnnotationToReview` 响应中补充:
> - `itemIndex`(number):当前 annotation 所属 assignment 在同一任务、同一 Labeler 有效作业中的 1-based 题序,用于和 Labeler 答题页“第 N 题”对齐。
> - `previousAnswerJson`(object, 可空):上一轮提交的答案。`revisionNo > 1` 且该字段非空时,中栏渲染「第 N-1 轮 / 第 N 轮」双列对比,本轮相比上一轮有改动的字段绿色高亮;首轮或字段为空时回退单列答案展示。
> - `schemaFields`(array):提交时 schema 快照中的字段定义,用于 Reviewer 直接修订时复用表单渲染器。缺失时前端禁用「直接修订」。
> - `aiResult.version`(string, 可空):AI 规则/模型版本标识,如 `v2.3 · doubao-pro-32k`,展示在 AI 预审卡右上角。
> - `aiResult.modelName`(string, 可空):本次调用的模型名,备用展示。
> - `reviewTimeline`(array):按 assignment 聚合的多轮审核时间线,覆盖历史非 voided annotation revision。每轮包含 AI 预审事件和人工复审事件,标题如“第 1 轮 AI预审 / 第 1 轮人工复审 / 第 2 轮 AI预审”。每项包含 `roundNo`、`stage`、`status`、`actor`、`decision`、`score`、`comment`、`reason`、`occurredAt`。
>
> 后续若要进一步支持「批量通过/打回」,需要后端补充(当前**未在前端预留 API 方法**):
> - 批量裁决接口(建议 `POST /api/reviewer/annotations/batch-decision`,body `{ annotationIds: string[], decision, note? }`);
>
> 批量接口落地前,工作台的批量操作暂未提供;审核时间线已由 `reviewTimeline` 字段提供。

#### `GET /api/reviewer/batches`

**作用**:按任务聚合生成 Reviewer 待审批次列表。

**鉴权**:Reviewer。

**Query**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `status` | string | 否 | `pending` / `in_review` / `completed` |
| `keyword` | string | 否 | 搜索任务名或任务 ID |
| `page` | number | 否 | 页码 |
| `pageSize` | number | 否 | 每页数量 |

**实现状态**:已实现。

**测试状态**:已通过后端打包;前端 Reviewer 队列构建通过。

#### `POST /api/reviewer/batches/{batchId}/claim`

**作用**:领取审核批次。当前不新增批次表,该接口返回对应 task 派生批次信息。

**鉴权**:Reviewer。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `GET /api/reviewer/batches/{batchId}/annotations`

**作用**:获取某个批次内待审 annotation。`batchId=all` 时返回全部。

**鉴权**:Reviewer。

**Query**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `decision` | string | 否 | 按最新人工审核结论过滤 |
| `page` | number | 否 | 页码 |
| `pageSize` | number | 否 | 每页数量 |

**实现状态**:已实现。

**测试状态**:已通过后端打包;前端 AI 审核页复用该接口构建通过。

#### `POST /api/reviewer/annotations/{annotationId}/decision`

**作用**:提交人工审核结论,并通过状态机更新 annotation / assignment / item 状态。

**鉴权**:Reviewer。

**Request Body**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `decision` | string | 是 | `APPROVE` / `RETURN` / `REVISE` / `ESCALATE` |
| `reason` | string | RETURN 时必填 | 审核意见;打回原因 |
| `note` | string | 否 | 兼容旧字段;`reason` 为空时后端会使用 `note` |
| `escalate` | boolean | 否 | 兼容字段 |
| `answerJson` | object | REVISE 时必填 | Reviewer 修订后的答案 |

**状态变化**:

- `APPROVE`:annotation -> `accepted`,assignment -> `accepted`,item -> `accepted`。
- `RETURN`:annotation -> `returned`,assignment -> `returned`,item -> `returned`。
- `ESCALATE`:写入 `human_reviews`,annotation 保持 `reviewing`。
- `REVISE`:原 annotation -> `revised`,写入 `human_reviews(decision='revise', diff_json={before,after,changedFields})`;同 assignment 创建新的 latest annotation revision,新 revision 直接为 `accepted`,assignment -> `accepted`,item -> `accepted`,不重跑 AI。

**错误码**:

- `RETURN_REASON_REQUIRED`:打回缺少原因。
- `REVISION_ANSWER_REQUIRED`:直接修订缺少 `answerJson` 或不是 object。
- `REVISION_SCHEMA_MISSING`:原 annotation 缺少可用 schema 快照。
- `NO_REVISION_CHANGE`:修订答案与原答案无变化。
- `ANSWER_VALIDATION_FAILED`:修订答案未通过 schema 必填/选项/结构化校验。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `GET /api/reviewer/disputes`

**作用**:获取争议样本列表。当前从 `human_reviews.decision = 'escalate'` 派生。

**鉴权**:Reviewer。

**响应补充字段**:

- `raisedById`:升级该争议样本的 Reviewer ID。
- `escalationStageLabel`:升级发生在人工审核哪个阶段；当前只返回 `初审升级` 或 `复审升级`。
- `canResolve`:当前登录 Reviewer 是否可以终审该争议；争议提出人本人返回 `false`。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `GET /api/reviewer/disputes/{disputeId}`

**作用**:获取争议样本详情，用于三栏处理台加载题目原文、标注答案、AI 预审结果和历史审核时间线。

**鉴权**:Reviewer。

**响应说明**:

- `dispute`:争议元信息，字段结构与 `GET /api/reviewer/disputes` 单项一致。
- `annotation`:复用 `AnnotationToReviewResponse`，包含：
  - `rawPayload`
  - `answerJson`
  - `previousAnswerJson`
  - `aiResult`
  - `reviewTimeline`
  - `humanReason`
  - `humanReviewedAt`
  - `humanReviewerName`

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/reviewer/disputes/{disputeId}/resolve`

**作用**:终审处理争议样本。

**鉴权**:Reviewer。

**Request Body**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `resolution` | string | 是 | `approve` / `reject` |
| `note` | string | 否 | 终审备注 |

**规则**:

- 争议提出人不能终审自己升级的争议样本。
- 当前 Reviewer 是争议提出人时返回 `DISPUTE_SELF_RESOLVE_FORBIDDEN`。
- 争议终审不再受原审核员分配关系限制；除升级人本人外，其他 Reviewer 可接手终审。
- `reject` 仍要求提供终审打回原因。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

### 7.3 AI Review 接口

#### `POST /api/ai-review/jobs/claim-next`

**作用**:领取下一个 pending AI 审核任务,状态迁移为 `running`。

**鉴权**:Owner / Reviewer / System Agent。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/ai-review/jobs/{jobId}/complete`

**作用**:写入 AI 审核结果,job 进入 `succeeded`,annotation 进入 `reviewing`。

**鉴权**:Owner / Reviewer / System Agent。

**Request Body**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `scores` | json | 否 | AI 分项评分 |
| `decision` | string | 是 | `PASS` / `REJECT` / `NEED_HUMAN_REVIEW` |
| `comment` | string | 否 | AI 评语 |
| `promptSnapshot` | string | 否 | Prompt 快照 |
| `responseJson` | json | 否 | 模型原始响应 |

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/ai-review/jobs/{jobId}/fail`

**作用**:AI 审核任务标记失败,job 进入 `failed`,annotation 保持当前状态。

**鉴权**:Owner / Reviewer / System Agent。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/ai-review/jobs/{jobId}/retry`

**作用**:失败的 AI 审核任务重新进入 `pending`。

**鉴权**:Owner / Reviewer / System Agent。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

### 7.4 Export 接口

#### `POST /api/exports`

**作用**:Owner 创建导出任务。任务必须已有 accepted annotation。

**鉴权**:Owner。

**Request Body**:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `taskId` | string | 是 | 任务 ID |
| `format` | string | 否 | `json` / `jsonl` / `csv` / `xlsx`,默认 json |
| `mappingJson` | json | 否 | 字段映射 |

**实现状态**:已实现。

**测试状态**:已通过后端打包;Owner 导出中心前端构建通过。

#### `GET /api/exports`

**作用**:Owner 查看自己的导出任务列表。

**鉴权**:Owner。

**实现状态**:已实现。

**测试状态**:已通过后端打包;Owner 导出中心前端构建通过。

#### `POST /api/exports/{exportId}/start`

**作用**:导出任务从 `pending` 进入 `running`。

**鉴权**:Owner。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/exports/{exportId}/complete`

**作用**:导出任务从 `running` 进入 `succeeded`,并把对应任务下 accepted annotation 标记为 `exported`。

**鉴权**:Owner。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

#### `POST /api/exports/{exportId}/fail`

**作用**:导出任务从 `running` 进入 `failed`,记录失败原因。

**鉴权**:Owner。

**实现状态**:已实现。

**测试状态**:已通过后端打包。

## 8.4 错误响应展示与独立模板绑定说明

### 通用错误响应

后端业务异常统一返回 JSON：

```json
{
  "code": "SCHEMA_NOT_FOUND",
  "message": "schema not found"
}
```

前端 `ApiError` 现在优先展示 `message`，其次展示 `code`，最后才使用 HTTP 状态文本。这样任务发布失败时不会只看到 `Not Found`，而能看到具体业务原因。

### 任务与独立模板绑定

任务发布接口仍使用原请求字段 `schemaVersionId`，绑定关系写入 `tasks.reward_rule.schemaVersionId`。后端任务列表、assignment 列表和答题页运行时读取模板时，会优先使用该字段指向的独立模板；没有该字段时再回退到旧的 `task_schema_versions.task_id` 最新版本。

实现状态：已实现。
测试状态：已通过接口直测、后端打包和前端构建。

## 8.5 任务软删除与作废状态接口变更

### 前端接口变更

| 前端方法 | 对应后端接口 | 作用 | 变更点 | 实现状态 | 测试状态 |
| --- | --- | --- | --- | --- | --- |
| `ownerApi.deleteTask(taskId)` | `DELETE /api/tasks/{taskId}` | Owner 删除任务 | 从物理删除改为软删除；任务隐藏，assignment / annotation 标记 `voided` | 已实现 | 后端打包、前端构建通过 |
| `labelerApi.listMarketTasks(query)` | `GET /api/market/tasks` | Labeler 任务市场 | 后端过滤 `tasks.deleted_at IS NULL`；卡片展示 `publishedAt` 和 `deadline` | 已实现 | 前端构建通过 |
| `labelerApi.listMyAssignments(status)` | `GET /api/assignments/mine` | Labeler 我的任务 | 后端过滤已删除任务和 `voided` assignment；响应新增 `publishedAt`、`deadline` | 已实现 | 前端构建通过 |
| `labelerApi.getAssignmentItem(id)` | `GET /api/assignments/{assignmentId}/item` | 进入答题页 | 任务已删除返回 `TASK_DELETED`；assignment 已作废返回 `ASSIGNMENT_VOIDED` | 已实现 | 后端打包通过 |
| `labelerApi.saveDraft(id, answerJson)` | `PUT /api/assignments/{assignmentId}/draft` | 保存草稿 | 已删除 / 作废任务禁止保存 | 已实现 | 后端打包通过 |
| `labelerApi.submitAnnotation(id, payload)` | `POST /api/assignments/{assignmentId}/submit` | 提交标注 | 已删除 / 作废任务禁止提交 | 已实现 | 后端打包通过 |

### 后端接口变更

| 接口 | 鉴权 | 作用 | 关键响应/错误 | 实现状态 | 测试状态 |
| --- | --- | --- | --- | --- | --- |
| `DELETE /api/tasks/{taskId}` | Owner | 软删除任务并作废相关标注 | 成功返回 `204 No Content`；任务不存在返回 `TASK_NOT_FOUND` | 已实现 | 后端打包通过 |
| `GET /api/market/tasks` | 登录用户 | 查询可认领任务 | 不返回 `deleted_at IS NOT NULL` 的任务；`publishedAt` 使用真实发布时间 | 已实现 | 后端打包通过 |
| `GET /api/assignments/mine` | Labeler | 查询我的任务 | 不返回已删除任务和 `voided` assignment；新增 `publishedAt`、`deadline` | 已实现 | 后端打包通过 |
| `GET /api/assignments/{assignmentId}/item` | Labeler | 查询作答题目 | 删除任务返回 `TASK_DELETED`；作废 assignment 返回 `ASSIGNMENT_VOIDED` | 已实现 | 后端打包通过 |
| `PUT /api/assignments/{assignmentId}/draft` | Labeler | 保存草稿 | 删除任务返回 `TASK_DELETED`；作废 assignment 返回 `ASSIGNMENT_VOIDED` | 已实现 | 后端打包通过 |
| `POST /api/assignments/{assignmentId}/submit` | Labeler | 提交标注 | 删除任务返回 `TASK_DELETED`；作废 assignment 返回 `ASSIGNMENT_VOIDED` | 已实现 | 后端打包通过 |

### 响应字段补充

`GET /api/assignments/mine` 的 `AssignmentResponse` 新增字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `publishedAt` | string | 任务发布时间，优先取 `tasks.published_at`，为空时回退任务创建时间 |
| `deadline` | string | 任务截止时间，未设置时为空字符串 |

### 状态与错误码

| 类型 | 值 | 说明 |
| --- | --- | --- |
| assignment status | `voided` | 任务被 Owner 删除后，相关认领作废 |
| annotation status | `voided` | 任务被 Owner 删除后，相关标注作废，`answer_json` 保留 |
| error code | `TASK_DELETED` | 任务已被软删除，禁止认领、保存草稿或提交 |
| error code | `ASSIGNMENT_VOIDED` | assignment 已作废，禁止继续操作 |

实现状态：已实现。
测试状态：已通过 `backend/.\mvnw.cmd -q -DskipTests package` 和 `frontend/npm.cmd run build`。


## 7. AI 预审规则 / 作业 (后端 Phase 5 预留)

前端 `frontend/src/api/aiReview.ts` 已封装,样例兜底数据见 `public/sample-datasets/ai-review-rules.json`、`ai-review-jobs.json`。

### 7.1 规则管理

#### `GET /api/ai-review/rules`

**鉴权**:Owner 角色。

**Query**:
- `status`: `enabled | disabled`(可选)
- `taskId`: string(可选,过滤作用任务)
- `page`、`pageSize`: number(可选)

**响应**:`{ items: AiReviewRule[], page, pageSize, total }`

#### `POST /api/ai-review/rules`

**Body**:`AiReviewRuleRequest`

```ts
{
  name: string;
  scopeTaskId?: string;
  promptTemplate: string;
  dimensions: { key, label, weight, maxScore }[];
  passThreshold: number;
  needHumanThreshold: number;
  maxRetry: number;
  retryBackoffSec: number;
  status: 'enabled' | 'disabled';
}
```

**响应**:`AiReviewRule`(含后端生成的 `ruleId / version / updatedAt / createdBy`)

**校验要求**:
- `dimensions` 权重之和 = 1(允许 ±0.001 容差)
- 评分维度统一为 100 分制,`dimensions[].maxScore` 必须为 `100`。
- 默认阈值为 `passThreshold=80`,`needHumanThreshold=70`。
- `passThreshold` 和 `needHumanThreshold` 必须位于 `0~100`。
- `passThreshold > needHumanThreshold`
- `scopeTaskId` 必须属于当前 Owner 或为空

#### `GET /api/ai-review/rules/{ruleId}`

返回 `AiReviewRule`。

#### `PUT /api/ai-review/rules/{ruleId}`

Body 同 `POST`,后端基于 `version` 做乐观锁。冲突返回 `409 RULE_VERSION_CONFLICT`。

#### `DELETE /api/ai-review/rules/{ruleId}`

204 No Content。

#### `POST /api/ai-review/rules/{ruleId}/toggle`

Body:`{ status: 'enabled' | 'disabled' }`

返回更新后的 `AiReviewRule`。

### 7.2 作业管理

#### `GET /api/ai-review/jobs`

**Query**:`status / ruleId / taskId / startAt / endAt / page / pageSize`

**响应**:`{ items: AiReviewJob[], page, pageSize, total }`

`AiReviewJob` 字段:`jobId, annotationId, taskId, taskTitle, ruleId?, ruleName?, status, decision?, totalScore?, roundNo?, itemIndex?, itemTotal?, attempts/retryCount, lastError/errorSummary?, createdAt, availableAt?, startedAt?, finishedAt?`

- `roundNo`:当前 AI job 对应 annotation 的 `revision_no`,用于 AI Reviewer 队列展示“第 N 轮”。
- `itemIndex`:该 AI job 对应 assignment 在任务中的 1-based 序号,用于 AI Reviewer 队列展示“第 N 题”。
- `itemTotal`:任务有效 assignment 总数,用于后续展示“第 N / M 题”。

评分口径:`totalScore` 为 0~100 加权综合分。

#### `GET /api/ai-review/jobs/{jobId}/timeline`

**权限**:`owner / reviewer / ai_reviewer`

**用途**:AI Reviewer 预审队列“处理日志 / 审计”使用。通过当前 job 定位所属 assignment,返回同一题所有非 voided annotation revision 的 AI job 生命周期,用于展示多轮预审历史。

**响应**:`AiReviewJobTimelineItem[]`

```ts
interface AiReviewJobTimelineItem {
  roundNo: number;
  stage: 'queue' | 'llm' | 'verdict' | 'error';
  title: string;
  status: string;
  decision?: 'PASS' | 'NEED_HUMAN_REVIEW' | 'REJECT';
  score?: number;
  message?: string;
  occurredAt?: string;
}
```

字段说明:
- `roundNo`:annotation revision 轮次。
- `stage`:AI job 生命周期阶段。
- `queue`:进入 AI 预审队列。
- `llm`:开始调用模型。
- `verdict`:AI 预审成功并生成结论。
- `error`:AI 预审失败。
- `decision / score`:仅 `verdict` 阶段通常存在。

排序:`revision_no ASC, annotation_id ASC, job.created_at ASC, job_id ASC`。

错误:
- `401 UNAUTHORIZED`:未登录。
- `403 FORBIDDEN`:不具备 AI Review 查看权限。
- `404 AI_REVIEW_JOB_NOT_FOUND`:job 不存在。

#### `POST /api/ai-review/jobs/{jobId}/retry`

将失败的作业重新入队。返回更新后的 `AiReviewJob`(`status` 通常变回 `pending`,`attempts` +1,`lastError` 清空)。

非 `failed` 状态返回 `409 JOB_NOT_RETRYABLE`。

#### `GET /api/ai-review/results/{annotationId}`

返回 `AiReviewResult`(对齐计划书 4.4):

```ts
{
  scores: { relevance?, accuracy?, format_compliance?, safety?, [key]: number };
  total_score: number;
  decision: 'PASS' | 'NEED_HUMAN_REVIEW' | 'REJECT';
  comment: string;
  risk_flags: string[];
  evidence: string[];
}
```

评分口径:
- `scores` 中每个维度分数为 `0~100`。
- `total_score` 为按维度 `weight` 加权后的 `0~100` 综合分。
- 默认判定阈值:`total_score >= 80` 为 `PASS`,`70 <= total_score < 80` 为 `NEED_HUMAN_REVIEW`,`total_score < 70` 为 `REJECT`。
- 当前产品语义保留 LLM 自行输出最终 `decision`;后端不按阈值强制覆盖。
- 输出契约要求 `decision` 与 `total_score + passThreshold + needHumanThreshold` 保持一致:高于 PASS 阈值不应输出 `REJECT`,低于 NEED_HUMAN 阈值不应输出 `PASS`。
- 后端发现明显不一致时记录 warning 日志,但本轮不改变写入结果。

不存在时返回 `404 RESULT_NOT_FOUND`。

## 8.6 Schema 联动动作扩展：显示并必填

本轮扩展动态表单 Schema 的 `reactions[].action`：

- `visibleRequired`：条件命中时显示目标字段并设为必填。
- `required`：兼容旧模板，运行时按 `visibleRequired` 处理。
- `visible`：条件命中时显示目标字段。
- `hidden`：条件命中时隐藏目标字段。
- `optional`：条件命中时取消目标字段条件必填。

前端接口影响：

1. `SchemaReactionAction` 增加 `visibleRequired`。
2. Designer 联动动作下拉新增“显示并必填”。
3. `LabelHubFormRenderer` 运行时根据联动规则控制字段显示和必填状态。
4. Labeler 提交时过滤隐藏字段，隐藏字段不会进入 `answerJson`。

后端接口影响：

1. `POST /api/schemas/validate`、`POST /api/schemas/{id}/publish` 的 Schema 白名单允许 `visibleRequired`。
2. `PUT /api/assignments/{assignmentId}/draft` 保存草稿时过滤隐藏字段。
3. `POST /api/assignments/{assignmentId}/submit` 提交标注时过滤隐藏字段，并按 `visibleRequired` 做权威必填校验。

错误码不新增，仍沿用现有 Schema 校验错误和 `INVALID_ANSWER_JSON` / 答案校验失败响应。

### 8.6.1 联动匹配值兼容规则

`reactions[].value` 支持两种写法：

- 选项真实值，例如 `option_b`。
- 选项展示文案，例如 `不通过`。

运行时会根据来源字段 `options` 自动把 value/label 互相映射。前端 Renderer 和后端提交校验保持一致。

### 8.7 模板删除状态判定修正

`DELETE /api/schemas/{versionId}` 删除规则调整：

- 仍然只允许删除管理语义上的草稿模板。
- 后端判定与列表展示统一：只有 `published` 视为已发布，其余历史/兼容状态按草稿态处理。
- 已发布模板仍需先调用 `POST /api/schemas/{versionId}/withdraw` 收回后再删除。

### 8.7.1 模板预览切题刷新说明

- 本次无新增、删除或修改后端接口。
- 模板搭建预览继续使用 `GET /api/datasets/{datasetId}/items` 获取数据集条目的 `raw_payload`。
- 前端切换预览数据集或样本条目时，会重新挂载 `LabelHubFormRenderer`，确保 ShowItem 按当前样本的 `rawPayload` 读取内容。
- 切换预览题目时会清空仅用于预览的 `answer` 状态，不影响 Labeler 答题页草稿、提交或历史回显。

### 8.7.2 模板 Designer 拖放辅助线说明

- 本次无新增、删除或修改后端接口。
- 模板 Designer 画布拖放仍使用现有 dnd-kit 前端交互，不改变 Schema 保存、发布和字段结构。
- 前端拖放辅助线改为绿色，并优先按当前指针位于目标字段卡片上半区或下半区决定插入位置。
- 拖到已有字段上半区表示插入到该字段前；拖到下半区表示插入到该字段后；拖到画布空白区仍按原逻辑追加到当前 Tab 末尾。

## 8.8 任务认领唯一键修正

`POST /api/tasks/{taskId}/claim` 路径不变，认领语义调整：

1. 同一任务内，同一个 Labeler 不能重复认领同一个 item。
2. 不同任务之间，允许同一个 Labeler 重新认领同一个 dataset item。
3. 数据库唯一键由旧的 `(item_id, labeler_id)` 调整为 `(task_id, item_id, labeler_id)`。
4. 如果数据库仍因并发或历史结构触发重复认领冲突，接口返回 409。

错误码补充：

```text
NO_AVAILABLE_ITEM
  HTTP: 409
  场景: 当前任务没有可认领数据项，或并发/历史唯一键冲突导致选中的 item 已不可认领。
  前端建议: 提示“该任务当前没有可认领的数据项”，并刷新任务市场列表。
```

数据库迁移：

```powershell
cmd /c "mysql -uroot -p123456 < backend\src\main\resources\db\migration\V8__scope_assignment_unique_key_by_task.sql"
```
## 接口变更：模板联动、Schema Digest 与标注快照

### 后端接口

1. `GET /api/assignments/{assignmentId}/item`
   - 作用：Labeler 获取答题页所需的题目、当前 schema、草稿和最新提交答案。
   - 新增响应字段：
     - `schemaDigest`：当前 schema JSON 的 SHA-256 摘要。
     - `latestAnnotation.schemaSnapshot`：该 annotation 提交时保存的完整 schema JSON，可用于历史答案只读回放。
   - 已实现：是。
   - 已测试：构建通过，接口运行前需要先执行 V9 数据库迁移。

2. `PUT /api/assignments/{assignmentId}/draft`
   - 作用：保存 Labeler 当前 assignment 的草稿。
   - 请求体新增字段：
     - `schemaDigest?: string`
   - 行为变更：
     - 如果请求携带 `schemaDigest` 且与当前 schema 不一致，返回 `409 SCHEMA_CHANGED`。
     - 后端会按当前运行时联动过滤隐藏字段，不保存隐藏答案。
   - 已实现：是。
   - 已测试：后端构建通过。

3. `POST /api/assignments/{assignmentId}/submit`
   - 作用：提交或重新提交 annotation。
   - 请求体新增字段：
     - `schemaDigest?: string`
   - 响应体新增字段：
     - `schemaSnapshot`：本次提交使用的完整 schema JSON。
   - 行为变更：
     - `schemaDigest` 不一致时返回 `409 SCHEMA_CHANGED`。
     - 新增写入 `annotations.schema_snapshot_json`。
     - 提交前过滤隐藏字段，并按 `visibleRequired` 执行条件必填校验。
   - 已实现：是。
   - 已测试：后端构建通过；实际写入前需执行 V9 迁移。

4. `POST /api/schemas/validate`
   - 作用：校验 Designer 当前 schema。
   - 行为变更：
     - 校验前会规范化联动规则。
     - 旧 `required` 自动按 `visibleRequired` 兼容。
     - 选项 label 会自动转换为对应 option value。
   - 已实现：是。
   - 已测试：后端构建通过。

5. `POST /api/schemas/{versionId}/publish`
   - 作用：发布模板。
   - 行为变更：
     - 发布前会回写规范化后的 schema JSON，避免旧草稿继续保留 `required` 或中文 label 匹配值。
   - 已实现：是。
   - 已测试：后端构建通过。

### 前端接口

1. `labelerApi.saveDraft(assignmentId, answerJson, schemaDigest?)`
   - 作用：保存草稿。
   - 变更：第三个参数传当前 schema digest。
   - 已实现：是。
   - 已测试：`frontend/npm.cmd run build` 通过。

2. `labelerApi.submitAnnotation(assignmentId, payload)`
   - 作用：提交 annotation。
   - 变更：`payload` 支持 `schemaDigest`。
   - 已实现：是。
   - 已测试：`frontend/npm.cmd run build` 通过。

3. `SchemaField.reactions[].action`
   - 作用：定义字段联动动作。
   - 变更：`required` 保留兼容，但保存/发布时会规范化为 `visibleRequired`。
   - 已实现：是。
   - 已测试：前后端构建通过。

4. `Annotation`
   - 作用：前端使用的 annotation 类型。
   - 新增字段：
     - `schemaSnapshot?: { fields?: SchemaField[] }`
   - 已实现：是。
   - 已测试：`frontend/npm.cmd run build` 通过。

5. `AssignmentItem`
   - 作用：答题页数据类型。
   - 新增字段：
     - `schemaDigest?: string`
   - 已实现：是。
   - 已测试：`frontend/npm.cmd run build` 通过。

### 新增错误码

1. `SCHEMA_CHANGED`
   - HTTP 状态：409。
   - 触发场景：Labeler 打开的答题页使用旧 schema digest 保存草稿或提交。
   - 前端处理建议：重新拉取 assignment item，用最新 schema 继续作答。

## 增量更新：Labeler 我的任务列表作答入口

### 后端接口

1. `GET /api/assignments/mine`
   - 作用：查询当前 Labeler 已领取的 assignment 列表。
   - 响应项新增字段：
     - `hasDraft: boolean`：该 assignment 是否已有草稿。
     - `hasSubmittedAnnotation: boolean`：该 assignment 是否已有非作废正式标注记录。
   - 用途：
     - 前端区分“开始答题”和“继续答题”。
     - 前端区分未提交作答和已提交答卷查看/修改。
   - 已实现：是。
   - 已测试：前后端构建通过。

### 前端接口

1. `Assignment`
   - 作用：Labeler 已领取 assignment 的前端类型。
   - 新增字段：
     - `hasDraft?: boolean`
     - `hasSubmittedAnnotation?: boolean`
   - 已实现：是。
   - 已测试：前端构建通过。

2. `MyTasks.tsx` 操作入口
   - 作用：展示每个任务的作答入口。
   - 行为变更：
     - 新领取未作答：开始答题。
     - 已有草稿：继续答题。
     - 已提交：查看/修改答卷。
     - 打回：继续修改。
     - 通过：查看答卷。
   - 已实现：是。
   - 已测试：前端构建通过。

## 增量更新：SchemaField 语义类型

### 后端接口

1. Schema 字段 `semanticType`
   - 适用接口：
     - `GET /api/schemas/{versionId}`
     - `POST /api/schemas/draft`
     - `PUT /api/schemas/{versionId}/draft`
     - `POST /api/schemas/{versionId}/publish`
     - `GET /api/assignments/{assignmentId}/item`
   - 字段含义：标注答案的语义类型，由后端根据 `kind` 派生。
   - 可选值：
     - `text`
     - `single_choice`
     - `multi_choice`
     - `tags`
     - `json`
     - `file`
     - `llm`
     - `display`
     - `layout`
   - 已实现：是。
   - 已测试：后端 package 与前端 build 通过。

2. `latestAnnotation.schemaSnapshot.fields[]`
   - 作用：已提交答案按提交时 Schema 快照回放。
   - 行为变更：新提交 annotation 的 schema snapshot 会包含 `semanticType`。
   - 已实现：是。
   - 已测试：后端 package 与前端 build 通过。

### 前端接口

1. `SchemaField.semanticType`
   - 作用：前端 Renderer、校验、答案回显使用的答案语义类型。
   - 兼容：旧数据缺少该字段时按 `kind` fallback。
   - 已实现：是。
   - 已测试：前端 build 通过。

## 增量更新：Labeler 任务级批量提交

### 后端接口

1. `POST /api/tasks/{taskId}/assignments/submit`
   - 作用：提交当前 Labeler 在该任务下已领取的全部非作废 assignment 草稿。
   - 认证角色：Labeler。
   - 请求体：无。
   - 成功响应：
     - `taskId: string`
     - `submittedCount: number`
     - `annotationIds: string[]`
   - 行为：
     - 所有 assignment 必须有草稿并通过后端答案校验。
     - 全部通过后统一创建 annotations、schema snapshots 和 AI review jobs。
     - 任一题失败则整体失败，不产生部分 annotation。
   - 错误码：
     - `BATCH_SUBMIT_INCOMPLETE`：存在缺草稿或校验失败题目，响应携带 `invalidItems`。
     - `TASK_DELETED`：任务已删除。
     - `TASK_NOT_PUBLISHED`：任务不可提交。
     - `TASK_EXPIRED`：任务已过截止时间。
     - `SCHEMA_WITHDRAWN`：任务模板被收回且不可运行。
   - `invalidItems` 项字段：
     - `assignmentId`
     - `itemId`
     - `index`
     - `reason`
     - `fieldErrors`
   - 已实现：是。
   - 已测试：后端 package 通过。

### 前端接口

1. `labelerApi.submitTaskAssignments(taskId)`
   - 作用：最后一题点击“提交”时统一提交该任务全部草稿。
   - 返回类型：`BatchSubmitResponse`。
   - 已实现：是。
   - 已测试：前端 build 通过。

2. `AnswerPage.tsx` 作答入口
   - 行为变更：
     - 非最后一题隐藏提交按钮。
     - 下一题前强制保存草稿。
     - 当前题校验失败仍允许跳转，但进度圆点标红。
     - 最后一题显示“提交”，成功后回到“我的任务”。
   - 已实现：是。
   - 已测试：前端 build 通过。

## 增量更新：续期任务批量提交修正

### `POST /api/tasks/{taskId}/assignments/submit`

- 认证角色：Labeler。
- 请求体：无。
- 成功响应字段保持不变：
  - `taskId: string`
  - `submittedCount: number`
  - `annotationIds: string[]`
- 批量提交口径：
  - 仅提交当前仍需要提交的 `claimed` assignment，以及返修窗口内的 `returned` assignment。
  - `submitted`、`ai_reviewing`、`reviewing`、`accepted`、`exported` 表示已提交、审核中或已完成，批量提交时跳过，不再返回 `ASSIGNMENT_NOT_EDITABLE`。
  - `returned` 但返修窗口已过期时仍返回 `409 RETURN_REWORK_EXPIRED`。
  - `claimed` 缺少草稿或草稿答案校验失败时仍返回 `409 BATCH_SUBMIT_INCOMPLETE`，响应携带 `invalidItems`。
- `submittedCount=0` 语义：
  - 本次没有新的 `claimed` 或可返修 `returned` 题需要提交。
  - 常见于续期任务中当前 Labeler 的该任务题目已经提交、进入审核或已通过。
  - 这是成功响应，不会重复生成 annotation revision。
- 状态机约束：
  - 不重新提交已经进入 AI 预审、人工审核或已 accepted 的历史 annotation。
  - 不改变单题提交、截止结算、返修窗口和续期发布语义。
## 增量更新：AI Agent、AI 审核规则与后台接口

### 角色

1. `ai_reviewer`
   - 用途：登录 `/ai-reviewer` 后台，查看/管理 AI 规则、Job 队列、结果详情和失败重试。
   - Demo 账号：`ai_reviewer / ai_reviewer123`。
2. `system_agent`
   - 用途：独立 Agent Worker 调后端接口领取和回写 AI job。
   - Demo 账号：`system_agent / agent123`。
   - 不开放前端登录入口。

### AI 规则接口

1. `GET /api/ai-review/rules`
   - Query：`status?`、`taskId?`、`page?`、`pageSize?`。
   - 响应：`PageResponse<AiReviewRuleResponse>`。
2. `POST /api/ai-review/rules`
   - 角色：`owner`、`reviewer`、`ai_reviewer`。
   - 请求字段：`name`、`scopeTaskId?`、`promptTemplate`、`dimensions[]`、`passThreshold`、`needHumanThreshold`、`maxRetry`、`retryBackoffSec`、`status`。
3. `GET /api/ai-review/rules/{ruleId}`
4. `PUT /api/ai-review/rules/{ruleId}`
5. `DELETE /api/ai-review/rules/{ruleId}`
   - 语义：软删除规则，不影响已创建 job 的 `rule_snapshot_json`。
6. `POST /api/ai-review/rules/{ruleId}/toggle`
   - 请求：`{ "status": "enabled" | "disabled" }`。

### AI Job 接口

1. `GET /api/ai-review/jobs`
   - Query：`status?`、`ruleId?`、`taskId?`、`page?`、`pageSize?`。
   - 响应字段包含：`jobId`、`annotationId`、`taskId`、`taskTitle`、`ruleId`、`ruleName`、`status`、`decision`、`totalScore`、`roundNo`、`itemIndex`、`itemTotal`、`retryCount`、`errorSummary`、`createdAt`、`availableAt`、`startedAt`、`finishedAt`。
2. `POST /api/ai-review/jobs/claim-next`
   - 角色：`system_agent`、`ai_reviewer`、`reviewer`、`owner`。
   - 行为：领取下一条 pending job，状态 `pending -> running`。
   - 响应：`job`、`taskId`、`taskTitle`、`annotationId`、`runToken`、`rawPayload`、`answerJson`、`schemaSnapshot`、`ruleSnapshot`。
3. `POST /api/ai-review/jobs/{jobId}/complete`
   - 请求：`runToken`、`scores`、`totalScore?`、`decision`、`comment`、`riskFlags`、`evidence`、`promptSnapshot`、`responseJson`、`modelName`、`latencyMs`。
   - 行为：job `running -> succeeded`，annotation `ai_reviewing -> reviewing`。
4. `POST /api/ai-review/jobs/{jobId}/fail`
   - 请求：`{ "runToken": "...", "errorSummary": "..." }`。
   - 行为：未超重试上限时 `failed -> pending`；超限后 annotation 进入 `reviewing` 人工兜底。
   - 错误码：`AI_REVIEW_RUN_STALE`：Agent 携带的 `runToken` 已失效，旧执行回写被拒绝。
5. `POST /api/ai-review/jobs/{jobId}/cancel`
   - 角色：`ai_reviewer`、`reviewer`、`owner`。
   - 请求：`{ "reason": "manual cancel and requeue" }`。
   - 行为：仅允许 running job，状态 `running -> pending`，清空旧 `runToken` 并重新排队。
6. `POST /api/ai-review/jobs/{jobId}/retry`
   - 行为：失败 job 手动 `failed -> pending`。
7. `GET /api/ai-review/jobs/{jobId}/timeline`
   - 角色：`ai_reviewer`、`reviewer`、`owner`。
   - 行为：按当前 job 所属 assignment 聚合同一题多轮 AI job 生命周期日志。
   - 响应：`roundNo`、`stage`、`title`、`status`、`decision`、`score`、`message`、`occurredAt`。
8. `GET /api/ai-review/results/{annotationId}`
   - 响应：`scores`、`total_score`、`decision`、`comment`、`risk_flags`、`evidence`、`promptSnapshot`、`responseJson`、`modelName`、`latencyMs`。

### 任务接口扩展

1. `POST /api/tasks`、`PUT /api/tasks/{taskId}`
   - 请求新增：`aiReviewRuleId?: string`。
   - 响应新增：`aiReviewRuleId?: string`、`aiReviewRuleName?: string`。
   - 规则：任务启用 AI 预审并发布时，必须能解析到一条启用中的 AI 规则。
2. `GET /api/market/tasks`
   - `aiReviewRule` 返回任务绑定规则名称，不再使用写死文案。

### Agent 配置

1. 服务目录：`agent/`。
2. 启动前环境变量：
   - `LABELHUB_BACKEND_BASE_URL=http://localhost:8080`
   - `LABELHUB_AGENT_USERNAME=system_agent`
   - `LABELHUB_AGENT_PASSWORD=agent123`
   - `LABELHUB_LLM_BASE_URL=https://www.pqapi.store/v1`
   - `LABELHUB_LLM_API_KEY=<本机环境变量，不写入仓库>`
   - `LABELHUB_LLM_MODEL=gpt-5.5`
   - `LABELHUB_LLM_REASONING_EFFORT=high`
   - `LABELHUB_LLM_WIRE_API=responses`
3. 构建：`agent/.\mvnw.cmd -q -DskipTests package`。
## AI 模型配置接口

### GET /api/ai-review/model-config

- 权限：`owner` / `ai_reviewer`
- 用途：读取当前 active 模型配置。未配置时返回 `204 No Content`。
- 响应字段：
  - `configId`
  - `providerName`
  - `notes`
  - `licenseUrl`
  - `apiBaseUrl`
  - `useFullUrl`
  - `modelName`
  - `reasoningEffort`
  - `wireApi`
  - `workerConcurrency`
  - `apiKeyMask`
  - `status`
  - `updatedAt`
  - `updatedBy`
- 安全约束：不返回明文 `apiKey`。

### PUT /api/ai-review/model-config

- 权限：`owner` / `ai_reviewer`
- 用途：创建或更新当前 active 模型配置。
- 请求体：
```json
{
  "providerName": "OpenAI-compatible",
  "notes": "test quota",
  "licenseUrl": "https://example.com/license",
  "apiBaseUrl": "https://example.com/v1",
  "useFullUrl": false,
  "modelName": "gpt-5.5",
  "reasoningEffort": "high",
  "wireApi": "responses",
  "workerConcurrency": 3,
  "apiKey": "sk-..."
}
```
- 语义：
  - `apiKey` 非空：使用 `LABELHUB_MODEL_CONFIG_SECRET` AES-GCM 加密后保存。
  - `apiKey` 空：保留旧密钥；首次保存必须提供。
  - 仅支持 `wireApi = responses`。
  - `reasoningEffort` 仅支持 `low / medium / high / xhigh`；历史数据中若仍有 `minimal`，前端编辑页会兼容映射为 `low`，保存后会落为 `low`。
  - `workerConcurrency` 缺省为 `3`，合法范围 `1-10`，Agent 启动时读取并创建对应数量 worker。
- 常见错误码：
  - `AI_MODEL_API_KEY_REQUIRED`
  - `AI_MODEL_CONFIG_SECRET_REQUIRED`
  - `INVALID_AI_MODEL_CONFIG`
  - `INVALID_AI_MODEL_URL`
  - `INVALID_AI_MODEL_REASONING_EFFORT`
  - `INVALID_AI_MODEL_WIRE_API`
  - `INVALID_AI_MODEL_WORKER_CONCURRENCY`

### GET /api/ai-review/model-configs

- 权限：`owner` / `ai_reviewer`
- 用途：模型配置页面读取全部配置。
- 响应：`AiModelConfigResponse[]`
- 排序：`active` 配置优先，其余按 `updatedAt DESC`。
- 响应字段同 `GET /api/ai-review/model-config`，不返回明文 `apiKey`。

### POST /api/ai-review/model-configs

- 权限：`owner` / `ai_reviewer`
- 用途：新建一个模型配置。
- 请求体同 `PUT /api/ai-review/model-config`。
- 语义：
  - `apiKey` 必填。
  - 当前没有 active 配置时，新建配置自动为 `active`。
  - 当前已有 active 配置时，新建配置为 `inactive`，需调用 activate 接口启用。
  - `reasoningEffort` 仅支持 `low / medium / high / xhigh`。
  - `workerConcurrency` 缺省为 `3`，合法范围 `1-10`。
- 常见错误码：
  - `AI_MODEL_API_KEY_REQUIRED`
  - `AI_MODEL_CONFIG_SECRET_REQUIRED`
  - `INVALID_AI_MODEL_CONFIG`
  - `INVALID_AI_MODEL_URL`
  - `INVALID_AI_MODEL_REASONING_EFFORT`
  - `INVALID_AI_MODEL_WIRE_API`
  - `INVALID_AI_MODEL_WORKER_CONCURRENCY`

### PUT /api/ai-review/model-configs/{configId}

- 权限：`owner` / `ai_reviewer`
- 用途：更新指定模型配置，保留该配置当前 `status`。
- 请求体同 `PUT /api/ai-review/model-config`。
- 语义：
  - `apiKey` 为空表示沿用该配置原密钥。
  - `apiKey` 非空则重新加密保存并更新 `apiKeyMask`。
  - active 配置更新后，Agent runtime 将读取更新后的 active 配置；Agent 并发数仍需重启 Agent 生效。
- 常见错误码：
  - `AI_MODEL_CONFIG_NOT_FOUND`
  - `AI_MODEL_API_KEY_REQUIRED`
  - `AI_MODEL_CONFIG_SECRET_REQUIRED`
  - `AI_MODEL_CONFIG_CHANGED`

### DELETE /api/ai-review/model-configs/{configId}

- 权限：`owner` / `ai_reviewer`
- 用途：删除 inactive 配置。
- 成功响应：`204/200` 空响应。
- 约束：
  - active 配置禁止删除，返回 `409 AI_MODEL_CONFIG_ACTIVE`。
  - 不存在返回 `404 AI_MODEL_CONFIG_NOT_FOUND`。

### POST /api/ai-review/model-configs/{configId}/activate

- 权限：`owner` / `ai_reviewer`
- 用途：启用指定配置作为当前 Agent runtime 配置。
- 响应：启用后的 `AiModelConfigResponse`。
- 语义：
  - 先将目标配置置为 `active`，确认成功后把其它 active 配置置为 `inactive`。
  - 不存在返回 `404 AI_MODEL_CONFIG_NOT_FOUND`。
  - 并发数变更需要重启 Agent 才生效。

### POST /api/ai-review/model-config/models

- 权限：`owner` / `ai_reviewer`
- 用途：后端代理请求模型供应商 `/models`，用于页面“获取模型列表”。
- 请求体：
```json
{
  "apiBaseUrl": "https://example.com/v1",
  "useFullUrl": false,
  "apiKey": "sk-..."
}
```
- 说明：
  - `apiKey` 为空时使用当前已保存配置中的密钥。
  - `useFullUrl = false` 时请求 `{apiBaseUrl}/models`。
  - `useFullUrl = true` 时如地址以 `/responses` 结尾，会推导为同级 `/models`。
- 响应：
```json
{
  "modelIds": ["gpt-5.5"]
}
```

### GET /api/ai-review/model-config/runtime

- 权限：仅 `system_agent`
- 用途：独立 Agent 领取 Job 前读取运行时 LLM 配置。
- 响应字段：
  - `apiBaseUrl`
  - `useFullUrl`
  - `modelName`
  - `reasoningEffort`
  - `wireApi`
  - `workerConcurrency`
  - `apiKey`
- 安全约束：该接口会返回明文 `apiKey`，只能由 `system_agent` 调用；`owner` / `ai_reviewer` / `reviewer` / `labeler` 均不可调用。
- Agent 并发语义：`workerConcurrency` 是单 Agent 进程的并发 worker 数，修改后需要重启 Agent 生效。

### AI Agent 并发执行

- 单个 Agent 进程启动时读取 `GET /api/ai-review/model-config/runtime`。
- 有 active 模型配置时优先使用 `workerConcurrency`。
- 无 active 模型配置时使用本地环境变量 `LABELHUB_AGENT_CONCURRENCY`，默认 `3`。
- 每个 worker 独立执行 `claimNext -> LLM review -> complete/fail`。
- 后端 `claim-next` 使用 `FOR UPDATE SKIP LOCKED` 并继续校验 `runToken`，避免重复领取和过期回写。
### POST /api/ai-review/jobs/batch-delete

- 权限：`owner` / `reviewer` / `ai_reviewer`
- 禁止：`system_agent`、`labeler`
- 用途：AI Reviewer / Owner 在 Job 队列批量删除已结束的 AI 审核 Job。
- 请求体：
```json
{
  "jobIds": ["35", "36"]
}
```
- 响应：
```json
{
  "deletedCount": 2,
  "deletedJobIds": ["35", "36"]
}
```
- 状态约束：
  - 允许删除：`failed`、`succeeded`
  - 不允许删除：`pending`、`running`
- 说明：
  - 删除前写入审计日志 `ai_review.delete`。
  - 删除 `ai_review_jobs` 后，关联 `ai_review_results` 依赖外键级联删除。
  - 运行中 Job 应先使用“取消并重新排队”，不能直接批量删除。
- 常见错误码：
  - `INVALID_AI_REVIEW_JOB_IDS`
  - `INVALID_AI_REVIEW_JOB_ID`
  - `AI_REVIEW_JOB_NOT_FOUND`
  - `AI_REVIEW_JOB_NOT_DELETABLE`
  - `FORBIDDEN`
## AI 预审规则权限说明

- AI 预审规则属于 Owner 端任务发布和质量策略配置能力。
- 以下规则接口仅允许 `owner` 调用：
  - `GET /api/ai-review/rules`
  - `POST /api/ai-review/rules`
  - `GET /api/ai-review/rules/{ruleId}`
  - `PUT /api/ai-review/rules/{ruleId}`
  - `DELETE /api/ai-review/rules/{ruleId}`
  - `POST /api/ai-review/rules/{ruleId}/toggle`
- `ai_reviewer` 不再允许维护规则；AI Reviewer 后台只保留作业概览、Job 队列、失败重试/取消、结果查看和模型配置。
- `reviewer`、`labeler`、`system_agent` 均不能管理规则。



## 9. Owner 端「人工审核」只读接口

> 对应《项目实施计划书》4.5 / 4.6 / 5.1。Owner 视角是只读跟踪 + 审计,与 Reviewer 端 `/api/reviewer/*` 裁决接口严格分离。
> 本节 5 个 GET 接口已由后端实现,前端不再回落 `owner-review-*.json` 样例数据;接口失败时显示真实错误和重试入口。

### 9.1 接口清单

1. `GET /api/reviews/overview?days=30`
   - 权限:仅 `owner`。
   - 作用:Owner 审核总览 KPI,包含待审、今日通过/打回、争议、抽检覆盖、双审一致率、返工率、审核员当日负载。
   - 查询前会触发任务截止结算。

2. `GET /api/reviews/tasks?status=all&keyword=&page=1&pageSize=20`
   - 权限:仅 `owner`。
   - 作用:按任务聚合人工审核结果进度。
   - `status`: `all / in_progress / completed / has_disputes`。
   - `pageSize` 最大 100。

3. `GET /api/reviews/tasks/{taskId}/annotations?page=1&pageSize=50&status=all`
   - 权限:仅 `owner`。
   - 作用:任务详情抽屉条目明细,只读。
   - `taskId` 必须属于当前 Owner 且任务未软删除。
   - `status`: `all / reviewing / approved / returned / revised / disputed`。

4. `GET /api/reviews/reviewers`
   - 权限:仅 `owner`。
   - 作用:返回系统内所有 active 且拥有 `reviewer` 角色的审核员,用于审计日志筛选。

5. `GET /api/reviews/audit-log?days=7&taskId=&reviewerId=&action=&page=1&pageSize=20`
   - 权限:仅 `owner`。
   - 作用:当前 Owner 任务相关的人工审核员操作日志。
   - 默认显示全部 Reviewer 的人工审核相关日志。
   - `reviewerId`:指定审核员 ID 时,只返回该审核员的日志。
   - `/owner/review` 页面摘要卡使用 `days=7`。
   - “查看全部日志”Drawer 使用 `days=365&pageSize=20`,按页加载当前审核员筛选下的日志。

6. `POST /api/reviews/batch-decision`
   - 本轮不实现。
   - Owner 人工审核页不做通过/打回/修订裁决,Reviewer 工作台仍是唯一人工裁决入口。

### 9.2 响应结构

`OwnerReviewOverview`:
- `rangeDays`:统计窗口天数。
- `pendingAnnotations`:当前 Owner 任务下 latest annotation 状态为 `submitted / ai_reviewing / reviewing` 的数量。
- `todayApproved`:今日人工审核通过数量,包含 `approve / approved / revise / revised`。
- `todayReturned`:今日人工审核打回数量,包含 `return / returned / reject / rejected`。
- `todayDisputes`:当前未解决或周期内发生过 `escalate` 的争议数量。
- `samplingCoverage`:周期内有人工 review 的 annotation / 周期内提交 annotation。
- `consistencyRate`:AI PASS 对人工通过、AI REJECT 对人工打回;`NEED_HUMAN_REVIEW` 不进分母。
- `returnRate`:周期内人工打回 / 周期内人工 review。
- `reviewerWorkloads`:审核员当日负载,字段为 `reviewerId / reviewerName / pending / reviewedToday / avgDurationSec / consistencyRate`;其中 `pending` 当前固定为 0,前端不展示。

`OwnerReviewTaskRow`:
- `taskId / taskTitle / taskType`:任务标识。
- `totalAnnotations`:任务下 latest 非 voided annotation 数。
- `approvedCount / returnedCount / inProgress / disputes`:任务审核进度计数。
- `samplingRatio`:任务下有人工 review 的 annotation / totalAnnotations。
- `totalReviewed`:任务下已有人工 review 的 annotation 数。
- `reviewerNames`:该任务最近参与人工 review 的审核员名称。
- `deadline`:任务截止时间,格式 `yyyy-MM-dd HH:mm`。
- `aiReviewEnabled`:任务是否启用 AI 预审。
- `updatedAt`:最近更新时间。

`OwnerReviewAnnotation`:
- `annotationId / itemId / labelerName / submittedAt`:标注基础信息。
- `status`: `reviewing / approved / returned / revised / disputed`。
- `aiDecision`: `PASS / REJECT / NEED_HUMAN_REVIEW`,无 AI 结果时为空。
- `lastDecision`: `APPROVE / RETURN / REVISE / ESCALATE`,无人工审核时为空。
- `lastReviewer`:最近一次人工审核员。
- `sampling`:是否已有人工 review。
- `updatedAt`:最近更新时间。

`OwnerReviewReviewer`:
- `reviewerId`:审核员用户 ID。
- `reviewerName`:审核员展示名称。

`ReviewAuditLogEntry`:
- `logId / entityType / entityId`:审计日志与实体标识。
- `taskId / taskTitle`:关联任务。
- `operatorName / operatorRole`:操作者。
- `action`:稳定动作名。
- `fromState / toState`:状态迁移。
- `reason`:打回或争议原因。
- `occurredAt`:发生时间。

### 9.3 错误码

- `UNAUTHORIZED`:缺少或无效 token。
- `FORBIDDEN`:非 Owner 访问。
- `REVIEW_TASK_NOT_FOUND`:任务不存在、已软删除或不属于当前 Owner。
- `INVALID_REVIEW_DAYS`:统计天数不在 1-365。
- `INVALID_REVIEW_STATUS`:任务/条目状态过滤非法。

### 9.4 前端行为

- `/owner/review` 页面不再显示「演示模式」。
- 页面不再展示“三阶段审核进度”和阶段筛选。
- 任务表用通过、打回、进行中胶囊展示审核结果。
- 审计日志下拉框显示「全部审核员」和系统内所有 Reviewer。
- 审计日志摘要卡高度与左侧任务表卡片协调,超出内容不撑高页面。
- 审计日志下拉框右侧提供“查看全部日志”按钮,右侧 Drawer 展示近 365 天分页日志。
- 主接口失败时清空当前数据并显示 Alert + 重试。
- 任务详情接口失败时抽屉内显示错误 Alert + 重试,不读取样例 JSON。
- 审核员负载卡只显示今日已审、平均耗时和一致率,不展示无法真实计算的个人待审数量。



## 10. 题目反馈真实闭环接口

> 对应《项目实施计划书》4.3 / 4.5 / 5.1 audit_logs 表。Labeler 在答题时遇到题目数据 / 模板 / 多模态资源异常时主动上报,后端写入 `issues` 表并同步写 `audit_logs`;Owner 在数据看板「题目反馈」抽屉中只读查看。

### 10.1 Labeler 上报题目问题

- HTTP:`POST /api/assignments/{assignmentId}/issues`
- 前端方法:`labelerApi.reportIssue(assignmentId, payload)`
- 权限:仅 `labeler`。
- 归属校验:`assignmentId` 必须属于当前登录 Labeler;非本人 assignment 返回 `ASSIGNMENT_NOT_FOUND`。
- 状态校验:assignment 非 `voided`,任务未软删除。
- 写入语义:
  - 新增一条 `issues.status = open` 记录。
  - 同步写 `audit_logs`,其中 `entity_type = assignment`,`action = assignment.issue_reported`,`reason = description`,`snapshot` 包含 `issueId / category / taskId / itemId`。

请求体 `ReportIssueRequest`:

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `category` | `'data_error' \| 'schema_mismatch' \| 'media_broken' \| 'duplicate' \| 'sensitive' \| 'other'` | 是 | 问题分类 |
| `description` | string | 是 | 去空白后长度 5~500 |

响应体 `ReportIssueResponse`:

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `issueId` | string | 题目反馈 ID |
| `assignmentId` | string | 作业 ID |
| `taskId` | string | 任务 ID |
| `itemId` | string | 题目 ID |
| `category` | string | 问题分类 |
| `description` | string | 问题描述 |
| `status` | string | 当前固定为 `open` |
| `createdAt` | string | 创建时间,格式 `yyyy-MM-dd HH:mm` |

常见错误:

- `401 UNAUTHORIZED`:未登录或 token 无效。
- `403 FORBIDDEN`:非 Labeler 角色。
- `404 ASSIGNMENT_NOT_FOUND`:assignment 不存在或不属于当前 Labeler。
- `400 INVALID_ISSUE_CATEGORY`:分类不在枚举范围内。
- `400 INVALID_ISSUE_DESCRIPTION`:描述长度不符合 5~500。
- `409 TASK_DELETED` / `409 ASSIGNMENT_VOIDED`:任务已软删或 assignment 已作废。

### 10.2 Owner 查询题目反馈

- HTTP:`GET /api/dashboard/issue-feedback`
- 前端方法:`dashboardApi.listIssueFeedback(params)`
- 权限:仅 `owner`。
- 数据范围:只返回当前 Owner 名下任务的反馈,且任务未软删除。
- 排序:`issues.created_at DESC, issues.id DESC`。

查询参数:

| 字段 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `page` | number | `1` | 页码,小于 1 时按 1 处理 |
| `pageSize` | number | `20` | 每页数量,最大 100 |
| `status` | `'open' \| 'all'` | `open` | 本轮只写入 `open`;`all` 为后续处理流预留 |

响应体:`PageResponse<IssueFeedbackResponse>`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `items` | `IssueFeedbackResponse[]` | 当前页反馈 |
| `page` | number | 当前页 |
| `pageSize` | number | 每页数量 |
| `total` | number | 满足条件的反馈总数 |

`IssueFeedbackResponse`:

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `issueId` | string | 题目反馈 ID |
| `assignmentId` | string | 作业 ID |
| `taskId / taskTitle` | string | 任务 ID 与标题 |
| `itemId` | string | 题目 ID |
| `labelerId / labelerName` | string | 上报 Labeler |
| `category / categoryLabel` | string | 分类枚举和中文展示 |
| `description` | string | 问题描述 |
| `status` | string | 当前固定为 `open` |
| `createdAt` | string | 创建时间,格式 `yyyy-MM-dd HH:mm` |

前端失败态:

- Owner 数据看板「题目反馈」不使用静态样例兜底。
- 接口加载中显示 Skeleton / List loading。
- 接口失败显示 Alert + 重试按钮。
- 无反馈显示 Empty。



## 11. 导出中心真实导出接口

> 对应《项目实施计划书》4.6 数据交付。导出中心已切换为真实执行流程。

### 11.1 前端方法

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 | 测试状态 |
| --- | --- | --- | --- | --- | --- |
| `exportApi.listExports()` | GET | `/exports` | 导出任务列表 | 已实现并对接后端 | 已构建验证 |
| `exportApi.getOverview()` | GET | `/exports/overview` | 导出 KPI 概览 | 已实现并对接后端 | 已构建验证 |
| `exportApi.getTaskOptions(taskId)` | GET | `/exports/tasks/{taskId}/options` | 获取任务可导出字段配置 | 已实现并对接后端 | 已构建验证 |
| `exportApi.createExport(payload)` | POST | `/exports` | 创建并立即执行导出 | 已实现并对接后端 | 已构建验证 |
| `exportApi.downloadExport(exportId)` | GET | `/exports/{exportId}/download` | 带 Token 下载导出文件 | 已实现并对接后端 | 已构建验证 |

### 11.2 `GET /exports/overview` 响应体 `ExportOverview`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `totalJobs` | number | 当前 Owner 导出任务总数 |
| `succeededJobs` | number | 成功数 |
| `failedJobs` | number | 失败数 |
| `monthlyExportedItems` | number | 本月导出条目总数 |
| `monthlyFileSizeBytes` | number | 本月导出文件总大小(字节) |

### 11.3 `GET /exports/tasks/{taskId}/options` 响应体 `ExportTaskOptions`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `taskId` | string | 任务 ID |
| `taskTitle` | string | 任务标题 |
| `acceptedCount` | number | 当前任务可导出的 accepted 条目数 |
| `fields` | `ExportFieldOption[]` | 可选导出字段 |

`ExportFieldOption`:

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `key` | string | 字段唯一键 |
| `label` | string | 默认显示名 |
| `source` | string | 字段来源(system / answer / item) |
| `path` | string | 取值路径 |
| `defaultSelected` | boolean | 是否默认勾选 |

### 11.4 `POST /exports` 请求体 `ExportRequest`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `taskId` | string | 任务 ID |
| `format` | string | `json` / `jsonl` / `csv` / `xlsx` |
| `mappingJson` | object? | 导出列配置，格式为 `{ columns: [{ key, label, source, path }] }` |

### 11.5 `ExportJob` 响应体

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `exportId` | string | 导出任务 ID |
| `taskId` | string | 任务 ID |
| `taskTitle` | string? | 任务标题 |
| `format` | string | 导出格式 |
| `status` | string | `pending / running / succeeded / failed` |
| `progress` | number | 导出进度 |
| `exportedCount` | number? | 导出条目数 |
| `fileSizeBytes` | number? | 导出文件大小(字节) |
| `downloadUrl` | string? | 下载路径 |
| `mappingJson` | object? | 字段映射快照 |
| `errorSummary` | string? | 失败摘要 |
| `createdAt` | string | 创建时间 |
| `updatedAt` | string | 更新时间 |
| `createdBy` | string? | 创建人 |

### 11.6 `GET /exports/{exportId}/download`

- 鉴权:Owner,且 exportId 必须属于当前用户。
- 前置条件:`export_jobs.status = 'succeeded'` 且文件存在于本地存储目录。
- 响应:直接返回文件流,`Content-Disposition: attachment; filename="..."`。
- 前端处理:使用 `fetch + Authorization + Blob` 下载,不再依赖 `window.open`。

### 11.7 兼容接口

- `POST /exports/{exportId}/start`
- `POST /exports/{exportId}/complete`
- `POST /exports/{exportId}/fail`

说明:这三个接口作为兼容保留,主流程已由 `POST /exports` 同步执行接管。



## 12. 任务截止结算与续期发布

### 12.1 状态机补充

- 任务状态仍为:`draft / published / paused / ended`。
- 新增合法流转:`ended -> published`,仅 Owner 可通过编辑任务并设置未来截止时间触发,用于续期发布剩余题。
- 截止结算由系统触发:
  - `published/paused -> ended`,审计 action=`task.settle_by_deadline`。
  - 有效草稿自动提交:`claimed/returned -> submitted`,审计 action=`annotation.auto_submit_by_deadline`。
  - 无草稿或草稿校验失败:`claimed/returned -> voided`,审计 action=`assignment.release_by_deadline`。

### 12.2 `GET /api/tasks`

- 权限:Owner。
- 行为:返回前会先结算所有已到截止时间的任务。
- 响应字段不变,但超过截止时间的任务会真实返回 `state='ended'`。

### 12.3 `GET /api/market/tasks`

- 权限:登录用户。
- 行为:返回前会先执行截止结算。
- 返回范围:
  - `published` 且未截止的任务。
  - 因 `task.settle_by_deadline` 自动结束的任务,用于 Labeler 侧展示“已截止”。
- `MarketTaskResponse` 新增字段:
  - `state`:任务状态。
  - `expired`:是否已截止或已截止结算。
  - `claimable`:当前是否可领取。
  - `statusLabel`:展示文案,如 `可认领 / 已截止 / 配额已满`。
- 前端失败态:任务广场不再使用静态样例降级;接口失败显示 Alert + 重试。

### 12.4 `POST /api/tasks/{taskId}/claim`

- 权限:Labeler。
- 行为:领取前先执行截止结算。
- 截止或未续期任务返回:
  - HTTP `409`
  - code=`TASK_EXPIRED`
  - message=`task deadline has passed`
- 续期后的任务只会从剩余 `pending` 题中创建新 assignment。

### 12.5 `PUT /api/tasks/{taskId}`

- 权限:Owner。
- 用途:编辑任务,也用于 ended 任务续期发布。
- 从 `ended` 续期为 `published` 时:
  - `deadline` 必须晚于当前时间。
  - 只恢复任务和最新 schema 状态。
  - 不恢复旧的 `voided` assignment。
- 常见错误:
  - `400 INVALID_DEADLINE`:续期发布未设置未来截止时间。
  - `409 INVALID_STATE_TRANSITION`:状态机不允许的流转。

### 12.6 `PUT /api/tasks/{taskId}/state`

- 权限:Owner。
- 用途:轻量状态流转。
- 不支持 `ended -> published` 续期,因为该接口没有 deadline 字段。
- 如需续期,应调用 `PUT /api/tasks/{taskId}` 并提交未来截止时间。
- 常见错误:
  - `400 DEADLINE_REQUIRED`:尝试通过轻量状态接口续期 ended 任务。

### 12.7 截止结算数据口径

- 有 draft 且通过当前 schema 完整校验:
  - 创建 annotation revision。
  - 删除 draft。
  - assignment 置 `submitted`。
  - item 置 `submitted`。
  - 按任务 AI 规则进入 AI 预审或人工审核。
- 无 draft 或 draft 校验失败:
  - 删除 draft。
  - assignment 置 `voided`。
  - item 回到 `pending`。
- 已提交、审核中、已通过的 annotation 不会被截止结算回滚。
## 13. 人工复审打回返修窗口

### 13.1 数据库字段

- `assignments.resubmit_deadline`:人工复审正式打回后的返修截止时间。
- 默认窗口:Reviewer 执行 `RETURN` 后 48 小时。
- 仅人工复审打回写入该字段;AI 预打回不写入。

### 13.2 `POST /api/reviewer/annotations/{annotationId}/decision`

- 当 `decision=RETURN`:
  - `reason` 必填,缺失返回 `RETURN_REASON_REQUIRED`。
  - annotation / assignment / item 更新为 `returned`。
  - assignment 写入 `resubmit_deadline = now + 48 hours`。
  - audit snapshot 记录 `resubmitDeadline`。
- 当 `decision=APPROVE` 或 `REVISE` 最终 accepted:
  - assignment 的 `resubmit_deadline` 会清空。

### 13.3 `GET /api/labeler/returned-items`

人工复审打回项新增返回字段:

- `resubmitDeadline`:返修截止时间,格式 `yyyy-MM-dd HH:mm`,无则为空字符串。
- `editable`:当前是否仍处于返修窗口内。
- `expiredReason`:返修窗口过期时为 `RETURN_REWORK_EXPIRED`,否则为空字符串。
- `actionable`:等同于人工打回且 `editable=true`。
- `actionText`:窗口内为“立即修改”,窗口过期为“返修已过期”,AI 预打回为“等待人工复审”。

### 13.4 `GET /api/assignments/{assignmentId}/item`

`AssignmentItemResponse` 新增字段:

- `resubmitDeadline`:人工打回返修截止时间。
- `lockReason`:不可编辑原因,可能值包括:
  - `RETURN_REWORK_EXPIRED`
  - `TASK_EXPIRED`
  - `TASK_NOT_PUBLISHED`
  - `ASSIGNMENT_NOT_EDITABLE`

权限口径:

- 普通 `claimed/submitted` 仍要求任务 `published` 且未超过任务 deadline。
- `returned` 且 `resubmitDeadline` 未过期时,允许编辑,即使任务已经 `ended`。

### 13.5 `PUT /api/assignments/{assignmentId}/draft`

- 返修窗口内的 `returned` assignment 允许保存草稿。
- 返修窗口过期返回:
  - HTTP `409`
  - code=`RETURN_REWORK_EXPIRED`
  - message=`returned assignment rework window has expired`

### 13.6 `POST /api/assignments/{assignmentId}/submit`

- 返修窗口内的 `returned` assignment 允许单题重提。
- 前端仅在从 `/labeler/returned` 点击“立即修改”进入答题页时显示“重新提交”;按钮位于答题卡底部上一题和下一题之间。
- 从“我的任务”、草稿箱、直接 URL 或答题页上一题/下一题/进度点进入同一 returned 题时,不显示返修“重新提交”按钮。
- “重新提交”不受“最后一题才显示提交按钮”的普通批量提交规则限制。
- 重提后创建新的 annotation revision,assignment/item 进入 `submitted`,并按任务 AI 规则进入 AI 预审或人工审核。
- 重提成功后清空 `assignments.resubmit_deadline`。
- 返修窗口过期返回 `409 RETURN_REWORK_EXPIRED`。

### 13.7 截止结算语义

- 截止结算继续处理普通未完成 assignment。
- 人工复审 returned assignment 不自动作废、不释放 item。
- AI 预打回不是正式打回,不获得返修窗口,仍需等待人工复审裁决。



## 12. AI 审核仪表盘预留接口

> 对应《项目实施计划书》4.4 / 4.6。前端已在 `frontend/src/api/aiDashboard.ts` 实现调用,后端待实现。
> 后端未就绪时,前端从 `GET /ai-review/jobs` 本地计算回落。

### 12.1 前端方法

| 前端方法 | HTTP | 调用路径 | 作用 | 实现状态 |
| --- | --- | --- | --- | --- |
| `aiDashboardApi.getKpi()` | GET | `/ai-review/dashboard/kpi` | KPI 概览 | 前端已预留,后端待实现 |
| `aiDashboardApi.getDecisionDistribution()` | GET | `/ai-review/dashboard/decisions` | 决策分布(环状图) | 前端已预留,后端待实现 |
| `aiDashboardApi.getDailyTrend(days)` | GET | `/ai-review/dashboard/trend?days=7` | 每日趋势(折线图) | 前端已预留,后端待实现 |
| `aiDashboardApi.getTaskVolumes()` | GET | `/ai-review/dashboard/tasks` | 任务维度审核量(柱状图) | 前端已预留,后端待实现 |

### 12.2 响应结构

#### `GET /ai-review/dashboard/kpi` → `AiDashboardKpi`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `totalJobs` | number | 总作业数 |
| `succeededJobs` | number | 成功数 |
| `failedJobs` | number | 失败数 |
| `pendingJobs` | number | 排队中 |
| `runningJobs` | number | 执行中 |
| `needHumanJobs` | number | 需人工复核 |
| `passRate` | number | AI 通过率 0-1 |
| `avgDurationSec` | number | 平均处理时长(秒) |

#### `GET /ai-review/dashboard/decisions` → `AiDecisionDistribution[]`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `decision` | `'PASS' \| 'NEED_HUMAN_REVIEW' \| 'REJECT'` | 决策类型 |
| `count` | number | 数量 |

#### `GET /ai-review/dashboard/trend?days=7` → `AiDailyTrend[]`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `date` | string | 日期 YYYY-MM-DD |
| `total` | number | 当日总处理量 |
| `pass` | number | PASS 数 |
| `needHuman` | number | NEED_HUMAN 数 |
| `reject` | number | REJECT 数 |

#### `GET /ai-review/dashboard/tasks` → `AiTaskVolume[]`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `taskId` | string | 任务 ID |
| `taskTitle` | string | 任务标题 |
| `total` | number | 总审核量 |
| `pass` | number | PASS 数 |
| `needHuman` | number | NEED_HUMAN 数 |
| `reject` | number | REJECT 数 |

### 12.3 后端实现建议

所有接口鉴权要求 `Owner` 或 `AI Reviewer` 角色。数据来源:

- `kpi`:从 `ai_review_jobs` 表按 status/decision 聚合 COUNT;`avgDurationSec` 从 `started_at → finished_at` 计算 AVG。
- `decisions`:从 `ai_review_jobs` 表 WHERE status='success' GROUP BY decision。
- `trend`:从 `ai_review_jobs` 表 WHERE created_at >= NOW() - INTERVAL {days} DAY GROUP BY DATE(created_at)。
- `tasks`:从 `ai_review_jobs j JOIN annotations a ON a.id = j.annotation_id JOIN assignments s ON s.id = a.assignment_id JOIN tasks t ON t.id = s.task_id` GROUP BY t.id。

### 12.4 前端兜底

后端 4 个接口全部失败时,前端调用 `GET /ai-review/jobs?page=1&pageSize=200`,用 `computeKpiFromJobs / computeDecisionsFromJobs / computeTrendFromJobs / computeTaskVolumesFromJobs` 四个本地函数计算回落数据,顶部显示「本地计算模式 · 仪表盘接口未就绪」Tag。


---

### Labeler LLM 标注助手

#### 任务字段扩展

`OwnerTask` / `CreateOwnerTaskRequest` 新增字段:

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `llmAssistEnabled` | boolean | 否 | 是否为标注员开启 LLM 标注助手,默认 false。开启后 Labeler 答题页右下角出现 AI 标注助手浮按钮 |

**实现状态**:
- 前端 Owner 任务发布弹窗 Switch 已落地,默认关闭。
- 前端 `AnswerPage` 根据 `AssignmentItem.llmAssistEnabled` 决定是否渲染助手面板。
- 后端沿用 `aiReviewEnabled` 模式,把字段写入 `tasks.reward_rule.llmAssistEnabled`,无需 schema migration。

#### `POST /api/labeler/assignments/{assignmentId}/assistant`

**作用**:Labeler 在标注页向 LLM 提问,获取参考思路。AI 标注助手不会直接写入答案字段。

**鉴权**:Labeler。

**Path**:

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| `assignmentId` | string | 当前答题作业 ID |

**Body**:

```json
{
  "question": "请帮我解释一下这道题要做什么",
  "history": [
    { "role": "user", "content": "上一轮问题" },
    { "role": "assistant", "content": "上一轮回答" }
  ]
}
```

**校验**:
- `question` 必填,1~500 字。
- `history` 可选,后端只取最近 8 条,并裁剪单条长度。
- assignment 必须属于当前 Labeler。
- assignment 必须可编辑。
- 当前任务必须启用 `llmAssistEnabled`。

**响应**:`text/event-stream`。

```text
event: delta
data: 这道题

event: delta
data: 需要先理解 prompt

event: done
data: DONE
```

失败时:

```text
event: error
data: LLM_ASSIST_REQUEST_FAILED
```

**说明**:
- 后端复用 `AiModelConfigRepository.findActive()` 取活跃模型,调用 OpenAI 兼容 `/chat/completions`,并强制 `stream: true`。
- 普通 baseUrl 请求 `{apiBaseUrl}/chat/completions`;fullUrl 若以 `/responses` 结尾会替换为 `/chat/completions`。
- 系统提示自动拼接 schema 字段定义 + raw_payload 的非媒体字段(媒体 URL mask)+ 安全规则:仅给参考思路,不能直接给答案。
- 任务级 `llmAssistEnabled === true` 才允许调用,否则返回 403 `LLM_ASSIST_DISABLED`。
- 不新增助手消息表;每次调用仅写 `audit_logs.snapshot_json` 摘要,包含 taskId/itemId/questionPreview/answerPreview/tokensUsed/modelName/status。
- 页面刷新后不恢复历史对话,前端只传当前 Drawer 本地会话 history。
- 前端只使用流式接口,不做普通 JSON 回退。
- 前端收到 `event: done` 后应立即视为本次回答完成并释放输入状态,不需要继续等待底层 HTTP 连接自然关闭。
- 客户端主动断开或超时中断时,后端不会再渲染普通错误页;审计摘要状态可为 `cancelled`。
- 安全配置只鉴权初始 `REQUEST`;`StreamingResponseBody` 产生的 Servlet `ASYNC/ERROR` 内部分发会放行,避免响应已提交后被二次鉴权拒绝。
- 前端使用 `react-markdown + remark-gfm` 渲染 AI Markdown:支持标题、列表、表格、任务列表、删除线、引用、链接、行内代码和代码块;接口仍传输模型原始文本片段。
- 前端渲染时启用 `skipHtml`,模型输出中的原始 HTML 不会作为 HTML 执行。
- 前端会兼容模型输出的压缩表格格式,例如 `表头 || --- || 行` 会在展示前归一化为合法 Markdown 表格;代码块内文本不会被归一化。

**错误码**:
- `LLM_ASSIST_DISABLED`:任务未启用 LLM 标注助手。
- `INVALID_ASSISTANT_QUESTION`:问题为空或超过长度限制。
- `LLM_ASSIST_MODEL_CONFIG_REQUIRED`:未配置 active 模型。
- `LLM_ASSIST_REQUEST_FAILED`:模型请求失败或返回空内容。

**实现状态**:已实现。

#### `GET /api/labeler/assignments/{assignmentId}/assistant/history`(规划中)

**作用**:拉取当前 assignment 的助手对话历史,用于刷新页面后恢复上下文。

**实现状态**:不实现。当前方案不持久化完整助手对话,仅保留审计摘要。

#### `GET /api/assignments/{assignmentId}/item` 字段补充

响应新增:

```json
{
  "llmAssistEnabled": true
}
```

说明:
- 来源为当前 assignment 所属 task 的 `tasks.reward_rule.llmAssistEnabled`。
- `false` 或缺失时,前端不渲染 LLM 标注助手入口。

#### Owner 任务接口字段补充

`POST /api/tasks`、`PUT /api/tasks/{taskId}` 请求体和 `GET /api/tasks` 响应补充:

```json
{
  "llmAssistEnabled": false
}
```

说明:
- 默认 `false`。
- 后端写入 `tasks.reward_rule.llmAssistEnabled`,不新增数据库字段。


### Owner 数据集类型输入交互说明

- 新建数据集抽屉的“数据集类型”输入框默认空白。
- 点击或聚焦输入框时展示内置预设:
  - `qa_quality · 问答质量评估`
  - `preference_compare · 偏好对比标注（Pairwise Preference / RLHF）`
- 用户也可以继续输入自定义类型。
- 接口请求体字段仍为 `kind`,语义不变;提交前前端会将 `pairwise_preference / pairwise_preference_rlhf / rlhf / 偏好对比...` 归一为 `preference_compare`。

### 登录/注册页中文化与字体说明

- 本轮只调整 `/login` 翻转卡片页面展示层:
  - 登录/注册可见文案改为简体中文。
  - 角色和演示账号按钮只改 label,不改 role value。
  - 字体使用苹方优先;无苹方环境加载自托管 `@fontsource/noto-sans-sc` 500/600 字重。
- 认证接口不变:
  - `POST /api/auth/login`
  - `POST /api/auth/register`
  - `GET /api/auth/me`
- 请求体、响应体、错误码和权限规则均无变化。

### Owner 审计日志高度同步包装层加固说明

- 本轮仍仅调整 `/owner/review` 前端布局，接口不变。
- 继续使用现有人工审核页接口，不新增接口、不新增字段、不改变筛选参数。
- 前端变化仅限布局状态:
  - 任务表卡片高度测量值写入右侧 `Space` 容器 CSS 变量。
  - 桌面端右侧审计日志所在 `.ant-space-item` 使用该高度。
  - 审计日志卡片填满该包装层。
- 请求体、响应体、错误码和权限规则均无变化。

### Owner 审计日志底部对齐任务表修正说明

- 本轮仍仅调整 `/owner/review` 前端布局，接口不变。
- 继续使用现有人工审核页接口，不新增接口、不新增字段、不改变筛选参数。
- 前端高度同步口径修正为:
  - 测量左侧任务表卡片底部坐标。
  - 测量右侧审计日志容器顶部坐标。
  - 桌面端审计日志高度 = `任务表卡片 bottom - 审计日志容器 top`。
- 这样右侧“审核员当日负载”仍自然展示，审计日志底部与左侧任务表卡片底部对齐。
- 请求体、响应体、错误码和权限规则均无变化。

### 登录框液态玻璃视觉说明

- 本轮只调整 `/login` 翻转卡片视觉:
  - 登录/注册卡片改为半透明液态玻璃效果。
  - 输入框、角色选择框和演示账号按钮同步玻璃化。
  - 主登录/注册按钮保留蓝色主操作识别。
- 认证接口不变:
  - `POST /api/auth/login`
  - `POST /api/auth/register`
  - `GET /api/auth/me`
- 请求体、响应体、错误码和权限规则均无变化。

### Owner 任务发布配额默认值说明

- 本轮只调整 Owner 任务发布抽屉前端表单默认值:
  - 新建任务时,“配额”默认取当前所选数据集的 `itemCount`。
  - 切换“关联数据集”或由 Schema 模板带出数据集时,新建任务配额同步为对应数据集条数。
  - 编辑已有任务时保留后端返回的 `quotaTotal`,不自动覆盖。
- 接口不变:
  - `POST /api/tasks`
  - `PUT /api/tasks/{taskId}`
  - `GET /api/tasks`
- 请求体、响应体、错误码和权限规则均无变化;前端仍按原字段提交 `quota`。

### Labeler 认领后任务市场刷新说明

- 本轮只调整前端状态刷新策略,接口不变:
  - `POST /api/tasks/{taskId}/claim`
  - `GET /api/market/tasks`
- Labeler 认领成功后,前端不再本地执行 `remainingQuota - 1`。
- 前端会立即重新请求 `GET /api/market/tasks?page=1&pageSize=50`,用后端返回的任务市场列表更新:
  - `remainingQuota`
  - `totalQuota`
  - `claimedByMe`
  - `claimable`
  - `statusLabel`
- 原因:任务市场配额应以后端按 `assignments` 聚合后的真实值为准,不同分发策略下一次认领影响的 assignment 数不一定是 1。
- 请求体、响应体、错误码和权限规则均无变化。

### Labeler 任务市场状态标签展示说明

- 本轮只调整任务市场卡片展示口径,接口不变:
  - `GET /api/market/tasks`
  - `POST /api/tasks/{taskId}/claim`
- 前端继续使用现有字段:
  - `assignStrategy`
  - `claimedByMe`
  - `claimable`
  - `expired`
  - `statusLabel`
- 卡片顶部状态标签规则:
  - 已认领:显示“已认领”。
  - 未认领且可领取:显示分发策略“先到先得 / 指派 / 配额抢单”。
  - 未认领且不可领取:显示后端 `statusLabel`,如“已截止 / 已暂停 / 配额已满”。
- 卡片下方不再重复显示分发策略;任务详情 Drawer 仍保留分发策略。
- 请求体、响应体、错误码和权限规则均无变化。

### Owner 人工审核页审计日志高度跟随任务表说明

- 本轮仅调整 `/owner/review` 前端布局，接口不变。
- 继续使用现有人工审核页接口:
  - `GET /api/owner/review/overview`
  - `GET /api/owner/review/tasks`
  - `GET /api/owner/review/audit-log`
  - `GET /api/owner/review/reviewers`
- 审计日志筛选、分页 Drawer 和数据字段均无变化。
- 前端仅新增组件内部高度测量状态:
  - 使用 `ResizeObserver` 监听左侧任务表卡片实际高度。
  - 桌面端把该高度应用到右侧“审计日志”卡片。
  - 移动端或未测得高度时回退自然高度。
- 请求体、响应体、错误码和权限规则均无变化。
## 2026-06-01 Owner Review 明细接口题号字段补充

### `GET /api/reviews/tasks/{taskId}/annotations?page=1&pageSize=50&status=all`

- 本轮向后兼容新增响应字段 `itemIndex`。
- `itemIndex`: number，当前标注员在该任务有效作业中的 1-based 题序，计算口径与 Reviewer 工作台 `AnnotationToReview.itemIndex` 一致。
- 原有 `annotationId`、`itemId`、`labelerName` 等字段保留不变。
- 前端 Owner 人工审核详情抽屉使用 `itemIndex` 主展示“第 N 题”，`labelerName` 单独展示标注员姓名。
- 无请求参数变化，无后端状态机和审核流程变化。

## 2026-06-02 Owner Review 明细接口默认排序说明

### `GET /api/reviews/tasks/{taskId}/annotations?page=1&pageSize=50&status=all`

- 请求参数、响应字段、错误码和权限规则不变。
- 默认返回顺序调整为按题号升序:
  - 主排序: `itemIndex ASC`。
  - 次排序: `annotationId ASC`，用于同题号异常重复时保持稳定顺序。
- 该排序在后端分页前生效，保证跨分页也按任务内题号顺序展示。
- 前端 Owner 人工审核详情抽屉默认展示为题号升序；如需按最近更新时间查看，后续应显式新增排序参数，而不是复用当前默认顺序。

## 2026-06-02 Owner Review 审计日志题目上下文与同题时间线

### `GET /api/reviews/audit-log`

- 请求参数、分页结构、权限规则不变。
- `ReviewAuditLogEntry` 新增响应字段:
  - `assignmentId`: string，可为空；同题上下文锚点。
  - `annotationId`: string，可为空；当前日志直接或间接关联的 annotation。
  - `itemId`: string，可为空。
  - `itemIndex`: number，可为空；任务内 1-based 题序。
  - `labelerName`: string，可为空。
  - `itemTitle`: string；正常为“任务名 · 第 N 题”，题号缺失时为“任务名 · 题号缺失”。
- 原 `entityType / entityId` 保留，用于追踪和调试。

### `GET /api/reviews/audit-log/{logId}/item-timeline`

- 作用：获取某条审计日志对应题目的全链路日志。
- 权限：Owner；只能访问当前 Owner 名下任务的日志。
- 路径参数：
  - `logId`: 审计日志 ID。
- 响应：`ReviewAuditItemTimeline`
  - `assignmentId / taskId / taskTitle / annotationId / itemId / itemIndex / labelerName / itemTitle`
  - `items`: `ReviewAuditLogEntry[]`
- 排序：`items` 按 `occurredAt DESC, logId DESC`。
- 同题聚合规则：
  - `assignment`: `audit_logs.entity_id = assignments.id`
  - `annotation`: `annotations.assignment_id = assignments.id`
  - `ai_review_job`: `ai_review_jobs.annotation_id -> annotations.assignment_id`
  - `human_review`: `human_reviews.annotation_id -> annotations.assignment_id`
- 错误：
  - `REVIEW_AUDIT_LOG_NOT_FOUND`: 日志不存在或不属于当前 Owner。
  - `REVIEW_AUDIT_ITEM_NOT_FOUND`: 日志无法解析到题目上下文。

### 前端展示补充

- `/owner/review` 审计日志列表主文案优先使用 `itemTitle`；缺失时按 `taskTitle + itemIndex` 生成题号化兜底，不再把 `entityType / entityId` 作为主展示。
- `entityType / entityId` 仅在悬停详情和完整时间线抽屉中作为追踪字段展示。
- 同一 `logId` 的同题时间线在页面生命周期内缓存；已缓存或正在加载时不会重复请求。
## 2026-06-02 Owner 审计日志悬停层宽度自适应说明

- 本轮仅调整 `/owner/review` 前端样式，接口无变化。
- 继续使用现有接口：
  - `GET /api/reviews/audit-log`
  - `GET /api/reviews/audit-log/{logId}/item-timeline`
- 请求参数、响应字段、错误码和权限规则均无变化。
- 前端变化仅为审计日志 Popover 普通摘要按内容自适应；加载中、失败和空状态保留较小可读宽度，并保留长文本换行约束。
## 2026-06-02 Owner 全部审计日志抽屉空白修正说明

- 本轮仅调整 `/owner/review` 前端样式，接口无变化。
- 继续使用现有接口：
  - `GET /api/reviews/audit-log`
  - `GET /api/reviews/audit-log/{logId}/item-timeline`
- 请求参数、响应字段、错误码、权限规则和分页策略均无变化。
- 前端变化仅为“全部审计日志”抽屉中日志区按内容高度收缩，分页器紧跟日志内容。
## 2026-06-02 Owner 审计日志悬停层右侧展示说明

- 本轮仅调整 `/owner/review` 前端 Popover 位置，接口无变化。
- 继续使用现有接口：
  - `GET /api/reviews/audit-log`
  - `GET /api/reviews/audit-log/{logId}/item-timeline`
- 请求参数、响应字段、错误码、权限规则和缓存策略均无变化。
- 前端变化仅为同题日志悬停层优先显示在日志行右侧，以利用抽屉右侧空白区域。
## 2026-06-02 Owner 审计日志 Popover 右侧锚点修正说明

- 本轮仅调整 `/owner/review` 前端悬停触发锚点，接口无变化。
- 继续使用现有接口：
  - `GET /api/reviews/audit-log`
  - `GET /api/reviews/audit-log/{logId}/item-timeline`
- 请求参数、响应字段、错误码、权限规则和缓存策略均无变化。
- 前端变化仅为“全部审计日志”抽屉中的日志行触发元素按内容宽度收缩，使 `rightTop` Popover 优先显示在日志文字右侧空白区域。
## 2026-06-02 Owner 同题日志摘要折叠说明

- 本轮仅调整 `/owner/review` 前端 Popover 摘要展示，接口无变化。
- 继续使用现有接口：
  - `GET /api/reviews/audit-log`
  - `GET /api/reviews/audit-log/{logId}/item-timeline`
- 请求参数、响应字段、错误码、权限规则和缓存策略均无变化。
- `item-timeline` 仍返回完整同题审计日志；前端仅在悬浮摘要中折叠同一次人工审核产生的 `annotation` 与 `assignment` 双实体日志，详情抽屉仍展示完整底层日志。

## 2026-06-02 Owner AI 同题日志摘要折叠说明

- 本轮仅调整 `/owner/review` 前端 Popover 摘要展示，接口无变化。
- 继续使用现有接口：
  - `GET /api/reviews/audit-log`
  - `GET /api/reviews/audit-log/{logId}/item-timeline`
- 请求参数、响应字段、错误码、权限规则和缓存策略均无变化。
- `item-timeline` 仍返回完整同题审计日志；前端在悬浮摘要中折叠 AI 预审阶段同义底层日志，并将 `ai_review.start` 区分显示为“AI 预审排队”或“AI 预审开始运行”。
- 详情抽屉仍展示完整底层 `entityType/entityId`。
## 2026-06-02 导出中心原题内容兜底说明

### 说明

- 导出接口本身没有新增或变更。
- `POST /api/exports`、`GET /api/exports`、`GET /api/exports/overview`、`GET /api/exports/tasks/{taskId}/options`、`GET /api/exports/{exportId}/download` 保持原样。
- 仅调整导出字段 `system.contentMarkdown` 的取值规则：
  - 优先使用 `items.content_markdown`。
  - 为空时回退到 `raw_payload.content_markdown / prompt / question / content / text / input / query / title / body / description / raw_text`。
## 2026-06-02 导出中心可重复导出接口修正

### `GET /api/exports/tasks/{taskId}/options`

- 返回字段：
  - `taskId`
  - `taskTitle`
  - `acceptedCount`：兼容字段，当前与可导出数量一致
  - `exportableCount`：当前任务最新且可导出的 annotation 数量，口径为 `accepted / exported`
  - `fields`
- `fields[].label` 默认返回中文展示名，重点字段为 `题目ID / 作业ID / 标注ID`。

### `POST /api/exports`

- 导出范围：
  - 任务下最新版本
  - assignment 非 `voided`
  - annotation 状态为 `accepted` 或历史 `exported`
- 成功后：
  - 只生成导出文件和 export job 记录
  - 不再修改 annotation 状态
  - 返回的 `exportedCount` 表示本次文件实际导出的条目数
- 失败时：
  - 无可导出数据返回 `NO_EXPORTABLE_ANNOTATION`

### 说明

- 现有 `GET /api/exports /overview /{exportId}/download` 接口路径不变。
- 历史 `exported` 标注继续可导出，前端创建抽屉按 `exportableCount` 判断按钮可用性。

## 2026-06-02 导出中心下载态接口修正

### `GET /api/exports`

- 返回字段新增：
  - `downloadedAt`: string?，首次成功下载时间
- 语义说明：
  - `status = succeeded` 仍表示导出文件已生成成功
  - 前端展示“已创建 / 已下载”时以 `downloadedAt` 是否存在为准
  - `downloadedAt` 的写入时机是前端确认本地保存成功后，不是文件流返回时

### `GET /api/exports/{exportId}/download`

- 路径和鉴权不变。
- 成功只返回文件流，不再直接写入 `export_jobs.downloaded_at`。
- 取消保存不会影响下载态，重复下载也不会改写首次数值。

### `POST /api/exports/{exportId}/download/confirm`

- 前端在本地保存成功后调用。
- 后端校验导出文件仍可访问后，写入 `export_jobs.downloaded_at`。
- 重复确认不会改写首次数值，文件缺失时返回 404。

### 前端展示约定

- 导出中心列表 `进度` 列使用两个短进度条：
  - 第一段表示文件已创建
  - 第二段表示文件已下载
- `状态` 列对 `succeeded` 记录展示为：
  - 未下载过: `已创建`
  - 已下载过: `已下载`

### 说明

- 本轮不变更 `POST /api/exports`、`GET /api/exports/overview`、`GET /api/exports/tasks/{taskId}/options` 的路径和请求结构。
- `exported` 仍只用于历史标注状态兼容，不作为导出下载态使用。
- 前端下载实现优先使用本地保存确认能力；若浏览器不支持确认保存，则不自动写入下载态。

## 2026-06-02 路由缺失错误返回约定

### 适用范围

- 当请求路径没有匹配到后端 Controller，例如运行实例尚未加载新增接口时。

### 响应

- HTTP 状态码: `404`
- 响应体:
  - `code`: `RESOURCE_NOT_FOUND`
  - `message`: `request path not found`

### 说明

- 该约定用于避免路由缺失被误报为 `500 internal server error`。
- `POST /api/exports/{exportId}/download/confirm` 仍需要后端重启后才会在运行实例中生效。
## 2026-06-02 Owner AI 预审总分着色

### 说明

- 本轮仅调整 `/owner/ai-review` 前端展示层，接口无新增、无变更。
- 列表和右侧结果抽屉中的总分统一按 `decision` 着色:
  - `PASS` -> 绿色
  - `NEED_HUMAN_REVIEW` -> 橙色
  - `REJECT` -> 红色
- 现有 AI 预审相关接口保持不变,仍沿用当前返回字段和语义。
## 2026-06-02 模型配置入口与返回交互说明

### 说明

- 本轮没有新增或变更后端接口。
- 模型配置相关接口保持不变，仍沿用现有 AI 预审模型配置接口。
- 前端变化仅包含：
  - Owner 侧边栏不再暴露 `/owner/settings/model` 菜单入口。
  - Owner 与 AI Reviewer 头像下拉进入模型配置页时携带来源路径 `location.state.from`。
  - 模型配置页返回按钮优先返回来源路径；无来源时按角色兜底返回默认页。

### 兼容性

- 直接访问 `/owner/settings/model` 和 `/ai-reviewer/settings/model` 仍然有效。
- API 请求路径、请求体、响应体、鉴权规则和错误码均无变化。
## 2026-06-02 导出中心搜索入口移除说明

### 说明

- 本轮没有新增或变更后端接口。
- `/owner/export` 页面移除了前端本地搜索输入框，不再按任务名、导出 ID 或其他字段做关键字过滤。
- 导出中心继续保留任务筛选、状态筛选、创建导出、刷新、详情查看和下载能力。

### 兼容性

- `GET /api/exports`、`GET /api/exports/overview`、`GET /api/exports/tasks/{taskId}/options`、`POST /api/exports`、`GET /api/exports/{exportId}/download` 等接口请求和响应结构均无变化。

## 2026-06-02 Labeler 打回项返修闭环接口扩展

### `GET /api/labeler/returned-items`

- 请求参数：
  - `source`: `all | human_return | reworked | reviewed | ai_pre_reject`
  - `page`: 页码
  - `pageSize`: 每页数量
- `source` 语义：
  - `all`: 全部打回项返修链路和 AI 预打回记录
  - `human_return`: 待修改，当前最新返修状态为人工复审打回
  - `reworked`: 已修改，人工打回后标注员已重提但尚未完成人工复审
  - `reviewed`: 已复审，重提后人工复审已给出通过、修订通过或升级复核结果
  - `ai_pre_reject`: AI 预打回，尚未进入正式人工复审打回链路的 AI REJECT 记录
- 响应字段新增：
  - `reworkStatus`: `RETURNED | REWORK_SUBMITTED | REVIEW_APPROVED | REVIEW_REVISED | REVIEW_ESCALATED | AI_PRE_REJECT`
  - `reworkStatusLabel`: 前端展示用返修状态标签
  - `reviewDecision`: 最新相关人工复审决策
  - `reviewResultLabel`: 前端展示用复审结果标签
  - `reviewResultReason`: 最新相关人工复审意见
  - `reviewedAt`: 最新相关人工复审时间
  - `reworkSubmittedAt`: 人工打回后重提时间

### 兼容性

- 接口路径、鉴权规则、分页结构和既有字段保持不变。
- 不新增数据库字段和迁移，状态由现有 `annotations`、`human_reviews`、`assignments` 记录推导。
- 再次打回的记录归入 `human_return`，在 `all` 中同一 assignment 只展示最新返修链路。

## 2026-06-02 返修题重新提交按钮显示修正说明

### 说明

- 本轮没有新增或变更后端接口。
- Labeler 答题页中，处于 `returned` 且仍可编辑的返修题会直接显示“重新提交”按钮。
- 返修重新提交仍沿用现有 `POST /api/assignments/{assignmentId}/submit`。

### 兼容性

- 请求路径、请求体、响应体、鉴权规则和错误码均无变化。
- 仅调整前端按钮显示条件，不改变返修截止时间、提交校验和提交成功后的流转逻辑。

## 2026-06-02 打回项新筛选值兼容说明

### `GET /api/labeler/returned-items`

- 正式 `source` 取值保持：
  - `all`
  - `human_return`
  - `reworked`
  - `reviewed`
  - `ai_pre_reject`
- 后端兼容别名：
  - `returned`、`pending_rework` -> `human_return`
  - `modified`、`resubmitted`、`rework_submitted` -> `reworked`
  - `review_result`、`reviewed_result`、`review_complete` -> `reviewed`
  - `ai_reject` -> `ai_pre_reject`

### 前端兼容行为

- 如果运行中的后端实例仍返回 `INVALID_RETURNED_ITEM_SOURCE`，打回项页不再展示红色错误条。
- 当前筛选会展示空态提示，提醒重启后端以启用新筛选。

### 兼容性

- 接口路径、分页结构、鉴权规则和正式字段不变。
- 本轮不改变返修状态推导逻辑和数据库结构。

## 2026-06-02 AI 预审队列 Evidence 可读化说明

### `GET /api/ai-review/results/{annotationId}`

- 本轮没有新增或变更后端接口。
- AI Reviewer 队列详情页继续消费响应中的 `evidence: string[]` 字段。
- 前端将 `evidence` 渲染为“AI 审核依据”列表，并保留折叠的原始 `evidence JSON`。

### 兼容性

- 请求路径、请求参数、响应结构、鉴权规则和错误码均无变化。
- 当前接口不返回真实标注提交答案 `answerJson` 或原始题面 `rawPayload`。
- 若后续需要在 AI 队列详情页展示真实提交内容，需要扩展后端响应或新增按 `annotationId` 查询的详情接口。

## 2026-06-02 打回项筛选运行产物刷新说明

### `GET /api/labeler/returned-items`

- 本轮没有新增或变更接口契约。
- 仍沿用既有 `source` 取值：
  - `all`
  - `human_return`
  - `reworked`
  - `reviewed`
  - `ai_pre_reject`
- 本轮修复点是后端运行产物刷新：确保运行中的 `LabelerReturnedItemsService` 已加载支持 `reworked/reviewed` 的 class。

### 验证结果

- 通过 8080 直连和 5173 Vite 代理均验证 `source=reworked`、`source=reviewed` 不再返回 `INVALID_RETURNED_ITEM_SOURCE`。
- 本地 `demo / demo123` 的 labeler 角色可返回：
  - `source=reworked`: `total=3`
  - `source=reviewed`: `total=7`

### 兼容性

- 请求路径、请求参数、响应字段、鉴权规则和错误码定义均无变化。
- 若再次出现同类 400，应确认后端是否 clean 构建并重启到最新 `target/classes`。

## 2026-06-02 Label Hub 公开首页前端路由说明

### 新增前端路由

- `GET /`
  - 页面：Label Hub 公开首页。
  - 用途：展示品牌、公开导航、登录入口、注册入口、支持的数据标注类型和页脚信息。
  - 后端 API：无新增调用。
- `GET /docs`
  - 页面：文档中心占位页。
  - 用途：预留后续文档入口。
  - 后端 API：无新增调用。
- `GET /about`
  - 页面：关于 Label Hub 占位页。
  - 用途：预留后续系统介绍入口。
  - 后端 API：无新增调用。
- `GET /login#signup`
  - 页面：现有登录页。
  - 用途：通过 hash 默认打开注册态。
  - 后端 API：仍沿用现有注册接口，路由本身不新增 API。

### 兼容性

- `/login`、`/owner`、`/labeler`、`/reviewer`、`/ai-reviewer` 等既有前端路由保持不变。
- 本轮仅调整前端路由和页面状态，不修改后端接口、数据库、鉴权规则、请求体、响应体或错误码。
- 公开页使用 `html.lh-public-page` 覆盖移动端最小宽度限制，该 class 只在公开首页、文档占位页和关于占位页挂载时生效，不影响后台工作台布局。

## 2026-06-02 Label Hub 公开首页 new-api 样式精修说明

### 路由与接口

- 本轮仅精修公开首页视觉样式。
- `GET /`、`GET /docs`、`GET /about`、`GET /login#signup` 的前端路由契约保持不变。
- 后端 API 无新增、无删除、无字段变更。

### 兼容性

- `/` 首页仍展示固定登录地址 `http://localhost:5173/login`。
- `登录` 按钮仍跳转 `/login`。
- `注册` 按钮仍跳转 `/login#signup`。
- 公开页样式仍只通过 `landing-*` 与 `html.lh-public-page` 生效，不影响后台工作台路由和布局。

## 2026-06-02 Label Hub 公开首页桌面控件样式追补说明

### 路由与接口

- 本轮仅追补公开首页桌面端 URL 胶囊、复制按钮、登录按钮和注册按钮样式。
- `GET /` 前端路由保持不变。
- `GET /login` 与 `GET /login#signup` 前端跳转保持不变。
- 后端 API 无新增、无删除、无字段变更。

### 兼容性

- `/` 首页 URL 胶囊仍展示并复制完整地址 `http://localhost:5173/login`。
- URL 胶囊视觉上拆分为基础地址 `http://localhost:5173` 与 endpoint `/login`，仅影响展示，不改变复制内容。
- `登录` 按钮仍跳转 `/login`。
- `注册` 按钮仍跳转 `/login#signup`。
- 公开页样式仍只通过 `landing-*` 与 `html.lh-public-page` 生效，不影响后台工作台路由和布局。

## 2026-06-02 Label Hub 公开首页桌面整体比例精修说明

### 路由与接口

- 本轮仅调整公开首页桌面端整体视觉比例。
- `GET /`、`GET /docs`、`GET /about`、`GET /login#signup` 的前端路由契约保持不变。
- 后端 API 无新增、无删除、无字段变更。

### 兼容性

- `/` 首页固定登录地址、复制内容、登录跳转和注册跳转均保持不变。
- `/docs` 与 `/about` 仍为前端占位页，不新增后端请求。
- 本轮样式修改仍限定在 `landing-*` 与 `html.lh-public-page` 作用域内，不影响 `/owner`、`/labeler`、`/reviewer`、`/ai-reviewer` 等后台路由布局。

## 2026-06-02 Label Hub 公开首页顶部磨砂玻璃样式说明

### 路由与接口

- 本轮仅调整公开首页顶部 header 视觉样式。
- `GET /`、`GET /docs`、`GET /about`、`GET /login#signup` 的前端路由契约保持不变。
- 后端 API 无新增、无删除、无字段变更。

### 兼容性

- `/` 首页文案、URL 胶囊复制内容、登录跳转和注册跳转均保持不变。
- `/docs` 与 `/about` 仍为前端占位页，不新增后端请求。
- header 改为 fixed 覆盖式磨砂层后，公开首页和占位页已补偿顶部留白。
- 本轮样式修改仍限定在 `landing-*` 与 `html.lh-public-page` 作用域内，不影响后台工作台路由布局。

## 2026-06-02 人工审核阶段命名字段说明

### `GET /api/labeler/returned-items`

- 本轮扩展响应字段：
  - `reviewStageNo`: 人工审核阶段序号，取当前展示 annotation 的 `revisionNo`
  - `reviewStageLabel`: 人工审核阶段展示标签，取值为 `初审 / 复审 / 终审`
- 阶段计算规则：
  - `reviewStageNo = 1`: `初审`
  - `reviewStageNo = 2`: `复审`
  - `reviewStageNo >= 3`: `终审`
- 兼容字段：
  - `reviewRoundNo` 继续返回，表示单个 annotation 内 `human_reviews.round_no`，不再作为业务阶段展示依据。

### 兼容性

- 请求路径、分页结构、鉴权规则和 `source` 查询值不变。
- `source=reviewed` 仍表示已完成人工审核结果的返修记录；前端展示名称改为“已审核”，避免与“复审”阶段混淆。
- 本轮不修改数据库结构、审核裁决请求体和 AI 预审接口。

## 2026-06-03 Label Hub 公开首页 URL 路径轮播说明

### 路由与接口

- `GET /` 前端公开首页保持不变。
- `GET /login` 前端登录入口保持不变。
- `GET /login#signup` 前端注册态入口保持不变。
- 后端 API 无新增、无删除、无字段变更。

### 展示与复制行为

- `/` 首页 URL 胶囊左侧固定显示基础地址 `http://localhost:5173`。
- URL 胶囊右侧蓝色路径区域轮流显示：
  - `/login`: 登录入口。
  - `/login#signup`: 注册入口。
- 复制按钮会复制当前展示的完整地址：
  - 显示 `/login` 时复制 `http://localhost:5173/login`。
  - 显示 `/login#signup` 时复制 `http://localhost:5173/login#signup`。

### 兼容性

- 登录按钮仍跳转 `/login`。
- 注册按钮仍跳转 `/login#signup`。
- `/docs` 与 `/about` 占位路由不变。
- 本轮样式与交互修改仍限定在公开首页 `landing-*` 作用域内，不影响后台工作台路由布局。

## 2026-06-03 Label Hub 公开首页视觉对齐补充说明

### 路由与接口

- 本轮仅调整 `/` 公开首页的首屏视觉样式与 URL 路径轮播尺寸。
- `GET /`、`GET /docs`、`GET /about`、`GET /login`、`GET /login#signup` 前端路由契约保持不变。
- 后端 API 无新增、无删除、无字段变更。

### 兼容性

- URL 胶囊仍轮流显示 `/login` 与 `/login#signup`。
- 复制按钮仍复制当前展示路径对应的完整地址。
- 登录按钮仍跳转 `/login`，注册按钮仍跳转 `/login#signup`。
- 样式修改仍限定在 `landing-*` 和公开页作用域内，不影响后台工作台路由。

## 2026-06-03 Label Hub 公开首页 SVG 图标资产说明

### 路由与接口

- 本轮仅替换 `/` 公开首页的按钮图标和支持区图标。
- `GET /`、`GET /docs`、`GET /about`、`GET /login`、`GET /login#signup` 前端路由契约保持不变。
- 后端 API 无新增、无删除、无字段变更。

### 前端资产

- `/` 首页新增引用以下静态 SVG 资产：
  - `frontend/images/登陆账号.svg`: 登录按钮图标。
  - `frontend/images/注册账号.svg`: 注册按钮图标。
  - `frontend/images/文本.svg`: 支持区文本图标。
  - `frontend/images/图片.svg`: 支持区图片图标。
  - `frontend/images/视频.svg`: 支持区视频图标。

### 兼容性

- 登录按钮仍跳转 `/login`。
- 注册按钮仍跳转 `/login#signup`。
- URL 胶囊路径轮播和复制行为保持不变。
- 样式修改仍限定在公开首页 `landing-*` 作用域内，不影响后台工作台路由。
## 2026-06-03 Label Hub 公开首页首屏高度与支持区图标样式说明

### 路由与接口

- `GET /`：公开首页入口保持不变，本轮仅调整首屏视觉高度和支持区图标尺寸。
- `GET /docs`：文档占位页保持不变。
- `GET /about`：关于占位页保持不变。
- `GET /login`：登录入口保持不变。
- `GET /login#signup`：注册态入口保持不变。

### 后端 API

- 本轮无新增、删除或修改后端 API。
- 数据库、鉴权、请求字段、响应字段均无变化。

### 前端样式契约

- `/` 首页 `.landing-support-icon` 桌面端尺寸调整为 52px。
- `/` 首页 `.landing-hero` 最小高度调整为 `100vh`，页脚默认不在首屏内展示，需要滚动后可见。
- 样式变更限定在公开首页 `landing-*` 作用域内，不影响后台工作台页面。
## 2026-06-03 Label Hub 公开首页 URL Hover 与支持图标源码级对齐说明

### 路由与接口

- `GET /`：公开首页入口保持不变，本轮只调整 URL 胶囊 hover/focus 视觉反馈和支持区图标尺寸。
- `GET /docs`：文档占位页保持不变。
- `GET /about`：关于占位页保持不变。
- `GET /login`：登录入口保持不变。
- `GET /login#signup`：注册态入口保持不变。

### 后端 API

- 本轮无新增、删除或修改后端 API。
- 数据库、鉴权、请求字段、响应字段均无变化。

### 前端样式契约

- `/` 首页 URL 胶囊桌面端宽度为 `448px`，高度为 `40px`。
- `/` 首页 URL 胶囊支持 hover 反馈：灰底加深、浅边线和柔和阴影。
- `/` 首页 URL 胶囊支持 `focus-within` 反馈，复制按钮聚焦时地址框同步显示输入框式焦点状态。
- `/` 首页支持区图标按 new-api classic 源码参数复刻：桌面端 48px 图标容器，约 40px 图标视觉尺寸。
- 样式变更限定在公开首页 `landing-*` 作用域内，不影响后台工作台页面。
## 2026-06-03 Label Hub 公开首页 URL 胶囊 Hover 样式说明

### 路由与接口

- `GET /`：公开首页入口保持不变，本轮只调整 URL 胶囊 hover/focus 视觉反馈。
- `GET /docs`：文档占位页保持不变。
- `GET /about`：关于占位页保持不变。
- `GET /login`：登录入口保持不变。
- `GET /login#signup`：注册态入口保持不变。

### 后端 API

- 本轮无新增、删除或修改后端 API。
- 数据库、鉴权、请求字段、响应字段均无变化。

### 前端样式契约

- `/` 首页 URL 胶囊 hover 背景与“注册”按钮 hover 背景保持一致：`rgba(220, 229, 236, 0.86)`。
- `/` 首页 URL 胶囊 hover/focus 不再额外显示边框高亮或阴影。
- `/` 首页 URL 胶囊内复制按钮在父级 hover 时同步进入浅灰反馈态。
- 样式变更限定在公开首页 `landing-*` 作用域内，不影响后台工作台页面。
## 2026-06-03 Label Hub 公开首页登录/注册跳转动画说明

### 路由与接口

- `GET /`：公开首页入口保持不变，本轮只增强“登录”和“注册”按钮点击后的前端过渡动画。
- `GET /login`：登录入口保持不变，首页点击“登录”后约 360ms 进入该路由。
- `GET /login#signup`：注册态入口保持不变，首页点击“注册”后约 360ms 进入该路由。
- `GET /docs`：文档占位页保持不变。
- `GET /about`：关于占位页保持不变。

### 后端 API

- 本轮无新增、删除或修改后端 API。
- 数据库、鉴权、请求字段、响应字段均无变化。

### 前端交互契约

- `/` 首页“登录”按钮点击后，从按钮中心播放蓝色柔光涟漪扩散和页面淡出动画，然后跳转 `/login`。
- `/` 首页“注册”按钮点击后，从按钮中心播放浅灰蓝柔光涟漪扩散和页面淡出动画，然后跳转 `/login#signup`。
- 动画执行期间锁定重复点击，但按钮不会进入 Ant Design 禁用灰态。
- 样式变更限定在公开首页 `landing-*` 作用域内，不影响后台工作台页面。
## 2026-06-03 Label Hub 公开首页 URL 胶囊透明度说明

### 路由与接口

- `GET /`：公开首页入口保持不变，本轮只调整 URL 胶囊和复制圆按钮透明度。
- `GET /login`：登录入口保持不变。
- `GET /login#signup`：注册态入口保持不变。
- `GET /docs`：文档占位页保持不变。
- `GET /about`：关于占位页保持不变。

### 后端 API

- 本轮无新增、删除或修改后端 API。
- 数据库、鉴权、请求字段、响应字段均无变化。

### 前端样式契约

- `/` 首页 URL 胶囊默认背景对齐 new-api classic 所用 Semi UI 浅色主题输入框背景：`rgba(46, 50, 56, 0.05)`。
- `/` 首页 URL 胶囊 hover/focus 背景为 `rgba(46, 50, 56, 0.09)`。
- `/` 首页 URL 胶囊 active 背景为 `rgba(46, 50, 56, 0.13)`。
- `/` 首页 URL 胶囊内复制圆按钮默认使用 `rgba(46, 50, 56, 0.09)`，hover 使用 `rgba(46, 50, 56, 0.13)`。
- 样式变更限定在公开首页 `landing-*` 作用域内，不影响后台工作台页面。
## 2026-06-03 Label Hub 公开页滚动条视觉隐藏说明

### 路由与接口

- `GET /`：公开首页入口保持不变，本轮只隐藏浏览器纵向滚动条外观。
- `GET /docs`：文档占位页入口保持不变，同样隐藏公开页滚动条外观。
- `GET /about`：关于占位页入口保持不变，同样隐藏公开页滚动条外观。
- `GET /login`：登录入口保持不变。
- `GET /login#signup`：注册态入口保持不变。

### 后端 API

- 本轮无新增、删除或修改后端 API。
- 数据库、鉴权、请求字段、响应字段均无变化。

### 前端样式契约

- 公开页通过 `html.lh-public-page` 作用域隐藏浏览器滚动条外观。
- Chrome/Edge/Safari 使用 `::-webkit-scrollbar` 隐藏滚动条。
- Firefox 使用 `scrollbar-width: none` 隐藏滚动条。
- 不设置 `overflow-y: hidden`，页面仍保留鼠标滚轮、触控板和键盘滚动能力。
- 后台工作台页面不挂载 `lh-public-page`，不受该规则影响。
## 2026-06-03 Label Hub 公开首页顶部导航栏透明度说明

### 路由与接口

- `GET /`：公开首页入口保持不变，本轮只调整顶部导航栏透明度和磨砂强度。
- `GET /docs`：文档占位页入口保持不变。
- `GET /about`：关于占位页入口保持不变。
- `GET /login`：登录入口保持不变。
- `GET /login#signup`：注册态入口保持不变。

### 后端 API

- 本轮无新增、删除或修改后端 API。
- 数据库、鉴权、请求字段、响应字段均无变化。

### 前端样式契约

- `/` 首页顶部 `.landing-header` 背景从 `rgba(255, 255, 255, 0.2)` 调整为 `rgba(255, 255, 255, 0.04)`。
- `/` 首页顶部 `.landing-header` 磨砂从 `blur(26px) saturate(180%)` 调整为 `blur(18px) saturate(160%)`。
- 顶部导航栏继续保留 `border-bottom: none` 与 `box-shadow: none`，避免出现分割线。
- 样式变更限定在公开页 `.landing-header`，不影响后台工作台页面。

## 2026-06-03 Owner 任务管理进度字段说明

### 接口

- `GET /api/tasks`：Owner 任务列表接口保持请求参数、鉴权和分页结构不变。

### 响应字段扩展

- `annotatedItemCount: number`
  - 任务下已标注题目数，按题目 `items.id` 去重。
  - 统计条件：题目存在有效 assignment，且该 assignment 有非空对象/数组草稿，或已有非 `voided` annotation。
- `publishedItemTotal: number`
  - 任务下题目总数，来自 `items.task_id = task.id` 的记录数。

### 兼容说明

- `quotaUsed` / `quotaTotal` 保持不变，继续表示认领配额相关数据。
- Owner 任务管理页的“进度”展示使用 `annotatedItemCount / publishedItemTotal`。
- 本轮无新增接口、无请求字段变化、无数据库迁移。

## 2026-06-03 Owner 任务管理进度统计口径修正说明

### 接口

- `GET /api/tasks`：接口路径、请求参数、鉴权、分页结构和响应字段名不变。

### 字段口径修正

- `annotatedItemCount`
  - 从当前任务有效 assignment 统计，而不是从 `items.task_id` 统计。
  - 有效 assignment：`assignments.task_id = task.id` 且 `assignments.status <> 'voided'`。
  - 已标注条件：存在非空对象/数组草稿，或存在非 `voided` annotation。
  - 统计时按 `item_id` 去重。
- `publishedItemTotal`
  - 指派任务 `assigned`：优先使用当前任务有效 assignment 的去重题目数。
  - 先到先得 `first-come` 和配额抢单 `quota`：优先使用任务 `quota`。
  - 兜底使用 `items.task_id = task.id` 的题目数，再兜底 assignment 去重题目数。

### 兼容说明

- 本轮不新增响应字段，不改变前端调用方式。
- `quotaUsed` / `quotaTotal` 仍保持原有认领配额语义。
- 本轮无数据库迁移。

## 2026-06-03 Owner 人工审核任务表状态气泡单行展示说明

### 接口

- 本轮无新增、删除或修改后端 API。
- `GET /api/owner-review/tasks` 等 Owner 人工审核接口请求参数、响应字段和统计口径均保持不变。

### 前端展示契约

- Owner 人工审核页任务表“通过 / 打回 / 进行中”三枚状态气泡固定同一行展示。
- 三枚气泡的颜色、图标、文案和数量来源保持不变。
- 下方“共 x 条 · 人工已审 x”说明文案保持不变。

## 2026-06-03 Owner 任务列表标注状态与审核状态接口说明

### 接口

- `GET /api/tasks`：Owner 任务列表接口保持路径、请求参数、鉴权和分页结构不变。

### 响应字段扩展

- `reviewStatus: string`
  - 可选值：`not_started`、`ai_prereviewing`、`human_first_review`、`human_second_review`、`human_final_review`、`completed`。
  - `not_started`：任务下无最新有效 annotation。
  - `ai_prereviewing`：存在最新有效 annotation 处于 `ai_reviewing`，且无更高优先级人工待审阶段。
  - `human_first_review`：存在最新有效 annotation 处于 `submitted/reviewing`，当前人工审核轮次为 1。
  - `human_second_review`：存在最新有效 annotation 处于 `submitted/reviewing`，当前人工审核轮次为 2。
  - `human_final_review`：存在最新有效 annotation 处于 `submitted/reviewing`，当前人工审核轮次为 3 或以上。
  - `completed`：当前任务暂无待审/预审中 annotation，但存在已完成审核结果。
- `reviewRound: number | null`
  - 人工审核状态下返回当前最高人工审核轮次。
  - 非人工审核状态返回 `null`。
  - 轮次口径为当前最新有效 annotation 的 `MAX(human_reviews.round_no) + 1`，无历史人工审核记录时为 1。

### 聚合口径

- 有效 assignment：`assignments.status <> 'voided'`。
- 最新有效 annotation：同一 assignment 下 `annotations.status <> 'voided'`，按 `revision_no DESC, id DESC` 取第一条。
- 多题目阶段不一致时，展示最高当前阶段：人工终审 > 人工复审 > 人工初审 > AI预审中 > 已完成 > 未开始。
- 历史已完成审核阶段不压过当前仍待处理的 annotation。

### 前端展示口径

- Owner 任务列表“标注状态”不是新增后端字段，由前端按 `state + quotaUsed` 派生：
  - `draft` 显示“草稿”。
  - `paused` 显示“已暂停”。
  - `ended` 显示“已结束”。
  - `published && quotaUsed === 0` 显示“发布中”。
  - `published && quotaUsed > 0` 显示“进行中”。
- `quotaUsed` 保持原接口语义：当前任务非 `voided` assignment 数量，包含领取模式和指派模式。
- 原 `state` 字段保留，用于任务生命周期、操作权限和状态流转，不建议前端用它单独展示“发布中/进行中”。

## 2026-06-03 新建标注任务模块化与分配能力接口说明

### 数据库迁移

- `V20__task_scope_and_review_allocations.sql`
  - `task_items(task_id, item_id, position_no)`：任务固定题目范围。
  - `task_labeler_allocations(task_id, labeler_id, item_count)`：指派模式标注员题量。
  - `task_reviewer_allocations(task_id, reviewer_id, item_count)`：审核员题量。
  - `task_review_items(task_id, item_id, reviewer_id)`：逐题审核员绑定。

### POST /api/tasks 与 PUT /api/tasks/{taskId}

- 鉴权：Owner。
- 新增请求字段：`itemSelectionMode`、`selectedItemIds`、`labelerAllocations`、`reviewerAllocations`。
- `itemSelectionMode` 支持 `all` 和 `partial`。
- `labelerAllocations` / `reviewerAllocations` 结构为 `{ userId: string; itemCount: number }[]`。
- 指派模式发布时，标注员分配总和必须等于任务题目数。
- 手动审核分配发布时，审核员分配总和必须等于任务题目数。
- 未传审核员分配时，后端按所有 active reviewer 自动平均分配。
- 已有 assignment 或 annotation 的任务禁止修改题目范围和分配配置。

### GET /api/tasks/{taskId}

- 鉴权：Owner。
- 响应包含：
  - `task: OwnerTask`。
  - `itemSelectionMode: "all" | "partial"`。
  - `selectedItemIds: string[]`。
  - `labelerAllocations: { userId, username, displayName, itemCount }[]`。
  - `reviewerAllocations: { userId, username, displayName, itemCount }[]`。

### GET /api/tasks/assignable-reviewers

- 鉴权：Owner。
- 响应：`{ userId, username, displayName }[]`。
- 用途：新建任务弹窗审核分配模块选择人工审核员。

### GET /api/datasets/{datasetId}/item-options

- 鉴权：Owner。
- 查询参数：`keyword?: string`、`page?: number`、`pageSize?: number`。
- 响应：分页结构 `{ items, page, pageSize, total }`。
- `items` 字段包含 `itemId`、`itemKey`、`label`、`mediaType`、`summary`。

### Reviewer 过滤规则

- Reviewer 批次、待审题列表、AI 预审转人工审核列表按 `task_review_items.reviewer_id = 当前用户` 过滤。
- 历史任务没有 `task_review_items` 记录时保持旧行为：所有 reviewer 可见。
- 提交人工审核裁决前校验当前 reviewer 是否绑定该 annotation 对应题目；不匹配返回 `REVIEWER_NOT_ASSIGNED`。

## 2026-06-03 Owner 任务列表状态与进度列顺序说明

### 接口

- 本轮无新增、删除或修改后端 API。
- `GET /api/tasks` 请求参数、分页结构、响应字段和字段口径均保持不变。

### 前端展示契约

- Owner 任务管理页表格列顺序调整为：任务、审核状态、标注状态、标注进度、分发策略、操作。
- “进度”列标题改为“标注进度”，展示口径仍为 `annotatedItemCount / publishedItemTotal`。
- “标注状态”和“审核状态”的取值、tooltip、筛选逻辑保持不变。

## 2026-06-03 Owner 任务列表审核状态筛选说明

### 接口

- 本轮无新增、删除或修改后端 API。
- `GET /api/tasks` 请求参数、分页结构、响应字段和审核状态聚合口径均保持不变。

### 前端筛选契约

- Owner 任务管理页工具栏新增“审核状态”筛选。
- 筛选选项包含：全部、未开始、AI预审中、人工初审、人工复审、人工终审、已完成。
- 筛选值直接匹配 `GET /api/tasks` 返回的 `reviewStatus`：
  - `not_started`
  - `ai_prereviewing`
  - `human_first_review`
  - `human_second_review`
  - `human_final_review`
  - `completed`
- 审核状态筛选与关键词、标注状态、分发策略筛选同时生效。

## 2026-06-03 Owner 任务管理与模板搭建刷新按钮说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 任务管理页刷新仍复用现有 `GET /api/tasks`。
- 模板搭建页刷新仍复用现有 `GET /api/schemas`。

### 前端交互契约

- Owner 任务管理页筛选工具栏新增“刷新”按钮，点击后重新加载任务列表。
- Owner 模板搭建页筛选工具栏新增“刷新”按钮，点击后重新加载模板列表。
- 刷新按钮使用 Reload 图标和按钮文案“刷新”。
- 刷新期间按钮进入 loading 状态。
- 刷新不会清空搜索关键词、标注状态筛选、审核状态筛选、分发策略筛选或模板状态筛选。

## 2026-06-03 AI Reviewer Job 队列证据字段溢出修复说明

### 接口

- 本轮无新增、删除或修改后端 API。
- AI 预审结果仍使用现有结果字段，`evidence` 字段结构和内容不变。

### 前端展示契约

- AI Reviewer Job 队列中“AI 预审结果”抽屉的“证据 / 引用字段”改为纵向展示。
- 单条证据文本过长时在抽屉卡片内部自动换行。
- 证据区域不再撑出抽屉横向边界。
- 评分明细、评估理由、风险标记和其它抽屉内容展示逻辑保持不变。

## 2026-06-04 Reviewer 争议样本产生机制接口说明

### POST /api/reviewer/annotations/{annotationId}/decision

- 鉴权：Reviewer。
- 新增/补强争议升级用法：
  - 请求体：`{ "decision": "ESCALATE", "reason": "争议原因" }`。
  - `reason` 必填；为空时返回 `DISPUTE_REASON_REQUIRED`。
  - 同一 annotation 已存在待处理争议时返回 `DISPUTE_ALREADY_OPEN`。
- 自动升级规则：
  - 普通审核入口中，当前 annotation `revisionNo >= 3` 且 Reviewer 提交 `RETURN` 时，后端自动改写为 `ESCALATE`。
  - 自动升级时 annotation 保持 `reviewing`，进入 `GET /api/reviewer/disputes` 待处理列表。
  - 第 1、2 版打回仍走原返修流程。
- 兼容说明：
  - 争议样本仍落库为 `human_reviews.decision = 'escalate'`。
  - 不新增数据库表，不改变已有 `APPROVE / RETURN / REVISE` 请求体结构。

### GET /api/reviewer/disputes

- 路径、参数和响应结构不变。
- 待处理争议来源包括 Reviewer 手动升级和第 3 版及以后打回自动升级。

### POST /api/reviewer/disputes/{disputeId}/resolve

- 鉴权：Reviewer。
- 请求体保持 `{ "resolution": "approve" | "reject", "note"?: string }`。
- `resolution = reject` 时前端会要求填写 `note`，后端按终审打回原因写入人工审核记录。
- 争议样本终审打回不再触发自动升级争议，保证争议可以关闭。

## 2026-06-03 AI Reviewer 作业概览后端接口说明

### 鉴权

- 以下接口均复用 AI Review 管理权限。
- 允许角色：`owner`、`reviewer`、`ai_reviewer`。

### GET /api/ai-review/dashboard/kpi

- 用途：AI Reviewer 作业概览顶部 KPI 和处理效率。
- 统计范围：未删除任务下的全量历史 AI Review Job。
- 响应字段：
  - `totalJobs: number`：Job 总数。
  - `succeededJobs: number`：状态为 `succeeded` 的 Job 数。
  - `failedJobs: number`：状态为 `failed` 的 Job 数。
  - `pendingJobs: number`：状态为 `pending` 的 Job 数。
  - `runningJobs: number`：状态为 `running` 的 Job 数。
  - `needHumanJobs: number`：AI 决策为 `NEED_HUMAN_REVIEW` 的 Job 数。
  - `passRate: number`：`PASS 数 / succeededJobs`，无成功 Job 时为 0。
  - `avgDurationSec: number`：`started_at` 到 `finished_at` 的平均秒数，缺失时间的记录不参与平均。

### GET /api/ai-review/dashboard/decisions

- 用途：决策分布环形图。
- 统计范围：未删除任务下的全量历史 AI Review Result。
- 响应：`{ decision: string; count: number }[]`。
- 固定返回三类决策：`PASS`、`NEED_HUMAN_REVIEW`、`REJECT`；无数据时数量为 0。

### GET /api/ai-review/dashboard/trend

- 用途：每日审核量趋势图。
- 查询参数：`days?: 7 | 30`，默认 7；其它值返回 400，错误码 `INVALID_AI_DASHBOARD_DAYS`。
- 统计口径：按 `ai_review_jobs.finished_at` 优先、`ai_review_results.created_at` 兜底的完成日期统计。
- 响应：`{ date: string; total: number; pass: number; needHuman: number; reject: number }[]`。
- 返回近 N 天完整日期序列，缺失日期自动补 0。

### GET /api/ai-review/dashboard/tasks

- 用途：任务维度审核量对比柱状图。
- 统计范围：未删除任务下成功完成且已有 AI 决策的 Job。
- 排序：按 `total` 倒序，最多返回 10 条。
- 响应字段：
  - `taskId: string`
  - `taskTitle: string`
  - `total: number`
  - `pass: number`
  - `needHuman: number`
  - `reject: number`

## 2026-06-03 首页主体位置调整说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无 TypeScript 类型变更。
- 无数据库迁移变更。

### 前端展示契约

- 公开首页 desktop 基础 `.landing-hero` 使用 `padding: 160px 16px 28px;`。
- 首页中间主体相对顶部整体下移约 64px，覆盖标题、说明、地址胶囊、登录/注册按钮和支持区。
- 移动端和平板端继续使用现有 `@media (max-width: 900px)` 覆盖规则。

## 2026-06-03 首页页脚开发者外链说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无路由变更和无数据接口变更。

### 前端展示契约

- 公开首页页脚“设计与开发由”后的三个名称均为外链。
- `肖强` -> `https://github.com/Qiang0129/ByteDance_competition`
- `Codex` -> `https://openai.com/zh-Hans-CN/codex/`
- `Claude` -> `https://claude.com/`
- 链接使用新窗口打开，保留原有蓝色强调样式。

## 2026-06-03 首页页脚备案外链说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无路由变更和无数据接口变更。

### 前端展示契约

- 公开首页页脚备案文案为外链。
- `Copyright © SCU-肖强 | 蜀ICP备2026020302号-1` -> `https://beian.miit.gov.cn/#/Integrated/recordQuery`
- 链接使用新窗口打开，默认继承备案行原有灰色文本样式。

## 2026-06-03 关于页 README 渲染与入场动画说明

### 前端路由

- `GET /about`：公开关于页，从占位页升级为“摘要区 + 完整 README 渲染区”。
- `GET /`：顶部“关于”导航点击新增前端玻璃淡入过渡动画，动画完成后进入 `/about`。
- `GET /docs`：文档占位页保持不变。
- `GET /login`：登录入口保持不变。
- `GET /login#signup`：注册态入口保持不变。

### 后端 API

- 本轮无新增、删除或修改后端 API。
- 数据库、鉴权、请求字段和响应字段均无变化。

### 前端展示契约

- `/about` 直接读取仓库根目录 `README.md`，通过 Vite `?raw` 作为字符串导入。
- README 使用 `ReactMarkdown` 和 `remark-gfm` 渲染，支持 GFM 表格、列表、代码块、链接和复选框。
- `/about` 桌面端提供 README 二级/三级标题目录，点击目录锚点滚动到正文对应章节。
- 公开导航中普通左键点击“关于”时播放约 360ms 玻璃淡入动画；修饰键点击和中键点击保留浏览器默认行为。
- Vite dev server 已允许读取前端目录和仓库根目录，避免开发环境无法访问根 README。

## 2026-06-04 关于页 README 阅读器布局修正说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。

### 前端展示契约

- `GET /about`：README 从整页长文档展示改为固定高度文档阅读器。
- 阅读器左侧为 README 二级/三级标题目录，右侧为 README 正文。
- README 正文在 `.landing-about-readme-body` 内部滚动，不再无限制拉长整页。
- 点击左侧目录项时，前端在 README 正文滚动容器内部定位对应标题并平滑滚动。
- 目录链接仍保留 `href="#..."`，点击后同步更新浏览器地址 hash。
- 根目录 `README.md` 内容来源不变，仍通过 Vite `?raw` 导入。

## 2026-06-04 关于页 README 目录点击定位修复说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 前端路由仍为 `GET /about`，路由路径不变。

### 前端交互契约

- `GET /about#章节名`：进入关于页后，前端会匹配 README 二级/三级目录标题并滚动右侧阅读器到对应章节。
- 点击左侧 README 目录项时，前端阻止浏览器默认整页锚点跳转，改为滚动 `.landing-about-readme-body` 内部正文面板。
- 点击后浏览器地址 hash 会同步为当前章节 id，便于复制和刷新后恢复定位。
- 右侧 README 正文滚动时，左侧目录 active 高亮会随当前章节同步变化。
- 根目录 `README.md` 内容来源和 Markdown 渲染方式不变。

## 2026-06-04 关于页 README 目录滚动二次修复说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 前端路由仍为 `GET /about`，路由路径不变。

### 前端交互契约

- 点击 `/about` 左侧 README 目录项时，右侧 `.landing-about-readme-body` 会滚动到对应章节标题在容器内的真实位置，而不是依赖浏览器默认整页锚点。
- 左侧当前目录项会自动保持在目录滚动容器可视区域内。
- 浏览器地址 hash、目录 active 高亮和右侧正文定位保持同步。

## 2026-06-04 关于页 README 目录定位精准修复说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 前端路由仍为 `GET /about`，路由路径不变。

### 前端交互契约

- 点击 `/about` 左侧 README 目录项时，右侧 `.landing-about-readme-body` 会将对应章节标题滚动到正文可视区域顶部附近。
- 目标章节标题默认保留约 20px 上边距，避免贴边或被阅读器头部遮挡。
- 浏览器地址 hash、左侧 active 高亮和右侧实际显示章节保持同步。
- 根目录 `README.md` 内容来源和 Markdown 渲染方式不变。

## 2026-06-04 README 当前系统状态更新说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 前端路由仍为 `GET /about`，路由路径不变。

### 文档展示契约

- 根目录 `README.md` 已更新为当前系统状态说明。
- `/about` 页面仍通过 Vite `?raw` 导入根目录 `README.md` 并渲染。
- README 覆盖当前系统能力、技术栈、目录结构、环境变量、数据库迁移、启动顺序、演示账号、常用入口、主要 API、样例数据、构建检查和常见问题。
- `infra/docker-compose.yml` 当前仍标记为占位，README 中不承诺 Docker Compose 一键启动。

## 2026-06-04 关于页 README 代码块复制按钮说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 前端路由仍为 `GET /about`，路由路径不变。

### 前端交互契约

- `/about` 页面 README Markdown 代码块右上角提供复制按钮。
- 复制按钮默认隐藏，鼠标悬停到代码块或按钮获得键盘焦点时显示。
- 点击复制按钮会复制当前代码块纯文本内容。
- 复制成功或失败通过页面消息提示反馈。
- 根目录 `README.md` 内容来源和 Markdown 渲染方式不变。

## 2026-06-04 关于页 README 目录选中态弱化说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 前端路由仍为 `GET /about`，路由路径不变。

### 前端展示契约

- `/about` 左侧 README 目录 active 项不再显示左侧深色竖条。
- active 项保留浅蓝底和蓝色文字，用于表示当前章节。
- 目录点击、hash 同步和右侧 README 定位行为不变。

## 2026-06-04 Reviewer 争议样本页布局收敛说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 继续使用现有接口：
  - `GET /api/reviewer/disputes`
  - `GET /api/reviewer/disputes/{disputeId}`
  - `POST /api/reviewer/disputes/{disputeId}/resolve`

### 前端交互契约

- Reviewer「争议样本」页改为与 AI 审核工作台一致的三栏布局。
- 左栏负责搜索、状态筛选和争议样本选择。
- 中栏展示题目原文 / 原始数据、标注答案和 AI 预审结果。
- 右栏展示争议信息、审核时间线和终审操作。
- 终审操作只在右栏详情区出现；列表项不提供直接终审按钮。
- 已解决争议展示只读结果；`canResolve=false` 的争议展示“需其他 Reviewer 终审”并禁用终审。

## 2026-06-04 Reviewer 答案展示标签化说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 继续使用 `AnnotationToReviewResponse.schemaFields` 作为前端展示映射依据。

### 前端展示契约

- Reviewer 审核工作台和争议详情中的「标注答案」不再直接展示内部字段名和内部选项值。
- 字段名优先展示 `schemaFields[].label`，例如 `quality_result` 展示为 `质检结果`。
- 单选、多选、标签字段的值优先展示 `schemaFields[].options[].label`，例如 `option_a` 展示为 `通过`。
- 当缺少 schema 快照、字段或选项 label 时，前端回退显示原始 key/value，避免隐藏真实数据。

## 2026-06-04 关于页主题强调色联动说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 前端路由仍为 `GET /about`，路由路径不变。

### 前端展示契约

- `/about` 页面内的强调文字和相关浅色强调背景跟随当前主题主色变量。
- 受影响位置包括 About 胶囊标签、摘要卡片标签、README 标题标签、README 目录 hover/active、Markdown 链接和引用块强调边框。
- 主题来源继续使用现有 `ThemeProvider` 写入的 `--lh-primary` 与 `--lh-primary-bg-*` CSS 变量。
- README 内容来源、目录点击定位、代码块复制和后端接口契约不变。

## 2026-06-04 公开页导航双向动画说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 前端公开页路由仍为 `/`、`/docs`、`/about`，路径不变。

### 前端交互契约

- 公开页顶部导航在目标路径不同于当前路径时，先播放现有玻璃扩散动画，再执行前端路由跳转。
- `/` 点击“关于”进入 `/about`、`/about` 点击“首页”返回 `/` 都使用同一套导航动画。
- 点击当前页导航项不触发重复跳转。
- 非普通左键点击或带 Ctrl / Meta / Shift / Alt 的点击不拦截，保留浏览器默认打开行为。

## 2026-06-04 登录注册页入场动画衔接说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 无数据库迁移、鉴权、请求字段和响应字段变化。
- 前端路由仍为 `GET /login` 与 `GET /login#signup`，路径不变。

### 前端交互契约

- 从首页点击“登录”进入 `/login` 时，登录页播放一次来自首页的入场衔接动画。
- 从首页点击“注册”进入 `/login#signup` 时，登录页默认显示注册态并播放一次入场衔接动画。
- 直接访问或刷新 `/login`、`/login#signup` 不播放该首页衔接动画。
- 登录页内部“注册 / 登录”切换继续使用原有翻转动画。
- 登录成功后跳转工作台继续使用现有离场动画。

## 2026-06-04 Reviewer 争议解决状态与终审时间线说明

### 接口

- 本轮无新增、删除或修改接口路径。
- 继续使用：
  - `GET /api/reviewer/disputes`
  - `GET /api/reviewer/disputes/{disputeId}`
  - `POST /api/reviewer/disputes/{disputeId}/resolve`
- 无数据库迁移、鉴权、请求字段变化。

### 响应语义

- `GET /api/reviewer/disputes/{disputeId}` 的 `annotation.reviewTimeline` 保持原字段结构不变。
- `reviewTimeline` 现在按 assignment 返回更完整的审核链路：
  - 每个 annotation 的 AI 预审节点。
  - 每条 `human_reviews` 人工审核节点，包括 `ESCALATE` 升级争议记录。
  - 无人工审核记录时保留 pending 人工审核节点。
- 前端按现有字段映射展示：
  - `decision=ESCALATE` 显示为“初审升级 / 复审升级”。
  - 已解决争议中的最后 `decision=APPROVE` 显示为“终审通过”。
  - 已解决争议中的最后 `decision=RETURN` 显示为“终审驳回”。

### 前端交互契约

- 终审提交成功后，前端重新请求 `GET /api/reviewer/disputes/{disputeId}` 刷新详情。
- 争议详情头状态不再固定显示“争议处理中”，而是根据 `dispute.status`、`dispute.canResolve` 和 `annotation.decision` 显示已解决、待终审或需他人终审。

## 2026-06-04 Reviewer 工作概览与审核报表合并说明

### 现有接口

- 工作概览继续使用现有接口：
  - `GET /api/reviewer/overview?days=30`
  - `GET /api/reviewer/batches?status=pending&page=1&pageSize=5`
- 本轮不新增真实后端实现、不新增数据库迁移、不改变鉴权规则。

### 前端预留接口

- `GET /api/reviewer/reports/summary?days=30`
  - 用途：获取 Reviewer 审核报表汇总。
  - 预留响应字段：
    - `rangeDays`
    - `approveRate`
    - `returnRate`
    - `disputeRate`
    - `aiConsistencyRate`
    - `reviewedTotal`
    - `trend`: `{ label, approve, return, dispute }[]`
- `GET /api/reviewer/reports/export?days=30&format=csv`
  - 用途：导出当前 Reviewer 的审核明细 CSV。
  - 可选参数：
    - `taskId`
    - `decision`: `APPROVE / RETURN / REVISE / ESCALATE`
  - 预期响应：`text/csv` 文件流。

### 审核明细导出字段建议

- 审核时间。
- 任务 ID、任务名。
- 题目 ID、标注 ID。
- 审核阶段：初审 / 复审 / 终审。
- 审核结论：通过 / 打回 / 修订 / 升级争议。
- 审核原因。
- AI 预审结论与分数。
- 标注员。
- Reviewer。

### 前端展示契约

- Reviewer 侧边栏不再显示独立“审核报表”入口。
- `/reviewer/reports` 保留兼容重定向到 `/reviewer`。
- 工作概览页内展示审核报表模块，并保留“导出审核明细”按钮作为后续后端接入入口。
- 待审队列不展示任务优先级。

## 2026-06-04 Reviewer 审核报表趋势图说明

### 接口

- 本轮无新增、删除或修改后端 API。
- Reviewer 工作概览继续使用：
  - `GET /api/reviewer/overview?days=30`
  - `GET /api/reviewer/batches?status=pending&page=1&pageSize=5`
- 审核报表趋势图当前使用前端静态每日示例数据，不请求尚未实现的报表接口。

### 后续接口接入约定

- 后续可通过预留接口 `GET /api/reviewer/reports/summary?days=7|30` 替换静态趋势数据。
- 当前预留 `ReviewerReportSummary.trend` 字段保持 `{ label, approve, return, dispute }[]` 结构。
- 接入真实数据时，前端将其映射为图表数据：
  - `date`: 来自 `label`。
  - `pass`: 来自 `approve`。
  - `return`: 来自 `return`。
  - `dispute`: 来自 `dispute`。
  - `total`: `approve + return + dispute`。

### 前端展示契约

- 审核报表趋势图标题为“每日审核量趋势”。
- 支持 `近 7 天 / 近 30 天` 切换。
- 支持 `折线图 / 柱状图` 切换。
- 指标包含通过、打回、争议、总量，颜色分别为绿色、橙色、红色、灰色。
- “导出审核明细”按钮继续保留为后续导出入口。

## 2026-06-04 Reviewer 报表导出按钮风格说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 导出按钮仍沿用前端预留行为，真实导出接口仍为后续 `GET /api/reviewer/reports/export?days=30&format=csv`。

### 前端展示契约

- “导出审核明细”按钮复用顶部“领取审核任务”同款系统主按钮风格。
- 小屏下按钮允许换行到标题下一行，避免挤压审核报表标题。

## 2026-06-04 Owner 题目反馈已查看判定说明

### 新增接口

- `PATCH /api/dashboard/issue-feedback/viewed`
  - 用途：Owner 将当前已展示的题目反馈标记为已查看。
  - 权限：仅 Owner 可调用。
  - 请求体：
    ```json
    {
      "issueIds": ["211", "210"]
    }
    ```
  - 响应：
    ```json
    {
      "markedCount": 2
    }
    ```

### 接口规则

- 只更新当前 Owner 名下任务的反馈。
- 只更新当前状态仍为 `open` 的反馈。
- 重复调用同一批 `issueIds` 时，已变为 `viewed` 的反馈不会再次计数。
- 空 `issueIds` 或空请求体返回 `markedCount: 0`。
- 非正整数或非数字 `issueIds` 返回 `INVALID_ISSUE_ID`。

### 现有接口扩展

- `GET /api/dashboard/issue-feedback?status=open`：继续返回待查看反馈。
- `GET /api/dashboard/issue-feedback?status=viewed`：返回已查看反馈。
- `GET /api/dashboard/issue-feedback?status=all`：返回全部反馈。
- 反馈条目 `itemId` 在 Owner 看板中作为展示题号使用，优先返回任务内 `task_items.position_no`，历史数据缺少绑定时回退为数据库 `issues.item_id`。

### 前端展示契约

- Owner 看板初次加载只显示待查看数量，不自动标记。
- Owner 打开或刷新题目反馈弹窗后，仅本次展示的最近 5 条 `open` 反馈会被标记为 `viewed`。
- 标记成功后弹窗内对应条目显示“已查看”，看板待查看数量按实际 `markedCount` 扣减。

## 2026-06-04 Owner 题目反馈题号展示修正说明

### 接口

- 本轮无新增、删除后端 API。
- 调整现有 `GET /api/dashboard/issue-feedback` 的展示字段语义：
  - `itemId` 优先返回任务内题目序号 `task_items.position_no`。
  - 如果历史数据缺少 `task_items` 绑定，`itemId` 回退为数据库题目 ID `issues.item_id`。

### 前端展示契约

- Owner“题目反馈”弹窗继续展示 `题目:{item.itemId}`。
- 展示值应为任务中的题目序号，而不是数据库 `items.id`。
- 弹窗布局、已查看状态、待查看数量扣减逻辑不变。

## 2026-06-04 Owner 题目反馈已查看展示保留说明

### 接口

- 本轮无新增、删除或修改后端 API。
- 继续使用现有接口：
  - `GET /api/dashboard/issue-feedback?status=open`：用于获取待查看反馈数量。
  - `GET /api/dashboard/issue-feedback?status=all&pageSize=5`：用于弹窗展示最近 5 条反馈，包含 `open` 和 `viewed`。
  - `PATCH /api/dashboard/issue-feedback/viewed`：用于将本次展示中仍为 `open` 的反馈标记为已查看。

### 前端展示契约

- Owner 看板入口和弹窗顶部的数量仍表示待查看反馈数量。
- “题目反馈”弹窗展示最近 5 条全部反馈，不因标记为已查看而从当前列表消失。
- 已查看反馈在弹窗中显示状态标签“已查看”。
- 标记已查看时只提交当前展示列表中状态为 `open` 的反馈，避免重复标记已查看历史记录。

## 2026-06-04 文档中心前端路由说明

- `GET /docs`
  - 前端公开路由，从占位页升级为文档中心。
  - 支持按分类浏览项目资料、技术文档、演示素材、AI Coding 记录和外部接口入口。
  - 支持 Markdown 直接渲染、DOCX 浏览器内预览、可用本地资源下载、缺失资源待补充提示。
  - `接口文档.md` 页面内提供 Swagger UI 外部链接：`http://127.0.0.1:8080/swagger-ui/index.html`。
- 后端 API、数据库、鉴权逻辑无变更。

## 2026-06-04 文档中心样式兜底修复说明

- `GET /docs`
  - 前端路由不变。
  - 本轮仅修复文档中心 UI 样式加载方式，确保分类栏、文档列表、预览阅读器和操作按钮应用 `landing-docs-*` 样式。
- 后端 API、数据库、鉴权逻辑无变更。

## 2026-06-04 文档中心左侧展开式导航说明

- `GET /docs`
  - 前端公开路由不变。
  - 页面交互从“三列分类/列表/预览”调整为“两列左侧展开导航/右侧预览”。
  - 预览、下载、Swagger 外链能力不变。
- 后端 API、数据库、鉴权逻辑无变更。

## 2026-06-04 Reviewer 工作概览报表接口

### `GET /api/reviewer/reports/summary`

- 用途：Reviewer 工作概览页审核报表 summary 和每日趋势。
- 权限：Reviewer 登录用户，仅统计当前 Reviewer 本人审核记录。
- 查询参数：
  - `days`: `7` 或 `30`。缺省为 `30`；小于等于 7 按 7 天处理，其它值按 30 天处理。
- 响应示例：

```json
{
  "rangeDays": 30,
  "approveRate": 0.911,
  "returnRate": 0.073,
  "disputeRate": 0.016,
  "aiConsistencyRate": 0.94,
  "reviewedTotal": 1840,
  "trend": [
    {
      "label": "2026-06-04",
      "approve": 12,
      "return": 1,
      "dispute": 0
    }
  ]
}
```

### `GET /api/reviewer/reports/export`

- 用途：导出当前 Reviewer 本人审核明细。
- 权限：Reviewer 登录用户，仅导出当前 Reviewer 本人写入的 `human_reviews`。
- 查询参数：
  - `days`: `7` 或 `30`，缺省为 `30`。
  - `format`: 当前仅支持 `csv`，缺省为 `csv`。
  - `taskId`: 可选，按任务 ID 过滤。
  - `decision`: 可选，支持 `APPROVE`、`RETURN`、`REVISE`、`ESCALATE`、`ALL`；`ALL` 或缺省表示不过滤。
- 响应：
  - Content-Type: `text/csv; charset=UTF-8`
  - Content-Disposition: `attachment; filename*=UTF-8''reviewer-review-details-{days}d.csv`
  - 文件内容带 UTF-8 BOM。
- CSV 字段：
  - 审核时间
  - 任务 ID
  - 任务名
  - 题目 ID
  - 标注 ID
  - 审核阶段
  - 审核结论
  - 审核原因
  - AI 预审结论
  - AI 预审分数
  - 标注员
  - Reviewer

### 统计口径

- 时间范围：`human_reviews.created_at >= today - (days - 1)`。
- 通过类：`approve + revise`。
- 打回类：`return`。
- 争议类：`escalate`。
- AI 一致率只统计 AI 明确 `PASS / REJECT` 的记录：
  - AI `PASS` + 人工 `approve/revise` 算一致。
  - AI `REJECT` + 人工 `return/escalate` 算一致。
  - AI `NEED_HUMAN_REVIEW` 不进入分母。
- 抽检覆盖率用于 Reviewer 工作概览质量指标：
  - 当前 Reviewer 审核过的 AI `PASS` 样本数 / 当前 Reviewer 可访问的 AI `PASS` 样本总数。

## 2026-06-04 Reviewer 待审队列接口修复说明

### `GET /api/reviewer/batches`

- 本轮无新增接口、无参数变更、无响应字段变更。
- 修复内容：
  - 修复后端动态 SQL 中 `HAVING` 与 `ORDER BY` 片段粘连导致的 MySQL 语法错误。
  - 修复后工作概览“待审队列”应从真实后端接口加载，不再因接口失败回退到本地 sample 数据。
- 现有查询参数保持不变：
  - `status`: `pending`、`in_review`、`completed` 或空。
  - `keyword`: 可选，按任务标题或任务 ID 搜索。
  - `page`: 页码。
  - `pageSize`: 每页数量。

## 2026-06-04 Reviewer 报表趋势图切换动画说明

- 本轮无新增、删除或修改后端 API。
- 继续使用现有接口：
  - `GET /api/reviewer/reports/summary?days=7|30`
  - `GET /api/reviewer/reports/export?days=30&format=csv&taskId=&decision=`
- 仅调整 Reviewer 工作概览前端表现：
  - 7/30 天切换时保留图表容器，并在容器内显示加载状态。
  - 折线图 / 柱状图切换时使用前端轻量进入动画。
  - 报表数据字段、统计口径和 CSV 导出字段不变。

## 2026-06-04 Reviewer 工作概览演示数据闪屏修复说明

- 本轮无新增、删除或修改后端 API。
- 继续使用现有接口：
  - `GET /api/reviewer/overview`
  - `GET /api/reviewer/batches?status=pending&page=1&pageSize=5`
  - `GET /api/reviewer/reports/summary?days=7|30`
- 前端展示契约调整：
  - 接口返回前，Reviewer 工作概览 KPI 和质量指标显示加载占位，不显示演示数字。
  - 待审队列接口失败时显示错误提示和空态，不再回退本地 sample 数据。
  - 报表 summary 未返回前，报表 KPI 显示 `-`，不显示 `0.0%` 假态。
## 2026-06-04 文档中心顶部 Hero 展示说明

- `GET /docs`
  - 前端页面顶部从普通文档中心标题区调整为 New API 风格的大幅 Hero 展示区。
  - 顶部新增“快速开始”锚点入口，点击后滚动到当前页面的文档工作台 `#docs-workbench`。
  - 顶部新增“参考源码”外链入口，打开 `https://github.com/QuantumNous/new-api`。
  - 顶部保留 Swagger 外链入口，打开 `http://127.0.0.1:8080/swagger-ui/index.html`。
  - 页面下方文档分类、文档预览、下载和外链能力不变。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-04 文档中心顶部 Hero 精准对齐说明

- `GET /docs`
  - 顶部 Hero 使用 `frontend/images/文档中心.png` 展示系统截图，不再显示右上角大 Logo。
  - “快速开始”按钮为前端页面内交互：播放玻璃淡入转场，选中技术文档分类下的 `API 文档说明`，并滚动到 `#docs-workbench`。
  - “GitHub”按钮打开 `https://github.com/Qiang0129/ByteDance_competition`。
  - “Swagger”按钮继续打开 `http://127.0.0.1:8080/swagger-ui/index.html`。
  - 文档预览、下载、分类和外部接口能力不变。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-04 文档中心 Hero 细节对齐修正说明

- `GET /docs`
  - 顶部 Hero 背景噪点改为细砂颗粒效果，标题样式、按钮位置和右下角截图圆角细节已调整。
  - 标题下方说明文案已移除。
  - “快速开始”仍为前端页面内交互：播放玻璃淡入转场，选中技术文档分类下的 `API 文档说明`，并滚动到 `#docs-workbench`。
  - “GitHub”和“Swagger”按钮行为不变。
  - 后端 API、数据库、鉴权逻辑均无变更。

## 2026-06-05 Owner 审核分布标题与升级争议口径修正说明

- 本轮无新增、删除后端 API。
- `GET /api/dashboard/review-distribution`
  - 响应字段保持 `aiPass / aiNeedHuman / aiReject / humanPass / humanReturned / humanDisputed`。
  - `humanDisputed` 统计口径修正为复用 Owner 争议样本统计：按当前 Owner 和选定周期内 `human_reviews.decision = 'escalate'` 的去重 annotation 数统计。
  - `humanPass / humanReturned` 仍只统计普通人工审核通过与打回修改，不包含升级争议。
- `GET /api/dashboard/review-distribution/report`
  - CSV 内容继续包含“升级争议”行。
  - 下载文件名改为 `review-distribution-{year}.csv`。
- 前端展示契约
  - Owner 数据看板卡片标题从“AI 审核分布”改为“审核分布”。
  - 下载提示文案同步使用“审核分布报告”。

## 2026-06-05 Owner 人工审核统计分配边界修正说明

- 本轮无新增、删除后端 API。
- 统计契约更新：
  - Owner 相关接口中的人工审核统计只纳入当前 Owner 任务下已分配 Reviewer 的 `human_reviews`。
  - 任务级分配要求：`human_reviews.reviewer_id` 必须存在于同任务的 `task_reviewer_allocations.reviewer_id`。
  - 题目级分配要求：如果任务存在 `task_review_items`，还必须满足该 Reviewer 被分配到当前 `task_id + item_id`。
- 受影响接口：
  - `GET /api/dashboard/review-distribution`
  - `GET /api/dashboard/review-distribution/report`
  - `GET /api/dashboard/disputes`
  - Owner 审核后台中基于人工审核汇总的任务列表、工作量、题目详情和统计接口。
- 不受影响：
  - AI 预审结果统计仍按当前 Owner 的任务/题目归属统计。
  - 标注员提交、任务基础数量、数据库结构、鉴权接口和前端 TypeScript 类型不变。

## 2026-06-05 Owner 数据看板 KPI 人数口径调整说明

- 本轮无新增、删除后端 API。
- `GET /api/dashboard/overview`
  - `kpis.activeLabelers` 替换为 `kpis.labelerCount`。
  - `kpis.submittedToday` 替换为 `kpis.reviewerCount`。
  - `labelerCount` 统计全平台 active 且拥有 `labeler` 角色的去重用户数。
  - `reviewerCount` 统计全平台 active 且拥有 `reviewer` 角色的去重用户数。
  - `kpis.deltas` 不再返回 `activeLabelers / submittedToday`，前端这两张卡展示固定说明。
- 前端展示契约
  - Owner 首页 KPI 标题显示“标注员数量”和“审核员数量”。
  - 两张人数卡底部显示“全平台可用角色”，不显示百分比趋势。

## 2026-06-05 Owner KPI 角色人员弹窗接口说明

- 新增接口：`GET /api/dashboard/role-users?role=labeler|reviewer`。
- 权限：仅 Owner 可调用。
- 用途：
  - Owner 数据看板点击“标注员数量”KPI 图标时查询 `role=labeler`。
  - 点击“审核员数量”KPI 图标时查询 `role=reviewer`。
- 响应：`DashboardItemsResponse<DashboardRoleUserResponse>`。
- 查询口径：
  - 全平台 active 用户。
  - `deleted_at IS NULL`。
  - 拥有指定角色。
  - 返回每个用户的全部角色标签，保证多角色账号展示清晰。
- 本轮无数据库结构变更，无任务分配、审核流转和 KPI 数值口径变更。

## 2026-06-05 Owner 任务进度图表接口说明

- 新增接口：`GET /api/dashboard/task-progress-chart`。
- 权限：仅 Owner 可调用。
- 响应：`DashboardItemsResponse<TaskProgressResponse>`。
- 用途：Owner 数据看板“任务进度”容器专用图表数据源。
- 统计口径与既有 `GET /api/dashboard/task-progress` 保持一致：
  - `total` 优先 items 数，其次任务 quota，最后非 voided assignment 数。
  - `approved` 统计 assignment 状态 `accepted / exported`。
  - `returned` 统计 assignment 状态 `returned`。
  - `pending` 由后端按 `max(total - approved - returned, 0)` 返回。
- 前端展示契约：
  - 默认折线图。
  - 支持切换柱状图。
  - tooltip 展示任务标题、总量、通过、打回、待处理。
- 本轮无数据库结构、鉴权规则、任务流转和旧 `task-progress` 接口删除变更。
## 2026-06-05 Owner KPI 角色人员弹窗玻璃质感说明

- 本轮无新增、删除或修改后端 API。
- `GET /api/dashboard/role-users?role=labeler|reviewer`
  - 响应结构和查询口径保持不变。
  - Owner 数据看板角色人员弹窗仅调整为半透明玻璃质感面板。
  - “标注人员”和“审核人员”弹窗共用同一视觉样式。
- 数据库结构、鉴权、KPI 数值、人员列表字段和前端类型均不变。
## 2026-06-05 Owner KPI 角色人员弹窗玻璃质感强化说明

- 本轮无新增、删除或修改后端 API。
- `GET /api/dashboard/role-users?role=labeler|reviewer`
  - 响应结构、查询口径和前端类型保持不变。
  - 仅增强角色人员弹窗展示：新增 Modal root 定向样式，遮罩层和内容层都采用玻璃质感。
  - 面板透明度降低，背景模糊更明显，避免弹窗继续呈现为纯白卡片。
- 数据库结构、鉴权、KPI 数值和人员列表字段均不变。
## 2026-06-05 文档页顶部 Hero 恢复说明

- `GET /docs`
  - 顶部 Hero 视觉恢复到上一版：无右侧大 Logo，标题为“沉淀项目文档，连接 标注流程.”。
  - “快速开始”仍为前端页面内交互：播放玻璃淡入转场，选中技术文档分类下的 `API 文档说明`，并滚动到 `#docs-workbench`。
  - “GitHub”仍打开 `https://github.com/Qiang0129/ByteDance_competition`。
  - “Swagger”仍打开 `http://127.0.0.1:8080/swagger-ui/index.html`。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-05 文档页 Hero 外框颜色统一修复说明

- `GET /docs`
  - 顶部 Hero 大矩形外框改为统一、低感知的外侧中性色描边，不再使用叠加在渐变内部的半透明边框。
  - 页面标题、按钮、右下角截图、文档中心工作台、文档预览、下载和分类交互均不变。
  - “快速开始”仍会播放玻璃淡入转场，选中技术文档分类下的 `API 文档说明`，并滚动到 `#docs-workbench`。
  - 后端 API、数据库、鉴权逻辑均无变更。

## 2026-06-05 Labeler 答题页贡献与本题历史 UI 说明

- 本轮无新增、删除或修改后端 API。
- `GET /api/assignments/{assignmentId}/item`
  - 响应结构保持不变。
  - Labeler 答题页新增“我的贡献（本任务）/ 本题历史”前端空态 UI。
  - 当前无真实接口字段时，贡献数字显示 `-`，本题历史显示“暂无本题历史记录”，不使用演示数据。
- 后续如需接入真实数据，建议在答题页题目详情响应中补充：
  - 当前标注员在本任务内的贡献统计：已提交、通过、打回。
  - 当前题目的链路历史：提交、AI 预审、人工审核、返修中/已修改等事件。

## 2026-06-05 Labeler 答题页贡献卡两列布局调整说明

- 本轮无新增、删除或修改后端 API。
- `GET /api/assignments/{assignmentId}/item`
  - 响应结构保持不变。
  - Labeler 答题页“我的贡献（本任务）/ 本题历史”信息卡仅调整为桌面端两列展示。
  - 数据空态策略保持不变：贡献数字显示 `-`，本题历史显示“暂无本题历史记录”。

## 2026-06-05 Labeler 答题页贡献数字圆环样式说明

- 本轮无新增、删除或修改后端 API。
- `GET /api/assignments/{assignmentId}/item`
  - 响应结构保持不变。
  - Labeler 答题页“我的贡献（本任务）”三项指标仅将空态数字展示改为 SVG 圆环。
  - 数据空态策略保持不变：圆环中心显示 `-`，不使用演示数字。
## 2026-06-05 文档页 Hero 底部白线移除说明

- `GET /docs`
  - 顶部 Hero 截图上方过渡层去除了横向白色分隔线。
  - 页面标题、按钮、外框、右下角截图、文档中心工作台、文档预览、下载和分类交互均不变。
  - “快速开始”仍会播放玻璃淡入转场，选中技术文档分类下的 `API 文档说明`，并滚动到 `#docs-workbench`。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-05 文档页 Hero 文字按钮整体上移说明

- `GET /docs`
  - 顶部 Hero 左侧胶囊标签、标题和按钮整体上移，属于前端视觉位置调整。
  - 页面标题文案、按钮、外框、右下角截图、文档中心工作台、文档预览、下载和分类交互均不变。
  - “快速开始”仍会播放玻璃淡入转场，选中技术文档分类下的 `API 文档说明`，并滚动到 `#docs-workbench`。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-05 文档页 Hero 磨砂背景质感复刻说明

- `GET /docs`
  - 顶部 Hero 背景调整为更接近 new-api 截图的多色雾化与高密度细砂颗粒效果。
  - 页面标题文案、按钮、外框、右下角截图、文档中心工作台、文档预览、下载和分类交互均不变。
  - “快速开始”仍会播放玻璃淡入转场，选中技术文档分类下的 `API 文档说明`，并滚动到 `#docs-workbench`。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-05 文档页 Hero 磨砂背景缓慢漂移动画说明

- `GET /docs`
  - 顶部 Hero 磨砂背景增加低速漂移动画，粉色、蓝紫和青色区域会缓慢移动。
  - 页面标题文案、按钮、外框、右下角截图、文档中心工作台、文档预览、下载和分类交互均不变。
  - “快速开始”仍会播放玻璃淡入转场，选中技术文档分类下的 `API 文档说明`，并滚动到 `#docs-workbench`。
  - 系统设置 `prefers-reduced-motion: reduce` 时会关闭该背景漂移动画。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-05 关于页背景统一说明

- `GET /about`
  - 页面背景从 Hero 区和 README 区分段背景调整为统一页面级渐变背景。
  - README 阅读器仍保持白色卡片和独立滚动容器，保证文档可读性。
  - 顶部导航、摘要卡片、README 渲染、左侧目录点击定位和页脚行为均不变。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-05 关于页 README 目录折叠说明

- `GET /about`
  - README 阅读器左侧目录新增前端折叠/展开按钮。
  - 收起目录后左侧保留窄栏按钮，README 正文区域扩展显示。
  - 展开后恢复目录列表，目录点击定位、URL hash 更新和当前标题高亮逻辑保持不变。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-05 文档页主题色与背景动画速度说明

- `GET /docs`
  - 文档中心 Hero 和文档工作台高亮色改为跟随系统当前主题色变量。
  - Hero 磨砂背景保留多层渐变和细砂颗粒，但主色由 `--lh-primary` 系列变量驱动。
  - Hero 背景漂移动画速度调快，粉色/蓝紫/主题色区域移动更容易被感知。
  - 文档资源、预览、下载、分类展开、快速开始、GitHub 和 Swagger 行为均不变。
  - 后端 API、数据库、鉴权逻辑均无变更。
## 2026-06-05 文档中心滚轮浏览修复说明

- `GET /docs`
  - 左侧资源列表支持鼠标滚轮浏览全部资源。
  - 右侧文档预览区支持鼠标滚轮浏览 Markdown 和 DOCX 的完整内容。
  - 页面整体仍可滚动到 Hero、文档工作台和页脚。
  - 文档资源、预览渲染、下载、分类展开、快速开始、GitHub 和 Swagger 行为均不变。
  - 后端 API、数据库、鉴权逻辑均无变更。

## 2026-06-05 文档中心侧栏折叠与 DOCX 自适应说明

- `GET /docs`
  - 文档中心左侧导航新增收起/展开能力。
  - 左侧资源列表隐藏可见滚动条，但保留滚轮、触控板和键盘滚动浏览能力。
  - 侧栏收起后，右侧文档预览区域自动扩宽；再次展开后恢复原布局。
  - DOCX 预览会根据右侧预览区域宽度自动缩放，尽量完整显示 Word 页面左右内容。
  - Markdown、图片、缺失资源、Swagger 外链和下载行为保持不变。
  - 本轮仅为前端交互和展示调整，无后端 API、数据库和鉴权变更。
## Labeler 单题提交审核入口调整

### 接口变更
- 本次不新增后端接口、不新增数据库迁移。
- Labeler 答题页主提交入口改为复用现有单题提交接口：
  - `POST /api/assignments/{assignmentId}/submit`

### 前端调用语义
- 点击答题页底部“提交审核 / 重新提交审核”时，只提交当前 `assignmentId`。
- 后端完成答案校验、annotation 创建和 AI 预审入队；未开启 AI 预审时进入人工审核。
- 提交成功后前端自动进入下一题；最后一题则重新请求当前题详情刷新状态。

### 保留接口
- `POST /api/tasks/{taskId}/assignments/submit` 继续保留用于兼容旧批量提交流程。
- 本轮答题页不再将该接口作为普通提交主流程。

## 2026-06-05 构建产物忽略规则说明

- 本轮仅更新仓库忽略规则：
  - 忽略 `backend/target/`
  - 忽略 `frontend/.vite/`
- 无新增接口。
- 无接口参数、响应结构或调用语义变更。
## Labeler AI 助手浮动按钮拖动说明

### 接口变更
- 本次无新增、删除或修改后端 API。
- LLM 助手仍使用现有接口：
  - `POST /api/labeler/assignments/{assignmentId}/assistant`

### 前端交互变更
- Labeler 答题页 AI 助手浮动入口支持鼠标/触摸拖动。
- 按钮坐标保存在浏览器 `localStorage`，不上传后端。
- 点击打开 Drawer、流式对话、复制回答、清空对话等接口调用逻辑不变。

## Labeler 答题页贡献与本题历史字段说明

### 接口
- `GET /api/assignments/{assignmentId}/item`

### 响应新增字段
- `contribution`
  - `submittedCount`: number，当前 Labeler 在本任务内已提交过的题目数，按 assignment 去重。
  - `approvedCount`: number，当前 Labeler 在本任务内通过类题目数，包含人工通过、修订通过以及已进入 accepted/exported 的题目。
  - `returnedCount`: number，当前 Labeler 在本任务内历史上被人工打回过的题目数，后续通过后仍保留历史计数。
  - `rejectedCount`: number，当前 Labeler 在本任务内争议终审驳回/人工终审拒绝的题目数；不统计 AI 预审 `REJECT`，不统计普通初审/复审打回。
  - `disputedCount`: number，当前 Labeler 在本任务内曾被升级争议的题目数，按 assignment 去重。
- `itemHistory`
  - `id`: string，前端列表 key。
  - `type`: string，取值为 `submit`、`ai_review`、`human_review`、`rework`。
  - `title`: string，展示标题，例如 `提交`、`重新提交`、`AI 预审（Revision 2）`、`初审`、`复审`、`终审`、`初审升级`、`复审升级`、`修改中`。
  - `actor`: string，操作者；Labeler 自己显示 `我`，AI 显示 `AI Agent`，人工审核显示真实 Reviewer 名称。
  - `decision`: string | null，标准化大写决策值，例如 `SUBMIT`、`PASS`、`REJECT`、`NEED_HUMAN_REVIEW`、`APPROVE`、`REVISE`、`RETURN`、`ESCALATE`、`REWORKING`。
  - `reason`: string | null，人工审核原因。
  - `comment`: string | null，AI 预审说明或当前状态说明。
  - `score`: number | null，AI 预审总分。
  - `occurredAt`: string，发生时间；当前修改中可为空。
  - `status`: string，取值为 `pending`、`completed`、`current`。

### 统计与权限口径
- 该接口仍只允许当前 Labeler 访问自己的 assignment。
- 贡献统计限定为当前 Labeler + 当前任务。
- 本题历史限定为当前 assignment 链路，不展示同题其他标注员的记录。
- 本轮不新增数据库表和迁移。
- 前端“本题历史”的“全部”按钮始终显示，点击后使用当前响应中的 `itemHistory` 打开右侧 Drawer 展示完整历史；无历史时 Drawer 显示空态。

## Labeler 工作概览 KPI 展示说明

### 接口
- `GET /api/labeler/overview`

### 本轮变更
- 无新增、删除或修改后端字段。
- Labeler 工作概览第四个 KPI 从“平均耗时”改为“审核通过率”。
- 前端复用已有字段：
  - `heroStats.reviewPassRate`：作为主值，按百分比显示。
  - `heroStats.weeklySubmitted`：作为趋势文案显示本周提交量。
- `kpis.avgDurationSec` 和 `todayProgress.avgDurationSec` 仍保留在接口中；今日进度预估提示可继续使用。

## 2026-06-05 文档中心折叠态与 DOCX 显示修复说明

### 前端路由
- `GET /docs`

### 交互修正
- 左侧侧栏收起后只保留展开按钮，不再显示被压缩的分类/资源文字。
- 点击展开按钮后恢复完整侧栏和当前选中资源。
- DOCX 预览允许随右侧显示区域适度放大，侧栏收起后文档页面会更充分占用右侧宽度。
- DOCX 仍按右侧可用宽度计算缩放，避免左右内容被裁切。

### 接口影响
- 本轮无后端 API 变更。
- 无数据库变更。
- 无鉴权逻辑变更。
- 文档下载、Swagger 外链、Markdown 预览和资源清单均保持不变。

## 2026-06-05 文档中心预览全屏说明

### 前端路由
- `GET /docs`

### 交互新增
- 左侧侧栏收起按钮样式对齐 `/about` README 目录按钮，位置调整为侧栏标题右侧。
- 右侧文档预览头部新增“全屏”按钮。
- 点击“全屏”后，当前文档预览区域进入全屏观看模式。
- 全屏模式下：
  - 文档内容仍可滚动浏览。
  - 下载和外部链接按钮保持可用。
  - 再次点击“退出全屏”或按 Esc 可恢复原布局。

### 接口影响
- 本轮无后端 API 变更。
- 无数据库变更。
- 无鉴权逻辑变更。
- 文档资源清单、下载、Swagger 外链、Markdown 和 DOCX 渲染入口均保持不变。

## 2026-06-05 Owner KPI 角色人员悬浮展示说明

### 接口
- 本轮无新增、删除或修改后端 API。
- Owner 数据看板角色人员列表继续使用现有接口：
  - `GET /api/dashboard/role-users?role=labeler|reviewer`

### 前端展示契约
- “标注员数量”和“审核员数量”KPI 左上角图标由点击打开大 Modal，调整为鼠标悬停展示小型 Popover。
- Popover 悬停打开时按角色懒加载人员列表，已加载角色在本页内复用缓存。
- Popover 展示人员姓名、用户名、账号状态和角色标签；接口失败时在 Popover 内显示错误和重试按钮。
- 角色人员统计口径、接口鉴权、请求参数和响应字段均保持不变。

## 2026-06-05 Owner KPI 角色人员 Popover 滚动条说明

### 接口
- 本轮无新增、删除或修改后端 API。
- 角色人员列表仍使用：
  - `GET /api/dashboard/role-users?role=labeler|reviewer`

### 前端展示契约
- 角色人员 Popover 内部列表隐藏可见滚动条。
- 列表滚动能力保留，人员较多时仍可通过滚轮、触控板或键盘滚动查看。
- KPI 统计口径、接口鉴权、请求参数和响应字段均保持不变。

## 2026-06-05 文档页外层背景说明

### 前端路由
- `GET /docs`

### 前端展示契约
- 文档页 Hero 左右留白、Hero 下方到文档工作台之间的外层区域，使用和公开首页一致的白底柔光背景。
- Hero 内部磨砂噪点卡片、按钮、截图、快速开始跳转、GitHub 外链、Swagger 外链保持不变。
- 文档中心侧边栏、文档预览、下载和全屏交互保持不变。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 文档页快速开始按钮样式说明

### 前端路由
- `GET /docs`

### 前端展示契约
- `/docs` 顶部 Hero 中“快速开始”主按钮为无描边渐变胶囊样式。
- 按钮默认态和 hover 态均不显示边框线条。
- `GitHub`、`Swagger` 次按钮样式保持不变。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 文档页滚动落点贴齐导航说明

### 前端路由
- `GET /docs`

### 前端交互契约
- 点击顶部 Hero 的“快速开始”按钮后，文档工作台继续通过原生 `scrollIntoView` 进入视口。
- 文档工作台顶部落点调整为与固定导航栏下沿基本贴齐。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 文档页快速开始原生滚动说明

### 前端路由
- `GET /docs`

### 前端交互契约
- 点击顶部 Hero 的“快速开始”按钮后，继续自动切换到 `技术文档 / API 文档说明`。
- 资源切换完成后，文档工作台使用浏览器原生 `scrollIntoView` 进入视口。
- 通过 `.landing-docs-workbench` 的 `scroll-margin-top` 控制最终阅读落点，效果等同于用户用鼠标滚轮自然把页面向上滑动。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 文档页快速开始真实滚动说明

### 前端路由
- `GET /docs`

### 前端交互契约
- 点击顶部 Hero 的“快速开始”按钮后，继续自动切换到 `技术文档 / API 文档说明`。
- 页面不进入 quick reading 阅读态，Hero 不压缩、不隐藏文案、不移动截图。
- 通过 `document.scrollingElement` 执行真实页面平滑滚动，效果等同于用户用鼠标滚轮把页面向上滑动。
- 滚动结束后会按当前 DOM 位置进行一次校准，保证文档工作台停在合适阅读位置。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 文档页快速开始阅读态说明

### 前端路由
- `GET /docs`

### 前端交互契约
- 点击顶部 Hero 的“快速开始”按钮后，页面进入 quick reading 阅读态。
- 阅读态下继续自动选中 `技术文档 / API 文档说明`。
- Hero 区域自动收短，主文案和按钮淡出，底部截图保留少量露出。
- 文档工作台上移成为主要阅读区域。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 文档页快速开始滚动可靠性说明

### 前端路由
- `GET /docs`

### 前端交互契约
- 点击顶部 Hero 的“快速开始”按钮后，继续自动切换到 `技术文档 / API 文档说明`。
- 自动滚动改为在目标资源和左侧资源列表渲染完成后执行。
- 首次滚动后会基于实时 DOM 位置补滚一次，保证最终停在文档工作台阅读位置。
- `/docs` 背景层级规则不再覆盖公开导航的 fixed 定位。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 文档页快速开始阅读位置说明

### 前端路由
- `GET /docs`

### 前端交互契约
- 点击顶部 Hero 的“快速开始”按钮后，继续自动选中 `技术文档 / API 文档说明`。
- 页面滚动目标调整为文档工作台成为主要阅读区域的位置，而不是仅让工作台标题避开导航遮挡。
- 滚动前等待资源列表和预览头部渲染完成，再按实时 DOM 位置计算落点。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 文档页快速开始跳转说明

### 前端路由
- `GET /docs`

### 前端交互契约
- 点击 `/docs` 顶部 Hero 的“快速开始”按钮后：
  - 自动切换到 `技术文档` 分类。
  - 左侧面板进入资源列表模式。
  - 自动选中并展示 `API 文档说明`。
  - 页面滚动到文档工作台时预留顶部偏移，避免固定公开导航遮挡标题。
  - 播放柔光上移转场动画。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 文档页外层光晕增强说明

### 前端路由
- `GET /docs`

### 前端展示契约
- `/docs` 外层页面背景在白底基础上增加蓝青柔光层，红框所示留白区域不再呈现纯白。
- Hero 内部磨砂噪点、按钮、截图和下方文档工作台功能保持不变。
- 文档预览、下载、全屏、GitHub 外链和 Swagger 外链保持不变。

### 接口影响
- 本轮无新增、删除或修改后端 API。
- 无数据库变更。
- 无鉴权逻辑变更。

## 2026-06-05 Owner 审核员邀请注册链接接口

### 新增接口
- `POST /api/auth/reviewer-invitations`
  - 鉴权：Owner。
  - 作用：生成审核员专用注册链接 token。
  - 响应：`{ token: string, expiresAt: string }`。
  - 语义：token 24 小时有效，单次使用；数据库仅保存 token hash。
- `GET /api/auth/reviewer-invitations/validate?token=...`
  - 鉴权：公开接口。
  - 作用：校验邀请链接是否仍可用于注册。
  - 响应：`{ valid: boolean, reason?: "invalid" | "expired" | "used", expiresAt?: string }`。

### 注册接口扩展
- `POST /api/auth/register`
  - 请求体新增可选字段：`inviteToken?: string`。
  - 无 `inviteToken`：后端强制创建 `labeler` 账号，不再信任请求体 `role` 字段。
  - 有有效 reviewer invite token：创建 `reviewer` 账号，并将邀请记录标记为已使用。
  - token 已使用：返回 `409 INVITATION_USED`。
  - token 无效：返回 `400 INVALID_INVITATION`。
  - token 过期：返回 `400 INVITATION_EXPIRED`。

### 前端展示契约
- Owner 数据看板“审核员数量”悬浮 Popover 中可生成审核员邀请链接。
- 生成链接格式：`/login?reviewerInvite=<token>#signup`。
- 被邀请人打开链接后仍使用现有注册界面，注册成功后使用“人工审核员”角色登录。
- 普通公开注册仍创建标注员账号。

## 2026-06-05 登录页自动角色入口说明

### 接口
- 本轮无新增、删除或修改后端 API。
- `POST /api/auth/login` 请求体中的 `role` 字段继续保留兼容旧调用，但新版前端登录页不再发送该字段。

### 前端行为
- 登录页只要求输入用户名和密码。
- 登录成功后根据响应 `user.roles` 自动进入对应工作台。
- 多角色账号沿用前端 `resolveLandingPath` 的既有优先级，不再由用户在登录页选择角色。
- 演示账号按钮仍可用，但也不再显式传登录角色。

## 2026-06-05 Labeler 工作概览今日奖励接口说明

### 接口扩展
- `GET /api/labeler/overview`
  - `kpis` 新增字段：`todayReward: number`。
  - 含义：当前登录标注员今日通过入库题目对应的奖励估算金额。
  - 统计口径：当天通过入库的最新有效 annotation 关联任务 `reward_rule.rewardPerItem` 累加；`assignments.status = voided`、`annotations.status = voided`、已删除任务不计入；通过时间优先取 `human_review.approve` / `human_review.revise.accept` 审计时间。

### 响应字段片段
```json
{
  "kpis": {
    "activeTasks": 0,
    "submittedToday": 0,
    "returnedItems": 0,
    "avgDurationSec": 0,
    "todayReward": 0.0
  }
}
```

### 接口影响
- 无新增接口。
- 无数据库结构变更。
- 保留 `avgDurationSec` 兼容旧前端。

## 2026-06-06 Labeler 奖励通过入库口径接口说明

### 接口变更
- `GET /api/labeler/overview`
  - 原有 `heroStats.monthlyRewardEstimate` 与 `kpis.todayReward` 字段名保持兼容。
  - `heroStats.monthlyRewardEstimate` 改为本月通过入库奖励估算。
  - `kpis.todayReward` 改为今日通过入库奖励估算。

### 统计口径
- 只统计当前 Labeler、未删除任务、非作废 assignment 的最新非作废 annotation。
- 仅当 assignment 和 annotation 均为 `accepted/exported` 通过类状态时计入。
- 每个 assignment 只按最新有效 annotation 计一次，避免返修多 revision 重复计费。
- 奖励金额继续取 `tasks.reward_rule.rewardPerItem`，缺失按 0 处理。
- 通过时间优先取 annotation 审计日志 `human_review.approve` 或 `human_review.revise.accept` 的 `created_at`；缺失时回退到人工审核时间或 annotation 更新时间。

### 前端展示
- Labeler 工作概览 Hero 右侧展示“本月通过奖励”。
- KPI 第四项展示“今日通过奖励”。
- 当前字段仍为奖励估算，不代表正式财务结算或提现记录。
## 2026-06-06 Labeler 本月通过奖励明细接口说明

### 接口变更
- `GET /api/labeler/overview`
  - 在 `heroStats` 内新增 `monthlyRewardDetails` 数组。
  - 不新增接口路径，不新增数据库结构，不改变 `monthlyRewardEstimate` 和 `todayReward` 字段名。

### `monthlyRewardDetails` 字段
```json
[
  {
    "taskId": "1",
    "assignmentId": "10",
    "annotationId": "100",
    "taskTitle": "QA 质量评估",
    "itemId": "20",
    "itemIndex": 1,
    "itemTitle": "QA 质量评估 · 第 1 题",
    "acceptedAt": "2026-06-06 20:30",
    "rewardPerItem": 0.6
  }
]
```

### 统计口径
- 明细仅包含当前 Labeler 本月通过入库的奖励题目。
- 仅当 assignment 与最新有效 annotation 均为 `accepted/exported` 时进入明细。
- 通过时间优先取 annotation 审计日志 `human_review.approve` 或 `human_review.revise.accept`；缺失时回退到人工审核时间或 annotation 更新时间。
- 明细按 `acceptedAt DESC, annotationId DESC` 排序。
- `heroStats.monthlyRewardEstimate` 由 `monthlyRewardDetails.rewardPerItem` 汇总并保留两位小数，确保 Hero 金额与悬停弹窗列表一致。

### 前端展示
- Labeler 工作概览页悬停“本月通过奖励”时展示 `monthlyRewardDetails`。
- 前端第一层 Popover 按 `taskId + taskTitle` 聚合展示任务名、获奖题数和奖励小计。
- 点击某个任务汇总项后打开右侧 Drawer，展示该任务下的题目级明细：`itemTitle`、题目 ID、通过时间和单价。
- Popover 中的合计/小计金额，以及 Drawer 中的合计/单价金额，统一使用奖励金额气泡样式展示；字段和值不变。
- 第一层 Popover 不再提供“查看详情”按钮；Drawer 内也不提供跳转答题页入口。
## 2026-06-05 文档页快速开始定位调整接口说明

### 接口变更
- 无新增后端 API。
- 无删除后端 API。
- 无修改后端 API。
- 无 TypeScript 接口类型变更。

### 前端展示契约
- 公开文档页点击“快速开始”后，前端切换到技术文档分类下的“API 文档说明”。
- 点击后不再显示全屏过渡光带。
- 文档工作台滚动到固定导航下方并保留视觉间距，避免被上方截图预览区域遮挡。

## 2026-06-05 Labeler 任务市场 KPI 统计接口

### 新增接口
- `GET /api/market/tasks/stats`
  - 鉴权：当前登录用户必须包含 `labeler` 角色。
  - 作用：返回任务市场顶部 KPI 的真实后端统计。

### 响应字段
```json
{
  "availableTasks": 0,
  "avgRewardPerItem": 0.0,
  "expiringSoonTasks": 0
}
```

### 字段说明
- `availableTasks`：当前标注员可领取或可继续处理的任务数量。
- `avgRewardPerItem`：可领取任务的平均单价，按任务维度平均 `reward_rule.rewardPerItem`，保留两位小数。
- `expiringSoonTasks`：可领取任务中 deadline 在当前时间到 48 小时内的任务数量。

### 统计口径
- 任务必须未删除、状态为 `published`、未过截止时间。
- 普通抢单和配额模式：任务还有可领取题目，且当前标注员未超过每人领取上限。
- 指派模式：当前标注员已有该任务的非 voided assignment，且 assignment 状态为 `claimed` 或 `returned`。
- 不计入已截止、已暂停、配额耗尽、题目耗尽、当前标注员已达领取上限的任务。

### 前端接入
- `labelerApi.getMarketTaskStats()` 调用该接口。
- `TaskMarket.tsx` 顶部 KPI 使用该接口返回值，不再从当前页面列表临时派生。
## 2026-06-05 发布任务表单缺失字段提示接口说明

### 接口变更
- 无新增后端 API。
- 无删除后端 API。
- 无修改后端 API。
- 无 TypeScript 请求/响应类型变更。

### 前端展示契约
- Owner 在新建/编辑标注任务抽屉内提交时，如果表单字段校验失败，前端会弹出全局 `message.error` 小提示。
- 提示会使用第一个错误字段的校验文案，并自动切换到该字段所在页签。
- 原有字段下方校验文案、任务发布成功提示、业务规则错误提示保持不变。
## 2026-06-05 草稿保存完整性校验说明

### 接口变更
- 无新增后端 API。
- 无删除后端 API。
- 无修改后端 API 路径。
- `POST /api/tasks` 与 `PUT /api/tasks/{taskId}` 请求结构保持不变。

### 前端调用契约
- `status=draft`：前端“存为草稿”读取当前表单值直接保存，不触发表单完整性校验。
- `status=published`：前端“立即发布/续期发布”继续触发表单完整性校验，缺失字段会显示全局 `message.error` 提示。
- 新建草稿如果标题为空，前端使用“未命名任务草稿”作为请求标题兜底。

## 2026-06-05 任务市场已领取任务去作答按钮说明

### 接口影响
- 本轮无新增、删除或修改后端接口。
- `GET /api/market/tasks` 返回的 `claimedByMe`、`claimable`、`remainingQuota` 语义保持不变。

### 前端交互修正
- 已领取任务：`claimedByMe = true` 时按钮显示“去作答”，不再受 `claimable=false` 或剩余配额为 0 影响。
- 未领取任务：仍按 `claimable=false`、过期、配额为 0 禁用认领按钮。
## 2026-06-05 未命名任务草稿标识

### 接口变更
- 本轮无新增、删除或修改后端 API。
- 继续复用现有 Owner 任务创建/更新接口保存草稿。

### 前端展示契约
- Owner 保存草稿时，如果标题为空，前端会提交自动生成标题：`未命名任务草稿 MM-DD HH:mm`。
- Owner 任务管理列表中，`state='draft'` 且标题以 `未命名任务草稿` 开头的任务会被识别为自动命名草稿。
- 自动命名草稿在列表中使用弱化标题样式，并显示“自动命名”标签。

### 兼容性
- 后端 `title` 字段仍保持非空语义。
- 现有任务接口请求体和响应体结构不变。
- 正式发布任务仍需要完整表单校验；草稿保存仍允许部分字段缺失。

## 2026-06-05 任务市场已截止任务按钮禁用说明

### 接口影响
- 本轮无新增、删除或修改后端接口。
- 前端继续使用 `GET /api/market/tasks` 返回的 `expired`、`statusLabel`、`claimedByMe`、`claimable` 字段。

### 前端交互修正
- 已截止任务：无论是否已领取，按钮显示“已截止”并禁用。
- 未截止已领取任务：按钮显示“去作答”，可跳转到 `/labeler/my-tasks`。
- 未领取任务：仍按 `claimable=false`、配额为 0 禁用认领按钮。
## 2026-06-05 Owner 侧边栏分组标题英文化

### 接口变更
- 本轮无新增、删除或修改后端 API。
- 本轮无请求体、响应体和 TypeScript 接口结构变更。

### 前端展示契约
- Owner 侧边栏分组标题展示为英文：
  - `DATA PRODUCTION`
  - `REVIEW & QA`
  - `DATA DELIVERY`
- 分组下具体菜单项文案和路由保持不变。
## 2026-06-05 文档页快速开始按钮 Hover 视觉修正

### 接口变更
- 本轮无新增、删除或修改后端 API。
- `GET /docs` 路由和文档资源切换逻辑不变。

### 前端展示契约
- `/docs` 顶部 Hero 中“快速开始”按钮默认态保持蓝色主按钮样式。
- 鼠标移入“快速开始”按钮时，按钮背景改为半透明蓝色渐变玻璃态。
- “GitHub”“Swagger”按钮的 hover 行为保持不变。

### 兼容性
- 不影响“快速开始”触发的资源切换、自动滚动和转场逻辑。
- 不影响文档中心其他按钮、预览区、下载或全屏功能。
## 2026-06-05 文档页快速开始按钮边框线清理

### 接口变更
- 本轮无新增、删除或修改后端 API。
- `/docs` 路由行为和文档资源切换逻辑不变。

### 前端展示契约
- `/docs` 顶部 Hero 中“快速开始”按钮不再使用透明边框占位。
- 主按钮默认态和 hover 态均不应出现可见细边框线。
- “GitHub”“Swagger”按钮维持现有白底描边样式。

### 兼容性
- 不影响“快速开始”按钮点击后的资源切换、自动滚动和转场逻辑。
- 不影响文档中心其他按钮、下载、全屏和预览功能。
## 2026-06-05 文档页顶部标签可读性修复

### 接口变更
- 本轮无新增、删除或修改后端 API。
- `/docs` 路由、文档资源切换逻辑和按钮交互不变。

### 前端展示契约
- `/docs` 顶部 Hero 标签“数据标注文档中心”保持原文案和位置不变。
- 标签底色调整为更偏白的玻璃底，文字颜色调整为更深的蓝色。
- 标签在当前蓝色噪点背景下应保持清晰可读。

### 兼容性
- 不影响顶部 Hero 其他元素的布局和交互。
- 不影响文档中心预览、下载、全屏和滚动行为。
## 2026-06-05 Owner 数据看板真实任务节点与截止预警

### 新增接口：任务关键节点
- Method: `GET`
- Path: `/api/dashboard/task-milestones`
- Query:
  - `limit`: 可选，默认 `4`，最大 `12`
- 权限：Owner
- Response:
```json
{
  "items": [
    {
      "taskId": "19",
      "title": "测试3",
      "total": 30,
      "approved": 4,
      "returned": 0,
      "pending": 26,
      "status": "published",
      "reviewStatus": "ai_prereviewing",
      "currentPhase": "ai_review"
    }
  ]
}
```
- `currentPhase` 取值：
  - `published`
  - `ai_review`
  - `human_review`
  - `delivered`

### 新增接口：临近截止预警
- Method: `GET`
- Path: `/api/dashboard/deadline-alerts`
- Query:
  - `limit`: 可选，默认 `4`，最大 `12`
- 权限：Owner
- Response:
```json
{
  "items": [
    {
      "taskId": "19",
      "title": "测试3",
      "pending": 26,
      "deadline": "2026-06-06 20:00",
      "hoursLeft": 24,
      "riskLevel": "warn"
    }
  ]
}
```
- `riskLevel` 取值：
  - `critical`: 剩余时间小于等于 12 小时
  - `warn`: 剩余时间小于等于 24 小时
  - `normal`: 剩余时间大于 24 小时

### 兼容接口说明
- `GET /api/dashboard/role-breakdown` 保持不变；前端展示标题调整为“标注员任务类型分布”。
- `GET /api/dashboard/disputes?days=7|14|30` 保持不变；前端增加无数据空状态。
- `GET /api/dashboard/task-progress` 和 `GET /api/dashboard/task-progress-chart` 保持不变。

### 前端展示契约
- “任务关键节点”只使用 `task-milestones.currentPhase` 展示阶段，不再按通过率推断。
- “临近截止预警”只使用 `deadline-alerts.hoursLeft` 和 `deadline-alerts.riskLevel` 展示剩余时间与风险色，不再前端模拟。
- 底部四个容器没有数据时显示空状态。
## 2026-06-05 Labeler 工作概览待处理类型分布

### 接口扩展
- Method: `GET`
- Path: `/api/labeler/overview`
- 权限：Labeler
- 本次新增响应字段：`pendingTypeDistribution`

```json
{
  "pendingTypeDistribution": [
    {
      "key": "text",
      "label": "Text",
      "count": 12
    },
    {
      "key": "image",
      "label": "Image",
      "count": 3
    }
  ]
}
```

### 字段说明
- `key`: 题目媒体类型，来自 `items.media_type`；为空时按 `text` 处理。
- `label`: 前端展示名称，复用工作概览已有题目类型映射。
- `count`: 当前标注员待处理 assignment 数量。

### 统计口径
- 统计当前登录标注员的 assignment。
- `assignments.status IN ('claimed', 'returned')` 视为待处理。
- 只统计未删除任务：`tasks.deleted_at IS NULL`。
- 按 `items.media_type` 聚合，空值或空字符串归为 `text`。

### 兼容说明
- 原 `supportedItemTypes` 字段保留，避免旧前端或其他调用方破坏兼容。
- 当前 Labeler 工作概览展示使用 `pendingTypeDistribution`；字段缺失时前端显示空态。
## 2026-06-05 文档页顶部标签白字无边框修复

### 接口变更
- 本轮无新增、删除或修改后端 API。
- `/docs` 路由、资源切换和页面交互逻辑不变。

### 前端展示契约
- `/docs` 顶部 Hero 标签“数据标注文档中心”文字改为白色。
- 标签不再使用可见边框描边。
- 标签仍保留原有位置、尺寸和胶囊结构。

### 兼容性
- 不影响 Hero 其他元素布局和视觉层级。
- 不影响文档中心预览、下载、全屏和滚动行为。
## 2026-06-05 Owner 截止预警跟进入口

### Frontend Route Contract
- `GET /owner/tasks?focusTaskId={taskId}`
- 用途：Owner 数据看板“临近截止预警”点击“跟进”后的前端定位入口。
- 行为：
  - 任务管理页读取 `focusTaskId`。
  - 自动将任务 ID 写入搜索关键词，过滤出对应任务。
  - 对匹配任务行增加轻量高亮。

### Backend API
- 本轮无新增、删除或修改后端 API。
- 继续复用现有任务列表加载接口和 Owner 任务管理页前端过滤逻辑。
## 2026-06-05 Owner 任务跟进高亮样式修复

- 本轮无新增、删除或修改后端 API。
- `/owner/tasks?focusTaskId={taskId}` 前端路由契约保持不变。
- 仅调整任务管理页目标任务行的前端高亮样式：整行浅蓝背景，第一列左侧一条提示线。
## 2026-06-05 任务市场任务摘要 LLM 助手字段

### GET `/api/market/tasks`

本次为任务市场任务摘要响应新增字段：

- `llmAssistEnabled: boolean`
  - 含义：任务发布时 Owner 是否开启 LLM 标注助手。
  - 来源：任务 `reward_rule` 元数据中的 `llmAssistEnabled`，由后端通过 `TaskMetadata.resolvedLlmAssistEnabled()` 解析。
  - 前端用途：Labeler 任务市场卡片和任务详情抽屉在该字段为 `true` 时展示 `LLM助手` 气泡。

### 兼容性

- 接口路径、查询参数和原有字段不变。
- 新字段为向后兼容扩展，不改变任务领取、作答、截止和 LLM 助手调用逻辑。
## 2026-06-05 文档预览全屏过渡动画

### 前端页面行为
- `GET /docs`
  - 文档预览区“全屏”按钮进入页面内 fixed 全屏观看态，并播放进入动画。
  - 全屏态按钮文案切换为“退出全屏”。
  - 点击“退出全屏”或按 Esc 时先播放退场动画，再恢复普通文档中心布局。
  - 全屏态保留当前文档内容、下载按钮、滚动能力和文档预览能力。

### 后端 API
- 本轮无新增、删除或修改后端 API。
- 文档资源下载、Swagger 外链和现有公开路由契约均保持不变。

### 兼容性
- 本轮全屏为前端页面内全屏，不强依赖浏览器原生 Fullscreen API，因此 Esc 退出也能保持动画一致。

## 2026-06-05 模板 Schema 多 Tab 元数据

### Schema Tab 类型

```json
{
  "id": "annotation",
  "label": "标注"
}
```

### 字段归属

- `SchemaField.layout.tab: string`
  - 含义：字段所属的模板 Tab ID。
  - 缺失或引用不存在的 Tab 时，前端和后端运行时按默认 `annotation` 处理。

### 更新接口

- `GET /api/schemas/{versionId}`
  - 响应新增 `tabs?: SchemaTab[]`。
- `POST /api/schemas/draft`
  - 请求新增可选 `tabs?: SchemaTab[]`。
- `PUT /api/schemas/{versionId}/draft`
  - 请求新增可选 `tabs?: SchemaTab[]`。
- `POST /api/schemas/validate`
  - 请求新增可选 `tabs?: SchemaTab[]`。
- Labeler 答题详情接口
  - 响应新增 `tabs?: SchemaTab[]`，用于 `LabelHubFormRenderer` 按 Tab 渲染表单。
- Reviewer 待审标注响应
  - 响应新增 `schemaTabs?: SchemaTab[]`，用于就地修订时保持和 Labeler 一致的表单分组。

### 兼容性

- 接口路径、权限和原有字段保持不变。
- 旧模板没有 `tabs` 时自动补默认：

```json
[
  {
    "id": "annotation",
    "label": "标注"
  }
]
```

- 本轮不新增数据库表或迁移，`tabs` 作为 Schema JSON 快照元数据保存。
- 提交答案结构不新增 Tab 字段，仍只提交可提交表单字段。

## 2026-06-06 模板 Designer 空画布拖拽落点修复

- 本轮无新增、删除或修改后端 API。
- Schema `tabs`、`fields`、`layout.tab` 契约保持不变。
- 仅调整 `/owner/templates/designer` 前端页面中 Tabs 内容区和空画布 droppable 区域的展示契约：空 Tab 下整块中间画布都应支持左侧物料拖入。
## 2026-06-06 任务市场卡片默认边框收敛

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。

### 说明

- 本次只调整 Labeler 任务市场卡片的前端默认边框样式。
- `GET /api/market/tasks` 等任务市场接口契约保持不变。
## 2026-06-06 文档预览全屏隐藏公开导航

### 前端页面行为
- `GET /docs`
  - 文档预览进入全屏态时隐藏公开页面顶部导航栏和页脚。
  - 点击“退出全屏”或按 Esc 完成退场动画后，公开导航栏和页脚恢复显示。
  - 全屏态继续保留当前文档内容、下载按钮和滚动能力。

### 后端 API
- 本轮无新增、删除或修改后端 API。
- 文档资源下载、Swagger 外链和公开路由契约均保持不变。
## 2026-06-06 任务市场卡片白色轮廓移除

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。

### 说明

- 本次只调整 Labeler 任务市场卡片默认边框与悬停描边样式。
- 任务市场相关后端接口契约保持不变。
## 2026-06-06 模板 Designer 多 Tab 拖拽与重命名修复

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库变更。

### 说明

- 本轮仅修复 `/owner/templates/designer` 前端交互。
- Schema `tabs`、字段 `layout.tab`、Labeler 答题接口 `tabs`、Reviewer 修订接口 `schemaTabs` 契约保持不变。
- Designer 中每个 Tab 的画布落点现在按 Tab 独立识别，Tab 重命名会继续通过现有 Schema 保存/发布流程持久化。
## 2026-06-06 后台公共顶部搜索框移除

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库变更。

### 说明

- 本轮仅移除后台公共顶部栏中的全局搜索框。
- 各业务页面内部的搜索/筛选输入保留，相关接口契约保持不变。
## 2026-06-06 文档预览全屏与下载提示

### 前端页面行为
- `GET /docs`
  - 点击文档预览区“全屏”后显示 toast：“全屏模式”。
  - 点击“退出全屏”或按 Esc 退出全屏后显示 toast：“退出全屏”。
  - 点击有效“下载”操作后显示 toast：“正在下载”。
  - toast 样式复用系统 Ant Design message，全屏动画、文档预览和下载能力保持不变。

### 后端 API
- 本轮无新增、删除或修改后端 API。
- 文档资源下载仍由前端静态资源链接或 Markdown Blob 触发。
## 2026-06-06 渐变主题白色边框收敛

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。

### 说明

- 本轮仅调整后台渐变主题下的前端视觉样式。
- 影响范围为顶部导航、主内容容器、后台内容卡片和常见玻璃态分段容器的默认描边表现。
- 所有业务接口契约保持不变。
## 2026-06-06 暗色模式侧边栏白色边框修复

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。

### 说明

- 本轮仅调整暗色模式下左侧导航栏外壳和底部分割线的前端样式。
- 导航路由、菜单数据和后端接口契约保持不变。
## 2026-06-06 暗色模式侧栏白色边框修复

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。

### 说明

- 本轮仅调整后台暗色模式下左侧导航栏的前端视觉样式。
- 所有业务接口契约、路由、权限逻辑和菜单数据保持不变。
## 2026-06-06 侧边栏折叠按钮位置与样式调整

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。

### 说明

- 本轮仅调整后台布局中的侧边栏折叠按钮位置与前端样式。
- 桌面端折叠入口从 Header 左侧迁移到侧边栏右侧悬浮按钮。
- 移动端抽屉侧栏入口仍保留在 Header 左侧。
- 导航路由、菜单结构和后端接口契约保持不变。
## 2026-06-06 侧边栏折叠按钮裁切与菜单项宽度修复

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。

### 说明

- 本轮仅调整后台侧边栏折叠按钮层级和菜单项视觉宽度。
- 侧边栏折叠/展开状态仍为前端本地 UI 状态。
- 后端接口、导航路由和菜单权限契约保持不变。
## 2026-06-06 系统默认渐变主题

### 前端行为
- 新用户或未保存外观偏好的浏览器环境，系统默认使用渐变样式版本。
- 已保存 `lh.theme.styleVersion` 的浏览器继续使用用户选择，不强制覆盖。
- 外观设置页仍支持在“默认版本”和“渐变版本”之间切换。

### 后端 API
- 无新增、删除或修改后端 API。
- 无请求参数、响应字段、数据库和鉴权逻辑变更。
## 2026-06-06 侧边栏选中菜单胶囊长度微调

- 本次无接口变更。
- 仅调整 `frontend/src/styles/global.css` 中侧边栏展开态菜单项的视觉宽度。
- 不新增、不修改、不删除任何后端 API。

## 2026-06-06 README 结构化重写

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无鉴权、路由、数据库和 Agent 接口契约变更。

### 文档影响

- `README.md` 从完整清单型文档调整为项目入口型文档。
- API 内容改为保留接口分组、Swagger UI、OpenAPI JSON、`接口文档.md`、前端 API 封装和后端 Controller 入口。
- 后续新增或修改接口时，仍需要在本文件中追加具体接口说明。
## 2026-06-06 侧边栏选中菜单右侧缩短

- 本次无接口变更。
- 仅调整 `frontend/src/styles/global.css` 中侧栏菜单项的左右留白。
- 导航路由、菜单权限和后端 API 均保持不变。
## 2026-06-06 关于页 README 目录折叠

### 前端页面行为
- `GET /about`
  - README 左侧目录默认只显示二级大标题。
  - 点击二级大标题后，在左侧展开或收起其下三级标题。
  - 点击目录项仍会定位右侧 README 正文到对应章节。
  - hash 或手动滚动命中三级标题时，会自动展开对应父级目录。

### 后端 API
- 无新增、删除或修改后端 API。
- 无请求参数、响应字段、数据库和鉴权逻辑变更。
## 2026-06-06 关于页大标题点击仅展开收起

### 前端页面行为
- `GET /about`
  - 点击 README 左侧二级大标题时，只展开或收起该标题下的三级目录。
  - 二级大标题点击不再滚动右侧 README 正文，也不更新 URL hash。
  - 点击三级目录标题仍会定位右侧 README 正文到对应章节。

### 后端 API
- 无新增、删除或修改后端 API。
- 无请求参数、响应字段、数据库和鉴权逻辑变更。
## 2026-06-06 关于页 README 目录滚动条隐藏

### 前端页面行为
- `GET /about`
  - 左侧 README 目录侧栏不再显示可见滚动条。
  - 目录侧栏仍保留滚轮、触控板和键盘滚动能力。
  - 右侧 README 正文滚动表现不变。

### 后端 API
- 无新增、删除或修改后端 API。
- 无请求参数、响应字段、数据库和鉴权逻辑变更。
## 2026-06-06 顶部用户头像边缘黑线修复

- 本次无接口变更。
- 仅调整顶部 Header 用户头像的前端 class 和 CSS。
- 用户信息、登录状态、权限和后端 API 均保持不变。
## 2026-06-06 文档页资源列表滚动条隐藏

### 前端页面行为
- `GET /docs`
  - 左侧文档资源列表不再显示可见滚动条。
  - 资源列表仍保留滚轮、触控板和键盘滚动能力。
  - 右侧文档预览区域滚动表现不变。

### 后端 API
- 无新增、删除或修改后端 API。
- 无请求参数、响应字段、数据库和鉴权逻辑变更。
## 2026-06-06 模板 Designer Tab 重命名按钮无响应修复

- 本次无接口变更。
- 仅调整 `/owner/templates/designer` 前端 Tab 重命名交互。
- Schema `tabs`、字段 `layout.tab`、保存草稿和发布接口契约均保持不变。
## 2026-06-06 关于页无子级二级标题点击跳转

### 前端页面行为
- `GET /about`
  - 有三级子目录的二级 README 标题点击时，仅展开或收起子目录。
  - 没有三级子目录的二级 README 标题点击时，会定位右侧 README 正文到对应章节。
  - 三级 README 标题点击定位行为保持不变。

### 后端 API
- 无新增、删除或修改后端 API。
- 无请求参数、响应字段、数据库和鉴权逻辑变更。
## 2026-06-06 模板 Designer Tab 重命名单击生效

- 本次无接口变更。
- 仅调整 `/owner/templates/designer` 前端 Tab 重命名按钮事件时机。
- Schema `tabs`、字段 `layout.tab`、保存草稿和发布接口契约均保持不变。
## 2026-06-06 侧边栏展开内容挤压修复

### 接口变更

- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。

### 说明

- 本轮仅调整后台桌面端侧边栏折叠 / 展开的前端动画体验。
- 导航菜单数据、路由、角色权限和后端接口契约保持不变。
## 2026-06-06 模板 Designer Tab 重命名父级状态托管
- 本次无接口变更。
- 仅调整 `/owner/templates/designer` 前端 Tab 重命名的状态托管、悬停提示和编辑按钮事件处理。
- 编辑按钮改为在点击捕获阶段启动重命名，指针事件只阻止 Tabs 父层冒泡；后端 API 契约不受影响。
- Schema `tabs`、字段 `layout.tab`、保存草稿和发布接口契约均保持不变。
## 2026-06-06 文档中心手动预览与分阶段进度优化

### 前端页面行为
- `GET /docs`
  - Markdown、DOCX、图片资源首次选中后不再自动展示内容。
  - 用户点击“预览”后才开始加载资源。
  - DOCX 资源加载中显示分阶段进度：准备预览、下载文件、解析文档、生成页面、完成布局、预览完成。
  - DOCX 加载过程中可点击“取消”，取消后回到未预览占位状态。
  - 当前页面会话内，同一资源预览成功后再次切回会直接显示缓存内容。
  - 缺失资源和 Swagger 外部链接资源保持原有展示与跳转方式。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和下载资源地址变更。
## 2026-06-06 文档中心全屏目录滚动条与收起动画修复

### 前端页面行为
- `GET /docs`
  - 大 Markdown 进入全屏预览后，左侧标题目录在内容超出可视高度时显示可见滚动条。
  - 全屏目录收起和展开时，左侧栏宽度、目录内容透明度和正文区域宽度同步过渡。
  - 目录收起后，左侧保留 `52px` 窄栏，展开按钮固定在窄栏顶部居中位置。
  - 普通模式文档目录仍保持原有隐藏滚动条观感。
  - DOCX、图片、Swagger 外链、缺少标题结构的 Markdown 全屏行为保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和下载资源地址变更。
## 2026-06-06 文档中心预览进度条颜色修复

### 前端页面行为
- `GET /docs`
  - 手动预览加载态的进度条灰色轨道内会显示彩色填充条。
  - 填充条继续随当前预览进度推进，并保留蓝绿渐变和流动高光动画。
  - 大 Markdown 的 `1500ms` 最短加载反馈、标题目录分节渲染、缓存、下载、取消加载和全屏能力保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和下载资源地址变更。
## 2026-06-06 文档中心 Markdown 大文件性能优化

### 前端页面行为
- `GET /docs`
  - `阶段计划.md`、`阶段实现.md`、`接口文档.md` 不再随页面初始加载进入主 bundle。
  - 用户点击“预览”后，前端动态加载对应 Markdown 源文本。
  - 超过 80KB 的 Markdown 使用分块虚拟渲染，只渲染当前可见区域附近内容。
  - 同一 Markdown 在当前页面会话内预览成功后会缓存源文本，再次切回不重复加载。
  - 对尚未预览的动态 Markdown 点击“下载”时，前端会先动态加载源文本，再生成本地 Blob 下载。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和下载资源地址变更。
## 2026-06-06 文档中心大 Markdown 进度动画与滚动修复

### 前端页面行为
- `GET /docs`
  - 大 Markdown 点击“预览”后，加载进度至少展示约 `1500ms`。
  - Markdown 加载期间进度条会持续推进，并带有流动高光动画。
  - 超过 `80KB` 的 Markdown 仍使用分块虚拟渲染。
  - 虚拟 Markdown 滚动统一使用右侧文档预览区外层滚动容器。
  - 同一页面会话内预览成功后的 Markdown 缓存行为保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和下载资源地址变更。
## 2026-06-06 文档中心大 Markdown 按标题分节渲染

### 前端页面行为
- `GET /docs`
  - 超过 `80KB` 且包含 H2/H3 标题的 Markdown 预览成功后，左侧资源面板会切换为当前文档标题目录。
  - 文档目录提供“文件列表”返回按钮，返回后可继续选择其他资源。
  - 默认选中文档第一个 H2/H3 标题，右侧只渲染当前标题对应章节。
  - 点击 H2 会渲染该 H2 的引导内容；若 H2 下存在 H3，则展开或收起子目录。
  - 点击 H3 会渲染该 H3 到下一个 H2/H3 前的内容。
  - 小 Markdown、无 H2/H3 标题的大 Markdown、DOCX、图片、缺失资源和外部链接展示逻辑保持不变。
  - 预览缓存、下载、全屏、取消加载和 1500ms 动态进度条行为保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和下载资源地址变更。

## 2026-06-07 文档中心资源列表精简说明

### 前端页面行为
- `GET /docs`
  - 分类进入后的左侧资源列表项只展示文件图标、文件名和文件类型气泡。
  - 文件简介不再显示在资源列表项中。
  - 文件类型气泡移动到文件名下方，并与文件名左侧对齐。
  - 分类卡片页、分类标题说明、右侧预览区说明、资源选择、预览、下载和外部链接行为保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和下载资源地址变更。

## 2026-06-07 文档中心系统架构图图片接入说明

### 前端页面行为
- `GET /docs`
  - 技术文档分类中的“系统架构图”资源由缺失占位改为可预览图片资源。
  - 图片来源为 `submission/screenshots/系统架构图.png`。
  - 资源类型气泡显示为 `PNG`。
  - 点击预览后复用现有图片预览组件展示系统架构图。
  - 下载时使用文件名 `系统架构图.png`。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和下载资源地址变更。

## 2026-06-07 文档中心图片 Ctrl 滚轮缩放说明

### 前端页面行为
- `GET /docs`
  - 图片类资源预览区域支持 `Ctrl + 滚轮` 放大和缩小。
  - 缩放仅在鼠标位于图片预览区域时生效，不影响 Markdown、DOCX、外部链接和缺失资源预览。
  - 缩放锚点按鼠标所在位置计算，便于查看图片局部细节。
  - 普通预览和全屏预览共享当前缩放比例，切换全屏不重置。
  - 未按 `Ctrl` 时，滚轮继续用于预览区域正常滚动。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由、下载资源地址和资源清单契约变更。

## 2026-06-07 文档中心图片缩放左侧不可达修复说明

### 前端页面行为
- `GET /docs`
  - 图片类资源放大后，横向滚动条拖到最左可以看到图片最左侧内容。
  - 图片类资源放大后，横向滚动条拖到最右仍可以看到图片最右侧内容。
  - 普通预览和全屏预览均使用同一套正向坐标舞台布局。
  - `Ctrl + 滚轮` 缩放范围、缩放速度、鼠标位置锚点和全屏共享缩放比例保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由、下载资源地址和资源清单契约变更。

## 2026-06-07 关于页 README 预览、全屏与下载说明

### 前端页面行为
- `GET /about`
  - 首次未预览时，README 正文区域显示 `README.md` 预览卡片。
  - 预览卡片内仅提供“预览”按钮，下载入口统一保留在阅读器右上角。
  - 点击“预览”后写入 `localStorage`：`labelhub.about.readme.previewed = "true"`，并展示 README Markdown 正文。
  - 刷新或重新进入 `/about` 时，如果本地持久标记存在，则直接展示 README 正文。
  - README 阅读器右上角提供“全屏 / 退出全屏”和“下载”按钮。
  - “下载”按钮在前端通过 Blob 生成当前 `README.md` 文件，不调用后端下载接口。
  - 全屏采用页面内 fixed 阅读模式，保留左侧 README 目录和右侧正文滚动。
  - 全屏模式支持按钮退出和 Esc 退出。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由、文件存储和下载接口变更。

## 2026-06-07 关于页全屏目录折叠与提示层修复说明

### 前端页面行为
- `GET /about`
  - README 全屏阅读器左侧目录使用独立折叠状态。
  - 全屏桌面模式下目录默认展开，小屏全屏模式下目录默认收起。
  - 全屏目录按钮可在展开和 52px 窄栏收起状态之间切换。
  - 全屏目录折叠操作不影响普通模式目录折叠状态。
  - README 复制代码、下载、进入全屏和退出全屏提示挂载到 README 阅读器内部。
  - 全屏模式下提示显示在全屏阅读器层内，不再显示到底层公开页。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由、文件存储、README 下载内容和本地存储 key 变更。

## 2026-06-07 LLM 触发组件属性配置 Select 宽度修复说明

### 前端页面行为
- `GET /owner/templates/designer`
  - 选中 `LLM 触发组件` 后，右侧属性面板中的“目标字段”和“上下文 raw 字段”选择器铺满属性面板宽度。
  - “上下文 raw 字段”多选下拉不再因为触发框过窄而显示成竖向窄条。
  - 同一属性面板内的 Input、TextArea、Select、Picker 等控件统一保持 `100%` 宽度兜底。
  - LLM 触发组件字段级 Prompt、目标字段、上下文 raw 字段和按钮文案保存逻辑保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无 Schema 配置字段变更。
- 无数据库、鉴权、路由、模型调用和审计逻辑变更。

## 2026-06-07 LLM 触发组件字段级生成接口说明

### Schema 配置契约
- `llm-trigger` 是表单内操作控件，不作为可提交答案字段进入 `answerJson`。
- Owner 在模板字段 `componentProps` 中配置字段级生成能力：
  - `promptTemplate: string`：字段级生成 prompt。
  - `contextPaths: string[]`：从当前题 `raw_payload` 取上下文的路径；为空时使用整题 raw payload。
  - `targetField: string`：生成结果要应用到的目标答案字段。
  - `buttonText?: string`：触发按钮文案。
  - `outputMode?: "structured"`：本期固定按结构化输出处理。
- 该配置随 schema 版本发布并冻结，不新增独立 prompt 表。
- 目标字段允许文本、富文本、单选、多选、标签、JSON、文件/图片占位字段等可提交字段。

### 后端 API
- `POST /api/labeler/assignments/{assignmentId}/llm-trigger`
- 鉴权：
  - 需要登录。
  - 当前用户必须具备 `labeler` 角色。
  - 只能调用本人 assignment。
  - assignment 必须处于当前可编辑状态。
- 请求体：

```json
{
  "triggerFieldName": "ai_helper",
  "targetFieldName": "quality_result",
  "currentAnswerJson": {
    "quality_result": "pass"
  }
}
```

- 响应类型：`text/event-stream`。
- SSE 事件：
  - `delta`：模型流式文本片段，用于前端实时展示建议。
  - `result`：最终结构化结果。
  - `error`：失败原因或错误码。
  - `done`：流式结束。
- `result` 事件数据：

```json
{
  "displayText": "建议选择通过，因为回答完整且符合题目要求。",
  "normalizedValue": "pass",
  "targetFieldName": "quality_result",
  "targetSemanticType": "single_choice"
}
```

### 后端校验与规范化
- 后端根据 assignment 当前 schema 校验：
  - `triggerFieldName` 必须存在且 `kind` 为 `llm-trigger`。
  - `targetFieldName` 必须存在且是可提交字段。
  - 若触发字段配置了 `componentProps.targetField`，请求目标字段必须与配置一致。
- 模型输出必须是 JSON，结构为：

```json
{
  "displayText": "给标注员看的建议说明",
  "value": "目标字段候选值"
}
```

- 目标字段规范化规则：
  - `text` / `file`：返回字符串。
  - `single_choice`：返回目标字段合法 `options[].value`，允许模型输出 label 后由后端映射成 value。
  - `multi_choice` / `tags`：返回合法 `options[].value[]`，允许 label 映射。
  - `json`：返回可解析 JSON 字符串。
- 非法选项、非法 JSON、空结果或模型配置缺失会通过 `error` 事件返回，不允许前端写入非法值。

### 前端页面行为
- `GET /owner/templates/designer`
  - LLM 触发组件右侧属性面板支持配置目标字段、上下文 raw 字段、字段级 Prompt 和按钮文案。
  - Designer 预览展示真实触发控件，但不调用模型，只提示“预览环境不调用模型”。
- `GET /labeler/answer/{assignmentId}`
  - Labeler 点击触发按钮后，前端通过 SSE 流式展示建议。
  - 生成结果不会自动覆盖答案。
  - 点击“应用到字段”后，前端将后端返回的 `normalizedValue` 写入目标字段。
  - 目标字段已有值时，应用前提示覆盖确认。

### 审计
- 每次调用写入 `audit_logs`。
- 记录 assignment、task、item、触发字段、目标字段、prompt 摘要、结果摘要、模型名、状态和错误摘要。

### 兼容性说明
- 不新增数据库表。
- 不调整 AI 自动预审 Agent 接口。
- 不改变草稿保存和答案提交接口。
- 右下角 Labeler 答题助手仍使用既有 `/api/labeler/assignments/{assignmentId}/assistant`，与本字段级 LLM 触发组件相互独立。

## 2026-06-07 文档全屏目录父级展开不自动收起说明

### 前端页面行为
- `GET /docs`
  - Markdown 资源进入全屏预览后，点击带子项的二级目录只展开或收回该分组，不再自动收起左侧全屏目录面板。
  - 手机全屏下点击三级子目录或无子项标题时，仍按现有逻辑自动收起左侧全屏目录面板。
  - 右上角“收起目录”按钮仍可直接收起目录，窄栏展开按钮仍可重新展开目录。
  - 普通非全屏文档目录、桌面端目录、文档解析、资源数据、预览渲染和下载逻辑保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和资源清单契约变更。

## 2026-06-07 关于页摘要卡片移动端底部间距修复说明

### 前端页面行为
- `GET /about`
  - 手机端顶部摘要区“协作角色 / 技术栈 / AI 扩展”三张卡片增加底部安全间距。
  - 390px / 375px 手机视口下，摘要段落最后一行文字不再贴近卡片底部边框。
  - 摘要卡片背景、圆角、阴影、纵向排列和桌面端三列布局保持不变。
  - README 阅读器、目录、全屏、下载和提示语逻辑保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和资源数据变更。

## 2026-06-07 关于页提示语按文档页样式与位置对齐说明

### 前端页面行为
- `GET /about`
  - README 阅读器复制代码、下载、进入全屏和退出全屏提示重新使用全局 Ant Design message。
  - 关于页提示语样式、位置和动画与 `/docs` 文档页保持一致。
  - 不再使用关于页局部 `.landing-about-message-host` 提示容器。
  - README 全屏目录折叠/展开、Esc 退出、预览门禁、下载内容和本地持久预览标记保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由、文件存储、README 下载内容和本地存储 key 变更。

## 2026-06-07 模板 Designer 拖放辅助线移动刷新修复

### 前端页面行为
- `GET /owner/templates/designer`
  - 模板 Designer 画布拖放辅助线同时在 `onDragMove` 与 `onDragOver` 中刷新。
  - 拖拽物在同一个字段卡片内部上下移动时，会实时按当前指针位置切换顶部或底部绿线。
  - 字段创建、字段排序、Schema 保存、Schema 发布和画布空白区追加逻辑保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和权限逻辑变更。

## 2026-06-07 文档资源工作台移动端分类区高度优化说明

### 前端页面行为
- `GET /docs`
  - 手机端文档资源工作台上方资源分类区高度增加。
  - 390px / 375px 手机视口下默认至少完整露出“项目资料 / 技术文档 / 演示素材”三个分类。
  - 资源分类区仍支持内部滚动查看剩余分类。
  - 下方预览阅读窗、资源列表两步切换、全屏、下载和桌面端工作台布局保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和权限逻辑变更。

## 2026-06-07 文档资源工作台移动端适配说明

### 前端页面行为
- `GET /docs`
  - 手机端下方文档资源工作台改为单列卡片布局：资源选择区在上方，预览阅读窗在下方。
  - 手机端资源分类、资源列表和文档目录继续复用现有两步切换与返回逻辑，只调整窄屏展示方式。
  - 手机端预览区保持固定阅读窗，标题、说明、“全屏 / 下载”等操作按钮纵向排布并允许换行。
  - 手机端 Markdown、DOCX、图片、外部链接、缺失资源和手动预览态的渲染逻辑保持不变，仅做窄屏宽度、间距和滚动兜底。
  - 桌面端资源工作台两列布局、侧栏收起、资源选择、预览、下载和全屏行为保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和权限逻辑变更。

## 2026-06-07 AI Prompt 模板统一外置说明

### Prompt 模板目录
- 新增项目级模板目录：`agent/prompts`。
- 新增模板文件：
  - `ai-review-default.md`：AI 预审默认审核 prompt。
  - `ai-review-scoring-instructions.md`：AI 预审固定评分口径和 JSON 输出约束。
  - `labeler-assistant-system.md`：Labeler 答题助手 system prompt。
- Agent 和 Backend 均读取文件系统目录，不依赖单个模块的 classpath 资源。

### 环境配置
- 新增统一配置项：`labelhub.prompts.dir`。
- 默认值：`${LABELHUB_PROMPTS_DIR:agent/prompts}`。
- 可通过环境变量 `LABELHUB_PROMPTS_DIR` 指向绝对路径或相对路径。
- 模板文件缺失、为空或读取失败时，服务回退代码内置默认 prompt。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和权限逻辑变更。
- Owner AI 预审规则中的 `promptTemplate` 继续作为业务配置存储在数据库中，并优先于默认模板使用。

## 2026-06-07 文档中心移动端 Hero 区域适配说明

### 前端页面行为
- `GET /docs`
  - 手机端文档中心 Hero 卡片整体下移，避免顶部徽标和标题被公开页固定 Header 遮挡。
  - 手机端 Hero 标题字号、行高和间距收紧，保证“沉淀项目文档，管理数据资产，连接 · 标注。”完整可读。
  - 手机端文档页和 Hero 宽度约束在当前视口内，避免下方工作台宽内容把 Hero 横向撑开。
  - 手机端隐藏 Hero 装饰截图，避免左下角图片裁切不完整。
  - 手机端“快速开始 / GitHub / Swagger”三个快捷入口保持原有目标不变，仅调整为更适合窄屏的纵向等宽排列。
  - 桌面端 Hero 视觉、装饰截图、快捷入口和文档工作台行为保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和权限逻辑变更。

## 2026-06-07 公开首页手机版本号位置与尺寸调整说明

### 前端页面行为
- `GET /`
  - 手机端公开首页 Header 第一行保持品牌 `Label Hub` 在左侧。
  - 手机端版本号胶囊通过 `margin-left: auto` 停靠在第一行右侧。
  - 手机端版本号胶囊和内部圆点尺寸缩小，避免与品牌文字挤在一起。
  - 公开页导航“首页 / 文档 / 关于”继续保持第二行横向排列。
  - 桌面端 Header 布局、版本号来源和公开页跳转逻辑保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和权限逻辑变更。

## 2026-06-07 模板预览选项文案不同步修复

### 前端页面行为
- `GET /owner/templates/designer`
  - 模板 Designer 预览抽屉的共享 Renderer 会在字段结构、选项 label/value、Tab 或 ShowItem 当前展示值变化时重新挂载字段渲染组件。
  - 单选、多选、标签选择会展示当前模板 schema 中的真实选项 label，例如“通过 / 不通过”。
  - 选项提交值仍保持 schema 中的 `options[].value`，不改变已有答案结构。
- `GET /labeler/answer/{assignmentId}`
  - Labeler 答题页继续复用同一个 `LabelHubFormRenderer`。
  - 已发布任务展示的选项 label 以 assignment 返回的 schema 快照为准。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由和权限逻辑变更。
## 2026-06-07 关于页全屏提示语层级对齐说明

### 前端页面行为
- `GET /about`
  - README 阅读器在全屏模式下触发进入全屏、退出全屏、复制代码和下载 README 时，Ant Design message 显示在全屏阅读器上方。
  - 提示语继续使用公开页全局 Ant Design message，样式、动画和顶部居中位置与 `/docs` 文档页保持一致。
  - 本次仅提升公开页 `.ant-message` 的层级，不新增关于页局部提示容器。
  - README 预览门禁、全屏目录折叠、Esc 退出、下载内容和 `labelhub.about.readme.previewed` 本地存储 key 保持不变。
- `GET /docs`
  - 文档页继续使用原有 Ant Design message 提示行为。
  - 公开页 message 层级提升后，全屏提示仍显示在文档预览全屏层上方。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由、文件存储、README 下载内容和本地存储 key 变更。
## 2026-06-07 登录后移动端侧边导航栏适配说明

### 前端页面行为
- `GET /owner`
- `GET /labeler`
- `GET /reviewer`
- `GET /ai-reviewer`
  - 登录后后台页面继续使用统一 `AppLayout`。
  - 手机端 `max-width: 768px` 下，左侧导航仍通过 Header 汉堡按钮打开，通过遮罩点击关闭。
  - 移动端侧边导航宽度最终调整为 `clamp(216px, 68vw, 264px)`，随手机视口宽度响应式变化，并在常见手机上保留更明确的右侧遮罩关闭区域。
  - 移动端侧边导航使用 fixed 抽屉 `inset: 0 auto 0 0` 铺满当前可视区域，并保留 `100vh / 100dvh` 最小高度兜底；菜单区独立滚动，底部版本信息保留在抽屉底部。
  - 桌面端侧边栏宽度、折叠按钮、展开动画、菜单选中态和 Header 布局保持不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由权限、菜单数据和登录逻辑变更。

## 2026-06-07 LLM 触发组件多目标字段生成接口说明

### 后端 API
- `POST /api/labeler/assignments/{assignmentId}/llm-trigger`
  - 鉴权：Labeler JWT。
  - 响应类型：`text/event-stream`。
  - 功能：Labeler 在答题页点击模板内 `LLM 触发组件` 后，后端根据 assignment、schema、触发字段和目标字段配置调用当前激活模型，返回字段级 AI 建议。

### 请求体
```json
{
  "triggerFieldName": "ai_helper",
  "targetFieldName": "quality",
  "targetFieldNames": ["quality", "reason", "keywords"],
  "currentAnswerJson": {
    "quality": "pass"
  }
}
```

- `triggerFieldName`：必填，当前 LLM 触发组件的 `fieldName`。
- `targetFieldNames`：新增，多目标字段列表；字段必须属于该组件 `componentProps.targetFields` 或旧 `targetField` 配置，且必须是可提交字段。
- `targetFieldName`：旧单目标字段参数，继续兼容；当 `targetFieldNames` 为空时可作为单字段请求使用。
- `currentAnswerJson`：当前表单答案快照，用于模型上下文，不直接落库。

### SSE 事件
- `delta`
  - 模型流式文本片段，用于前端实时展示。
- `result`
  - 最终规范化结果，推荐读取 `results[]`。
  - 单目标场景额外保留旧字段 `targetFieldName / targetSemanticType / normalizedValue`。
```json
{
  "displayText": "整体建议说明",
  "results": [
    {
      "targetFieldName": "quality",
      "targetSemanticType": "single_choice",
      "displayText": "建议选择通过",
      "normalizedValue": "pass"
    },
    {
      "targetFieldName": "reason",
      "targetSemanticType": "text",
      "displayText": "理由建议",
      "normalizedValue": "回答完整且格式正确"
    }
  ],
  "targetFieldName": "quality",
  "targetSemanticType": "single_choice",
  "normalizedValue": "pass"
}
```
- `error`
  - 模型调用、字段校验或结果规范化失败。
  - 多目标结果采用全量成功策略，任一目标字段缺失或非法时返回错误，不返回部分可应用结果。
- `done`
  - 本次 SSE 结束。

### 前端展示契约
- Owner 模板 Designer 中 `LLM 触发组件` 使用 `componentProps.targetFields: string[]` 保存多个目标字段。
- 旧模板中的 `componentProps.targetField: string` 继续按单元素数组兼容。
- Labeler 端生成结果仅展示建议，必须由标注员手动点击“应用全部”或“应用此字段”写入表单。
- LLM 触发组件本身仍不进入 `answerJson`；最终答案仍通过现有草稿保存和提交接口落库。

## 2026-06-07 数据集页移动端适配说明

### 前端页面行为
- `GET /owner/datasets`
  - 手机端继续使用现有数据集列表、详情卡和数据预览数据源。
  - 手机端数据集列表改为内容自适应高度，少量数据不再保留桌面端固定三卡高度造成的大留白，多数据时仍在列表内部滚动。
  - 手机端详情卡头部允许标题、类型标签和“添加数据 / 刷新数据”按钮换行展示。
  - 手机端元数据区展示为三列紧凑网格，字段映射区展示为单列阅读列表。
  - 手机端字段映射中的 `必填` 作为小红色胶囊标签显示，不再被字段说明样式拉成长条。
  - 手机端数据预览弱化为搜索入口，不再显示“手机端已简化数据预览”说明提示；完整表格、分页和复杂筛选继续在桌面端展示。
  - 手机端数据集页通过前端布局约束禁止页面级横向滑动，标题区、数据集列表、详情卡片、字段映射和搜索入口均在卡片内换行展示。
  - 手机端字段映射默认折叠，点击“字段映射”标题后展开或收起；桌面端继续默认展开。
  - 手机端数据预览在搜索框下展示前 5 条匹配数据卡片，点击卡片复用现有数据条目详情抽屉。
  - 手机端数据预览匹配结果超过 5 条时，底部提供“查看全部”按钮；点击后在当前卡片内展开全部当前匹配结果，再次点击可收起回前 5 条。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、数据集导入格式、删除、追加数据和权限逻辑变更。

## 2026-06-07 登录后后台移动端根宽度说明

### 前端页面行为
- 登录后后台在 `max-width: 768px` 手机端解除 `html / body / #root` 的 1024px 最小宽度限制。
- `.app-shell.is-mobile`、内部 `Layout` 和 `.app-content` 在手机端按视口宽度布局，页面整体不应再因为根节点宽度产生横向滑动。
- 复杂表格仍使用卡片内部横向滚动兜底，避免把整页撑宽。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由权限和业务数据流变更。

## 2026-06-07 Labeler LLM 助手悬浮按钮拖拽边界说明

### 前端页面行为
- `GET /labeler/answer/{assignmentId}`
  - LLM 标注助手悬浮入口按钮支持拖拽到当前可视区底边，按钮底边可贴近屏幕底部但不会被裁切。
  - 悬浮按钮仍限制在当前可视区域内，不能被拖出屏幕外。
  - 已保存的位置继续使用 `labelhub:labeler-assistant-trigger-position`，刷新页面后按当前可视区重新校正。
  - 移动端动态视口变化时通过 `visualViewport.resize` 和 `window.resize` 重新校正悬浮按钮位置。
  - 单击悬浮按钮打开 LLM 助手抽屉，拖拽松手不触发打开抽屉。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由、LLM 助手流式响应和业务数据流变更。

## 2026-06-07 LLM 触发组件任务语义化配置接口补充

### Schema 配置契约
- `llm-trigger` 仍是表单内操作控件，不进入 `answerJson`。
- `SchemaField.componentProps` 新增可选字段：
  - `taskInstruction?: string`：Owner 配置的数据集和标注任务语义说明，例如说明 `PREFERRED=A` 在业务上代表什么。
  - `outputInstruction?: string`：Owner 配置的输出要求，例如要求建议文案使用“通过 / 不通过”，不要展示内部 option value。
- 继续兼容既有字段：
  - `promptTemplate?: string`：生成规则 / 判断规则。
  - `contextPaths?: string[]`：raw payload 上下文字段路径；为空时使用整题 raw payload。
  - `targetFields?: string[]`：多目标字段。
  - `targetField?: string`：旧模板单目标字段兼容。
  - `buttonText?: string`：按钮文案。

### Labeler LLM Trigger SSE
- 请求路径不变：`POST /api/labeler/assignments/{assignmentId}/llm-trigger`。
- 请求体结构保持兼容：`triggerFieldName`、`targetFieldName?`、`targetFieldNames?`、`currentAnswerJson`。
- `result` 事件中 `results[]` 新增字段：
  - `normalizedDisplayValue?: string`：按目标字段 options label 或字段类型转换后的 Labeler 可见建议值。
- 单目标兼容字段同步可返回：
  - `normalizedDisplayValue?: string`
- `normalizedValue` 仍是最终写入 `answerJson` 的内部提交值；前端展示优先使用 `normalizedDisplayValue`。

### 行为约束
- 后端 prompt 会包含任务标题、任务语义说明、生成规则、输出要求、目标字段 label 和 options 映射。
- 对单选、多选、标签字段，模型输出仍必须匹配合法 `options[].value`；展示文案使用 `options[].label`。
- 本次不新增数据库表，不修改 AI 自动预审 Agent 接口，不修改答案保存和提交接口。

## 2026-06-07 LLM 触发组件联动过滤接口补充

### 请求
- 请求路径不变：`POST /api/labeler/assignments/{assignmentId}/llm-trigger`。
- 请求体不新增必填字段，继续使用：
  - `triggerFieldName`
  - `targetFieldName?`
  - `targetFieldNames?`
  - `currentAnswerJson`

### SSE `result`
- `results[]` 现在允许返回目标字段子集。
- 后端会按 `currentAnswerJson + 模型本次生成值` 计算 schema 联动状态：
  - 最终隐藏的目标字段会被过滤，不出现在 `results[]`。
  - 最终可见且配置为目标字段的字段必须返回合法结果，否则返回 `INVALID_LLM_TRIGGER_RESULT`。
- `normalizedValue` 仍是写入 `answerJson` 的内部提交值。
- `normalizedDisplayValue` 仍是 Labeler 可见建议值。

### SSE `error`
- 当模型结果缺少最终可见目标字段、选项非法或 JSON 不合法时，`error` 事件可返回业务错误码 `INVALID_LLM_TRIGGER_RESULT`。
- 通用模型调用失败仍可能返回 `LLM_TRIGGER_REQUEST_FAILED`。

### 兼容性
- 答案保存和提交接口不变。
- 旧模板 `targetField` 单目标配置继续兼容。
- 条件显示字段可以继续配置为 `targetFields`，本次由后端按联动状态决定是否返回该字段建议。

## 2026-06-07 LLM 触发组件点击状态反馈说明

### 接口
- 无新增接口。
- `POST /api/labeler/assignments/{assignmentId}/llm-trigger` 请求体和 SSE 响应结构不变。
- 旧模板 `componentProps.targetField` 与新模板 `componentProps.targetFields` 继续兼容。

### 前端行为
- Labeler 点击“生成建议”后，生成中、流式文本、结果和错误状态由 `LabelHubFormRenderer` 统一持有。
- Formily 字段因联动或渲染规则重挂载时，不应丢失 LLM 触发组件的当前状态反馈。
- 本次只调整前端状态承载方式，不改变后端鉴权、模型调用、结果规范化和答案写入规则。
## 2026-06-07 Labeler LLM 助手滚动后拖拽坐标系说明

### 前端页面行为
- `GET /labeler/answer/{assignmentId}`
  - LLM 助手悬浮入口按钮通过前端 Portal 挂载到 `document.body`，避免工作台外壳 transform 影响 fixed 定位。
  - 悬浮按钮拖拽边界统一按当前视口坐标计算，滚动到页面底部后仍可拖到当前可视区底边。
  - 已保存位置继续使用 `labelhub:labeler-assistant-trigger-position`，刷新或视口变化时按当前视口重新校正。
  - 单击打开 LLM 助手抽屉，拖拽松手不触发打开抽屉。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由、LLM 助手流式响应和业务数据流变更。

## 2026-06-07 模板搭建页移动端适配说明

### 前端页面行为
- `GET /owner/templates`
  - 手机端模板列表使用 `template-page` 专属布局约束。
  - 模板总数、已发布、草稿统计卡在手机端显示为三列紧凑布局。
  - 搜索、状态筛选和刷新按钮在手机端按容器换行，不撑出页面。
  - 模板卡手机端保留“打开 Designer”主按钮，复制 / 导出 / 删除收进“更多”菜单。
- `GET /owner/templates/designer`
  - 手机端 Designer 使用“物料 / 画布 / 属性”分区编辑。
  - 点击物料会追加字段并切换到画布分区；点击画布字段会切换到属性分区。
  - 手机端物料区以横向胶囊列表展示，继续复用原点击追加和拖拽能力。
  - 顶部手机端保留返回、预览、保存草稿、保存并发布等主操作，导出和收回发布等次要操作收进“更多”菜单。
  - 桌面端模板列表和 Designer 三栏布局不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、Schema 字段协议、任务绑定、保存、发布、复制、删除和导出逻辑变更。

## 2026-06-07 Designer 手机端顶部操作区说明

### 前端页面行为
- `GET /owner/templates/designer`
  - 手机端顶部操作区使用紧凑按钮组。
  - 草稿态外显 `预览 / 保存草稿 / 保存并发布 / 更多`。
  - 已发布态隐藏不可用的 `保存草稿 / 保存并发布`，只外显 `预览 / 更多`。
  - “更多”菜单继续提供当前版本、绑定任务、导出 Schema JSON 和收回发布等次要操作。
  - 桌面端顶部按钮区保持原展示方式。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、Schema 字段协议、保存、发布、导出和收回发布逻辑变更。

## 2026-06-07 Designer 手机端顶部按钮错位修复说明

### 前端页面行为
- `GET /owner/templates/designer`
  - 手机端顶部操作区改用专用 `designer-mobile-actions` 容器渲染。
  - 已发布态手机端只显示 `预览 / 更多`，不再因隐藏按钮外层 wrapper 产生空白占位。
  - 草稿态手机端显示 `预览 / 保存草稿 / 保存并发布 / 更多`。
  - 桌面端继续使用原顶部 `designer-topbar-actions` 按钮组。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、Schema 字段协议、保存、发布、导出和收回发布逻辑变更。
## 2026-06-08 任务管理页移动端适配说明

### 前端页面行为
- `GET /owner/tasks`
  - 桌面端继续使用现有任务表格、分页、筛选栏和发布抽屉表单。
  - 手机端在 `.app-shell.is-mobile` 断点下隐藏桌面任务表格，改用任务卡片列表展示当前 `filteredRows`。
  - 手机端任务卡片展示任务标题、自动命名标签、标注状态、审核状态、任务 ID、Owner、创建时间、分发策略、总配额和标注进度。
  - 手机端任务卡片主按钮按状态展示“发布 / 续期发布 / 详情”，均打开现有任务发布或详情抽屉。
  - 手机端暂停、恢复、结束和删除操作收进“更多”菜单；删除仍弹出原确认框。
  - 手机端列表默认展示 10 条匹配结果，点击“加载更多”继续展示后续 10 条；搜索或筛选变化后重置为前 10 条。
  - 手机端筛选入口调整为搜索框、筛选按钮和刷新按钮；状态、审核状态、分发策略在底部筛选 Drawer 中选择。
  - 手机端发布/编辑任务 Drawer 继续使用基础信息、数据范围、标注分发、审核分配四段 Tabs，表单控件单列铺满，底部按钮全宽堆叠。
  - 手机端“部分题目”选择使用卡片式选择列表，继续写入原 `selectedItemIds` 表单字段；桌面端继续使用原小表格。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由权限和任务状态流转逻辑变更。
- `OwnerTask`、`OwnerTaskDetail`、`CreateOwnerTaskRequest` 等公共类型不变。
## 2026-06-08 任务卡片展开收起按钮说明

### 前端页面行为
- `GET /owner/tasks`
  - 手机端任务卡片默认收起。
  - 收起态保留任务标题、自动命名标签、标注状态、审核状态、标注进度、主按钮和更多菜单。
  - 点击“展开”后显示任务 ID、Owner、创建时间、分发策略、总配额和每人最多可认领等完整元信息。
  - 点击“收起”后隐藏元信息，恢复紧凑卡片。
  - 多张任务卡片可独立展开或收起，展开状态仅在当前页面会话中保存。
  - 搜索、筛选、加载更多、更多菜单、发布、续期发布和详情按钮行为不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由权限、任务状态流转和公共类型变更。
## 2026-06-08 富文本物料契约说明

### Schema 配置
- `rich-text` 仍作为可提交字段使用，不新增接口路径。
- 字段结构保持兼容：
  - `kind: "rich-text"`
  - `semanticType: "text"`
  - `placeholder?: string`
  - `maxLength?: number`
- 后端模板保存、发布、校验接口不变，继续按现有 `SchemaField` JSON 保存。

### 答案保存与提交
- Labeler 答题页将富文本作为 Markdown 编辑。
- 草稿保存与正式提交接口不变：
  - `PUT /api/assignments/{assignmentId}/draft`
  - `POST /api/assignments/{assignmentId}/submit`
- `answer_json[fieldName]` 保存 Markdown 原文字符串，不保存 HTML，不保存结构化对象。
- `maxLength` 按 Markdown 原文字符串长度计算，继续沿用现有前后端长度校验。

### 展示与导出
- 前端只读展示使用 Markdown 渲染，并禁用 HTML 渲染。
- Markdown 链接新窗口打开，使用 `rel="noreferrer"`。
- JSON / JSONL / CSV / XLSX 导出仍输出 Markdown 原文字符串，不导出 HTML。
- 历史纯文本富文本答案无需迁移，会按普通 Markdown 文本兼容显示。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由权限、任务状态和导出逻辑变更。

## 2026-06-08 移动端顶部导航固定说明

### 变更范围
- 登录后的工作台移动端布局统一固定顶部导航栏。
- 生效范围为 `.app-shell.is-mobile .app-header`，即所有使用 `AppLayout` 的登录后页面。
- 桌面端顶部栏、侧边栏、路由、菜单和用户操作逻辑不变。

### 前端行为
- 移动端顶部导航栏固定在视口顶部，不随页面主体滚动离开视口。
- 主内容区在移动端增加顶部让位，避免内容被固定导航栏遮挡。
- 顶部导航高度使用基础高度 `56px` 并叠加 `safe-area-inset-top`。
- 移动端侧边抽屉遮罩继续覆盖顶部导航栏，避免抽屉打开时误触导航按钮。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由权限和公共类型变更。

## 2026-06-08 移动端顶部导航 fixed 失效修复说明

### 变更范围
- 仅修复登录后工作台移动端全局布局 CSS。
- 生效范围为 `.app-shell.is-mobile.app-shell-enter` 和现有移动端固定顶部导航。
- 桌面端入场动画、顶部栏、侧边栏、路由和菜单逻辑不变。

### 前端行为
- 移动端工作台外壳不再保留包含 `transform` 的入场动画结果。
- 移动端顶部导航栏继续使用 `position: fixed`，并相对浏览器视口固定。
- 主内容区顶部让位、移动端侧边抽屉遮罩层级和任务页移动端列表逻辑不变。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由权限和公共类型变更。

## 2026-06-08 手机端任务卡片列表展开收回说明

### 变更范围
- 仅调整 `/owner/tasks` 手机端任务卡片列表展示行为。
- 桌面端任务表格、分页、筛选、发布抽屉和任务状态流转逻辑不变。
- 单张任务卡片内部元信息“展开 / 收起”按钮不变。

### 前端行为
- 手机端任务列表默认展示当前筛选结果前 3 张任务卡片。
- 当匹配任务数大于 3 条时，列表底部显示“展开全部（共 N 条）”。
- 点击展开后显示当前筛选结果的全部任务卡片，按钮变为“收回（显示前 3 条）”。
- 点击收回后恢复展示前 3 张，并自动滚回任务列表顶部。
- 搜索关键词、标注状态、审核状态或分发策略变化时，列表恢复默认收回态。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无数据库、鉴权、路由权限和公共类型变更。
## 2026-06-08 Reviewer 富文本答案展示优化说明

### 前端页面行为
- Reviewer AI 工作台和争议详情中，`rich-text` 答案改为块级展示。
- 字段标签显示在富文本内容上方，Markdown 内容占满答案区域宽度。
- 普通文本、单选、多选、标签、JSON 等字段继续使用原左右键值布局。
- 多轮对比时，富文本字段的变更态使用整块轻量高亮。

### 后端 API
- 无新增接口。
- 无请求参数变更。
- 无响应字段变更。
- 无 schema 字段变更。
- 无数据库、鉴权、路由权限、答案保存和导出逻辑变更。
- `answer_json[fieldName]` 仍保存 Markdown 原文字符串。
